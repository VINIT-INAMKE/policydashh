---
phase: 08-dashboards-notifications
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/(workspace)/dashboard/page.tsx
  - app/(workspace)/dashboard/_components/stat-card.tsx
  - app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx
  - app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx
  - app/(workspace)/dashboard/_components/research-lead-dashboard.tsx
  - app/(workspace)/dashboard/_components/admin-dashboard.tsx
  - app/(workspace)/dashboard/_components/auditor-dashboard.tsx
  - app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx
  - app/(workspace)/dashboard/_components/observer-dashboard.tsx
  - app/(workspace)/layout.tsx
  - app/(workspace)/_components/notification-bell.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - UX-07

must_haves:
  truths:
    - "Authenticated user sees a role-specific dashboard on /dashboard based on their role"
    - "Policy Lead dashboard shows stat cards (open feedback, active CRs, policies, published versions), feedback inbox widget, active CRs widget, section health widget"
    - "Stakeholder dashboard shows what-changed-since-last-visit section, assigned sections widget, pending feedback widget, upcoming workshops placeholder"
    - "Research Lead dashboard shows claims-without-evidence widget with stat cards"
    - "Admin dashboard shows stat row (users, policies, versions ready to publish, open feedback), pending publish widget, user management widget"
    - "Auditor dashboard shows recent audit activity widget with stat cards and export controls"
    - "Workshop Moderator dashboard shows placeholder state for all widgets"
    - "Observer dashboard shows published policies widget"
    - "Notification bell icon is visible in workspace header for all authenticated users"
  artifacts:
    - path: "app/(workspace)/dashboard/page.tsx"
      provides: "Role-switch dispatcher Server Component"
      contains: "switch"
    - path: "app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx"
      provides: "Policy Lead dashboard with feedback inbox, CRs, section health"
    - path: "app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx"
      provides: "Stakeholder dashboard with assigned sections, pending feedback, what changed"
    - path: "app/(workspace)/dashboard/_components/research-lead-dashboard.tsx"
      provides: "Research Lead dashboard with evidence gaps"
    - path: "app/(workspace)/dashboard/_components/admin-dashboard.tsx"
      provides: "Admin dashboard with user management and publish controls"
    - path: "app/(workspace)/dashboard/_components/auditor-dashboard.tsx"
      provides: "Auditor dashboard with audit trail preview"
    - path: "app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx"
      provides: "Workshop Moderator stub dashboard"
    - path: "app/(workspace)/dashboard/_components/observer-dashboard.tsx"
      provides: "Observer read-only dashboard"
    - path: "app/(workspace)/_components/notification-bell.tsx"
      provides: "Notification bell client component with unread badge"
  key_links:
    - from: "app/(workspace)/dashboard/page.tsx"
      to: "src/db/schema/users.ts"
      via: "db.query for user role lookup"
      pattern: "users.clerkId"
    - from: "app/(workspace)/layout.tsx"
      to: "app/(workspace)/_components/notification-bell.tsx"
      via: "component import in header"
      pattern: "NotificationBell"
---

<objective>
Build all 7 role-specific dashboards and the notification bell for the workspace header.

Purpose: This is the core UI for Phase 8 -- every user sees a personalized dashboard on login, and the notification bell provides persistent notification access. The dashboards query existing data (feedback, CRs, versions, sections, evidence, audit events) to surface role-relevant content.

Output: Role-switch dashboard page, 7 role-specific dashboard components, notification bell in header, reusable stat card component.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboards-notifications/08-RESEARCH.md
@.planning/phases/08-dashboards-notifications/08-UI-SPEC.md
@.planning/phases/08-dashboards-notifications/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/db/schema/users.ts (after Plan 01):
```typescript
export const users = pgTable('users', {
  id, clerkId, phone, email, name, role, orgType,
  lastVisitedAt: timestamp('last_visited_at', { withTimezone: true }),
  createdAt, updatedAt
})
```

From src/db/schema/feedback.ts:
```typescript
export const feedbackItems = pgTable('feedback', {
  id, readableId, sectionId, documentId, submitterId,
  feedbackType, priority, impactCategory, title, body,
  status (feedbackStatusEnum: submitted|under_review|accepted|partially_accepted|rejected|closed),
  isAnonymous, decisionRationale, reviewedBy, reviewedAt,
  resolvedInVersionId, createdAt, updatedAt
})
```

From src/db/schema/changeRequests.ts:
```typescript
export const changeRequests = pgTable('change_requests', {
  id, readableId, documentId, ownerId, title, description,
  status (crStatusEnum: drafting|in_review|approved|merged|closed),
  approverId, approvedAt, mergedBy, mergedAt, mergedVersionId,
  closureRationale, createdAt, updatedAt
})
export const documentVersions = pgTable('document_versions', {
  id, documentId, versionLabel, mergeSummary, createdBy, crId,
  createdAt, sectionsSnapshot, changelog, publishedAt, isPublished
})
```

From src/db/schema/documents.ts:
```typescript
export const policyDocuments = pgTable('policy_documents', { id, title, description, createdAt, updatedAt })
export const policySections = pgTable('policy_sections', { id, documentId, title, orderIndex, content, createdAt, updatedAt })
```

From src/db/schema/evidence.ts:
```typescript
export const evidenceArtifacts = pgTable('evidence_artifacts', { id, title, type, url, fileName, fileSize, uploaderId, createdAt })
export const feedbackEvidence = pgTable('feedback_evidence', { feedbackId, artifactId })
```

From src/db/schema/sectionAssignments.ts:
```typescript
export const sectionAssignments = pgTable('section_assignments', { id, userId, sectionId, assignedBy, createdAt })
```

From src/db/schema/audit.ts:
```typescript
export const auditEvents = pgTable('audit_events', { id, timestamp, actorId, actorRole, action, entityType, entityId, payload, ipAddress })
```

From src/trpc/client.tsx:
```typescript
export const trpc = createTRPCReact<AppRouter>()
```

From src/trpc/server.tsx:
```typescript
export const api = cache(async () => createCaller(...))
```

From app/(workspace)/layout.tsx:
```typescript
// Header with WorkspaceNav and UserButton
// NotificationBell should be added between WorkspaceNav and UserButton
```

From src/lib/constants.ts:
```typescript
export const ROLES = { ADMIN: 'admin', POLICY_LEAD: 'policy_lead', RESEARCH_LEAD: 'research_lead', WORKSHOP_MODERATOR: 'workshop_moderator', STAKEHOLDER: 'stakeholder', OBSERVER: 'observer', AUDITOR: 'auditor' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page with role-switch, stat card, and 4 dashboards (Policy Lead, Stakeholder, Admin, Research Lead)</name>
  <files>
    app/(workspace)/dashboard/page.tsx,
    app/(workspace)/dashboard/_components/stat-card.tsx,
    app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx,
    app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx,
    app/(workspace)/dashboard/_components/admin-dashboard.tsx,
    app/(workspace)/dashboard/_components/research-lead-dashboard.tsx
  </files>
  <read_first>
    app/(workspace)/dashboard/page.tsx,
    app/(workspace)/layout.tsx,
    src/db/schema/users.ts,
    src/db/schema/feedback.ts,
    src/db/schema/changeRequests.ts,
    src/db/schema/documents.ts,
    src/db/schema/evidence.ts,
    src/db/schema/sectionAssignments.ts,
    .planning/phases/08-dashboards-notifications/08-UI-SPEC.md (lines 157-370),
    node_modules/next/dist/docs/ (read App Router guide before writing page)
  </read_first>
  <action>
IMPORTANT: Read the Next.js docs at `node_modules/next/dist/docs/` before writing any page files. Heed all deprecation notices.

Reference the 08-UI-SPEC.md for exact layout, copy, spacing, and color specifications.

1. Create `app/(workspace)/dashboard/_components/stat-card.tsx`:
   - A reusable server component (no 'use client')
   - Props: `{ icon: React.ReactNode, value: number | string, label: string }`
   - Renders a `Card` (shadcn) with bg-muted: icon (20px, muted-foreground, top-right), value (text-[28px] font-semibold leading-tight), label (text-xs text-muted-foreground)
   - min-height 96px per UI-SPEC spacing exceptions
   - Add `aria-label="${value} ${label}"` for accessibility

2. Replace `app/(workspace)/dashboard/page.tsx`:
   - Server Component that reads user from DB using Clerk auth:
     ```typescript
     const { userId } = await auth()
     const user = await db.query.users.findFirst({ where: eq(users.clerkId, userId!) })
     ```
   - Render shared heading: `<h1>` "Dashboard" (text-xl font-semibold) + Badge with role display name (secondary variant)
   - Below heading: greeting line using time-of-day: "Good morning/afternoon/evening, {user.name || 'there'}." (text-sm text-muted-foreground)
   - Switch on `user.role` to render role-specific component, passing `userId={user.id}` and `lastVisitedAt={user.lastVisitedAt}`:
     - `policy_lead` -> PolicyLeadDashboard
     - `stakeholder` -> StakeholderDashboard
     - `admin` -> AdminDashboard
     - `research_lead` -> ResearchLeadDashboard
     - `auditor` -> AuditorDashboard
     - `workshop_moderator` -> WorkshopModeratorDashboard
     - default -> ObserverDashboard
   - Map role values to display names: admin->"Admin", policy_lead->"Policy Lead", research_lead->"Research Lead", workshop_moderator->"Workshop Moderator", stakeholder->"Stakeholder", observer->"Observer", auditor->"Auditor"

3. Create `app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx` (UX-02):
   - Server Component. Props: `{ userId: string, lastVisitedAt: Date | null }`
   - Fetch data using direct DB queries (NOT tRPC hooks -- Server Component pattern):
     - `openFeedbackCount`: COUNT feedback WHERE status IN ('submitted', 'under_review')
     - `activeCRCount`: COUNT change_requests WHERE status NOT IN ('merged', 'closed')
     - `policiesCount`: COUNT policy_documents
     - `publishedVersionsCount`: COUNT document_versions WHERE is_published = true
     - `recentFeedback`: SELECT top 5 feedback items WHERE status = 'submitted' ORDER BY createdAt DESC, JOIN policySections for section name
     - `activeCRs`: SELECT top 5 change_requests WHERE status NOT IN ('merged', 'closed') ORDER BY updatedAt DESC
     - `sectionHealth`: For each section, count open feedback items and open CRs. Compute health: Good (0 high-priority open, 0 blocked CRs), Warning (1-3 open or 1 in_review CR), Critical (4+ open or high-priority unresolved or approved CR unpublished >7 days)
   - Use Promise.all for parallel data fetching
   - Stat row: 4 StatCard components across top (MessageSquare, GitPullRequest, FileText, BookOpen icons from lucide-react)
   - Two-column layout on lg+ (grid grid-cols-1 lg:grid-cols-[3fr_2fr] gap-4):
     - Left: Feedback Inbox widget (h2 "Feedback Inbox", subtitle "{N} items needing triage", "Triage Feedback" button linking to /feedback, max 5 items, "View all feedback" footer). Each feedback row shows: FB-NNN badge (font-mono text-muted-foreground), title (truncate), section name (text-xs text-muted-foreground), priority badge (reuse Phase 4 pattern)
     - Left below: Active CRs widget (h2 "Active Change Requests", max 5 items, "View all CRs" footer). Each CR row: CR-NNN badge (font-mono), title, status badge, age
     - Right: Section Health widget (h2 "Section Health", policy selector via Select component, section rows with health pills -- Good/Warning/Critical using semantic colors from UI-SPEC: green `oklch(0.527 0.154 150)`, amber `oklch(0.769 0.188 70)`, red `oklch(0.577 0.245 27)` with bg-[color]/20 text-[color] pattern)
   - "What changed" banner at top if `lastVisitedAt` is set and there is feedback submitted after it: green dot (8px) + "{N} section(s) changed since your last visit." + "See what changed" link
   - All empty states per UI-SPEC copywriting contract

4. Create `app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx` (UX-04):
   - Server Component. Props: `{ userId: string, lastVisitedAt: Date | null }`
   - Fetch data:
     - `assignedSections`: SELECT section_assignments JOIN policy_sections JOIN policy_documents WHERE userId = props.userId
     - `pendingFeedback`: SELECT top 5 feedback WHERE submitterId = props.userId AND status NOT IN ('closed') ORDER BY updatedAt DESC
     - `changedSections`: If lastVisitedAt is set, find sections (from assigned) where section.updatedAt > lastVisitedAt OR document_versions published after lastVisitedAt contain those sections
   - Single column layout, max-w-3xl mx-auto
   - "What Changed Since Your Last Visit" section (only if lastVisitedAt and changes exist): green dot + "{N} section(s) updated" + rows with section name + policy name + "View changes" link
   - "Your Assigned Sections" widget: section rows with section name, policy name (text-xs text-muted-foreground), "Submit Feedback" outline button linking to feedback form
   - "Your Pending Feedback" widget: max 5 items with FB-NNN badge, title, status badge, section name. "View all feedback outcomes" footer linking to /feedback/outcomes
   - "Upcoming Workshops" widget: placeholder -- Calendar icon + "Workshop scheduling is coming soon." (muted-foreground)
   - All empty states per UI-SPEC copywriting contract

5. Create `app/(workspace)/dashboard/_components/admin-dashboard.tsx` (UX-05):
   - Server Component. Props: `{ userId: string }`
   - Fetch data:
     - `totalUsers`: COUNT users
     - `activePolicies`: COUNT policy_documents
     - `versionsReadyToPublish`: SELECT document_versions WHERE is_published = false (unpublished versions)
     - `openFeedback`: COUNT feedback WHERE status IN ('submitted', 'under_review')
     - `usersByRole`: GROUP BY role COUNT from users table
   - Stat row: 4 StatCard (Users, FileText, BookOpen, MessageSquare icons)
   - Two-column on lg+:
     - Left: "Ready to Publish" widget (version rows with policy name, version label font-mono, date, "Publish" button -- links to version detail). Empty state: "No versions awaiting publication."
     - Left below: "User Management" widget: "Manage Users" primary CTA linking to /users. Role breakdown list: one row per role with role name + count badge
   - All empty states per UI-SPEC copywriting contract

6. Create `app/(workspace)/dashboard/_components/research-lead-dashboard.tsx` (UX-03):
   - Server Component. Props: `{ userId: string }`
   - Fetch data:
     - `feedbackWithoutEvidence`: LEFT JOIN feedback to feedback_evidence, WHERE feedback_evidence.artifactId IS NULL. Count and select top items.
     - `totalEvidence`: COUNT evidence_artifacts
   - Stat row: 2 StatCard (AlertCircle, FileSearch icons)
   - Two-column on lg+:
     - Left: "Claims Without Evidence" widget (h2, "{N} items" subtitle, "Review Evidence" primary CTA linking to /feedback filtered, max 5 feedback rows with FB-NNN badge, title, feedback type badge, section name, "Attach Evidence" ghost button). "View all evidence gaps" footer.
     - Right: stats/summary card
   - Empty state: CheckCircle icon + "All feedback items have supporting evidence."
  </action>
  <acceptance_criteria>
    - grep "switch" app/(workspace)/dashboard/page.tsx returns match
    - grep "PolicyLeadDashboard" app/(workspace)/dashboard/page.tsx returns match
    - grep "StakeholderDashboard" app/(workspace)/dashboard/page.tsx returns match
    - grep "AdminDashboard" app/(workspace)/dashboard/page.tsx returns match
    - grep "ResearchLeadDashboard" app/(workspace)/dashboard/page.tsx returns match
    - grep "StatCard" app/(workspace)/dashboard/_components/stat-card.tsx returns match
    - grep "Feedback Inbox" app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx returns match
    - grep "Section Health" app/(workspace)/dashboard/_components/policy-lead-dashboard.tsx returns match
    - grep "Assigned Sections" app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx returns match
    - grep "What Changed" app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx returns match
    - grep "Workshop scheduling is coming soon" app/(workspace)/dashboard/_components/stakeholder-dashboard.tsx returns match
    - grep "Claims Without Evidence" app/(workspace)/dashboard/_components/research-lead-dashboard.tsx returns match
    - grep "Ready to Publish" app/(workspace)/dashboard/_components/admin-dashboard.tsx returns match
    - grep "User Management" app/(workspace)/dashboard/_components/admin-dashboard.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    Dashboard page renders role-switch based on user.role. Policy Lead sees feedback inbox, active CRs, section health. Stakeholder sees assigned sections, pending feedback, what-changed, workshops placeholder. Admin sees user management, pending publish, system stats. Research Lead sees claims without evidence. All with correct UI-SPEC layout, copy, and empty states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remaining dashboards (Auditor, Workshop Moderator, Observer) and notification bell in header</name>
  <files>
    app/(workspace)/dashboard/_components/auditor-dashboard.tsx,
    app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx,
    app/(workspace)/dashboard/_components/observer-dashboard.tsx,
    app/(workspace)/_components/notification-bell.tsx,
    app/(workspace)/layout.tsx
  </files>
  <read_first>
    app/(workspace)/layout.tsx,
    app/(workspace)/_components/workspace-nav.tsx,
    src/db/schema/audit.ts,
    src/db/schema/changeRequests.ts,
    src/trpc/client.tsx,
    src/server/routers/notification.ts (created in Plan 01),
    .planning/phases/08-dashboards-notifications/08-UI-SPEC.md (lines 362-470 for Auditor/Workshop Mod/Observer/NotifBell),
    node_modules/next/dist/docs/ (read App Router guide)
  </read_first>
  <action>
IMPORTANT: Read the Next.js docs at `node_modules/next/dist/docs/` before writing any page files. Heed all deprecation notices.

Reference 08-UI-SPEC.md for exact layout, copy, spacing, and color specifications.

1. Create `app/(workspace)/dashboard/_components/auditor-dashboard.tsx` (UX-06):
   - Server Component. Props: `{ userId: string }`
   - Fetch data:
     - `recentAuditEvents`: SELECT top 10 audit_events ORDER BY timestamp DESC
     - `auditCount7d`: COUNT audit_events WHERE timestamp > NOW() - 7 days
     - `totalAuditCount`: COUNT audit_events
   - Single column layout, max-w-3xl mx-auto
   - Stat row: 2 StatCard (Activity, Shield icons)
   - "Recent Audit Activity" widget: h2, "View Full Audit Trail" primary CTA (accent, linking to /audit -- Phase 9 route, disabled or links to placeholder), max 10 audit event rows. Each row: action badge (muted), actor name (text-sm), object description (text-sm truncate), timestamp (text-xs text-muted-foreground, right-aligned). Use date-fns `formatDistanceToNow` for relative timestamps.
   - "Export Controls" widget: h2, "Export Audit Log (CSV)" outline button with Download icon. Below: info notice "Evidence pack export (ZIP) is available in the compliance module." (Phase 9 placeholder)
   - Empty states per UI-SPEC copywriting contract

2. Create `app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx` (UX-07):
   - Server Component. Props: `{ userId: string }`
   - NO data fetching (workshops don't exist until Phase 10)
   - Single column layout, max-w-3xl mx-auto
   - Stat row: 2 StatCard (Calendar, Paperclip icons) -- both values are 0
   - "Upcoming Workshops" widget: h2, "Manage Workshops" primary CTA (disabled, accent), placeholder: Calendar icon (32px, muted-foreground) + "Workshop management is coming soon."
   - This is explicitly a stub per research recommendation for UX-07

3. Create `app/(workspace)/dashboard/_components/observer-dashboard.tsx`:
   - Server Component. Props: `{ userId: string }`
   - Fetch data:
     - `publishedPolicies`: SELECT policy_documents that have at least one documentVersion WHERE is_published = true. Include latest version label and publishedAt date.
     - `openConsultations`: COUNT policy_documents that have feedback items in status 'submitted' or 'under_review'
   - Single column layout, max-w-3xl mx-auto
   - Stat row: 2 StatCard (BookOpen, MessageSquare icons)
   - "Published Policies" widget: h2, rows with policy name, version label (font-mono badge), publish date (text-xs text-muted-foreground), "View" ghost button linking to policy detail page. Empty: BookOpen icon + "No published policies yet."

4. Create `app/(workspace)/_components/notification-bell.tsx`:
   - Client component ('use client')
   - Import `trpc` from `@/src/trpc/client`
   - Import `Bell, MessageSquare, GitPullRequest, BookOpen, UserPlus` from `lucide-react`
   - Import `Popover, PopoverTrigger, PopoverContent` from shadcn (already installed)
   - Import `Button`, `Badge`, `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`, `ScrollArea`, `Skeleton` from shadcn
   - Query `trpc.notification.unreadCount.useQuery(undefined, { refetchInterval: 10_000 })` for badge count
   - Query `trpc.notification.list.useQuery({ limit: 20 })` when popover is open
   - Bell trigger: Button ghost size="icon", Bell icon (16px). When unreadCount > 0: absolute Badge (accent bg, white text, rounded-full, 16px, top-right) showing count (cap at "9+" for 10+). `aria-label="Notifications, {N} unread"` when N > 0, else `aria-label="Notifications"`
   - Popover content: width 360px, max-height 480px
     - Header: "Notifications" (h2 text-xl font-semibold) + "Mark all as read" ghost button (only when unread > 0, calls `trpc.notification.markAllRead.useMutation` then invalidates queries)
     - Tabs: "All" / "Unread". Filter list client-side for "Unread" tab.
     - ScrollArea with notification items:
       - Each item: 44px min-height, px-4 py-2. Unread: bg-muted, title font-semibold. Read: bg-transparent, title font-normal.
       - Icon per type: MessageSquare (feedback), GitPullRequest (CR), BookOpen (version), UserPlus (section assignment). 16px. Accent for unread, muted-foreground for read.
       - Title (truncate 1 line), body (truncate 2 lines, text-xs text-muted-foreground), timestamp (text-xs text-muted-foreground, ml-auto)
       - On click: call markRead mutation, navigate to linkHref using router.push
     - Footer: "View all notifications" ghost button linking to /notifications (only when total > 5)
     - Empty state: Bell icon (32px, muted-foreground) + "No notifications yet."
     - Loading state: 3 skeleton items (h-10 w-full)
   - Use `formatDistanceToNow` from date-fns for relative timestamps

5. Update `app/(workspace)/layout.tsx`:
   - Import `NotificationBell` from `./_components/notification-bell`
   - Add `<NotificationBell />` in the header between `<WorkspaceNav />` and `<UserButton />`:
     ```
     <div className="flex items-center gap-4">
       <NotificationBell />
       <UserButton />
     </div>
     ```
   - Keep existing structure. Only modify the right side of the header flex to wrap NotificationBell + UserButton together.
  </action>
  <acceptance_criteria>
    - grep "Recent Audit Activity" app/(workspace)/dashboard/_components/auditor-dashboard.tsx returns match
    - grep "Export Controls" app/(workspace)/dashboard/_components/auditor-dashboard.tsx returns match
    - grep "Workshop management is coming soon" app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx returns match
    - grep "Published Policies" app/(workspace)/dashboard/_components/observer-dashboard.tsx returns match
    - grep "use client" app/(workspace)/_components/notification-bell.tsx returns match
    - grep "unreadCount" app/(workspace)/_components/notification-bell.tsx returns match
    - grep "refetchInterval" app/(workspace)/_components/notification-bell.tsx returns match
    - grep "NotificationBell" app/(workspace)/layout.tsx returns match
    - grep "markAllRead" app/(workspace)/_components/notification-bell.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    Auditor, Workshop Moderator, and Observer dashboards render correctly. Notification bell appears in workspace header with unread badge, popover dropdown with item list, mark-read functionality, and 10-second polling. All 7 role dashboards complete. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 7 role dashboard files exist under `app/(workspace)/dashboard/_components/`
- Dashboard page.tsx has role-switch dispatcher
- Notification bell is in workspace header (layout.tsx)
- Each dashboard follows UI-SPEC layout, copy, and component specifications
</verification>

<success_criteria>
- Every authenticated user sees their role-specific dashboard at /dashboard
- Policy Lead: feedback inbox + CRs + section health (UX-02)
- Stakeholder: assigned sections + pending feedback + what changed + workshops placeholder (UX-04)
- Research Lead: claims without evidence + evidence stats (UX-03)
- Admin: users + publish controls + system overview (UX-05)
- Auditor: audit trail + export controls (UX-06)
- Workshop Moderator: stub dashboard (UX-07)
- Observer: published policies (UX-01 default)
- Notification bell visible with unread count badge
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboards-notifications/08-02-SUMMARY.md`
</output>
