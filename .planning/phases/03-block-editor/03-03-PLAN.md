---
phase: 03-block-editor
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/(workspace)/policies/[id]/_components/image-block-view.tsx
  - app/(workspace)/policies/[id]/_components/file-attachment-view.tsx
  - app/(workspace)/policies/[id]/_components/link-preview-view.tsx
  - app/(workspace)/policies/[id]/_components/code-block-view.tsx
  - src/lib/tiptap-extensions/build-extensions.ts
  - src/lib/tiptap-extensions/callout-node.ts
  - src/lib/tiptap-extensions/link-preview-node.ts
  - app/api/uploadthing/core.ts
  - app/api/uploadthing/route.ts
  - src/lib/uploadthing.ts
  - app/(workspace)/policies/[id]/_components/block-editor.tsx
autonomous: false
requirements:
  - EDIT-02
  - EDIT-05

user_setup:
  - service: uploadthing
    why: "Image and file upload storage for editor media blocks"
    env_vars:
      - name: UPLOADTHING_TOKEN
        source: "Uploadthing Dashboard (uploadthing.com) -> Settings -> API Keys -> copy token"
    dashboard_config: []

must_haves:
  truths:
    - "User can insert an image block via slash command and upload an image file"
    - "Uploaded image renders in the editor with correct aspect ratio"
    - "User can insert a file attachment block and upload a document"
    - "File attachment shows filename, size, and download link"
    - "User can insert a link preview block that displays title, description, and OG image"
    - "Code blocks render with syntax highlighting and a language selector"
    - "Code blocks have a copy button that copies content to clipboard"
  artifacts:
    - path: "app/(workspace)/policies/[id]/_components/image-block-view.tsx"
      provides: "Image block NodeView with upload, progress, caption, and alt text"
      min_lines: 60
    - path: "app/(workspace)/policies/[id]/_components/file-attachment-view.tsx"
      provides: "File attachment NodeView with upload and download"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/_components/link-preview-view.tsx"
      provides: "Link preview NodeView with OG data fetching"
      min_lines: 50
    - path: "app/(workspace)/policies/[id]/_components/code-block-view.tsx"
      provides: "Code block NodeView with language selector and copy"
      min_lines: 40
    - path: "app/api/uploadthing/core.ts"
      provides: "Uploadthing file router with image and document upload routes"
      exports: ["ourFileRouter"]
    - path: "app/api/uploadthing/route.ts"
      provides: "Next.js API route handler for Uploadthing"
    - path: "src/lib/uploadthing.ts"
      provides: "Typed Uploadthing client hooks"
  key_links:
    - from: "app/(workspace)/policies/[id]/_components/image-block-view.tsx"
      to: "src/lib/uploadthing.ts"
      via: "useUploadThing hook for image upload"
      pattern: "useUploadThing"
    - from: "app/(workspace)/policies/[id]/_components/file-attachment-view.tsx"
      to: "src/lib/uploadthing.ts"
      via: "useUploadThing hook for file upload"
      pattern: "useUploadThing"
    - from: "app/api/uploadthing/route.ts"
      to: "app/api/uploadthing/core.ts"
      via: "createRouteHandler(ourFileRouter)"
      pattern: "ourFileRouter"
    - from: "src/lib/tiptap-extensions/build-extensions.ts"
      to: "app/(workspace)/policies/[id]/_components/image-block-view.tsx"
      via: "Image extension addNodeView pointing to ImageBlockView"
      pattern: "ImageBlockView|addNodeView"
---

<objective>
Add media and advanced block capabilities to the editor: Uploadthing file upload infrastructure, interactive image blocks (upload, caption, alt text), file attachment blocks, link preview blocks with OG data, and enhanced code blocks with language selector and copy button. This completes all EDIT-05 media requirements and polishes EDIT-02 advanced block types.

Purpose: Complete the editor's media story so users can embed images, attach files, and create rich link previews within policy sections.
Output: Fully functional media blocks, Uploadthing integration, code block enhancement, final human verification.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-block-editor/03-RESEARCH.md
@.planning/phases/03-block-editor/03-UI-SPEC.md
@.planning/phases/03-block-editor/03-01-SUMMARY.md
@.planning/phases/03-block-editor/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From src/lib/tiptap-extensions/build-extensions.ts:
```typescript
export function buildExtensions(options?: {
  onSlashCommand?: Partial<SuggestionOptions>
}): Extensions[]
// Includes Image, FileHandler, Table, CodeBlockLowlight, LinkPreview, etc.
```

From src/lib/tiptap-extensions/link-preview-node.ts:
```typescript
export const LinkPreview: Node
// name: 'linkPreview', group: 'block', atom: true
// attributes: url, title, description, image
```

From app/(workspace)/policies/[id]/_components/block-editor.tsx:
```typescript
// Main editor with useEditor, EditorContent, DragHandle
// Extensions from buildExtensions()
// Auto-save via debounced tRPC mutation
```

From @tiptap/extension-image (npm):
```typescript
// Image extension supports addNodeView() for custom React rendering
// Configure with: Image.configure({ allowBase64: false })
```

From @tiptap/extension-file-handler (npm):
```typescript
// FileHandler.configure({ onDrop, onPaste, allowedMimeTypes })
// onDrop: (editor, files, pos) => void
// onPaste: (editor, files) => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Uploadthing and create media block NodeView components</name>
  <files>
    app/api/uploadthing/core.ts,
    app/api/uploadthing/route.ts,
    src/lib/uploadthing.ts,
    app/(workspace)/policies/[id]/_components/image-block-view.tsx,
    app/(workspace)/policies/[id]/_components/file-attachment-view.tsx,
    app/(workspace)/policies/[id]/_components/link-preview-view.tsx,
    app/(workspace)/policies/[id]/_components/code-block-view.tsx
  </files>
  <read_first>
    node_modules/next/dist/docs/ (for API route export convention in Next.js 16),
    package.json (confirm uploadthing is installed),
    src/lib/tiptap-extensions/build-extensions.ts,
    src/lib/tiptap-extensions/link-preview-node.ts,
    .planning/phases/03-block-editor/03-UI-SPEC.md (Image Block, File Attachment Block, Link Preview Block, Code Block sections),
    .planning/phases/03-block-editor/03-RESEARCH.md (Pitfall 7: File Upload Without API Route)
  </read_first>
  <action>
    **CRITICAL: Read `node_modules/next/dist/docs/` FIRST for the correct API route export convention in Next.js 16.** The Uploadthing route handler must follow the current Next.js pattern (not your training data pattern).

    **Step 1: Create Uploadthing file router `app/api/uploadthing/core.ts`:**
    - Import `createUploadthing` and `type FileRouter` from 'uploadthing/next'
    - Define two file routes:
      1. `imageUploader`: accepts image/* up to 10MB. Middleware checks auth (import auth from @clerk/nextjs/server). On complete, return { url }.
      2. `documentUploader`: accepts pdf, docx, doc, xlsx, pptx up to 25MB. Same auth check.
    - Export `type OurFileRouter = typeof ourFileRouter`

    **Step 2: Create API route handler `app/api/uploadthing/route.ts`:**
    - Read `node_modules/next/dist/docs/` for the correct route handler export pattern
    - Import `createRouteHandler` from 'uploadthing/next' (or 'uploadthing/server' depending on version)
    - Import `ourFileRouter` from './core'
    - Export GET and POST handlers (or whatever Next.js 16 expects)

    **Step 3: Create typed client `src/lib/uploadthing.ts`:**
    - Import `generateReactHelpers` from '@uploadthing/react'
    - Import `type OurFileRouter` from '@/app/api/uploadthing/core'
    - Export `{ useUploadThing, uploadFiles }` from generateReactHelpers

    **Step 4: Create `image-block-view.tsx`** — React NodeView for image blocks:
    - Import NodeViewWrapper from @tiptap/react
    - Import useUploadThing from @/src/lib/uploadthing
    - States: idle (drop zone), uploading (progress bar), uploaded (image rendered)
    - Idle state: dashed border container, 200px min-height, ImageIcon (32px, muted-foreground) + "Drop image here or click to upload". Hidden file input triggered on click.
    - Drag-over: border-primary, bg-primary/5
    - Uploading: linear progress bar (4px height, bg-primary), percentage text
    - Uploaded: `<img>` tag with src from node.attrs.src, max-width 100%, rounded-md, border
    - Caption: editable span below image, 12px italic muted-foreground, placeholder "Add a caption..."
    - When upload completes: call updateAttributes({ src: uploadedUrl }) to persist in Tiptap JSON
    - Alt text enforcement: if no alt text, show small warning badge. Alt text stored in node.attrs.alt
    - Max file size: 10MB (enforced in Uploadthing route + client-side check with toast error)
    - Error state: border-destructive, error message per UI-SPEC copywriting

    **Step 5: Create `file-attachment-view.tsx`** — React NodeView for file attachments:
    - NodeViewWrapper rendering a compact card: bg-muted, border, rounded-md, padding 8px 16px, row layout
    - Icon: Paperclip (16px, muted-foreground)
    - Filename: 14px/600, truncated at 200px max-width (from node.attrs.filename)
    - File size: 12px/400, muted-foreground (format: "2.4 MB")
    - Download button: ExternalLink icon button, ghost, opens node.attrs.url in new tab
    - Upload state: same dashed border drop zone pattern as image
    - When upload completes: updateAttributes({ url, filename, filesize })
    - Max file size: 25MB

    **Step 6: Create `link-preview-view.tsx`** — React NodeView for link previews:
    - NodeViewWrapper with bg-muted, border, rounded-md, overflow-hidden
    - On mount (useEffect): if node.attrs.url exists but title is null, fetch OG metadata
    - OG fetch: call a simple fetch to `/api/og-preview?url=encodeURIComponent(url)` — but since we don't have that API yet, use a simpler approach: fetch the URL client-side and parse basic `<title>` and `<meta og:*>` tags. If CORS blocks it, show fallback.
    - Loading state: Skeleton (120px image placeholder + 2 skeleton lines) per UI-SPEC
    - Loaded state: OG image (120px, object-cover), URL row (12px muted-foreground with Globe icon), Title (14px/600, 2-line clamp), Description (12px/400, 2-line clamp)
    - Error state: Globe icon (24px) + "Unable to load preview" + raw URL as clickable link
    - On successful fetch: updateAttributes({ title, description, image }) to persist in JSON

    **Step 7: Create `code-block-view.tsx`** — Enhanced code block with language selector and copy:
    - NodeViewWrapper with bg-muted, border, rounded-md, padding 16px
    - Language selector: top-right corner, small select/dropdown. Options: Plain Text, JavaScript, TypeScript, Python, SQL, Bash, JSON, HTML, CSS. Value from node.attrs.language, onChange calls updateAttributes({ language })
    - Copy button: top-right (beside language selector), Copy icon 28px ghost. On click: navigator.clipboard.writeText(node.textContent), show sonner toast "Copied!"
    - Code content: NodeViewContent renders the highlighted code. Font: var(--font-geist-mono), 13px, line-height 1.6
    - Empty placeholder: "// Start typing your code..." in muted-foreground monospace

    **Step 8: Wire NodeView components into extensions.**
    Update `src/lib/tiptap-extensions/build-extensions.ts`:
    - Import the NodeView components from the policy page _components directory. PROBLEM: build-extensions.ts is in src/lib but NodeView components are in app/ — this creates an import cycle. Solution: Instead of importing NodeViews in build-extensions, use addNodeView() in the extension definitions (callout-node.ts, link-preview-node.ts) by accepting a component parameter, or use lazy registration.
    - Better approach: In `block-editor.tsx`, AFTER calling buildExtensions(), override the NodeView on specific extensions:
      ```typescript
      const extensions = buildExtensions(...)
      // Replace Image extension with one that has a NodeView
      // Use Image.extend({ addNodeView: () => ReactNodeViewRenderer(ImageBlockView) })
      ```
    - OR: Make buildExtensions accept a `nodeViews` option map and apply addNodeView where provided
    - Whichever approach, ensure: Image extension uses ImageBlockView, LinkPreview uses LinkPreviewView, CodeBlockLowlight uses CodeBlockView
    - Update FileHandler.configure onDrop/onPaste handlers in block-editor.tsx to trigger Uploadthing upload and insert Image or File nodes at the drop position

    Also update callout-node.ts to add the NodeView from callout-block-view.tsx (created in Plan 02).
  </action>
  <acceptance_criteria>
    - grep -r "createUploadthing" app/api/uploadthing/core.ts returns match
    - grep -r "ourFileRouter" app/api/uploadthing/route.ts returns match
    - grep -r "useUploadThing" src/lib/uploadthing.ts returns match
    - grep -r "NodeViewWrapper" app/(workspace)/policies/[id]/_components/image-block-view.tsx returns match
    - grep -r "useUploadThing" app/(workspace)/policies/[id]/_components/image-block-view.tsx returns match
    - grep -r "NodeViewWrapper" app/(workspace)/policies/[id]/_components/file-attachment-view.tsx returns match
    - grep -r "NodeViewWrapper" app/(workspace)/policies/[id]/_components/link-preview-view.tsx returns match
    - grep -r "NodeViewWrapper" app/(workspace)/policies/[id]/_components/code-block-view.tsx returns match
    - grep -r "navigator.clipboard" app/(workspace)/policies/[id]/_components/code-block-view.tsx returns match
    - grep -r "ReactNodeViewRenderer" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - app/api/uploadthing/core.ts exists
    - app/api/uploadthing/route.ts exists
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Uploadthing file router with imageUploader (10MB) and documentUploader (25MB) routes
    - API route handler follows Next.js 16 conventions
    - Typed client hooks exported from src/lib/uploadthing.ts
    - ImageBlockView: upload zone, progress bar, rendered image, caption, alt text
    - FileAttachmentView: upload zone, compact card with icon/name/size/download
    - LinkPreviewView: OG data fetch, skeleton, loaded card, error fallback
    - CodeBlockView: language selector, copy button, syntax highlighted code
    - All NodeViews wired into their extensions via ReactNodeViewRenderer
    - FileHandler onDrop/onPaste trigger Uploadthing uploads
    - TypeScript compilation passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final verification of complete block editor with media</name>
  <what-built>
    Complete Notion-quality block editor with all block types, media support, slash commands, drag-and-drop, formatting toolbar, and auto-save. This is the full Phase 3 deliverable.
  </what-built>
  <how-to-verify>
    **Prerequisites:** Ensure UPLOADTHING_TOKEN is set in .env.local (if not, image/file upload will fail but everything else should work).

    1. Run `npm run dev` and navigate to a policy document
    2. Select a section and click "Edit section"

    **Slash commands (EDIT-01):**
    3. Type "/" in an empty line — verify menu appears with all 15 block types
    4. Type "/h1" — verify filtering, select Heading 1
    5. Type "/callout" — verify callout block inserts with emoji and styled container
    6. Type "/toggle" — verify toggle block with collapsible content
    7. Type "/table" — verify 3x3 table inserts with header row
    8. Type "/code" — verify code block with language selector and copy button

    **Block types (EDIT-02):**
    9. Verify each block type renders correctly per spec: H1/H2/H3 sizes, blockquote with left border, code block with syntax highlighting
    10. In code block: select "JavaScript" from language dropdown, type `const x = 1;` — verify syntax coloring
    11. Click copy button on code block — verify clipboard and "Copied!" toast

    **Drag-and-drop (EDIT-03):**
    12. Create 3+ blocks, hover over one — verify grip handle appears
    13. Drag a block up or down — verify it reorders with drop indicator line

    **Formatting (EDIT-04):**
    14. Select text, click Bold in toolbar — verify bold applied
    15. Click Link button — verify link editor popover with URL input
    16. Enter "https://example.com" and click Apply — verify link is created
    17. Click on link — verify link editor opens to edit/remove

    **Media (EDIT-05):**
    18. Type "/image" and select Image — verify upload drop zone appears
    19. Upload a small image — verify progress bar, then rendered image
    20. Click below image — verify caption field (if implemented)
    21. Type "/file" — verify file attachment upload zone
    22. Upload a PDF — verify filename, size, and download link appear
    23. Type "/link-preview" — verify URL input or paste area, then preview card

    **Save persistence:**
    24. Make several edits, wait 2 seconds — verify "Saved" indicator
    25. Refresh page, re-select section — verify all content persisted including images and formatting

    **Error handling:**
    26. Try navigating away with unsaved changes — verify browser warning dialog
    27. Check browser console — verify no hydration errors or SSR warnings
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` — zero errors
2. Tests: `npm test` — all tests pass (including extension and mutation tests from Plan 01)
3. Dev server: `npm run dev` loads without errors
4. Uploadthing: Image and file uploads work end-to-end (requires UPLOADTHING_TOKEN)
5. Human: Full editor flow verified with all block types, media, slash commands, DnD, formatting
</verification>

<success_criteria>
- Image blocks: upload via drop zone or file picker, show progress, render uploaded image
- File attachment blocks: upload documents, show filename/size/download link
- Link preview blocks: fetch OG metadata, show title/description/image or error fallback
- Code blocks: language selector with 9 languages, syntax highlighting, copy to clipboard
- Uploadthing API route serves uploads correctly
- All media blocks persist in Tiptap JSON and survive page refresh
- FileHandler onDrop/onPaste creates correct block types from dropped files
- No TypeScript errors, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-block-editor/03-03-SUMMARY.md`
</output>
