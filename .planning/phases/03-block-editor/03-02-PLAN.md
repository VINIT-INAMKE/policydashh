---
phase: 03-block-editor
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(workspace)/policies/[id]/_components/block-editor.tsx
  - app/(workspace)/policies/[id]/_components/editor-toolbar.tsx
  - app/(workspace)/policies/[id]/_components/slash-command-menu.tsx
  - app/(workspace)/policies/[id]/_components/floating-link-editor.tsx
  - app/(workspace)/policies/[id]/_components/callout-block-view.tsx
  - app/(workspace)/policies/[id]/_components/section-content-view.tsx
  - app/(workspace)/policies/[id]/page.tsx
autonomous: false
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04

must_haves:
  truths:
    - "User can type in the editor and see text appear in real time"
    - "User can type '/' at start of empty paragraph and see the slash command menu"
    - "User can select a block type from the slash menu and it inserts into the editor"
    - "User can drag blocks to reorder them via the grip handle"
    - "User can apply bold, italic, underline, strikethrough, and inline code via toolbar"
    - "User can insert and edit links via toolbar button and floating link editor"
    - "User can insert callout, toggle, table, code block, and quote via slash commands"
    - "Content auto-saves on blur after 1.5s debounce"
    - "Edit mode shows 'Edit section' / 'Save changes' toggle"
  artifacts:
    - path: "app/(workspace)/policies/[id]/_components/block-editor.tsx"
      provides: "Main Tiptap editor component with useEditor, EditorContent, DragHandle"
      min_lines: 80
    - path: "app/(workspace)/policies/[id]/_components/editor-toolbar.tsx"
      provides: "Fixed formatting toolbar with block type dropdown and mark buttons"
      min_lines: 60
    - path: "app/(workspace)/policies/[id]/_components/slash-command-menu.tsx"
      provides: "Slash command popover with shadcn Command component"
      min_lines: 50
    - path: "app/(workspace)/policies/[id]/_components/floating-link-editor.tsx"
      provides: "Link edit popover with URL input and apply/remove buttons"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/_components/callout-block-view.tsx"
      provides: "React NodeView for callout blocks"
      min_lines: 20
    - path: "app/(workspace)/policies/[id]/_components/section-content-view.tsx"
      provides: "Updated to conditionally render BlockEditor in edit mode"
  key_links:
    - from: "app/(workspace)/policies/[id]/_components/block-editor.tsx"
      to: "src/lib/tiptap-extensions/build-extensions.ts"
      via: "import { buildExtensions }"
      pattern: "import.*buildExtensions"
    - from: "app/(workspace)/policies/[id]/_components/block-editor.tsx"
      to: "src/server/routers/document.ts"
      via: "trpc.document.updateSectionContent.useMutation()"
      pattern: "updateSectionContent"
    - from: "app/(workspace)/policies/[id]/_components/slash-command-menu.tsx"
      to: "src/lib/tiptap-extensions/slash-command-extension.ts"
      via: "getSlashCommandItems for item rendering"
      pattern: "getSlashCommandItems|slashCommandItems"
    - from: "app/(workspace)/policies/[id]/page.tsx"
      to: "app/(workspace)/policies/[id]/_components/section-content-view.tsx"
      via: "SectionContentView with canEdit prop"
      pattern: "canEdit"
---

<objective>
Build the full block editor UI: the BlockEditor component with Tiptap EditorContent, EditorToolbar with formatting buttons, SlashCommandMenu popover, FloatingLinkEditor, CalloutBlockView, DragHandle integration, auto-save via debounced tRPC mutation, and replace the read-only SectionContentView with the interactive editor for users with edit permission.

Purpose: This is the core editor experience — everything a user touches when editing a policy section. It consumes the extensions and mutation from Plan 01 and makes the editor interactive.
Output: Fully functional block editor integrated into the policy detail page with all text-based block types working.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-block-editor/03-RESEARCH.md
@.planning/phases/03-block-editor/03-UI-SPEC.md
@.planning/phases/03-block-editor/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/tiptap-extensions/build-extensions.ts:
```typescript
export function buildExtensions(options?: {
  onSlashCommand?: Partial<SuggestionOptions>
}): Extensions[]
```

From src/lib/tiptap-extensions/slash-command-extension.ts:
```typescript
export interface SlashCommandItem {
  title: string
  description: string
  searchTerms: string[]
  icon: string  // lucide icon name
  command: (props: { editor: Editor; range: Range }) => void
}
export function getSlashCommandItems(query: string): SlashCommandItem[]
export const SlashCommands: Extension
```

From src/lib/tiptap-extensions/callout-node.ts:
```typescript
export const Callout: Node  // name: 'callout', group: 'block', content: 'block+'
```

From src/server/routers/document.ts:
```typescript
updateSectionContent: requirePermission('section:manage')
  .input(z.object({
    id: z.string().uuid(),
    documentId: z.string().uuid(),
    content: z.record(z.unknown()),
  }))
  .mutation(...)
```

From app/(workspace)/policies/[id]/page.tsx (existing):
```typescript
// Currently passes section to SectionContentView
<SectionContentView section={selectedSection} />
```

From app/(workspace)/policies/[id]/_components/section-content-view.tsx (existing):
```typescript
interface Section {
  id: string
  title: string
  content: Record<string, unknown> | null
  updatedAt: string
}
interface SectionContentViewProps {
  section: Section
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlockEditor component with DragHandle, auto-save, and edit mode toggle</name>
  <files>
    app/(workspace)/policies/[id]/_components/block-editor.tsx,
    app/(workspace)/policies/[id]/_components/callout-block-view.tsx,
    app/(workspace)/policies/[id]/_components/section-content-view.tsx,
    app/(workspace)/policies/[id]/page.tsx
  </files>
  <read_first>
    app/(workspace)/policies/[id]/page.tsx,
    app/(workspace)/policies/[id]/_components/section-content-view.tsx,
    src/lib/tiptap-extensions/build-extensions.ts,
    src/lib/tiptap-extensions/callout-node.ts,
    .planning/phases/03-block-editor/03-UI-SPEC.md (BlockEditor section, Save Strategy section, Edit mode toggle, Callout Block),
    .planning/phases/03-block-editor/03-RESEARCH.md (Pattern 1, 2, 5)
  </read_first>
  <action>
    **Create `callout-block-view.tsx`** — React NodeView component for callout blocks:
    - Import `NodeViewWrapper`, `NodeViewContent` from `@tiptap/react`
    - Render: NodeViewWrapper with `div` containing emoji prefix (from node.attrs.emoji, default lightbulb) and NodeViewContent for editable inner content
    - Styling per UI-SPEC: bg-muted, border-l-3 border-border, rounded-r-md, padding 24px
    - Empty placeholder: "Type a note or callout..." in muted-foreground

    **Create `block-editor.tsx`** — Main editor component ('use client'):
    - Import `useEditor`, `EditorContent` from `@tiptap/react`
    - Import `DragHandle` from `@tiptap/extension-drag-handle-react`
    - Import `buildExtensions` from `@/src/lib/tiptap-extensions/build-extensions`
    - Import `useDebouncedCallback` from `use-debounce`
    - Import `trpc` from `@/src/trpc/client`
    - Props: `section: { id: string; documentId: string; title: string; content: Record<string, unknown> | null; updatedAt: string }`
    - Use `useEditor` with:
      - extensions: buildExtensions({ onSlashCommand: { ... } }) — pass suggestion render callback (wired in Task 2)
      - content: section.content ?? { type: 'doc', content: [{ type: 'paragraph' }] }
      - immediatelyRender: false (CRITICAL for Next.js SSR)
      - editorProps: { attributes: { class: 'prose max-w-none focus:outline-none' } }
    - Auto-save: useDebouncedCallback wrapping trpc.document.updateSectionContent.useMutation()
      - Debounce delay: 1500ms
      - onUpdate callback: set isDirty=true, call debouncedSave(editor.getJSON())
      - onBlur callback: if isDirty, flush save immediately
    - Save state indicator: track 'idle' | 'saving' | 'saved' | 'error' state
      - On mutate start: 'saving'
      - On success: 'saved', then timeout to 'idle' after 3s
      - On error: 'error', show toast via sonner
    - Navigation guard: useEffect with beforeunload listener when isDirty
    - DragHandle: Render `<DragHandle editor={editor}>` with GripVertical icon (16px, muted-foreground at 60% opacity, foreground on hover, cursor-grab)
    - Render EditorContent with className per UI-SPEC: max-w-[768px], padding xl (32px) top/bottom, padding lg (24px) left/right
    - Export as default for dynamic import

    **Update `section-content-view.tsx`:**
    - Add `canEdit` boolean prop (derive from user role: admin or policy_lead)
    - Add `documentId` string prop
    - When canEdit=true and isEditing=true: render BlockEditor via `dynamic(() => import('./block-editor'), { ssr: false })` — SSR disabled to avoid DragHandle hydration issue
    - When canEdit=true and isEditing=false: show "Edit section" button (Button outline, size="sm", Pencil icon)
    - When canEdit=false: render read-only content (keep existing renderTiptapToText for now; upgrade to Tiptap read-only render if time permits)
    - Show auto-save indicator next to title: "Saving..." with Loader2 spin, "Saved" with CheckCircle, "Error saving" in destructive color
    - Show "Save changes" button (Button default, size="sm", Check icon) that flushes the save

    **Update `page.tsx`:**
    - Pass `canEdit` prop to SectionContentView — derive from user role. For now, always pass `canEdit={true}` (all authenticated users viewing the detail page are already gated by document:read; actual edit permission is enforced by the tRPC mutation's section:manage gate). The UI shows the edit button to admin/policy_lead roles. To determine role, the page can use the user context from Clerk or a simple flag. For simplicity, pass `canEdit={true}` now and let the mutation enforce access.
    - Pass `documentId={id}` to SectionContentView
    - Update the Section type passed to include documentId

    **Styling:** Apply all CSS classes from UI-SPEC:
    - Editor canvas: white background, max-w-[768px], py-8 px-6
    - Block hover: bg-muted/30, rounded
    - Block focus: ring-1 ring-primary, rounded
    - Drag handle: positioned absolute left -24px from block, visible on block hover
    - Add global CSS for Tiptap prosemirror content in `app/globals.css` or a colocated CSS file:
      - `.ProseMirror h1 { font-size: 28px; font-weight: 600; line-height: 1.2 }`
      - `.ProseMirror h2 { font-size: 24px; font-weight: 600; line-height: 1.2 }`
      - `.ProseMirror h3 { font-size: 20px; font-weight: 600; line-height: 1.2 }`
      - `.ProseMirror blockquote { border-left: 3px solid var(--muted-foreground); padding-left: 16px; font-style: italic }`
      - `.ProseMirror pre { background: var(--muted); border: 1px solid var(--border); border-radius: 6px; padding: 16px; font-family: var(--font-geist-mono); font-size: 13px; line-height: 1.6 }`
      - `.ProseMirror table { border-collapse: collapse; width: 100% }` etc per UI-SPEC
      - `.ProseMirror div[data-type="callout"] { ... }` per UI-SPEC callout spec
      - `.ProseMirror details { ... }` per UI-SPEC toggle spec
      - `.ProseMirror .is-empty::before { content: attr(data-placeholder); color: var(--muted-foreground) }` for placeholder
  </action>
  <acceptance_criteria>
    - grep -r "useEditor" app/(workspace)/policies/[id]/_components/block-editor.tsx returns matches
    - grep -r "immediatelyRender.*false" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "DragHandle" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "buildExtensions" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "updateSectionContent" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "useDebouncedCallback" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "beforeunload" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "canEdit" app/(workspace)/policies/[id]/_components/section-content-view.tsx returns match
    - grep -r "dynamic.*block-editor" app/(workspace)/policies/[id]/_components/section-content-view.tsx returns match
    - grep -r "NodeViewWrapper" app/(workspace)/policies/[id]/_components/callout-block-view.tsx returns match
    - app/(workspace)/policies/[id]/_components/block-editor.tsx exists with 80+ lines
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - BlockEditor renders Tiptap EditorContent with all extensions from buildExtensions()
    - DragHandle shows GripVertical on block hover, enables drag-and-drop reorder
    - Auto-save fires 1.5s after last edit via debounced tRPC mutation
    - beforeunload guard prevents navigation with unsaved changes
    - Save state indicator shows Saving.../Saved/Error
    - SectionContentView conditionally renders BlockEditor (edit mode) or read-only text
    - Policy detail page passes canEdit and documentId props
    - CalloutBlockView renders styled callout with emoji prefix
    - TypeScript compilation passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create EditorToolbar, SlashCommandMenu, and FloatingLinkEditor</name>
  <files>
    app/(workspace)/policies/[id]/_components/editor-toolbar.tsx,
    app/(workspace)/policies/[id]/_components/slash-command-menu.tsx,
    app/(workspace)/policies/[id]/_components/floating-link-editor.tsx,
    app/(workspace)/policies/[id]/_components/block-editor.tsx
  </files>
  <read_first>
    app/(workspace)/policies/[id]/_components/block-editor.tsx (from Task 1),
    src/lib/tiptap-extensions/slash-command-extension.ts,
    .planning/phases/03-block-editor/03-UI-SPEC.md (EditorToolbar, SlashCommandMenu, FloatingLinkEditor, Block type items with icons),
    .planning/phases/03-block-editor/03-RESEARCH.md (Pattern 3, 6),
    components/ui/popover.tsx,
    components/ui/command.tsx
  </read_first>
  <action>
    **Create `editor-toolbar.tsx`** — Fixed toolbar above editor canvas:
    - Props: `editor: Editor | null` (from @tiptap/react)
    - Block type dropdown (Button + custom dropdown): shows current block type name. Options: Text, Heading 1, Heading 2, Heading 3, Quote, Code Block. On select, calls editor.chain().focus().setNode() or toggleNode(). Width: 120px fixed. ChevronDown icon right.
    - Text format group: Bold (Ctrl+B), Italic (Ctrl+I), Underline (Ctrl+U), Strikethrough (Ctrl+Shift+S), Inline Code (Ctrl+E) — each a Button ghost/icon 32x32px. Active state: secondary variant when editor.isActive('bold') etc.
    - Links group: Insert/Edit Link button (Link icon). On click, opens FloatingLinkEditor.
    - Lists group: Bullet List (List icon), Ordered List (ListOrdered icon)
    - Insert group: Insert Table (Table2 icon — inserts 3x3 table), Insert Image placeholder (ImageIcon — placeholder, full functionality in Plan 03)
    - Every button wrapped in Tooltip with label + shortcut (e.g., "Bold (Ctrl+B)")
    - Separator components between groups: Separator vertical h-4 mx-1
    - Toolbar container: bg-muted/50 backdrop-blur, border-b, sticky top-0, z-10, h-[36px], flex items-center gap-0.5 px-2
    - Disabled state: all buttons disabled when editor is null or not editable
    - Use icons from lucide-react: Bold, Italic, Underline, Strikethrough, Code, Link, List, ListOrdered, Table2, ImageIcon, ChevronDown, Type, Heading1, Heading2, Heading3, Quote, Code2

    **Create `slash-command-menu.tsx`** — Slash command popover:
    - This is the React "render" component for the @tiptap/suggestion plugin
    - Uses forwardRef to expose `onKeyDown` handler to suggestion plugin
    - Uses shadcn `Command` component inside a positioned div
    - Positioned via the `clientRect` callback from suggestion plugin (absolute positioning at cursor)
    - Props passed by suggestion: items (SlashCommandItem[]), command (function to execute selected item), clientRect (function returning DOMRect)
    - Render: CommandGroup per category ("Text blocks", "Advanced", "Media") containing CommandItems
    - Each CommandItem: icon (from lucide-react, 16px) + title + description (12px muted-foreground)
    - Navigation: ArrowUp/ArrowDown via Command component keyboard handling, Enter to select
    - Style per UI-SPEC: max-height 320px, min-width 240px, shadow-md, border, rounded-md, overflow-y-auto
    - Empty state: CommandEmpty "No matching blocks"
    - On item select: call `command(item)` which triggers the item.command function from getSlashCommandItems

    **Wire slash menu into block-editor.tsx:**
    - In buildExtensions call, pass the onSlashCommand suggestion options:
      ```typescript
      suggestion: {
        items: ({ query }) => getSlashCommandItems(query),
        render: () => {
          let component: ReactRenderer<SlashCommandMenuRef>
          let popup: Instance<Props>[]  // tippy instance
          return {
            onStart: (props) => { ... mount SlashCommandMenu as ReactRenderer ... },
            onUpdate: (props) => { ... update component props ... },
            onKeyDown: (props) => { ... forward keydown to component ... },
            onExit: () => { ... destroy component and popup ... },
          }
        },
      }
      ```
    - Use `ReactRenderer` from `@tiptap/react` and `tippy` from `tippy.js` (Tiptap bundles tippy.js via @tiptap/suggestion) for positioning the popup
    - Alternative approach if tippy is not available: use a React portal with absolute positioning based on clientRect

    **Create `floating-link-editor.tsx`** — Link editing popover:
    - Uses shadcn Popover component
    - Props: `editor: Editor`, `isOpen: boolean`, `onClose: () => void`
    - URL input: Input component, placeholder "Paste or type URL...", autofocus on open
    - "Open in new tab" checkbox toggle
    - "Apply link" Button (default, size="sm") — calls editor.chain().focus().extendMarkRange('link').setLink({ href: url, target: newTab ? '_blank' : null }).run()
    - "Remove link" Button (ghost, size="sm", destructive text) — only shown when editing existing link. Calls editor.chain().focus().unsetLink().run()
    - URL validation: must start with http://, https://, mailto:, or / (relative path). Show error below input: "Enter a valid URL" in destructive color, 12px
    - On open: populate URL from current link mark if cursor is inside one (editor.getAttributes('link').href)
    - On Escape or click outside: close popover
    - Width: 280px per UI-SPEC

    **Wire link editor into block-editor.tsx:**
    - Add state: `const [linkEditorOpen, setLinkEditorOpen] = useState(false)`
    - Pass setLinkEditorOpen to EditorToolbar's link button onClick
    - Render FloatingLinkEditor conditionally when linkEditorOpen is true
    - Position near the current selection using editor.view.coordsAtPos

    **Wire toolbar into block-editor.tsx:**
    - Render EditorToolbar above EditorContent, passing the editor instance
    - Toolbar is sticky at top of editor scroll container
  </action>
  <acceptance_criteria>
    - grep -r "CommandGroup\|CommandItem" app/(workspace)/policies/[id]/_components/slash-command-menu.tsx returns matches
    - grep -r "editor.chain.*toggleBold\|toggleItalic\|toggleUnderline" app/(workspace)/policies/[id]/_components/editor-toolbar.tsx returns matches
    - grep -r "setLink\|unsetLink" app/(workspace)/policies/[id]/_components/floating-link-editor.tsx returns matches
    - grep -r "EditorToolbar" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "SlashCommandMenu\|getSlashCommandItems" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "FloatingLinkEditor\|linkEditorOpen" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "Tooltip" app/(workspace)/policies/[id]/_components/editor-toolbar.tsx returns matches (tooltips on buttons)
    - app/(workspace)/policies/[id]/_components/editor-toolbar.tsx exists with 60+ lines
    - app/(workspace)/policies/[id]/_components/slash-command-menu.tsx exists with 50+ lines
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - EditorToolbar renders 5 button groups with separators, all wired to editor commands
    - Block type dropdown shows current block type, allows switching
    - Text format buttons show active state when mark is applied at cursor
    - SlashCommandMenu appears on '/' keystroke with filtered command list
    - Items grouped into Text blocks / Advanced / Media categories
    - Arrow key navigation and Enter selection work
    - FloatingLinkEditor allows inserting/editing/removing links with URL validation
    - All three components integrated into BlockEditor
    - TypeScript compilation passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify core editor experience</name>
  <what-built>
    Complete block editor integrated into the policy detail page. All text-based block types (paragraph, headings, quote, code, callout, toggle, table, divider, lists), slash command menu, formatting toolbar, drag-and-drop block reorder, link editing, and auto-save.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to a policy document with sections
    2. Click on a section in the sidebar to select it
    3. Click "Edit section" button — editor should appear
    4. Type text — verify it appears in the editor
    5. Type "/" at the start of an empty line — verify slash command menu appears with block type list
    6. Select "Heading 1" from slash menu — verify block transforms to H1
    7. Try other slash commands: Quote (/quote), Code Block (/code), Callout (/callout), Toggle (/toggle), Table (/table), Divider (/divider)
    8. In the toolbar: click Bold, Italic, Underline — verify marks toggle on selected text
    9. Click the Link button — verify link editor popover opens with URL input
    10. Hover over a block — verify drag handle (grip icon) appears on the left
    11. Drag a block up or down — verify it reorders
    12. Click away from the editor — verify "Saving..." then "Saved" indicator appears
    13. Click "Save changes" — verify save completes
    14. Refresh the page and re-select the section — verify content persisted
    15. Verify no console errors related to hydration or SSR
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` — zero errors
2. Tests: `npm test` — all tests pass
3. Dev server: `npm run dev` starts without errors
4. Human verification: editor loads, slash commands work, formatting applies, drag-drop reorders, auto-save persists
</verification>

<success_criteria>
- BlockEditor component renders Tiptap editor with 15+ extensions
- Slash command menu opens on "/" with 15 block types, filterable by typing
- Toolbar has 5 groups: block type, text format, links, lists, insert
- DragHandle shows on block hover, enables drag-and-drop reorder
- Auto-save fires 1.5s after edits, shows Saving/Saved/Error indicator
- Navigation guard warns on unsaved changes
- Link editor validates URLs and applies/removes link marks
- Content persists to database via updateSectionContent mutation
- All block types render with correct styling per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/03-block-editor/03-02-SUMMARY.md`
</output>
