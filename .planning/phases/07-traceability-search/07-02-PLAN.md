---
phase: 07-traceability-search
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - app/(workspace)/policies/[id]/traceability/page.tsx
  - app/(workspace)/policies/[id]/traceability/_components/matrix-table.tsx
  - app/(workspace)/policies/[id]/traceability/_components/matrix-filter-panel.tsx
  - app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx
  - app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx
  - app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx
  - app/(workspace)/policies/[id]/traceability/_components/search-view.tsx
  - app/(workspace)/_components/workspace-nav.tsx
autonomous: true
requirements: [TRACE-02, TRACE-03, TRACE-04, TRACE-05, TRACE-06]

must_haves:
  truths:
    - "User can see a traceability matrix grid with columns: Feedback, CR, Section, Version, Decision, Rationale"
    - "User can filter the matrix by org type, section, decision outcome, and version range"
    - "User can select a section and see 'What Changed and Why' with version transition cards"
    - "User can view per-stakeholder feedback outcomes with version influence labels"
    - "User can click Export CSV and Export PDF buttons to download files"
    - "Traceability link appears in workspace navigation"
  artifacts:
    - path: "app/(workspace)/policies/[id]/traceability/page.tsx"
      provides: "Traceability hub page with tabs"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/traceability/_components/matrix-table.tsx"
      provides: "Matrix grid with all chain columns"
      min_lines: 60
    - path: "app/(workspace)/policies/[id]/traceability/_components/matrix-filter-panel.tsx"
      provides: "Filter panel with org type, section, decision, version range"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx"
      provides: "Per-section what-changed-and-why view"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx"
      provides: "Per-stakeholder outcomes with version influence"
      min_lines: 40
    - path: "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
      provides: "CSV and PDF export buttons with loading states"
      min_lines: 20
  key_links:
    - from: "app/(workspace)/policies/[id]/traceability/page.tsx"
      to: "src/server/routers/traceability.ts"
      via: "trpc.traceability.matrix.useQuery"
      pattern: "trpc\\.traceability\\."
    - from: "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
      to: "app/api/export/traceability/csv/route.ts"
      via: "fetch('/api/export/traceability/csv')"
      pattern: "/api/export/traceability"
    - from: "app/(workspace)/_components/workspace-nav.tsx"
      to: "app/(workspace)/policies/[id]/traceability/page.tsx"
      via: "Link href traceability"
      pattern: "traceability"
---

<objective>
Build the Traceability UI: matrix grid, filter panel, per-section view, per-stakeholder view, export buttons, and workspace navigation integration.

Purpose: This is the most visible manifestation of PolicyDash's core value — the traceability matrix and supporting views. Users need to see, filter, and navigate the full feedback-to-version chain.

Output: 8 UI component files implementing 3 tabs (Matrix, By Section, By Stakeholder) + search stub + export actions + workspace nav link.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-traceability-search/07-RESEARCH.md
@.planning/phases/07-traceability-search/07-UI-SPEC.md
@.planning/phases/07-traceability-search/07-01-SUMMARY.md

@app/(workspace)/policies/[id]/versions/page.tsx
@app/(workspace)/policies/[id]/feedback/page.tsx
@app/(workspace)/policies/[id]/feedback/_components/filter-panel.tsx
@app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx
@app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx
@app/(workspace)/_components/workspace-nav.tsx
@src/trpc/client.ts
@src/server/routers/traceability.ts

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From src/server/routers/traceability.ts (created by 07-01):
```typescript
// matrix procedure returns rows shaped as:
{
  feedbackId: string
  feedbackReadableId: string
  feedbackTitle: string
  feedbackStatus: string
  feedbackDecisionRationale: string | null
  feedbackIsAnonymous: boolean
  submitterName: string | null
  submitterOrgType: string | null
  crId: string | null
  crReadableId: string | null
  crTitle: string | null
  crStatus: string | null
  sectionId: string | null
  sectionTitle: string | null
  versionId: string | null
  versionLabel: string | null
  feedbackCreatedAt: string
}

// sectionChain procedure returns:
{
  sectionId, sectionTitle, crId, crReadableId, crTitle, crStatus,
  feedbackId, feedbackReadableId, feedbackTitle, feedbackStatus,
  feedbackDecisionRationale, versionId, versionLabel, mergedAt
}

// stakeholderOutcomes procedure returns:
{
  feedbackId, readableId, title, status, decisionRationale,
  resolvedInVersionId, versionLabel, sectionId, sectionTitle, createdAt
}
```

From app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx:
```typescript
// StatusBadge renders feedback status with semantic colors
export function StatusBadge({ status }: { status: string })
```

From app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx:
```typescript
export function CRStatusBadge({ status }: { status: string })
```

Export Route Handlers:
- GET /api/export/traceability/csv?documentId={id}&...filters
- GET /api/export/traceability/pdf?documentId={id}&...filters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Traceability page frame with Matrix tab, filter panel, and workspace nav</name>
  <read_first>
    - app/(workspace)/policies/[id]/versions/page.tsx (reference for 'use client' + useParams pattern)
    - app/(workspace)/policies/[id]/feedback/page.tsx (reference for tab/filter page pattern)
    - app/(workspace)/policies/[id]/feedback/_components/filter-panel.tsx (reference for filter panel pattern with useState)
    - app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx (import for matrix rows)
    - app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx (import for matrix rows)
    - app/(workspace)/_components/workspace-nav.tsx (add Traceability link)
    - app/(workspace)/policies/[id]/layout.tsx (check for sub-nav pattern for policy detail routes)
    - .planning/phases/07-traceability-search/07-UI-SPEC.md (full spec for layout, tabs, matrix, filter panel, copy, colors)
    - src/trpc/client.ts (understand trpc import path)
    - src/server/routers/traceability.ts (understand procedure shapes for useQuery)
    - AGENTS.md (check Next.js docs guidance)
    - components/ui/tabs.tsx (understand shadcn Tabs API)
    - components/ui/table.tsx (understand shadcn Table API)
    - components/ui/select.tsx (understand Select API)
    - components/ui/checkbox.tsx (understand Checkbox API)
    - components/ui/skeleton.tsx (skeleton loading pattern)
    - components/ui/tooltip.tsx (tooltip for truncated rationale)
    - components/ui/button.tsx (button variants)
    - components/ui/badge.tsx (badge for IDs)
    - components/ui/card.tsx (card component)
    - components/ui/sheet.tsx (sheet for mobile filter panel)
  </read_first>
  <files>
    app/(workspace)/policies/[id]/traceability/page.tsx,
    app/(workspace)/policies/[id]/traceability/_components/matrix-table.tsx,
    app/(workspace)/policies/[id]/traceability/_components/matrix-filter-panel.tsx,
    app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx,
    app/(workspace)/policies/[id]/traceability/_components/search-view.tsx,
    app/(workspace)/_components/workspace-nav.tsx
  </files>
  <action>
**Step 1: Update workspace-nav.tsx.**

The workspace nav currently has 3 items: Dashboard, Policies, Feedback. This is a global nav, NOT a per-policy sub-nav. The Traceability page is at `/policies/[id]/traceability` which is document-scoped, so it should NOT go in the global workspace nav. Instead, the traceability link should appear in the same navigation pattern as Feedback, Change Requests, and Versions within the policy detail layout.

Check the policy detail layout: `app/(workspace)/policies/[id]/layout.tsx`. If there is a sub-navigation for the policy detail pages (e.g., a tab bar or sidebar within the policy), add "Traceability" there. If the pattern is just direct page routes with no sub-nav, add it as a link in the policy detail page sidebar or header.

If there is no existing sub-nav pattern for policy detail routes, leave workspace-nav.tsx unchanged and instead add the traceability link in the policy detail page layout.

**Step 2: Create `app/(workspace)/policies/[id]/traceability/page.tsx`.**

Follow `'use client'` pattern per all existing pages. Use `useParams<{ id: string }>()` to get documentId.

Layout per UI-SPEC:
- Page heading row: `<h1>` "Traceability" (20px/600) on left. Export buttons on right (ExportActions component).
- Result count span below heading: "{N} feedback items" in muted-foreground 12px.
- Tab navigation using shadcn `Tabs` component. 4 tabs:
  - "Traceability Matrix" (default, value="matrix") with Network icon (16px from lucide-react)
  - "By Section" (value="section") with FileText icon
  - "By Stakeholder" (value="stakeholder") with User icon
  - "Search" (value="search") with Search icon
- Tab state synced to URL query param `?tab=` using `useSearchParams` + `useRouter.replace`. Default to "matrix".
- Tab content area renders the appropriate component based on active tab.
- Matrix tab: two-column layout. Left: MatrixFilterPanel (240px, border-r, bg-muted). Right: MatrixTable (flex-1, overflow-x-auto).
- On mobile (<1024px): filter panel collapses to Sheet triggered by a "Filters" button.

State management:
- Filter state: `useState<FilterState>` with shape `{ orgTypes: string[], sectionId?: string, decisionOutcomes: string[], versionFrom?: string, versionTo?: string }`.
- Pass single-value filter conversions to tRPC: the tRPC matrix procedure accepts single orgType/decisionOutcome. For multi-select (orgTypes checkbox group), pass the first selected value or undefined. OR better: update the tRPC procedure to accept arrays if needed. Use client-side multi-filter pattern from Phase 4: server accepts single value, client filters for multi-select display.
- Matrix data: `trpc.traceability.matrix.useQuery({ documentId, ...filters })`.
- Section list for filter: `trpc.document.getById.useQuery({ id: documentId })` or use a sections list if available.

**Step 3: Create `matrix-filter-panel.tsx`.**

Per UI-SPEC filter panel spec:
- Heading: "FILTERS" (12px/400, uppercase, tracking-wide).
- Filter: Org Type — Checkbox group. Options: Government, Industry, Legal, Academia, Civil Society, Internal.
- Filter: Section — Select dropdown. Label "Section". Options populated from document sections.
- Filter: Decision Outcome — Checkbox group. Options: Accepted, Partially Accepted, Rejected, Closed.
- Filter: Version Range — Two Select fields. Labels "From version" / "To version". Options from available version labels.
- "Clear all" ghost button — only shown when any filter is active.
- Props: `{ filters, onFiltersChange, sections, versions }`.
- Follow the FilterState/onFiltersChange pattern from Phase 4 feedback filter-panel.tsx.
- For mobile: export a `MobileFilterTrigger` sub-component that renders a Sheet with the same filter content inside.

**Step 4: Create `matrix-table.tsx`.**

Per UI-SPEC matrix table spec:
- shadcn `Table` component with `TableCaption` "Traceability Matrix".
- Column headers (bg-muted, 12px/400, uppercase): Feedback, Change Request, Section, Version, Decision, Rationale.
- Column widths per spec: Feedback 200px min, CR 200px min, Section 160px min, Version 100px min, Decision 140px min, Rationale 240px max.
- First column (Feedback) is sticky: `position: sticky; left: 0; z-index: 1; bg-background` on the `<td>` element.
- Container: `overflow-x-auto` with `min-width: 800px`.

Row rendering:
- Props: `{ rows: MatrixRow[] }` where MatrixRow is the return type of traceability.matrix.
- **Client-side deduplication**: Group rows by feedbackId. For each unique feedbackId, display one row. If a feedback item appears multiple times due to multiple sections per CR, show the section names comma-separated or show the first section.
- FB cell: Badge with FB-NNN (monospace via `font-geist-mono`) + feedback title (truncated). Click opens FeedbackDetail Sheet if that component exists from Phase 4, otherwise navigates to feedback detail.
- CR cell: Badge with CR-NNN (monospace) + CR title (truncated). If crId is null, show "--" in muted-foreground. Click navigates to `/policies/[id]/change-requests/[crId]`.
- Section cell: Badge with section name.
- Version cell: Badge with version label (monospace). If null, show "--". Click navigates to `/policies/[id]/versions`.
- Decision cell: Reuse Phase 4 StatusBadge component. Import from `../../feedback/_components/status-badge`.
- Rationale cell: 14px/400 text, truncated to 2 lines with CSS (`line-clamp-2`). Full text in Tooltip on hover.

Row states:
- Hover: `bg-muted/50`.
- Empty state: centered div with Network icon (48px, muted-foreground), heading "No traceability data yet", body copy per UI-SPEC.
- Loading: 6 Skeleton rows with 6 columns each.

**Step 5: Create `export-actions.tsx`.**

Two buttons in a flex row:
- "Export CSV": Button default variant, size="sm". Download icon (from lucide-react) + "Export CSV" label.
- "Export PDF": Button outline variant, size="sm". FileText icon + "Export PDF" label.

onClick handlers:
- Construct URL: `/api/export/traceability/csv?documentId=${documentId}` (plus current filter params).
- For CSV: `window.open(url)` or `fetch(url).then(res => res.blob()).then(blob => { const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'traceability.csv'; a.click() })`.
- Same pattern for PDF with pdf endpoint.
- Loading state: replace icon with Loader2 (animate-spin), label changes to "Exporting..." / "Generating...", button disabled.
- Error state: show toast via sonner on fetch failure with message "Export failed. Check your connection and try again."
- Props: `{ documentId: string, filters: FilterState }`.

**Step 6: Create `search-view.tsx` STUB.**

Create a minimal placeholder component that Plan 03 will replace with the full implementation:
```typescript
'use client'

export function SearchView({ documentId }: { documentId: string }) {
  return (
    <div className="flex flex-col items-center justify-center py-16 text-muted-foreground">
      <p className="text-sm">Search view loading...</p>
    </div>
  )
}
```
This stub allows page.tsx to import SearchView without a build error. Plan 03 will overwrite this file with the full debounced search implementation.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f "app/(workspace)/policies/[id]/traceability/page.tsx"
    - grep -q "'use client'" "app/(workspace)/policies/[id]/traceability/page.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/matrix-table.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/matrix-filter-panel.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/search-view.tsx"
    - grep -q "trpc.traceability.matrix" "app/(workspace)/policies/[id]/traceability/page.tsx"
    - grep -q "Export CSV" "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
    - grep -q "Export PDF" "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
    - grep -q "/api/export/traceability" "app/(workspace)/policies/[id]/traceability/_components/export-actions.tsx"
    - grep -q "FILTERS" "app/(workspace)/policies/[id]/traceability/_components/matrix-filter-panel.tsx"
    - grep -q "Traceability Matrix" "app/(workspace)/policies/[id]/traceability/page.tsx"
    - grep -q "sticky" "app/(workspace)/policies/[id]/traceability/_components/matrix-table.tsx"
  </acceptance_criteria>
  <done>
    Traceability page renders at /policies/[id]/traceability with 4-tab layout (Matrix tab active by default). Matrix tab shows two-column layout with 240px filter panel (org type, section, decision outcome, version range) and horizontally scrollable table with sticky first column. Export CSV and Export PDF buttons trigger file downloads from Route Handlers. Search tab renders placeholder stub. Workspace nav or policy sub-nav includes Traceability link.
  </done>
</task>

<task type="auto">
  <name>Task 2: By Section, By Stakeholder tabs</name>
  <read_first>
    - app/(workspace)/policies/[id]/traceability/page.tsx (just created — understand tab structure)
    - .planning/phases/07-traceability-search/07-UI-SPEC.md (sections: Tab By Section, Tab By Stakeholder, Component TraceabilityChainBadge)
    - src/server/routers/traceability.ts (sectionChain and stakeholderOutcomes procedure shapes)
    - app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx (reuse)
    - app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx (reuse)
    - app/(workspace)/feedback/outcomes/page.tsx (existing outcomes page — evaluate whether to update or supersede)
    - app/(workspace)/feedback/outcomes/_components/outcomes-list.tsx (existing outcomes component)
    - components/ui/card.tsx (Card component API)
    - components/ui/select.tsx (Select for section/stakeholder selectors)
    - components/ui/badge.tsx (badge component)
    - components/ui/skeleton.tsx (loading states)
  </read_first>
  <files>
    app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx,
    app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx,
    app/(workspace)/policies/[id]/traceability/_components/traceability-chain-badge.tsx
  </files>
  <action>
**Step 1: Create `traceability-chain-badge.tsx` (shared component).**

Per UI-SPEC TraceabilityChainBadge spec:
- Renders inline: `[FB-NNN] -> [CR-NNN] -> [Section name] -> [v0.2]`
- Each element: Badge component (variant=secondary/outline), monospace for IDs and version labels (`font-mono text-xs`), plain text for section name.
- Arrow separator: `->` in muted-foreground, mx-1.
- Each badge is a clickable Link: FB -> feedback detail, CR -> CR detail, Version -> versions page.
- If CR not present: show `[FB-NNN] -> [--] -> [Section name] -> [--]` with "--" in muted-foreground.
- Props: `{ feedbackReadableId, crReadableId?, sectionTitle?, versionLabel?, documentId }`.

**Step 2: Create `section-chain-view.tsx` (TRACE-04).**

Per UI-SPEC "By Section" tab spec:
- Layout: single column, max-width 800px, mx-auto.
- Top: Section selector using shadcn `Select`. Label "Select a section". Options populated from `trpc.document.getById` (sections list) or a dedicated sections query.
- When no section selected: empty state with FileText icon (48px, muted-foreground), "Select a section above to see what changed and why."
- When section selected: heading `<h2>` "Changes to: {section title}" (20px/600).
- Data: `trpc.traceability.sectionChain.useQuery({ sectionId }, { enabled: !!sectionId })`.
- Group results by version transition: group rows by versionLabel/versionId. Each group becomes a Card.
- Card layout per UI-SPEC:
  - CardHeader: version transition `<h3>` "v{from} -> v{to} . {merge date}" in 12px/400 muted-foreground. (If only one version, show "-> v{label} . {date}").
  - CR link row: GitPullRequest icon + CR-NNN badge (monospace) + CR title + CRStatusBadge. Clicking navigates to CR detail page.
  - Linked feedback list: each row is FB-NNN badge (monospace) + feedback title + StatusBadge. Clicking could open FeedbackDetail Sheet or navigate.
  - Decision rationale: "Rationale: {rationale text}" in 14px/400 body text. Full text, not truncated.
- Empty state (section has no changes): GitCommit icon (48px, muted-foreground), "No changes recorded for this section yet."
- Loading: 2 Skeleton cards with inner rows.

**Step 3: Create `stakeholder-outcomes.tsx` (TRACE-05).**

Per UI-SPEC "By Stakeholder" tab spec:
- Layout: single column, max-width 800px, mx-auto.
- Stakeholder selector: only shown for Admin/Policy Lead roles. Use shadcn Select. Label "View outcomes for". Options: users with Stakeholder role (fetch via `trpc.user.list` or a new lightweight query). For Stakeholder role: auto-scope to own feedback, hide selector.
- Determine current user role client-side: use existing auth pattern (e.g., useAuth from Clerk or user context if available).
- Data: `trpc.traceability.stakeholderOutcomes.useQuery({ documentId, userId })`.
- Summary stats row: 4 stat cards in a flex row. Each: number (20px/600) + label (12px/400, muted-foreground). Stats: Total, Accepted, Rejected, Pending. Compute from data: total=all, accepted=status accepted+partially_accepted, rejected=status rejected, pending=status submitted+under_review.
- Outcome cards: one Card per feedback item.
  - CardHeader row: FB-NNN badge (monospace, muted) + Title (14px/600, truncated 1 line) + Outcome badge (StatusBadge, aligned right).
  - Below: Section name + "." + date submitted, both in 12px/400 muted-foreground.
  - CardBody: "Decision: {rationale text}" in 14px/400. Full text.
  - Version influence note (only when resolvedInVersionId is set): CheckCircle icon (12px, green from lucide-react) + "Influenced version {versionLabel}" in 12px/400 muted-foreground.
  - CardFooter (only if outcome is Rejected and no version influence): Info icon (12px, muted-foreground) + "No version influence recorded" in 12px/400.
- Empty state: User icon (48px, muted-foreground), "No feedback outcomes to display."
- Loading: Skeleton stats row (4 x h-10 w-24) + 3 skeleton cards.

**Decision on Phase 4 Outcomes page:** The Phase 7 By Stakeholder tab supersedes the Phase 4 `/feedback/outcomes` page by adding version influence data. Add a note/link from the old outcomes page to the new traceability stakeholder view. Do NOT delete the old page (it serves stakeholders who browse without a document context). The Phase 7 tab is document-scoped (within `/policies/[id]/traceability`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f "app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx"
    - test -f "app/(workspace)/policies/[id]/traceability/_components/traceability-chain-badge.tsx"
    - grep -q "sectionChain" "app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx"
    - grep -q "stakeholderOutcomes" "app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx"
    - grep -q "Influenced version" "app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx"
    - grep -q "Select a section" "app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx"
    - grep -q "No changes recorded" "app/(workspace)/policies/[id]/traceability/_components/section-chain-view.tsx"
    - grep -q "No feedback outcomes" "app/(workspace)/policies/[id]/traceability/_components/stakeholder-outcomes.tsx"
  </acceptance_criteria>
  <done>
    By Section tab shows a section selector and renders version transition cards grouped by merged version, each card containing CR link, linked feedback list with outcome badges, and full rationale text. By Stakeholder tab shows summary stats (Total, Accepted, Rejected, Pending) and outcome cards with version influence labels when resolvedInVersionId exists. TraceabilityChainBadge renders the FB->CR->Section->Version chain inline with clickable badges.
  </done>
</task>

</tasks>

<verification>
- `/policies/[id]/traceability` renders with 4 tabs visible
- Matrix tab shows filter panel + table with 6 columns
- By Section tab shows section selector, cards appear after selection
- By Stakeholder tab shows summary stats + outcome cards
- Export buttons trigger file download
- Traceability accessible from policy navigation
</verification>

<success_criteria>
- TRACE-02: Matrix grid visible with all chain columns
- TRACE-03: Filter panel with org type, section, decision, version range checkboxes/selects
- TRACE-04: By Section view shows "What changed and why" cards
- TRACE-05: By Stakeholder view shows feedback outcomes with version influence
- TRACE-06: Export CSV and PDF buttons trigger downloads
</success_criteria>

<output>
After completion, create `.planning/phases/07-traceability-search/07-02-SUMMARY.md`
</output>
