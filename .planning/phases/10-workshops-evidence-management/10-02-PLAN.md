---
phase: 10-workshops-evidence-management
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/(workspace)/workshops/page.tsx
  - app/(workspace)/workshops/new/page.tsx
  - app/(workspace)/workshops/[id]/page.tsx
  - app/(workspace)/workshops/[id]/edit/page.tsx
  - app/(workspace)/workshops/[id]/_components/artifact-list.tsx
  - app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx
  - app/(workspace)/workshops/[id]/_components/section-link-picker.tsx
  - app/(workspace)/workshops/[id]/_components/feedback-link-picker.tsx
  - app/(workspace)/workshops/_components/workshop-card.tsx
  - app/(workspace)/workshops/_components/delete-workshop-dialog.tsx
  - app/(workspace)/_components/workspace-nav.tsx
  - app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx
autonomous: true
requirements:
  - WS-01
  - WS-02
  - WS-03
  - WS-04
  - WS-05

must_haves:
  truths:
    - "Workshop Moderator sees 'Workshops' link in workspace nav"
    - "Workshop Moderator can create a workshop with title, description, date, duration, registration link"
    - "Workshop list page shows upcoming and past workshops in separate tabs"
    - "Workshop detail page shows metadata, linked sections, linked feedback, and artifacts"
    - "Workshop Moderator can upload/attach artifacts with type classification (promo, recording, summary, attendance, other)"
    - "Workshop Moderator can link/unlink policy sections to a workshop"
    - "Workshop Moderator can link/unlink feedback items to a workshop"
    - "Workshop Moderator dashboard shows real data (upcoming count, artifact count, top 3 upcoming)"
  artifacts:
    - path: "app/(workspace)/workshops/page.tsx"
      provides: "Workshop list with Upcoming/Past tabs"
      contains: "Upcoming"
    - path: "app/(workspace)/workshops/new/page.tsx"
      provides: "Create workshop form"
      contains: "Create Workshop"
    - path: "app/(workspace)/workshops/[id]/page.tsx"
      provides: "Workshop detail with two-column layout"
      contains: "Artifacts"
    - path: "app/(workspace)/workshops/[id]/edit/page.tsx"
      provides: "Edit workshop form"
      contains: "Save Changes"
    - path: "app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx"
      provides: "Artifact upload dialog with file/link toggle and type select"
      contains: "Attach Artifact"
    - path: "app/(workspace)/workshops/[id]/_components/section-link-picker.tsx"
      provides: "Section link picker sheet with cross-document section search"
      contains: "Link Policy Sections"
    - path: "app/(workspace)/workshops/[id]/_components/feedback-link-picker.tsx"
      provides: "Feedback link picker sheet with search"
      contains: "Link Feedback Items"
    - path: "app/(workspace)/_components/workspace-nav.tsx"
      provides: "Workshops nav link for workshop_moderator and admin roles"
      contains: "workshops"
    - path: "app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx"
      provides: "Real-data dashboard replacing stub"
      contains: "Manage Workshops"
  key_links:
    - from: "app/(workspace)/workshops/page.tsx"
      to: "src/server/routers/workshop.ts"
      via: "trpc.workshop.list.useQuery"
      pattern: "trpc\\.workshop\\.list"
    - from: "app/(workspace)/workshops/new/page.tsx"
      to: "src/server/routers/workshop.ts"
      via: "trpc.workshop.create.useMutation"
      pattern: "trpc\\.workshop\\.create"
    - from: "app/(workspace)/workshops/[id]/page.tsx"
      to: "src/server/routers/workshop.ts"
      via: "trpc.workshop.getById.useQuery"
      pattern: "trpc\\.workshop\\.getById"
    - from: "app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx"
      to: "app/api/uploadthing/core.ts"
      via: "useUploadThing('evidenceUploader')"
      pattern: "useUploadThing"
    - from: "app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx"
      to: "src/db/schema/workshops.ts"
      via: "direct DB queries (Server Component)"
      pattern: "import.*db.*from.*@/src/db"
---

<objective>
Build the complete workshop UI: list page with upcoming/past tabs, create/edit forms, detail page with artifact management and section/feedback linking, workspace nav integration, and replace the workshop moderator dashboard stub with real data.

Purpose: Workshop Moderators need a full CRUD surface to manage consultation events, attach artifacts, and link workshops to sections and feedback. This makes workshops a first-class entity in PolicyDash.
Output: 5 route pages (/workshops, /workshops/new, /workshops/[id], /workshops/[id]/edit), 5 components, updated workspace nav, and replaced dashboard stub.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-workshops-evidence-management/10-RESEARCH.md
@.planning/phases/10-workshops-evidence-management/10-UI-SPEC.md
@.planning/phases/10-workshops-evidence-management/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 that the executor will consume -->

From src/server/routers/workshop.ts (created in Plan 01):
```typescript
// workshop.create: input { title, description?, scheduledAt, durationMinutes?, registrationLink? }
// workshop.list: input { filter: 'upcoming' | 'past' | 'all' } → returns workshops with creatorName
// workshop.getById: input { workshopId } → workshop + sections + feedback + artifactCount
// workshop.update: input { workshopId, title?, description?, scheduledAt?, durationMinutes?, registrationLink? }
// workshop.delete: input { workshopId }
// workshop.attachArtifact: input { workshopId, title, type, url, artifactType, fileName?, fileSize? }
// workshop.removeArtifact: input { workshopId, artifactId }
// workshop.listArtifacts: input { workshopId } → artifacts with uploaderName, artifactType
// workshop.linkSection: input { workshopId, sectionId }
// workshop.unlinkSection: input { workshopId, sectionId }
// workshop.linkFeedback: input { workshopId, feedbackId }
// workshop.unlinkFeedback: input { workshopId, feedbackId }
```

From src/db/schema/workshops.ts (created in Plan 01):
```typescript
export const workshops = pgTable('workshops', {
  id, title, description, scheduledAt, durationMinutes, registrationLink, createdBy, createdAt, updatedAt
})
export const workshopArtifacts = pgTable('workshop_artifacts', { id, workshopId, artifactId, artifactType })
export const workshopSectionLinks = pgTable('workshop_section_links', { workshopId, sectionId })
export const workshopFeedbackLinks = pgTable('workshop_feedback_links', { workshopId, feedbackId })
```

From app/(workspace)/_components/workspace-nav.tsx:
```typescript
interface WorkspaceNavProps { userRole?: string }
// baseNavItems array to extend
// Conditional: if (userRole === 'admin' || userRole === 'auditor') → audit link
```

From app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx:
```typescript
interface WorkshopModeratorDashboardProps { userId: string }
// Currently a stub with StatCard placeholders — replace entirely
```

From app/(workspace)/dashboard/_components/stat-card.tsx:
```typescript
// StatCard component with props: { icon, value, label }
```

From app/(workspace)/policies/[id]/feedback/_components/evidence-attachment.tsx:
```typescript
// Reuse pattern: useUploadThing('evidenceUploader'), drop zone, file/link toggle
```

From src/trpc/client.ts:
```typescript
export const trpc = createTRPCReact<AppRouter>()
```

From src/lib/uploadthing.ts:
```typescript
export const { useUploadThing, uploadFiles } = generateReactHelpers<OurFileRouter>()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Workshop list, create, edit pages + workspace nav</name>
  <read_first>
    - app/(workspace)/policies/page.tsx (card grid layout pattern to match)
    - app/(workspace)/policies/new/page.tsx OR relevant create-form pattern
    - app/(workspace)/_components/workspace-nav.tsx (current nav items)
    - .planning/phases/10-workshops-evidence-management/10-UI-SPEC.md lines 135-206 (Workshop List, Create Workshop, Edit Workshop specs)
    - components/ui/card.tsx (Card component API)
    - components/ui/tabs.tsx (Tabs API)
  </read_first>
  <files>
    app/(workspace)/workshops/page.tsx
    app/(workspace)/workshops/new/page.tsx
    app/(workspace)/workshops/[id]/edit/page.tsx
    app/(workspace)/workshops/_components/workshop-card.tsx
    app/(workspace)/workshops/_components/delete-workshop-dialog.tsx
    app/(workspace)/_components/workspace-nav.tsx
  </files>
  <action>
1. **Workshop list page** (`app/(workspace)/workshops/page.tsx`):
   - 'use client' component
   - Page heading "Workshops" (h1, 20px/600)
   - "Create Workshop" Button (default variant, Plus icon, 16px) — visible only when canManage (role check). Link to /workshops/new via render prop: `render={<Link href="/workshops/new" />}`
   - Tabs component with "Upcoming" (default) and "Past" triggers
   - State: activeTab ('upcoming' | 'past'), pass as filter to trpc.workshop.list.useQuery({ filter: activeTab })
   - Workshop card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
   - Skeleton loading: 3 skeleton cards when isLoading
   - Empty state per tab: Calendar icon (48px, muted-foreground centered), heading + body copy from UI-SPEC copywriting contract (different text for upcoming vs past)
   - Role check for canManage: derive from `trpc.user.me` query or pass from layout. Use a simple approach: fetch user, check role === 'workshop_moderator' || role === 'admin'.

2. **WorkshopCard component** (`app/(workspace)/workshops/_components/workshop-card.tsx`):
   - Card with CardHeader, CardContent, CardFooter per UI-SPEC layout:
     - Header row: title (16px/600) left, DropdownMenu trigger (MoreHorizontal icon) right — role-gated
     - Header row 2: Calendar icon (14px, muted-foreground) + formatted date using `format(parseISO(scheduledAt), 'MMM d, yyyy · h:mm a')` from date-fns
     - CardContent: description truncated 2 lines (line-clamp-2, 14px/400, muted-foreground)
     - CardFooter row: Duration badge (Badge variant="secondary", "{N} min") left, "Register" ghost button (ExternalLink icon) right when registrationLink exists
   - DropdownMenu items: "Edit Workshop" (navigates to /workshops/[id]/edit), separator, "Delete Workshop" (destructive, triggers DeleteWorkshopDialog)
   - Card hover: border-primary/20, shadow-sm transition
   - Card click: navigate to /workshops/[id]
   - Props: workshop data object + canManage boolean

3. **DeleteWorkshopDialog** (`app/(workspace)/workshops/_components/delete-workshop-dialog.tsx`):
   - AlertDialog with title "Delete Workshop"
   - Description: "This will permanently delete \"{title}\" and all its artifacts. This action cannot be undone."
   - Cancel: "Keep Workshop" (AlertDialogCancel)
   - Confirm: "Delete Workshop" (AlertDialogAction, destructive variant)
   - On confirm: call trpc.workshop.delete.useMutation, toast success "Workshop deleted.", invalidate workshop.list, close dialog
   - Props: workshopId, workshopTitle, open, onOpenChange

4. **Create workshop page** (`app/(workspace)/workshops/new/page.tsx`):
   - 'use client' component
   - Back link: Button ghost size="sm" with ArrowLeft icon + "Back to Workshops" — links to /workshops
   - Page heading: h1 "Create Workshop" (20px/600)
   - Form inside Card component:
     - Title: Input, label "Title", placeholder "e.g., Stakeholder Consultation Session 1", required, autoFocus
     - Description: Textarea, label "Description", placeholder "Brief overview of the workshop's agenda and objectives", rows=4, optional
     - Date & Time: `<input type="datetime-local">` styled with shadcn Input className exactly as specified in UI-SPEC (the full className string from line 621). Label "Date & Time", required.
     - Duration: Input type="number" min="1", label "Duration (minutes)", placeholder "e.g., 90", optional
     - Registration Link: Input type="url", label "Registration Link", placeholder "https://...", optional
   - Footer: "Discard" (outline, navigates to /workshops) and "Create Workshop" (default, disabled until title and scheduledAt are non-empty)
   - On submit: trpc.workshop.create.useMutation. Convert datetime-local value to ISO string. On success: navigate to /workshops/[id], toast "Workshop created."
   - Loading state: submit button shows Loader2 animate-spin, disabled
   - Validation: title min 1 max 200, description max 2000, scheduledAt required, durationMinutes positive int, registrationLink HTTPS URL

5. **Edit workshop page** (`app/(workspace)/workshops/[id]/edit/page.tsx`):
   - Same layout as create page but:
     - Page heading: "Edit Workshop"
     - Back link: "Back to Workshop" links to /workshops/[id]
     - All fields pre-populated from trpc.workshop.getById
     - Submit button: "Save Changes"
     - On submit: trpc.workshop.update.useMutation. On success: navigate to /workshops/[id], toast "Workshop updated."

6. **Workspace nav update** (`app/(workspace)/_components/workspace-nav.tsx`):
   - Add workshop nav item conditionally: `if (userRole === 'workshop_moderator' || userRole === 'admin')` push `{ href: '/workshops', label: 'Workshops' }`
   - Insert BEFORE the audit check (so workshops appears before audit in nav order)

Import toast from 'sonner' directly (not shadcn wrapper). Use date-fns `format` and `parseISO` for date display. Use `useRouter` from 'next/navigation' for programmatic navigation.
  </action>
  <acceptance_criteria>
    - grep "Workshops" app/(workspace)/workshops/page.tsx returns page heading
    - grep "workshop.list" app/(workspace)/workshops/page.tsx returns tRPC query
    - grep "Create Workshop" app/(workspace)/workshops/new/page.tsx returns submit button text
    - grep "workshop.create" app/(workspace)/workshops/new/page.tsx returns mutation
    - grep "Save Changes" app/(workspace)/workshops/[id]/edit/page.tsx returns submit button text
    - grep "workshop.update" app/(workspace)/workshops/[id]/edit/page.tsx returns mutation
    - grep "Delete Workshop" app/(workspace)/workshops/_components/delete-workshop-dialog.tsx returns alert dialog title
    - grep "workshops" app/(workspace)/_components/workspace-nav.tsx returns nav item
    - grep "workshop_moderator" app/(workspace)/_components/workspace-nav.tsx returns role check
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Workshop list page with Upcoming/Past tabs, create/edit form pages, delete dialog, workshop card component, and workspace nav update all render and compile. Navigation flow works: list -> create -> detail, list -> card -> detail, detail -> edit -> detail.</done>
</task>

<task type="auto">
  <name>Task 2: Workshop detail page with artifacts, section/feedback pickers, and dashboard replacement</name>
  <read_first>
    - app/(workspace)/policies/[id]/feedback/_components/evidence-attachment.tsx (upload pattern to reuse)
    - app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx (artifact display pattern)
    - app/(workspace)/dashboard/_components/research-lead-dashboard.tsx (Server Component dashboard pattern)
    - app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx (stub to replace)
    - app/(workspace)/dashboard/_components/stat-card.tsx (StatCard props)
    - .planning/phases/10-workshops-evidence-management/10-UI-SPEC.md lines 209-335 (Workshop Detail, Attach Artifact dialog, Section Link Picker, Feedback Link Picker specs)
    - .planning/phases/10-workshops-evidence-management/10-UI-SPEC.md lines 392-405 (Workshop Moderator Dashboard spec)
    - src/lib/uploadthing.ts (useUploadThing import)
    - components/ui/dialog.tsx (Dialog API)
    - components/ui/command.tsx (Command search API)
  </read_first>
  <files>
    app/(workspace)/workshops/[id]/page.tsx
    app/(workspace)/workshops/[id]/_components/artifact-list.tsx
    app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx
    app/(workspace)/workshops/[id]/_components/section-link-picker.tsx
    app/(workspace)/workshops/[id]/_components/feedback-link-picker.tsx
    app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx
  </files>
  <action>
1. **Workshop detail page** (`app/(workspace)/workshops/[id]/page.tsx`):
   - 'use client' component
   - Fetch workshop data via trpc.workshop.getById.useQuery({ workshopId: params.id })
   - Two-column layout per UI-SPEC:
     - Left panel: 320px fixed, border-r, bg-muted. Contains:
       - Workshop title (h1, Display 28px/600)
       - Date row: Calendar icon (14px, muted-foreground) + formatted date
       - Duration row: Clock icon (14px, muted-foreground) + "{N} minutes" (conditional)
       - Registration link: Button ghost size="sm" with ExternalLink icon (conditional)
       - Edit button: Button outline size="sm" with Pencil icon, links to /workshops/[id]/edit. Role-gated.
       - Description: p tag, Body 14px/400, muted-foreground
       - Separator
       - "LINKED SECTIONS" heading (h2, Label 12px/400, uppercase tracking-wide)
       - "Link Section" button (ghost, size="sm", Plus icon). Role-gated. Opens SectionLinkPicker.
       - Linked section list: each item shows section title (14px/400) + document name (12px/400, muted-foreground) + Unlink X button (ghost, size="icon"). Unlink calls trpc.workshop.unlinkSection, toasts "Section unlinked."
       - Empty state: "No sections linked yet." (12px/400, muted-foreground)
       - Separator
       - "LINKED FEEDBACK" heading (same style)
       - "Link Feedback" button (same pattern). Opens FeedbackLinkPicker.
       - Linked feedback list: each item shows feedback ID (12px/400 font-mono, muted-foreground) + truncated title (14px/400) + Unlink X button. Unlink calls trpc.workshop.unlinkFeedback, toasts "Feedback item unlinked."
       - Empty state: "No feedback items linked yet."
     - Right content area (flex-1):
       - "Artifacts" heading (h2, 20px/600)
       - "Attach Artifact" button (default, Paperclip icon). Role-gated. Opens ArtifactAttachDialog.
       - ArtifactList component
   - Responsive: on < 1024px (lg), collapse to single-column stacked layout (left panel content first, then artifacts below)
   - Role check: canManage = user role is workshop_moderator or admin
   - Skeleton loading state for both panels

2. **ArtifactList component** (`app/(workspace)/workshops/[id]/_components/artifact-list.tsx`):
   - Props: workshopId, canManage
   - Fetches trpc.workshop.listArtifacts.useQuery({ workshopId })
   - Each artifact item per UI-SPEC layout:
     - Row with: File/Link icon (Paperclip for file, Link2 for link, 16px, muted-foreground)
     - Column: Title (14px/600) + ArtifactType Badge (secondary) on first row
     - Uploader name (12px/400, muted-foreground) + timestamp (12px/400, muted-foreground, use date-fns formatDistanceToNow) on second row
     - File info row (conditional, file type): fileName + fileSize human-readable
     - View/Open button (ghost, size="sm", ExternalLink icon, links to artifact URL in new tab)
     - Remove button (ghost, size="icon", Trash2 icon) — role-gated, confirms via AlertDialog "Remove Artifact": "This will remove \"{title}\" from this workshop. The evidence artifact record is preserved. This action cannot be undone."
   - Artifact type badge: capitalize first letter of artifactType, Badge variant="secondary"
   - Empty state: Paperclip icon (40px, muted-foreground centered), "No artifacts yet" + "Attach promo materials, recordings, summaries, or attendance records to this workshop."
   - On remove: trpc.workshop.removeArtifact.mutate, toast "Artifact removed.", invalidate listArtifacts

3. **ArtifactAttachDialog** (`app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx`):
   - Dialog (base-ui pattern, max-width 520px)
   - Title: "Attach Artifact", Description: "Upload a file or add a link to attach it to this workshop."
   - Type toggle: two Buttons in a row — "File" and "Link". Active = default variant, inactive = outline variant. State: artifactInputType
   - Artifact title field: Input, label "Title", required
   - Artifact type select: Select with options "Promo", "Recording", "Summary", "Attendance", "Other". Required.
   - File upload (file mode): Drop zone reusing the exact pattern from evidence-attachment.tsx (useUploadThing('evidenceUploader'), border-dashed, drag events, hidden file input). Same WCAG touch target (44px min-height).
   - Link URL field (link mode): Input type="url", label "URL", required
   - Upload progress: Progress component shown during file upload
   - Footer: "Discard" (outline) and "Attach Artifact" (default, disabled until title + type + file/url filled)
   - Flow: On file mode — upload file via useUploadThing, on upload complete call trpc.workshop.attachArtifact with URL + metadata. On link mode — call trpc.workshop.attachArtifact directly.
   - On success: toast "Artifact attached.", invalidate workshop.listArtifacts, close dialog
   - On error: toast from copywriting contract

4. **SectionLinkPicker** (`app/(workspace)/workshops/[id]/_components/section-link-picker.tsx`):
   - Sheet-style slide-in from right (base-ui Dialog, 480px width). Per Phase 4 pattern.
   - Title: "Link Policy Sections"
   - Search: Command > CommandInput, placeholder "Search sections..."
   - Fetch all sections from all documents via trpc.document.list + trpc.document.getById or a simpler approach: use a dedicated query or load all sections. Use trpc.document.list to get documents, then for each fetch sections. Alternatively, create a flat sections query. Choose simplest: fetch documents with their sections using available tRPC procedures.
   - Group sections by document title (h3, Label 12px/400, uppercase, muted-foreground)
   - Each section: Checkbox + section title (14px/400) + document name (12px/400, muted-foreground). Pre-checked if already linked (compare against workshop.getById response sections list).
   - Client-side filter on search input
   - Footer: "Save Links" (default) and "Discard" (outline)
   - On save: compute delta (newly checked vs unchecked). Call trpc.workshop.linkSection for each new link, trpc.workshop.unlinkSection for each removed link. Toast "Section linked." / "Section unlinked." Invalidate workshop.getById.
   - Save button disabled until at least one change from initial state

5. **FeedbackLinkPicker** (`app/(workspace)/workshops/[id]/_components/feedback-link-picker.tsx`):
   - Sheet-style (same as SectionLinkPicker)
   - Title: "Link Feedback Items"
   - Search: Input, placeholder "Search by ID or title..." — client-side filter
   - Fetch feedback items. Use trpc.feedback.list (or equivalent available query) to get all feedback.
   - Each item: Checkbox + feedback ID (12px/400, muted-foreground font-mono) + truncated title (14px/400) + status Badge. Pre-checked if already linked.
   - Same save/discard pattern as SectionLinkPicker. Delta computation for link/unlink calls.
   - Toast on success: "Feedback item linked." / "Feedback item unlinked."

6. **Replace WorkshopModeratorDashboard** (`app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx`):
   - Server Component with direct DB queries (same pattern as research-lead-dashboard.tsx)
   - Import db, workshops, workshopArtifacts from schema, gte, asc, count from drizzle-orm, Link from next/link
   - Promise.all:
     - Upcoming workshops: select top 3 from workshops where scheduledAt >= now, orderBy asc(scheduledAt)
     - Upcoming count: count(*) from workshops where scheduledAt >= now
     - Total artifacts count: count(*) from workshopArtifacts
   - Layout per UI-SPEC:
     - Two stat cards: "Upcoming Workshops" count + "Total Artifacts" count (reuse StatCard component)
     - Card with "Upcoming Workshops" title and "Manage Workshops" Button (default variant, render prop Link to /workshops)
     - List of top 3 upcoming workshops: title (14px/400) + date (12px/400, muted-foreground with Calendar icon)
     - Empty state: "No upcoming workshops. Create your first workshop to get started." (12px/400, muted-foreground)
   - "Manage Workshops" button uses `render={<Link href="/workshops" />}` pattern (not asChild)

Import toast from 'sonner' directly. Use date-fns format/parseISO/formatDistanceToNow. Use useRouter from 'next/navigation' for navigation.
  </action>
  <acceptance_criteria>
    - grep "workshop.getById" app/(workspace)/workshops/[id]/page.tsx returns query
    - grep "LINKED SECTIONS" app/(workspace)/workshops/[id]/page.tsx returns heading
    - grep "LINKED FEEDBACK" app/(workspace)/workshops/[id]/page.tsx returns heading
    - grep "Attach Artifact" app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx returns dialog title
    - grep "useUploadThing" app/(workspace)/workshops/[id]/_components/artifact-attach-dialog.tsx returns upload hook
    - grep "Link Policy Sections" app/(workspace)/workshops/[id]/_components/section-link-picker.tsx returns sheet title
    - grep "Link Feedback Items" app/(workspace)/workshops/[id]/_components/feedback-link-picker.tsx returns sheet title
    - grep "Manage Workshops" app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx returns CTA
    - grep "workshopArtifacts" app/(workspace)/dashboard/_components/workshop-moderator-dashboard.tsx returns DB import
    - grep "unlinkSection" app/(workspace)/workshops/[id]/page.tsx returns mutation call
    - grep "unlinkFeedback" app/(workspace)/workshops/[id]/page.tsx returns mutation call
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Workshop detail page renders two-column layout with metadata, linked sections/feedback (with link/unlink), artifact list with upload dialog. Section and feedback picker sheets work. Workshop moderator dashboard shows real data. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All workshop pages exist under app/(workspace)/workshops/
- Workspace nav shows "Workshops" link for workshop_moderator/admin roles
- Dashboard stub replaced with real data component
- Artifact attach dialog uses useUploadThing('evidenceUploader') pattern
- Section/feedback pickers use base-ui Dialog sheet pattern
</verification>

<success_criteria>
Workshop Moderator can navigate to /workshops, create workshops, view detail with two-column layout, attach artifacts (file upload + links), link/unlink sections and feedback items, and see real dashboard data. All roles can view workshop list and detail in read-only mode. TypeScript compilation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workshops-evidence-management/10-02-SUMMARY.md`
</output>
