---
phase: 10-workshops-evidence-management
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/(workspace)/feedback/evidence-gaps/page.tsx
  - app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx
  - app/(workspace)/dashboard/_components/research-lead-dashboard.tsx
autonomous: true
requirements:
  - EV-03
  - EV-04

must_haves:
  truths:
    - "Research Lead can view /feedback/evidence-gaps page listing all feedback items without evidence"
    - "Evidence-gaps page filters by document, section, and feedback type"
    - "Non-Research-Lead and non-Admin users are redirected away from /feedback/evidence-gaps"
    - "Evidence list items display uploader name and formatted timestamp"
    - "Research Lead dashboard links to /feedback/evidence-gaps"
  artifacts:
    - path: "app/(workspace)/feedback/evidence-gaps/page.tsx"
      provides: "Claims Without Evidence full page with filters and table"
      contains: "Claims Without Evidence"
    - path: "app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx"
      provides: "Enhanced evidence list with uploader name and timestamp display"
      contains: "uploaderName"
    - path: "app/(workspace)/dashboard/_components/research-lead-dashboard.tsx"
      provides: "Updated dashboard with link to /feedback/evidence-gaps"
      contains: "evidence-gaps"
  key_links:
    - from: "app/(workspace)/feedback/evidence-gaps/page.tsx"
      to: "src/server/routers/evidence.ts"
      via: "trpc.evidence.claimsWithoutEvidence.useQuery"
      pattern: "claimsWithoutEvidence"
    - from: "app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx"
      to: "src/server/routers/evidence.ts"
      via: "trpc.evidence.listByFeedback.useQuery (now returns uploaderName)"
      pattern: "uploaderName"
    - from: "app/(workspace)/dashboard/_components/research-lead-dashboard.tsx"
      to: "app/(workspace)/feedback/evidence-gaps/page.tsx"
      via: "Link href='/feedback/evidence-gaps'"
      pattern: "evidence-gaps"
---

<objective>
Build the "Claims Without Evidence" full page for Research Leads and enhance the evidence list component to display uploader metadata (EV-04).

Purpose: Research Leads need a dedicated view to identify and action feedback items lacking evidence — currently only a 5-item widget on the dashboard. Evidence artifacts need complete metadata display including who uploaded them and when.
Output: New /feedback/evidence-gaps page with filters and table, enhanced evidence-list.tsx with uploader name + timestamp, updated research lead dashboard link.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-workshops-evidence-management/10-RESEARCH.md
@.planning/phases/10-workshops-evidence-management/10-UI-SPEC.md
@.planning/phases/10-workshops-evidence-management/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/server/routers/evidence.ts (enhanced in Plan 01):
```typescript
// evidence.claimsWithoutEvidence: input { documentId?, sectionId?, feedbackType? }
// Returns: { id, readableId, title, feedbackType, sectionId, sectionName, documentId, documentTitle, createdAt }[]

// evidence.listByFeedback: input { feedbackId }
// Returns: { id, title, type, url, fileName, fileSize, uploaderId, uploaderName, createdAt }[]
// (uploaderName is NEW from Plan 01 EV-04 enhancement)

// evidence.listBySection: input { sectionId }
// Returns same shape as listByFeedback (with uploaderName)
```

From app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx:
```typescript
interface EvidenceListProps { feedbackId: string; canAdd: boolean }
// Currently shows: type icon, title/fileName, fileSize, download/open button
// EV-04 adds: uploaderName display, formatted timestamp
```

From app/(workspace)/dashboard/_components/research-lead-dashboard.tsx:
```typescript
// Has "Review Evidence" link pointing to "/feedback?evidence=none"
// And "View all evidence gaps" link also to "/feedback?evidence=none"
// Both should be updated to "/feedback/evidence-gaps"
```

From src/trpc/client.ts:
```typescript
export const trpc = createTRPCReact<AppRouter>()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Claims Without Evidence page + evidence list EV-04 enhancement + dashboard link</name>
  <read_first>
    - app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx (current component to enhance)
    - app/(workspace)/dashboard/_components/research-lead-dashboard.tsx (update links)
    - .planning/phases/10-workshops-evidence-management/10-UI-SPEC.md lines 337-389 (Claims Without Evidence page spec + Evidence Metadata Display spec)
    - components/ui/table.tsx (Table component API)
    - components/ui/select.tsx (Select component API for filters)
    - components/ui/badge.tsx (Badge for feedback type)
  </read_first>
  <files>
    app/(workspace)/feedback/evidence-gaps/page.tsx
    app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx
    app/(workspace)/dashboard/_components/research-lead-dashboard.tsx
  </files>
  <action>
1. **Claims Without Evidence page** (`app/(workspace)/feedback/evidence-gaps/page.tsx`):
   - 'use client' component
   - Role gate: fetch current user, if role is not 'research_lead' and not 'admin', redirect to /dashboard using router.replace. Show nothing while checking. Alternatively, use a Server Component wrapper that checks role and redirects, then render client content. Choose the simpler approach: client component with role check via trpc.user.me or similar query.
   - Page heading: h1 "Claims Without Evidence" (20px/600)
   - Page description: p "Feedback items with no supporting evidence attached." (14px/400, muted-foreground)
   - Fetch data: trpc.evidence.claimsWithoutEvidence.useQuery({ documentId, sectionId, feedbackType }) — pass current filter values
   - Count summary: p "{N} item{s} found" (14px/400) — N = data.length
   - Filter row (flex wrap gap-4):
     - Document filter: Select with "All Documents" default + each unique documentTitle from data. Client-side: derive unique documents from full unfiltered data set OR do a separate document list query. Simpler: use trpc.document.list to get documents, populate select options. On change, update documentId state. When document changes, reset sectionId.
     - Section filter: Select with "All Sections" default + sections from selected document. When documentId is set, load sections for that document. Client-side filter.
     - Feedback type filter: Select with "All Types" default + "Issue", "Suggestion", "Endorsement", "Evidence", "Question"
   - Filter behavior per UI-SPEC: three independent filters, Document narrows Section, all applied client-side after initial full-list fetch. Call the tRPC query with NO server filters initially (pass no params), then filter client-side. This matches the Phase 4 multi-filter pattern.
   - Actually, claimsWithoutEvidence accepts optional filters. Use them for server-side narrowing when only a single filter is applied, but for multi-filter, use client-side. Simplest approach: always send documentId if set, feedbackType if set, and filter sectionId client-side. This gives good server performance.
   - Results table using Table component:
     - Columns: ID (80px, font-mono 12px/400 muted-foreground), Title (flex-1, 14px/400 truncate), Type (120px, Badge variant="secondary"), Section (160px, 12px/400 muted-foreground), Submitted (100px, 12px/400 muted-foreground formatted date), Action (140px, Button ghost size="sm" with Paperclip icon "Attach Evidence")
     - "Attach Evidence" navigates to the feedback detail (e.g., /policies/[docId]/feedback/[feedbackId]) or opens evidence attachment inline. Simplest: link to a feedback detail route. The exact route depends on how feedback detail works. Use the pattern from research-lead-dashboard.tsx which links to `/feedback/${fb.id}/evidence`. Follow that.
     - Row hover: bg-muted/50
     - Use `aria-label="Attach evidence to {readableId}: {title}"` on the action button
   - Empty state: CheckCircle icon (40px, muted-foreground centered), "No gaps found" heading, "All feedback items have supporting evidence attached." body
   - Loading state: 5 skeleton table rows (Skeleton h-10 w-full)
   - Format submitted date with date-fns `format(parseISO(createdAt), 'MMM d, yyyy')`

2. **Enhance evidence-list.tsx** (`app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx`):
   - The listByFeedback query now returns `uploaderName` and `createdAt` from Plan 01 enhancement.
   - Update each evidence item display to add:
     - Below the title/fileName row, add a metadata row: "Uploaded by {uploaderName}" (12px/400, muted-foreground) + " · " + relative timestamp using date-fns formatDistanceToNow(parseISO(createdAt), { addSuffix: true }) — e.g., "3 days ago"
     - Wrap timestamp in `<time dateTime={createdAt}>` for accessibility
     - Fallback: if uploaderName is null/undefined, show "Unknown"
   - Keep existing file type icon (Paperclip for file, Link2 for link) and size/download button patterns
   - Add type badge: Badge variant="secondary" showing "File" or "Link" next to title

3. **Update research-lead-dashboard.tsx** (`app/(workspace)/dashboard/_components/research-lead-dashboard.tsx`):
   - Change the "Review Evidence" button link from `/feedback?evidence=none` to `/feedback/evidence-gaps`
   - Change the "View all evidence gaps" button link from `/feedback?evidence=none` to `/feedback/evidence-gaps`
   - Both are Button with render prop: update the href in the Link component

Import toast from 'sonner', date-fns functions from 'date-fns', Badge from '@/components/ui/badge'.
  </action>
  <acceptance_criteria>
    - grep "Claims Without Evidence" app/(workspace)/feedback/evidence-gaps/page.tsx returns page heading
    - grep "claimsWithoutEvidence" app/(workspace)/feedback/evidence-gaps/page.tsx returns tRPC query
    - grep "All Documents" app/(workspace)/feedback/evidence-gaps/page.tsx returns filter option
    - grep "Attach Evidence" app/(workspace)/feedback/evidence-gaps/page.tsx returns action button
    - grep "uploaderName" app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx returns metadata field
    - grep "formatDistanceToNow" app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx returns date formatting
    - grep "evidence-gaps" app/(workspace)/dashboard/_components/research-lead-dashboard.tsx returns updated link
    - grep "feedback?evidence=none" app/(workspace)/dashboard/_components/research-lead-dashboard.tsx returns NO match (old link removed)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Evidence-gaps page renders with filters and table showing all feedback without evidence. Evidence list shows uploader name and relative timestamp. Research Lead dashboard links to /feedback/evidence-gaps. Role-gated access enforced. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- /feedback/evidence-gaps page exists with role protection
- Evidence list component displays uploaderName and timestamp
- Research Lead dashboard links point to /feedback/evidence-gaps
</verification>

<success_criteria>
Research Lead can navigate to /feedback/evidence-gaps and see all feedback items without evidence, filter by document/section/type, and click "Attach Evidence" to action gaps. Evidence list throughout the app shows who uploaded each artifact and when. Dashboard widget links to the full page.
</success_criteria>

<output>
After completion, create `.planning/phases/10-workshops-evidence-management/10-03-SUMMARY.md`
</output>
