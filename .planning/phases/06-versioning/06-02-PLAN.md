---
phase: 06-versioning
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(workspace)/policies/[id]/versions/page.tsx
  - app/(workspace)/policies/[id]/versions/[versionId]/page.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-list.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-card.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-detail.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-changelog.tsx
  - app/(workspace)/policies/[id]/versions/_components/version-comparison-selector.tsx
  - app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx
  - app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx
  - app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05, VER-06, VER-07]

must_haves:
  truths:
    - "User can see a list of all versions for a document ordered newest first"
    - "User can see version label (v0.N in monospace), status badge (Draft/Published), creator, and date"
    - "User can view a version's auto-generated changelog showing CRs and feedback IDs"
    - "User can compare two versions and see section-level diff with green/red highlighting"
    - "Previous versions are displayed as read-only archives"
    - "Admin/Policy Lead can publish a version via confirmation dialog"
    - "Published versions show immutable lock indicator with no edit affordances"
    - "Admin/Policy Lead can manually create a version with notes"
  artifacts:
    - path: "app/(workspace)/policies/[id]/versions/page.tsx"
      provides: "Version history page entry point"
    - path: "app/(workspace)/policies/[id]/versions/_components/version-list.tsx"
      provides: "Left panel version list with selection state"
    - path: "app/(workspace)/policies/[id]/versions/_components/version-detail.tsx"
      provides: "Right panel version detail with changelog, diff, publish"
    - path: "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
      provides: "Inline diff renderer using diff package output"
    - path: "app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx"
      provides: "Publish confirmation with immutability warning"
    - path: "app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx"
      provides: "Manual version creation with notes field"
  key_links:
    - from: "app/(workspace)/policies/[id]/versions/_components/version-detail.tsx"
      to: "version.getById tRPC query"
      via: "trpc.version.getById.useQuery"
      pattern: "trpc\\.version\\.getById"
    - from: "app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx"
      to: "version.publish tRPC mutation"
      via: "trpc.version.publish.useMutation"
      pattern: "trpc\\.version\\.publish"
    - from: "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
      to: "version.diff tRPC query"
      via: "trpc.version.diff.useQuery"
      pattern: "trpc\\.version\\.diff"
---

<objective>
Build the complete version history UI: two-panel layout with version list (left) and version detail (right), changelog display, section-level diff comparison with inline highlighting, publish confirmation dialog, and manual version creation dialog.

Purpose: Delivers the user-facing version history, diff viewing, and publish workflow that completes all VER requirements.
Output: Version history page with 11 component files under app/(workspace)/policies/[id]/versions/
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-versioning/06-RESEARCH.md
@.planning/phases/06-versioning/06-UI-SPEC.md
@.planning/phases/06-versioning/06-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that the UI consumes -->

From src/server/services/version.service.ts (created in Plan 01):
```typescript
export interface SectionSnapshot {
  sectionId: string
  title: string
  orderIndex: number
  content: Record<string, unknown>
}

export interface ChangelogEntry {
  crId: string | null
  crReadableId: string | null
  crTitle: string
  summary: string
  feedbackIds: string[]
  affectedSectionIds: string[]
}
```

From src/server/routers/version.ts (created in Plan 01):
```typescript
// version.list returns:
{ id, documentId, versionLabel, mergeSummary, createdBy, crId, createdAt, isPublished, publishedAt, creatorName }[]

// version.getById returns:
{ id, documentId, versionLabel, mergeSummary, createdBy, crId, createdAt, isPublished, publishedAt, sectionsSnapshot, changelog, creatorName }

// version.diff returns:
{ sectionId, titleA, titleB, status: 'added'|'removed'|'modified'|'unchanged', diff: Change[]|null }[]

// version.createManual input:
{ documentId: string, notes: string (min 10, max 2000) }

// version.publish input:
{ id: string }
```

From src/lib/permissions.ts (extended in Plan 01):
```typescript
'version:read':    [ADMIN, POLICY_LEAD, AUDITOR, OBSERVER, RESEARCH_LEAD, STAKEHOLDER]
'version:manage':  [ADMIN, POLICY_LEAD]
'version:publish': [ADMIN, POLICY_LEAD]
```

From app/globals.css (extended in Plan 01):
```css
--diff-added-bg, --diff-added-text, --diff-removed-bg, --diff-removed-text
--status-cr-approved-bg, --status-cr-approved-text (reuse for Published badge)
--status-cr-merged-bg, --status-cr-merged-text (reuse for Immutable badge)
```

From app/(workspace)/policies/[id]/page.tsx (existing page pattern):
```typescript
// Client component using useParams, useRouter, trpc.xxx.useQuery
// Loading: Skeleton components
// Error: centered error message
// Two-panel: sidebar (280px) + content area (flex-1)
```

From app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx (back link pattern):
```tsx
<Button variant="ghost" size="sm">
  <ArrowLeft className="mr-1 h-4 w-4" />
  Back to Change Requests
</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version history page, list panel, detail view, status badge, changelog</name>
  <files>app/(workspace)/policies/[id]/versions/page.tsx, app/(workspace)/policies/[id]/versions/_components/version-list.tsx, app/(workspace)/policies/[id]/versions/_components/version-card.tsx, app/(workspace)/policies/[id]/versions/_components/version-detail.tsx, app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx, app/(workspace)/policies/[id]/versions/_components/version-changelog.tsx</files>
  <read_first>
    - app/(workspace)/policies/[id]/page.tsx (two-panel layout pattern)
    - app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx (server component page pattern with params Promise)
    - app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx (detail view with back link, tRPC query, loading/error)
    - app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx (list page with heading pattern)
    - app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx (dialog pattern)
    - components/ui/button.tsx (Button component API)
    - components/ui/skeleton.tsx (Skeleton API)
    - components/ui/scroll-area.tsx (ScrollArea API)
    - components/ui/separator.tsx (Separator API)
    - components/ui/select.tsx (Select API for mobile version selector)
    - src/server/routers/version.ts (version router procedures -- understand return shapes)
    - .planning/phases/06-versioning/06-UI-SPEC.md (complete UI contract)
    - node_modules/next/dist/docs/ (verify page component conventions for Next.js 16)
  </read_first>
  <action>
    Create the version history page and core display components per the UI-SPEC.

    **A. app/(workspace)/policies/[id]/versions/page.tsx** (per VER-05):
    - 'use client' directive
    - Import useParams from 'next/navigation', trpc client, useState
    - Get documentId from useParams<{ id: string }>()
    - State: selectedVersionId (string | null), default null (auto-select latest on data load)
    - Query: trpc.version.list.useQuery({ documentId })
    - Auto-select latest version (first in list) on initial load via useEffect
    - Layout: Two-panel on lg+, single column on mobile
      - Desktop (lg+): flex row, VersionList (280px fixed, border-r, bg-muted, sticky) + VersionDetail (flex-1, max-w-[800px])
      - Mobile (<lg): VersionList renders as a Select dropdown at top, VersionDetail below
    - Back link: Button ghost sm with ArrowLeft + "Back to Policy" -> router.push(`/policies/${documentId}`)
    - Page heading: h1 "Version History" at 20px/600
    - Pass selectedVersionId and onSelect to VersionList and VersionDetail

    **B. app/(workspace)/policies/[id]/versions/_components/version-list.tsx** (per VER-01, VER-05):
    - Props: versions array (from list query), selectedVersionId, onSelect callback, isLoading boolean
    - Desktop view (hidden on <lg): ScrollArea with panel heading "VERSIONS" (label 12px/400 uppercase tracking-wide), version count "{N} versions", then list of VersionCard items
    - Each VersionCard is a button. Active state: bg-background, left border 2px var(--primary). Hover: bg-background/50.
    - "New Version" button (Plus icon, outline, sm) at bottom of panel. This opens CreateVersionDialog (rendered in page.tsx or here). Only shown to Admin/Policy Lead -- check user role from tRPC context (use a separate trpc.user.me query or pass canManage prop from parent).
    - Loading state: 4 Skeleton items h-10 w-full
    - Mobile view (shown on <lg, hidden on lg+): Select component listing all versions, value=selectedVersionId

    **C. app/(workspace)/policies/[id]/versions/_components/version-card.tsx** (per VER-01):
    - Props: version object (from list), isSelected boolean, onSelect callback
    - Layout: full-width button with:
      - Row 1: version label badge (monospace 12px, bg-muted rounded px-1.5 py-0.5) + VersionStatusBadge (right-aligned)
      - Row 2: created date (12px, muted-foreground, format with date-fns: "MMM d, yyyy") + creator name (12px, muted-foreground)
    - Active: bg-background, border-l-2 border-primary
    - Version label uses font-mono (var(--font-geist-mono))

    **D. app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx** (per VER-06, VER-07):
    - Props: isPublished boolean, publishedAt string | null
    - Three states:
      1. Draft: bg-muted text-muted-foreground, text "Draft"
      2. Published: bg-[var(--status-cr-approved-bg)] text-[var(--status-cr-approved-text)], text "Published"
      3. Published + Immutable: bg-[var(--status-cr-merged-bg)] text-[var(--status-cr-merged-text)], Lock icon (14px) + text "Published . Immutable", aria-label="This version is immutable"
    - Note: For Phase 6, all published versions are immutable. Use the indigo treatment for all published versions.
    - Badge styling: inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs font-medium

    **E. app/(workspace)/policies/[id]/versions/_components/version-detail.tsx** (per VER-03, VER-05, VER-06, VER-07):
    - Props: versionId (string), documentId (string)
    - Query: trpc.version.getById.useQuery({ id: versionId }, { enabled: !!versionId })
    - Loading: Skeleton heading + metadata rows + changelog lines
    - Layout:
      1. Version label: h2 in font-mono, 20px/600 (e.g. "v0.2")
      2. VersionStatusBadge
      3. Metadata rows (12px/400, muted-foreground):
         - "Created by {name} on {date}" (format: "MMM d, yyyy 'at' h:mm a")
         - If crId: "From merge of CR-NNN" (monospace badge, link to /policies/{docId}/change-requests/{crId}). Else: "Manually created"
         - If published: "Published on {publishedAt date}"
      4. Separator
      5. Changelog section heading "CHANGELOG" (12px/400 uppercase tracking-wide muted-foreground)
      6. VersionChangelog component (renders changelog entries)
      7. Publish button (if Draft and user has version:publish permission): Button default with Lock icon + "Publish Version" -> opens PublishDialog
         OR immutable lock message (if Published): muted-foreground text "This version is published and immutable. No further edits are possible."
      8. Separator
      9. Diff section heading "SECTION DIFF" (12px/400 uppercase tracking-wide muted-foreground)
      10. VersionComparisonSelector + SectionDiffView (rendered below)

    **F. app/(workspace)/policies/[id]/versions/_components/version-changelog.tsx** (per VER-03):
    - Props: changelog (ChangelogEntry[] | null)
    - If null or empty: show muted text "No changelog entries."
    - For each entry: bullet point with:
      - CR badge: crReadableId in monospace badge (if not null)
      - CR title + summary text (14px/400/1.5)
      - Linked feedback: inline FB-NNN badges (monospace, 12px, bg-muted rounded px-1.5 py-0.5)
    - Manual entries (crId=null): show summary text only, no CR badge

    All components use 'use client' directive. All tRPC dates are strings (per Phase 2 decision) -- parse with new Date() for formatting with date-fns format().
  </action>
  <verify>
    <automated>cd D:/aditee/policydash && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test -f "app/(workspace)/policies/[id]/versions/page.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-list.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-card.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-detail.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-changelog.tsx"
    - grep -q "use client" "app/(workspace)/policies/[id]/versions/page.tsx"
    - grep -q "version.list" "app/(workspace)/policies/[id]/versions/page.tsx"
    - grep -q "version.getById" "app/(workspace)/policies/[id]/versions/_components/version-detail.tsx"
    - grep -q "font-mono" "app/(workspace)/policies/[id]/versions/_components/version-card.tsx"
    - grep -q "Published" "app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx"
    - grep -q "Immutable" "app/(workspace)/policies/[id]/versions/_components/version-status-badge.tsx"
    - grep -q "CHANGELOG" "app/(workspace)/policies/[id]/versions/_components/version-changelog.tsx"
    - TypeScript compiles (npx tsc --noEmit)
  </acceptance_criteria>
  <done>Version history page renders two-panel layout with version list (left) and detail (right), version labels in monospace, status badges (Draft/Published/Immutable), changelog with CR and feedback ID badges, metadata rows, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Publish dialog, create version dialog, comparison selector, section diff view</name>
  <files>app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx, app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx, app/(workspace)/policies/[id]/versions/_components/version-comparison-selector.tsx, app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx</files>
  <read_first>
    - app/(workspace)/policies/[id]/versions/_components/version-detail.tsx (just created -- where these plug in)
    - app/(workspace)/policies/[id]/versions/page.tsx (just created -- where CreateVersionDialog is triggered)
    - app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx (dialog pattern with textarea, char counter, loading state)
    - app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx (confirmation dialog with rationale pattern)
    - components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter API)
    - components/ui/textarea.tsx (Textarea API)
    - components/ui/select.tsx (Select, SelectTrigger, SelectValue, SelectContent, SelectItem API)
    - src/server/routers/version.ts (diff procedure return shape, createManual input, publish input)
    - .planning/phases/06-versioning/06-UI-SPEC.md (PublishVersionDialog, CreateVersionDialog, VersionComparisonSelector, SectionDiffView specs)
  </read_first>
  <action>
    Create the interactive components: publish confirmation, manual version creation, version comparison, and diff view.

    **A. app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx** (per VER-06, VER-07):
    - Props: open, onOpenChange, versionId (string), versionLabel (string), documentTitle (string), changelog (ChangelogEntry[] | null)
    - Mutation: trpc.version.publish.useMutation with onSuccess -> toast.success("Version {label} published."), invalidate version.list and version.getById, close dialog. onError -> toast.error("Couldn't publish the version. Check your connection and try again.")
    - Layout (max-width 520px dialog):
      1. DialogTitle: "Publish Version" (20px/600)
      2. DialogDescription: "Publishing makes this version visible on the public portal. Published versions are immutable -- they cannot be edited after publishing."
      3. Version context row: bg-muted rounded-md p-3, "Publishing: {versionLabel} (monospace) -- {documentTitle}" (14px/400)
      4. Changelog preview: bg-muted rounded-md p-4, max-h-[200px] overflow-y-auto. Label "CHANGELOG" (12px/400 uppercase muted-foreground). Content: changelog entries (14px/400/1.5) or "No changelog entries." if empty.
      5. Immutability warning: AlertTriangle icon (14px, muted-foreground) + "This action cannot be undone. Once published, this version is permanently locked." (14px/400, muted-foreground)
      6. Footer: justify-between layout (per a11y spec -- prevent accidental click on irreversible action). Cancel button (outline) on left. Publish button (default variant, Lock icon + "Publish Version") on right. Loading: Loader2 spin replacing Lock icon.

    **B. app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx** (per VER-02):
    - Props: open, onOpenChange, documentId (string)
    - State: notes (string), default ""
    - Prefetch: trpc.version.list.useQuery to compute next version label client-side (count existing + 1, format as v0.N). Or better: use a query that calls the backend getNextVersionLabel -- but that is not exposed as a separate tRPC procedure. Instead, compute from the version list: find the latest version label, parse v0.N, show v0.(N+1) as preview. If no versions, show v0.1.
    - Mutation: trpc.version.createManual.useMutation with onSuccess -> toast.success("Version {label} created."), invalidate version.list, close dialog, reset notes. onError -> toast.error("Couldn't create the version. Check your connection and try again.")
    - Layout (max-width 480px dialog):
      1. DialogTitle: "Create Version" (20px/600)
      2. DialogDescription: "Create a new version snapshot of the current document state."
      3. Version preview: Label (12px/400, muted-foreground) "New version label:" + computed label badge (monospace, bg-muted, rounded). Read-only.
      4. Textarea: Label "Version Notes *", placeholder "Describe the state of this version. This will appear in the changelog.", rows 4, required, minLength 10, maxLength 2000. Value bound to notes state.
      5. Character counter: "{N}/2000" right-aligned (12px/400, muted-foreground). Turn destructive color if notes.length < 10 and notes.length > 0.
      6. Footer: Cancel (outline) + "Create Version" (default). Disabled until notes.length >= 10. Loading: Loader2 spin.
    - Autofocus textarea on open.

    **C. app/(workspace)/policies/[id]/versions/_components/version-comparison-selector.tsx** (per VER-04):
    - Props: versions array (from list query), documentId, sections array (from document.getSections query -- fetch inline or accept as prop)
    - State: baseVersionId, targetVersionId, selectedSectionId, showDiff (boolean)
    - Fetch sections: trpc.document.getSections.useQuery({ documentId }) for section dropdown
    - Default: baseVersionId = second-to-last version (or null if only 1), targetVersionId = latest version, selectedSectionId = first section
    - Layout: Row on desktop (3 selects + swap + compare button), stack on mobile
      1. "Compare from" Select: lists all versions by label (monospace in options). Value: baseVersionId.
      2. Swap button: ghost, icon, ArrowLeftRight, aria-label="Swap versions". Swaps base and target.
      3. "to" Select: lists all versions by label. Value: targetVersionId.
      4. "Section" Select: lists all sections by title. Value: selectedSectionId.
      5. "Compare Versions" Button (default, GitCompare icon). Disabled if baseVersionId === targetVersionId. On click: set showDiff=true.
    - When showDiff=true, render SectionDiffView below with baseVersionId, targetVersionId, selectedSectionId
    - Error validation: if base == target, show muted text "Select two different versions to compare."

    **D. app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx** (per VER-04):
    - Props: versionAId, versionBId, versionALabel, versionBLabel, sectionId (optional -- filter diff result to one section)
    - Query: trpc.version.diff.useQuery({ versionAId, versionBId }, { enabled: !!versionAId && !!versionBId && versionAId !== versionBId })
    - Loading: 8 Skeleton rows h-5 w-full with 4px gaps
    - Error: "Couldn't load the diff. Try selecting the sections again." (muted-foreground)
    - Filter result to the selected sectionId if provided
    - For the selected section diff:
      - If status === 'unchanged': centered text "No changes in this section between {vA} and {vB}."
      - If status === 'added': centered text "This section had no content in version {vA}." + show full content as green-highlighted
      - If status === 'removed': centered text "This section had no content in version {vB}." + show full content as red-highlighted
      - If status === 'modified' and diff array exists: render inline diff
    - Diff rendering (inline approach per RESEARCH recommendation):
      - Container: border rounded-md overflow-hidden max-h-[60vh] overflow-y-auto
      - Two-column header (desktop >= 1024px): left "Base" + versionALabel (monospace 12px), right "Target" + versionBLabel (monospace 12px). bg-muted, border-b, p-2.
      - For each Change item in the diff array:
        - part.added: span with bg-[var(--diff-added-bg)] text-[var(--diff-added-text)]
        - part.removed: span with bg-[var(--diff-removed-bg)] text-[var(--diff-removed-text)] line-through
        - neither: span with text-foreground (unchanged)
      - Wrap in font-mono text-sm leading-relaxed
    - Mobile (< 1024px): single column inline diff (same rendering, no side-by-side split)
    - Handle null sectionsSnapshot gracefully: "Snapshot not available for this version."

    **Wire into existing components:**
    - In version-detail.tsx (Task 1): import and render PublishDialog (triggered by Publish button), VersionComparisonSelector (below changelog), SectionDiffView (inside VersionComparisonSelector)
    - In page.tsx (Task 1): import and render CreateVersionDialog (triggered by New Version button in VersionList)

    All components use 'use client'. Import toast from 'sonner' directly (not shadcn wrapper). Use Loader2 from lucide-react for loading spinners.
  </action>
  <verify>
    <automated>cd D:/aditee/policydash && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test -f "app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/version-comparison-selector.tsx"
    - Test -f "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
    - grep -q "version.publish" "app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx"
    - grep -q "AlertTriangle" "app/(workspace)/policies/[id]/versions/_components/publish-dialog.tsx"
    - grep -q "version.createManual" "app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx"
    - grep -q "2000" "app/(workspace)/policies/[id]/versions/_components/create-version-dialog.tsx"
    - grep -q "ArrowLeftRight" "app/(workspace)/policies/[id]/versions/_components/version-comparison-selector.tsx"
    - grep -q "version.diff" "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
    - grep -q "diff-added-bg" "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
    - grep -q "diff-removed-bg" "app/(workspace)/policies/[id]/versions/_components/section-diff-view.tsx"
    - grep -q "PublishDialog\|publish-dialog" "app/(workspace)/policies/[id]/versions/_components/version-detail.tsx"
    - grep -q "CreateVersionDialog\|create-version-dialog" "app/(workspace)/policies/[id]/versions/page.tsx"
    - TypeScript compiles (npx tsc --noEmit)
  </acceptance_criteria>
  <done>Publish dialog with immutability warning and changelog preview, create version dialog with notes textarea and character counter, version comparison selector with three selects and swap button, section diff view with inline green/red highlighting using CSS variables, all wired into version detail and page components, TypeScript compiles</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript compiles with zero errors
2. All 11 component files exist under app/(workspace)/policies/[id]/versions/
3. `npx vitest run --reporter=verbose` -- full test suite passes (no regressions)
4. grep confirms trpc.version.list, trpc.version.getById, trpc.version.publish, trpc.version.createManual, trpc.version.diff are all wired in the UI
5. grep confirms diff CSS variables (--diff-added-bg, --diff-removed-bg) used in section-diff-view.tsx
6. grep confirms all UI copy from UI-SPEC copywriting contract appears in components
</verification>

<success_criteria>
- Version history page at /policies/[id]/versions renders two-panel layout on desktop, single-column on mobile
- Version list shows all versions with monospace labels (v0.N), status badges (Draft/Published/Immutable), dates, creators
- Version detail shows changelog with CR/feedback ID badges, metadata, publish button or immutable indicator
- Publish dialog includes immutability warning, changelog preview, and confirmation flow
- Create version dialog has notes textarea with 10-char minimum, 2000-char max, character counter
- Version comparison selector allows selecting two versions and a section, rendering section-level diff
- Section diff view renders inline word-level diff with green (added) and red (removed) highlighting using CSS variables
- All components are client components using tRPC hooks
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-versioning/06-02-SUMMARY.md`
</output>
