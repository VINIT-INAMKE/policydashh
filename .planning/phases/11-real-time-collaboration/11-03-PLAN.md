---
phase: 11-real-time-collaboration
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - app/(workspace)/policies/[id]/_components/comment-bubble.tsx
  - app/(workspace)/policies/[id]/_components/comment-panel.tsx
  - app/(workspace)/policies/[id]/_components/comment-thread.tsx
  - app/(workspace)/policies/[id]/_components/block-editor.tsx
  - app/(workspace)/policies/[id]/_components/section-content-view.tsx
autonomous: false
requirements: [EDIT-08]

must_haves:
  truths:
    - "User can select text in the editor and see a 'Comment' bubble trigger above the selection"
    - "Clicking 'Comment' opens the CommentPanel with a NewCommentForm anchored to the selection"
    - "Posting a comment applies an InlineComment mark to the selected text and creates a thread in the DB"
    - "User can reply to existing comment threads inline"
    - "User can resolve and reopen comment threads"
    - "Comment anchors are highlighted in the editor with primary/15 background"
    - "CommentPanel shows open and resolved tabs with thread counts"
  artifacts:
    - path: "app/(workspace)/policies/[id]/_components/comment-bubble.tsx"
      provides: "Floating comment trigger above text selection"
      contains: "CommentBubble"
    - path: "app/(workspace)/policies/[id]/_components/comment-panel.tsx"
      provides: "Right-side panel with tabs, thread list, new comment form"
      contains: "CommentPanel"
    - path: "app/(workspace)/policies/[id]/_components/comment-thread.tsx"
      provides: "Individual thread with root comment, replies, resolve/reopen actions"
      contains: "CommentThread"
  key_links:
    - from: "app/(workspace)/policies/[id]/_components/comment-bubble.tsx"
      to: "app/(workspace)/policies/[id]/_components/comment-panel.tsx"
      via: "onCreateComment callback opens panel with selection context"
      pattern: "onCreateComment"
    - from: "app/(workspace)/policies/[id]/_components/comment-panel.tsx"
      to: "src/server/routers/comments.ts"
      via: "trpc.comments.create/list/resolve/reopen/delete/createReply mutations"
      pattern: "trpc\\.comments"
    - from: "app/(workspace)/policies/[id]/_components/comment-panel.tsx"
      to: "src/lib/tiptap-extensions/inline-comment-mark.ts"
      via: "editor.chain().setInlineComment(commentId) applies mark to selected text"
      pattern: "setInlineComment"
---

<objective>
Build the inline comment system: floating CommentBubble trigger on text selection, CommentPanel with threaded discussions, and comment anchor highlighting in the editor. Users can select text, leave comments anchored to that selection, reply to threads, and resolve/reopen discussions.

Purpose: Delivers EDIT-08 (inline comments anchored to text selections). This is the final requirement for Phase 11, enabling structured discussion within the collaborative editor.

Output: CommentBubble floating trigger, CommentPanel (320px right panel with open/resolved tabs), CommentThread component, reply system, resolve/reopen workflow, and comment anchor highlighting.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-real-time-collaboration/11-RESEARCH.md
@.planning/phases/11-real-time-collaboration/11-UI-SPEC.md
@.planning/phases/11-real-time-collaboration/11-01-SUMMARY.md
@.planning/phases/11-real-time-collaboration/11-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and Plan 02. -->

From src/lib/tiptap-extensions/inline-comment-mark.ts (Plan 01):
```typescript
export const InlineComment = Mark.create({
  name: 'inlineComment',
  // Attributes: commentId (data-comment-id)
  // Commands: setInlineComment(commentId: string), unsetInlineComment()
})
```

From src/server/routers/comments.ts (Plan 01):
```typescript
export const commentRouter = router({
  list: ...,          // input: { sectionId: string } => Array<Thread with replies>
  create: ...,        // input: { sectionId, commentId, body } => Thread
  createReply: ...,   // input: { threadId, body } => Reply
  resolve: ...,       // input: { threadId } => void
  reopen: ...,        // input: { threadId } => void
  delete: ...,        // input: { threadId } => void
})
```

From src/db/schema/collaboration.ts (Plan 01):
```typescript
export const commentThreads = pgTable('comment_threads', {
  id, sectionId, commentId, authorId, body, resolved, orphaned, createdAt, updatedAt
})
export const commentReplies = pgTable('comment_replies', {
  id, threadId, authorId, body, createdAt
})
```

From app/(workspace)/policies/[id]/_components/block-editor.tsx (after Plan 02):
```typescript
// BlockEditor now has:
// - providerRef (HocuspocusProvider or null)
// - connectionStatus state
// - editor (Tiptap Editor instance with InlineComment mark in extensions)
// - PresenceBar and ConnectionStatus in header
```

From UI-SPEC Component Inventory:
- CommentBubble: floating above selection, z-60, "Comment" button + MessageSquare icon
- CommentPanel: 320px right panel (inline on >= 1200px, Sheet on < 1200px)
- CommentThread: bg-background, border, 16px padding, replies indented 16px with border-l-2
- NewCommentForm: bg-muted, anchor preview text, Textarea 3 rows, Post/Cancel buttons
- ReplyForm: inline below replies, 2 rows Textarea
- Keyboard shortcut: Ctrl+Alt+M (Cmd+Option+M on Mac)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommentBubble, CommentPanel, CommentThread, and reply/resolve UI</name>
  <read_first>
    - .planning/phases/11-real-time-collaboration/11-UI-SPEC.md (full file -- all component specs, interaction states, copywriting)
    - app/(workspace)/policies/[id]/_components/block-editor.tsx (integration point, current layout)
    - src/lib/tiptap-extensions/inline-comment-mark.ts (setInlineComment/unsetInlineComment commands)
    - src/server/routers/comments.ts (tRPC router interface)
    - components/ui/button.tsx (Button component)
    - components/ui/sheet.tsx (Sheet for mobile CommentPanel)
    - components/ui/tabs.tsx (Tabs for Open/Resolved)
    - components/ui/scroll-area.tsx (ScrollArea for thread list)
    - components/ui/tooltip.tsx (Tooltip for button labels)
    - components/ui/dropdown-menu.tsx (DropdownMenu for thread actions)
    - components/ui/textarea.tsx (Textarea for comment input)
    - app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx (reference for right-side panel pattern)
  </read_first>
  <files>
    app/(workspace)/policies/[id]/_components/comment-bubble.tsx
    app/(workspace)/policies/[id]/_components/comment-panel.tsx
    app/(workspace)/policies/[id]/_components/comment-thread.tsx
  </files>
  <action>
    1. Create `app/(workspace)/policies/[id]/_components/comment-bubble.tsx`:
       - Floating trigger that appears above text selection when user has text selected
       - Uses Tiptap `BubbleMenu` extension or manual selection detection via `editor.on('selectionUpdate')` + `window.getSelection().getRangeAt(0).getBoundingClientRect()`
       - Position: 4px above selection's top edge, horizontally centered
       - Container: inline flex row, bg-background, border border-border, rounded-md, shadow-md, padding 4px 8px, gap-1
       - "Comment" button: Button ghost size="sm", MessageSquare icon (14px) + "Comment" text
       - On click: calls `onCreateComment({ from, to, text })` with selection range and selected text
       - Tooltip on button: "Comment (Ctrl+Alt+M)"
       - `aria-label="Add comment to selected text"` on button
       - z-index: z-60 (above remote cursors)
       - Keyboard shortcut: Register `Ctrl+Alt+M` (Mac: `Cmd+Option+M`) via editor keymap or document-level event listener
       - Disappears when selection collapses (empty selection)

    2. Create `app/(workspace)/policies/[id]/_components/comment-panel.tsx`:
       - Props: `{ editor: Editor; sectionId: string; isOpen: boolean; onClose: () => void; activeCommentId?: string | null; onCommentCreated?: () => void }`
       - Responsive behavior:
         - >= 1200px viewport: inline 320px panel to the right of editor, slides in (translateX animation 200ms)
         - < 1200px viewport: renders as Sheet (slide-in from right) using shadcn Sheet component
         - Use `useMediaQuery` or window.innerWidth check
       - Panel structure per UI-SPEC:
         - Header: "Comments" heading (20px/600) + close button (X icon, ghost, 32px)
         - Tabs: "Open ({N})" | "Resolved ({N})" using shadcn Tabs. Open is default.
         - Body: ScrollArea filling remaining height
         - NewCommentForm (when creating): bg-muted, anchor preview "Commenting on: \"{truncated selection at 40 chars}\"", Textarea 3 rows placeholder "Add a comment...", "Post comment" Button default size="sm" (disabled when empty), "Cancel" Button ghost size="sm". Character limit 2000, counter shown at 1800+.
         - CommentThreadList: renders CommentThread for each thread in active tab
         - Empty state (Open): "No comments yet" heading + "Select text in the document to leave a comment." body
         - Empty state (Resolved): "No resolved comments" + "Resolved threads will appear here."
       - Data fetching: `trpc.comments.list.useQuery({ sectionId })` -- refetch on mutation success
       - Loading state: 3 Skeleton threads (h-5 w-2/3 + h-10 w-full + h-4 w-1/4)
       - New comment flow:
         - Receives `pendingComment: { from: number; to: number; text: string } | null` from parent
         - When pendingComment is set: show NewCommentForm at top
         - On "Post comment": generate `commentId = crypto.randomUUID()`, call `editor.chain().focus().setTextSelection({ from, to }).setInlineComment(commentId).run()` to apply mark, then `trpc.comments.create.mutate({ sectionId, commentId, body })` to save thread
         - On "Cancel": clear pendingComment, deselect

    3. Create `app/(workspace)/policies/[id]/_components/comment-thread.tsx`:
       - Props: `{ thread: ThreadData; onResolve: (id) => void; onReopen: (id) => void; onDelete: (id) => void; onReply: (threadId, body) => void; currentUserId: string; isActive: boolean }`
       - Thread container: bg-background, border border-border, rounded-md, padding 16px, margin-bottom 8px
       - Resolved state: bg-muted, all text muted-foreground, "Resolved" Badge (secondary, 12px)
       - Thread header: avatar circle (20px, presence color if online, muted otherwise) + author name (12px/600) + relative timestamp (12px/400, muted-foreground). Right side: DropdownMenu (MoreHorizontal trigger, 24px)
       - DropdownMenu items per UI-SPEC:
         - Own open thread: "Resolve thread" (CheckCheck icon) + separator + "Delete comment" (destructive)
         - Other's open thread: "Resolve thread" only
         - Resolved thread: "Reopen thread" (RotateCcw icon)
       - Root comment body: 14px/400/1.5, mt-1 below header
       - Reply list: mt-2, each reply indented 16px left with border-l-2 border-border. Reply: author name + timestamp row, body below.
       - "Reply" button: Button ghost size="sm", 12px, muted-foreground. Below last reply.
       - "Resolve" button: Button outline size="sm" at thread bottom (open threads only)
       - Inline ReplyForm on "Reply" click: mt-2, ml-4, border-l-2. Textarea 2 rows, placeholder "Add a reply...", "Post reply" Button default size="sm" (disabled when empty), "Cancel" Button ghost size="sm". 2000 char limit.
       - Delete: immediate fire + toast("Comment deleted.") with "Undo" action (5s window via sonner)
       - Resolve: toast("Thread resolved.")
       - Reopen: toast("Thread reopened.")
       - Loading state on submit: Loader2 spinner on submit button, textarea disabled
       - Error: toast with appropriate message per UI-SPEC copywriting contract

    All timestamps: use relative time format (e.g., "2 minutes ago", "1 hour ago"). Use `Intl.RelativeTimeFormat` or a simple utility function -- no date-fns dependency needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "CommentBubble" app/(workspace)/policies/[id]/_components/comment-bubble.tsx returns match
    - grep -r "MessageSquare" app/(workspace)/policies/[id]/_components/comment-bubble.tsx returns match
    - grep -r "Ctrl+Alt+M\|Cmd+Option+M" app/(workspace)/policies/[id]/_components/comment-bubble.tsx returns match (at least one variant)
    - grep -r "CommentPanel" app/(workspace)/policies/[id]/_components/comment-panel.tsx returns match
    - grep -r "trpc.comments" app/(workspace)/policies/[id]/_components/comment-panel.tsx returns match
    - grep -r "setInlineComment" app/(workspace)/policies/[id]/_components/comment-panel.tsx returns match
    - grep -r "CommentThread" app/(workspace)/policies/[id]/_components/comment-thread.tsx returns match
    - grep -r "Resolve\|Reopen" app/(workspace)/policies/[id]/_components/comment-thread.tsx returns match
    - grep -r "DropdownMenu" app/(workspace)/policies/[id]/_components/comment-thread.tsx returns match
  </acceptance_criteria>
  <done>
    CommentBubble appears above text selection with "Comment" button and Ctrl+Alt+M shortcut. CommentPanel renders as inline panel (>= 1200px) or Sheet (< 1200px) with Open/Resolved tabs. CommentThread shows threaded comments with reply, resolve, reopen, delete. All tRPC comment mutations wired. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CommentBubble and CommentPanel into BlockEditor</name>
  <read_first>
    - app/(workspace)/policies/[id]/_components/block-editor.tsx (current state after Plan 02)
    - app/(workspace)/policies/[id]/_components/comment-bubble.tsx (Task 1)
    - app/(workspace)/policies/[id]/_components/comment-panel.tsx (Task 1)
    - app/(workspace)/policies/[id]/_components/section-content-view.tsx (may need layout adjustment)
    - .planning/phases/11-real-time-collaboration/11-UI-SPEC.md (lines 165-189 for layout change, lines 443-457 for panel states)
  </read_first>
  <files>
    app/(workspace)/policies/[id]/_components/block-editor.tsx
    app/(workspace)/policies/[id]/_components/section-content-view.tsx
  </files>
  <action>
    1. Update `block-editor.tsx` to integrate comment components:

       **New state:**
       - `commentPanelOpen: boolean` (default false)
       - `pendingComment: { from: number; to: number; text: string } | null` (default null)
       - `activeCommentId: string | null` (default null, set when clicking a comment anchor)

       **CommentBubble integration:**
       - Render `<CommentBubble editor={editor} onCreateComment={handleCreateComment} />` after EditorContent
       - `handleCreateComment({ from, to, text })`: sets `pendingComment`, opens CommentPanel

       **CommentPanel integration:**
       - Render `<CommentPanel>` conditionally when `commentPanelOpen` is true
       - Pass: editor, sectionId (section.id), isOpen, onClose, activeCommentId, pendingComment
       - On close: clear pendingComment, clear activeCommentId

       **Comment anchor click handler:**
       - When user clicks on a `.inline-comment-mark` span in the editor, extract `data-comment-id` from the clicked element
       - Set `activeCommentId` to that comment ID
       - Open CommentPanel scrolled to that thread
       - Add active highlight class to the anchor (`.inline-comment-mark.active`)
       - Implementation: Use editor's `handleClick` ProseMirror prop or a custom plugin

       **Layout adjustment for panel:**
       - When CommentPanel is open AND viewport >= 1200px: editor + panel side by side
       - Wrap editor + panel in a flex container: `<div className="flex">`
       - Editor div: `flex-1 max-w-[768px]`
       - CommentPanel div: `w-[320px] shrink-0` (right side)
       - When panel closed: editor uses full available width (existing behavior)

       **Comment panel toggle button:**
       - Add a toggle button in the header bar (near PresenceBar/ConnectionStatus): MessageSquare icon with count badge
       - Badge shows open comment count (from trpc.comments.list data)

    2. Update `section-content-view.tsx` if layout changes are needed:
       - The SectionContentView wraps BlockEditor -- ensure the wrapper div allows the flex layout to expand when CommentPanel opens
       - May need to remove any `max-width` constraints on the container or allow overflow

    3. Verify the full flow:
       - Select text -> CommentBubble appears -> Click "Comment" -> CommentPanel opens with NewCommentForm -> Type comment -> "Post comment" -> InlineComment mark applied to text + thread saved in DB -> Thread appears in panel
       - Click highlighted text -> CommentPanel opens scrolled to that thread
       - Reply to thread -> reply appears inline
       - Resolve thread -> moves to Resolved tab, anchor highlight removed
       - Reopen thread -> moves back to Open tab, anchor highlight restored
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "CommentBubble" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "CommentPanel" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "commentPanelOpen" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "pendingComment" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "activeCommentId" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - grep -r "inline-comment-mark" app/(workspace)/policies/[id]/_components/block-editor.tsx returns match
    - npm test passes
  </acceptance_criteria>
  <done>
    CommentBubble and CommentPanel are wired into BlockEditor. Full comment flow works: select text -> comment bubble -> create thread -> mark applied -> thread in panel. Click anchor to scroll to thread. Reply, resolve, reopen all functional. Layout adjusts for panel on desktop and mobile. All tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete real-time collaboration system</name>
  <files>app/(workspace)/policies/[id]/_components/block-editor.tsx</files>
  <action>
    Human verification of the complete Phase 11 collaboration feature set. All automated tasks are complete.
    Steps:
    1. Start the Hocuspocus server: cd hocuspocus-server and npm run dev
    2. Set NEXT_PUBLIC_HOCUSPOCUS_URL=ws://localhost:1234 in .env.local
    3. Start the Next.js dev server: npm run dev
    4. Open two browser windows/tabs to the same policy section
    5. Verify EDIT-06: Type in one window, text appears in the other within ~1 second
    6. Verify EDIT-07: Both windows show avatar in PresenceBar, remote cursor with name label, green connection dot
    7. Verify EDIT-08: Select text -> Comment bubble -> Post comment -> highlight + thread in panel. Reply, resolve, reopen.
    8. Verify offline fallback: Stop Hocuspocus -> red dot "Offline" -> changes auto-save via tRPC -> restart -> reconnects
  </action>
  <verify>Manual human verification of all 3 requirements (EDIT-06, EDIT-07, EDIT-08)</verify>
  <done>All three requirements verified: real-time editing works across browsers, presence indicators show connected users, inline comments with full lifecycle are functional.</done>
</task>

</tasks>

<verification>
- `npm test` passes (no regressions)
- `npx tsc --noEmit` compiles clean
- Full manual flow: select text -> comment -> post -> anchor highlight -> reply -> resolve -> reopen
- Two-browser test: simultaneous editing, presence indicators, cursor labels
- Offline fallback: auto-save when Hocuspocus disconnected
</verification>

<success_criteria>
Users can select text and leave inline comments anchored to text selections. Comments have threaded replies. Threads can be resolved and reopened. CommentPanel shows open/resolved tabs with correct counts. Comment anchors are highlighted in the editor. The full comment lifecycle (create, reply, resolve, reopen, delete) works via tRPC. Keyboard shortcut Ctrl+Alt+M opens comment creation. Panel is responsive (inline on desktop, Sheet on mobile).
</success_criteria>

<output>
After completion, create `.planning/phases/11-real-time-collaboration/11-03-SUMMARY.md`
</output>
