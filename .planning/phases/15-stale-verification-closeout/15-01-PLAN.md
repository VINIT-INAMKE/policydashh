---
phase: 15-stale-verification-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(workspace)/audit/_components/evidence-pack-dialog.tsx
  - app/(workspace)/dashboard/_components/auditor-dashboard.tsx
  - .planning/phases/04-feedback-system/04-VERIFICATION.md
  - .planning/phases/07-traceability-search/07-VERIFICATION.md
  - .planning/phases/09-public-portal-compliance/09-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FIX-05
  - FIX-06
  - EV-08

must_haves:
  truths:
    - "Clicking a feedback row in /policies/[id]/feedback opens FeedbackDetailSheet with triage actions and decision log"
    - "Traceability page is reachable via PolicyTabBar from any policy detail page (for roles with trace:read)"
    - "Auditor dashboard 'Export Evidence Pack (ZIP)' button opens EvidencePackDialog in-place (no /audit navigation)"
    - "Phase 4, 7, 9 VERIFICATION.md files reflect status: passed with a re-verified 2026-04-13 timestamp"
    - "REQUIREMENTS.md marks FIX-05, FIX-06, EV-08 as [x] complete"
  artifacts:
    - path: "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx"
      provides: "Feedback inbox that mounts FeedbackDetailSheet on card click (already wired — re-verified here)"
      contains: "<FeedbackDetailSheet"
    - path: "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx"
      provides: "Policy tab bar with discoverable Traceability tab (already wired — re-verified here)"
      contains: "/traceability"
    - path: "app/(workspace)/audit/_components/evidence-pack-dialog.tsx"
      provides: "EvidencePackDialog with optional `trigger` prop so non-audit pages can supply their own trigger"
      exports: ["EvidencePackDialog"]
    - path: "app/(workspace)/dashboard/_components/auditor-dashboard.tsx"
      provides: "Auditor dashboard whose Export Evidence Pack (ZIP) button opens the dialog directly"
      contains: "EvidencePackDialog"
    - path: ".planning/phases/04-feedback-system/04-VERIFICATION.md"
      provides: "Phase 4 verification updated to status: passed"
      contains: "status: passed"
    - path: ".planning/phases/07-traceability-search/07-VERIFICATION.md"
      provides: "Phase 7 verification updated to status: passed"
      contains: "status: passed"
    - path: ".planning/phases/09-public-portal-compliance/09-VERIFICATION.md"
      provides: "Phase 9 verification updated to status: passed"
      contains: "status: passed"
  key_links:
    - from: "app/(workspace)/dashboard/_components/auditor-dashboard.tsx"
      to: "app/(workspace)/audit/_components/evidence-pack-dialog.tsx"
      via: "direct component import + in-place render with custom trigger"
      pattern: "import \\{ EvidencePackDialog \\} from"
    - from: "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx"
      to: "app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx"
      via: "controlled mount via selectedFeedbackId state"
      pattern: "<FeedbackDetailSheet"
    - from: "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx"
      to: "app/(workspace)/policies/[id]/traceability/page.tsx"
      via: "Link href gated by canViewTrace"
      pattern: "/traceability"
---

<objective>
Close the three v0.1 audit gaps (FIX-05, FIX-06, EV-08) so no new v0.2 work layers onto stale `gaps_found` verifications.

Two of the three surfaces are already fixed in the codebase (Phase 13 consolidation re-wired FeedbackDetailSheet inside FeedbackInbox; Phase 13-03 added a Traceability tab to PolicyTabBar). This plan RE-VERIFIES those two against the current source, updates the Phase 4 and Phase 7 VERIFICATION.md files to `status: passed`, and applies the real code fix for EV-08 (auditor dashboard "Export Evidence Pack (ZIP)" button still navigates to `/audit` instead of opening the dialog).

Purpose: clear the v0.1 closeout gate before Phase 16 starts touching notification/feedback code paths.
Output: two VERIFICATION.md status flips, one ~5-line code fix to `evidence-pack-dialog.tsx`, one ~6-line edit to `auditor-dashboard.tsx`, and REQUIREMENTS.md checkbox updates for FIX-05/FIX-06/EV-08.

STRICTLY OUT OF SCOPE: no refactors, no cleanup, no warning-level fixes from the VERIFICATION reports (e.g. Phase 4 anti-pattern on `canTriage = true`, Phase 7 stakeholder-outcomes admin user selector, Phase 7 REQUIREMENTS.md checkbox updates for TRACE-01/SRCH-*). Those are separate requirements not listed here.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@AGENTS.md

# Phase source-of-truth verification reports (current status: gaps_found)
@.planning/phases/04-feedback-system/04-VERIFICATION.md
@.planning/phases/07-traceability-search/07-VERIFICATION.md
@.planning/phases/09-public-portal-compliance/09-VERIFICATION.md

# Source files this plan touches or verifies
@app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx
@app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx
@app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx
@app/(workspace)/audit/_components/evidence-pack-dialog.tsx
@app/(workspace)/audit/page.tsx
@app/(workspace)/dashboard/_components/auditor-dashboard.tsx

<interfaces>
<!-- Contracts the executor needs. Do NOT guess — these are extracted from the current files. -->

From app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx (lines 25–29):
```typescript
interface FeedbackDetailSheetProps {
  feedbackId: string | null
  open: boolean
  onOpenChange: (open: boolean) => void
}
```

From app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx (lines 182–186, CURRENT STATE — already wired):
```tsx
<FeedbackDetailSheet
  feedbackId={selectedFeedbackId}
  open={!!selectedFeedbackId}
  onOpenChange={(o) => { if (!o) setSelectedFeedbackId(null) }}
/>
```

From app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx (lines 33–38, CURRENT STATE — already wired):
```tsx
{
  label: 'Traceability',
  href: `/policies/${documentId}/traceability`,
  match: 'startsWith',
  visible: canViewTrace,
},
```

From app/(workspace)/audit/_components/evidence-pack-dialog.tsx (lines 28 and 108–110, CURRENT STATE — needs `trigger` prop):
```tsx
export function EvidencePackDialog() {
  // ... state hooks ...
  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogTrigger render={<Button size="sm"><FileArchive className="size-4" />Export Evidence Pack</Button>} />
      <DialogContent className="sm:max-w-[480px]">
```

From app/(workspace)/audit/page.tsx (lines 7 and 33, CURRENT STATE — props-less usage that MUST keep working):
```tsx
import { EvidencePackDialog } from './_components/evidence-pack-dialog'
// ...
<EvidencePackDialog />
```

From app/(workspace)/dashboard/_components/auditor-dashboard.tsx (lines 1–8 and 105–108, CURRENT STATE — the EV-08 bug):
```tsx
import Link from 'next/link'
// ... other imports ...
import { Download, FileArchive } from 'lucide-react'
import { Button } from '@/components/ui/button'

// inside Export Controls card:
<Button variant="outline" size="sm" render={<Link href="/audit" />}>
  <FileArchive className="size-4" />
  Export Evidence Pack (ZIP)
</Button>
```
</interfaces>

<investigation_result>
Orchestrator pre-flight confirmed these three files are in the state described above. The executor MUST still read each file via `read_first` before editing — line numbers may have shifted between plan-time and execute-time.

Key findings that shape this plan:
- FIX-05 is ALREADY wired in `feedback-inbox.tsx` (Phase 13 consolidation). Task 1 is re-verification only — no source edit, just a grep + VERIFICATION.md status flip.
- FIX-06 is ALREADY wired in `policy-tab-bar.tsx` (Phase 13-03 Plan). Task 2 is re-verification only — no source edit, just a grep + VERIFICATION.md status flip.
- EV-08 is the ONLY real code fix in this phase. `EvidencePackDialog` has a hardcoded internal DialogTrigger. Chosen approach: add an optional `trigger?: React.ReactNode` prop that replaces the default trigger when provided. This preserves backwards compatibility with the props-less call in `audit/page.tsx:33`.
</investigation_result>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-verify FIX-05 (FeedbackDetailSheet mounted) and flip Phase 4 VERIFICATION.md to passed</name>

  <read_first>
    - app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx (must confirm current state before claiming it's passed)
    - app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx (confirm props interface at lines 25–29 matches what's being passed from feedback-inbox.tsx)
    - app/(workspace)/policies/[id]/feedback/page.tsx (confirm FeedbackInbox is the entry point with documentId prop)
    - .planning/phases/04-feedback-system/04-VERIFICATION.md (read the full current file — you will update its frontmatter)
  </read_first>

  <action>
    This task is RE-VERIFICATION ONLY. No source file edits.

    Step 1. Run these grep commands from the repo root and capture the exact matching lines — the plan requires all three to hit:
      a. `grep -n "import { FeedbackDetailSheet" "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx"`
         Expected: line 13 (or wherever) shows `import { FeedbackDetailSheet } from './feedback-detail-sheet'`
      b. `grep -n "<FeedbackDetailSheet" "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx"`
         Expected: exactly one JSX usage inside the component return.
      c. `grep -n "feedbackId={selectedFeedbackId}" "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx"`
         Expected: exactly one match — proves the state variable is wired to the prop.

    Step 2. Confirm the three props on the rendered `<FeedbackDetailSheet>` JSX match the `FeedbackDetailSheetProps` interface at lines 25–29 of `feedback-detail-sheet.tsx`: `feedbackId`, `open`, `onOpenChange`. No extra props, no missing props.

    Step 3. If ANY step above fails, STOP. Do not update the VERIFICATION.md. Report "FIX-05 re-verification failed — the expected wiring is missing; fall back to gap closure planning" and exit this task.

    Step 4. If all checks pass, edit `.planning/phases/04-feedback-system/04-VERIFICATION.md` with the Edit tool. Update ONLY the YAML frontmatter at the top of the file:
      - Change `verified: 2026-03-25T05:30:00Z` to `verified: 2026-04-13T00:00:00Z`
      - Change `status: gaps_found` to `status: passed`
      - Change `score: 8/10 truths verified` to `score: 10/10 truths verified`
      - Change `re_verification: false` to `re_verification: true`
      - Leave the `gaps:` block in place BUT prepend a single-line top-level frontmatter field `re_verified_note: "FIX-05 re-verified 2026-04-13 — FeedbackDetailSheet is now imported and mounted in feedback-inbox.tsx with selectedFeedbackId state controlling open/onOpenChange. Gap closed via v0.1 Phase 13 consolidation, confirmed in v0.2 Phase 15."` directly under the `re_verification: true` line.

    Step 5. Also edit the body of the same file: update the status line in the `# Phase 4: Feedback System Verification Report` section from `**Status:** gaps_found` to `**Status:** passed (re-verified 2026-04-13)` and `**Re-verification:** No — initial verification` to `**Re-verification:** Yes — re-verified 2026-04-13 in v0.2 Phase 15`.

    Do NOT rewrite the full verification report. Do NOT touch the Observable Truths table, Anti-Patterns Found, Gaps Summary, or any other section. Only the frontmatter and the two header lines change.
  </action>

  <verify>
    <automated>grep -c "<FeedbackDetailSheet" "app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx" &amp;&amp; grep -c "^status: passed" ".planning/phases/04-feedback-system/04-VERIFICATION.md" &amp;&amp; grep -c "re_verified_note" ".planning/phases/04-feedback-system/04-VERIFICATION.md"</automated>
  </verify>

  <acceptance_criteria>
    - `feedback-inbox.tsx` contains the exact substring `<FeedbackDetailSheet` (grep -c returns ≥ 1)
    - `feedback-inbox.tsx` contains the exact substring `feedbackId={selectedFeedbackId}`
    - `feedback-inbox.tsx` contains the exact substring `import { FeedbackDetailSheet }`
    - `.planning/phases/04-feedback-system/04-VERIFICATION.md` frontmatter contains `status: passed` (exact line `status: passed`)
    - `.planning/phases/04-feedback-system/04-VERIFICATION.md` frontmatter contains `verified: 2026-04-13T00:00:00Z`
    - `.planning/phases/04-feedback-system/04-VERIFICATION.md` frontmatter contains `re_verification: true`
    - `.planning/phases/04-feedback-system/04-VERIFICATION.md` frontmatter contains `re_verified_note:` with the FIX-05 text
    - `.planning/phases/04-feedback-system/04-VERIFICATION.md` body contains `**Status:** passed (re-verified 2026-04-13)`
    - No source files under `app/`, `src/` are modified by this task (git diff of those dirs for this task is empty)
  </acceptance_criteria>

  <done>
    Phase 4 VERIFICATION.md is stamped `status: passed` with a re-verified 2026-04-13 timestamp, backed by three grep-confirmed wiring signals in feedback-inbox.tsx. No source code was changed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-verify FIX-06 (Traceability tab in PolicyTabBar) and flip Phase 7 VERIFICATION.md to passed</name>

  <read_first>
    - app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx (full file — confirm Traceability tab entry lines 33–38 still exist and the canViewTrace gate is in place)
    - app/(workspace)/policies/[id]/layout.tsx (find the layout that renders PolicyTabBar; confirm canViewTrace is computed from the user role server-side)
    - .planning/phases/07-traceability-search/07-VERIFICATION.md (read full file; you will update its frontmatter)
  </read_first>

  <action>
    RE-VERIFICATION ONLY. No source file edits.

    Step 1. Confirm the Traceability tab is present in PolicyTabBar:
      a. `grep -n "Traceability" "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx"` — expect the label string literal match.
      b. `grep -n "/traceability" "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx"` — expect at least one match for the href template.
      c. `grep -n "canViewTrace" "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx"` — expect at least two matches (prop type + usage in visible gate).

    Step 2. Open the layout that renders PolicyTabBar. Search for the file:
      `grep -rn "PolicyTabBar" "app/(workspace)/policies/[id]/"`
      Read the layout.tsx hit. Confirm it computes `canViewTrace` from the user role (e.g. via `can(user.role, 'trace:read')` or a similar permission helper) and passes it as a prop. If the layout does NOT pass `canViewTrace` at all, STOP and report failure — the tab would never render.

    Step 3. If all checks pass, edit `.planning/phases/07-traceability-search/07-VERIFICATION.md`. Update ONLY the YAML frontmatter:
      - Change `verified: 2026-03-25T00:00:00Z` to `verified: 2026-04-13T00:00:00Z`
      - Change `status: gaps_found` to `status: passed`
      - Change `score: 8/10 truths verified` to `score: 9/10 truths verified (truth #10 re-verified; TRACE-01 REQUIREMENTS.md discrepancy is a separate docs-only concern outside Phase 15 scope)`
      - Add a new top-level frontmatter field (insert after `score:`): `re_verification: true`
      - Add a new top-level frontmatter field: `re_verified_note: "FIX-06 re-verified 2026-04-13 — PolicyTabBar includes a Traceability entry (lines 33–38) with canViewTrace role gating. Added in v0.1 Phase 13-03; confirmed reachable in v0.2 Phase 15."`

    Step 4. Update the body header lines in the same file:
      - `**Status:** gaps_found` → `**Status:** passed (re-verified 2026-04-13)`
      - `**Re-verification:** No -- initial verification` → `**Re-verification:** Yes — re-verified 2026-04-13 in v0.2 Phase 15`

    Do NOT touch the Observable Truths table, the Requirements Coverage table, or any other section. Leave TRACE-01 / SRCH-* REQUIREMENTS.md discrepancies alone — they are not in scope for Phase 15.

    Do NOT touch `workspace-nav.tsx` — the traceability entry is document-scoped (per the Phase 07 decision log), and PolicyTabBar is the correct nav surface.
  </action>

  <verify>
    <automated>grep -c "Traceability" "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx" &amp;&amp; grep -c "canViewTrace" "app/(workspace)/policies/[id]/_components/policy-tab-bar.tsx" &amp;&amp; grep -c "^status: passed" ".planning/phases/07-traceability-search/07-VERIFICATION.md"</automated>
  </verify>

  <acceptance_criteria>
    - `policy-tab-bar.tsx` contains the exact substring `Traceability` (label)
    - `policy-tab-bar.tsx` contains the exact substring `/traceability` (href)
    - `policy-tab-bar.tsx` contains the exact substring `canViewTrace` (role gate prop)
    - The layout file at `app/(workspace)/policies/[id]/layout.tsx` (or whichever layout renders PolicyTabBar) contains the string `canViewTrace` and passes it as a prop — `grep -n "canViewTrace" app/(workspace)/policies/[id]/layout.tsx` returns ≥ 1 match
    - `.planning/phases/07-traceability-search/07-VERIFICATION.md` frontmatter contains the exact line `status: passed`
    - `.planning/phases/07-traceability-search/07-VERIFICATION.md` frontmatter contains `verified: 2026-04-13T00:00:00Z`
    - `.planning/phases/07-traceability-search/07-VERIFICATION.md` frontmatter contains `re_verification: true`
    - `.planning/phases/07-traceability-search/07-VERIFICATION.md` frontmatter contains `re_verified_note:` with the FIX-06 text
    - `.planning/phases/07-traceability-search/07-VERIFICATION.md` body contains `**Status:** passed (re-verified 2026-04-13)`
    - `app/(workspace)/_components/workspace-nav.tsx` is NOT modified (git diff on that file is empty)
    - No source files under `app/`, `src/` are modified by this task
  </acceptance_criteria>

  <done>
    Phase 7 VERIFICATION.md is stamped `status: passed` with a re-verified 2026-04-13 timestamp. The Traceability tab is confirmed wired via PolicyTabBar with proper role gating, making the traceability page discoverable from every policy detail page.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix EV-08 — EvidencePackDialog accepts optional trigger prop; auditor dashboard uses it in-place</name>

  <read_first>
    - app/(workspace)/audit/_components/evidence-pack-dialog.tsx (full file — you are about to change the component signature; know exactly what's there)
    - app/(workspace)/audit/page.tsx (confirm it calls `<EvidencePackDialog />` with zero props at line 33 — this is the backwards-compat constraint)
    - app/(workspace)/dashboard/_components/auditor-dashboard.tsx (full file — you are replacing the Export Evidence Pack button)
    - components/ui/dialog.tsx (skim the re-exported `DialogTrigger` component to confirm `render={<Button .../>}` is the accepted trigger pattern on this project — matches base-ui)
  </read_first>

  <behavior>
    - Backwards compatibility: `<EvidencePackDialog />` called with zero props (as in `audit/page.tsx:33`) MUST render the same default trigger button it does today (label `Export Evidence Pack`, `FileArchive` icon, `size="sm"` Button).
    - New capability: `<EvidencePackDialog trigger={<SomeJSX/>} />` MUST render the provided JSX node as the dialog trigger, replacing the default button. The dialog content, state, policy selector, checklist, progress, download, retry, and cleanup behaviors MUST be unchanged.
    - Auditor dashboard: clicking the `Export Evidence Pack (ZIP)` button in the Export Controls card MUST open `EvidencePackDialog` directly, with NO navigation to `/audit`. The button's visible label (`Export Evidence Pack (ZIP)`), variant (`outline`), size (`sm`), and `FileArchive` icon MUST be preserved exactly — the only change is what it triggers.
    - TypeScript compile (`pnpm exec tsc --noEmit`) MUST pass with zero errors after the change.
    - Lint (`pnpm lint app/\(workspace\)/audit/_components/evidence-pack-dialog.tsx app/\(workspace\)/dashboard/_components/auditor-dashboard.tsx`) MUST pass.
  </behavior>

  <action>
    Step 1. Edit `app/(workspace)/audit/_components/evidence-pack-dialog.tsx`:

      1a. Change the function signature to accept an optional `trigger` prop. Replace:
        ```tsx
        export function EvidencePackDialog() {
        ```
        with:
        ```tsx
        interface EvidencePackDialogProps {
          trigger?: React.ReactNode
        }

        export function EvidencePackDialog({ trigger }: EvidencePackDialogProps = {}) {
        ```
        (The `= {}` default parameter preserves zero-prop call compatibility at `audit/page.tsx:33`.)

      1b. Add `import type { ReactNode } from 'react'` OR use the `React.ReactNode` namespace form — whichever matches the rest of the codebase. Read the existing `'use client'` header and the `useState, useCallback` import on line 3 to decide: if other files in `app/(workspace)/audit/_components/` use `React.ReactNode`, use that. Otherwise add `import type { ReactNode } from 'react'` at the top and use `trigger?: ReactNode`.

      1c. Replace the existing DialogTrigger line (currently line 110):
        ```tsx
        <DialogTrigger render={<Button size="sm"><FileArchive className="size-4" />Export Evidence Pack</Button>} />
        ```
        with:
        ```tsx
        <DialogTrigger
          render={
            trigger !== undefined
              ? (trigger as React.ReactElement)
              : (
                <Button size="sm">
                  <FileArchive className="size-4" />
                  Export Evidence Pack
                </Button>
              )
          }
        />
        ```
        Note: `DialogTrigger` in this project uses base-ui's `render` prop pattern (not Radix `asChild`). The `render` prop expects a single ReactElement. The cast `trigger as React.ReactElement` is acceptable because the downstream contract (consumers pass a single JSX element, not an array) is enforced by convention in the two call sites.

    Step 2. Edit `app/(workspace)/dashboard/_components/auditor-dashboard.tsx`:

      2a. Add import at the top of the file, alongside the other `app/(workspace)/...` imports:
        ```tsx
        import { EvidencePackDialog } from '@/app/(workspace)/audit/_components/evidence-pack-dialog'
        ```
        (Use the project's module-path alias convention. Read the existing imports at lines 1–10 to confirm whether the project uses `@/app/...` or a relative path; match that exactly.)

      2b. Replace the existing Export Evidence Pack button at lines 105–108:
        ```tsx
        <Button variant="outline" size="sm" render={<Link href="/audit" />}>
          <FileArchive className="size-4" />
          Export Evidence Pack (ZIP)
        </Button>
        ```
        with:
        ```tsx
        <EvidencePackDialog
          trigger={
            <Button variant="outline" size="sm">
              <FileArchive className="size-4" />
              Export Evidence Pack (ZIP)
            </Button>
          }
        />
        ```

      2c. Leave the "Export Audit Log (CSV)" button (line 101) untouched — it correctly exports to `/api/export/traceability/csv` and is NOT in scope.
      2d. Leave the "View Full Audit Trail" button (line 58) untouched — it correctly links to `/audit` per the Phase 9 spec.
      2e. Do NOT remove the `Link` import from line 1 of auditor-dashboard.tsx — it is still used by the View Full Audit Trail button. Do NOT remove the `FileArchive` import either.

    Step 3. Run the TypeScript compiler from the repo root:
      `pnpm exec tsc --noEmit`
      This MUST exit 0. If it errors on a type mismatch between `DialogTrigger`'s `render` prop and `React.ReactElement`, inspect the actual `render` prop type in `components/ui/dialog.tsx` and adjust the cast to match the exact expected type.

    Step 4. Run lint on the two touched files:
      `pnpm lint app/\(workspace\)/audit/_components/evidence-pack-dialog.tsx app/\(workspace\)/dashboard/_components/auditor-dashboard.tsx`
      This MUST exit 0.

    Step 5. Update `.planning/phases/09-public-portal-compliance/09-VERIFICATION.md`:
      - Frontmatter: change `status: gaps_found` to `status: passed`, change `verified:` timestamp to `2026-04-13T00:00:00Z`, change `score: 12/13` to `score: 13/13 truths verified`, add `re_verification: true`, add `re_verified_note: "EV-08 fixed 2026-04-13 — EvidencePackDialog accepts optional trigger prop; auditor-dashboard.tsx now mounts the dialog in-place with its own Export Evidence Pack (ZIP) button. No /audit navigation required."`
      - In the gaps list, change the EV-08 gap's `status: partial` to `status: fixed`.
      - In the body, change `**Status:** gaps_found (1 warning-level gap)` to `**Status:** passed (re-verified 2026-04-13)` and update the Re-verification line similarly.

    Step 6. Update `.planning/REQUIREMENTS.md` lines 148–149 and 176 — change:
      - `- [ ] **FIX-05**:` → `- [x] **FIX-05**:` (line 148)
      - `- [ ] **FIX-06**:` → `- [x] **FIX-06**:` (line 149)
      - `- [ ] **EV-08**:` → `- [x] **EV-08**:` (line 176)
      Leave FIX-07 (line 150), EV-05/EV-06/EV-07 (lines 173–175), and every other unrelated requirement untouched.
  </action>

  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; grep -c "EvidencePackDialog" "app/(workspace)/dashboard/_components/auditor-dashboard.tsx" &amp;&amp; grep -c "trigger?:" "app/(workspace)/audit/_components/evidence-pack-dialog.tsx" &amp;&amp; ! grep -q 'render={<Link href="/audit" />}[^<]*<FileArchive' "app/(workspace)/dashboard/_components/auditor-dashboard.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` from the repo root exits with code 0
    - `pnpm lint app/\(workspace\)/audit/_components/evidence-pack-dialog.tsx app/\(workspace\)/dashboard/_components/auditor-dashboard.tsx` exits with code 0
    - `app/(workspace)/audit/_components/evidence-pack-dialog.tsx` contains the exact substring `trigger?:` (the optional prop declaration)
    - `app/(workspace)/audit/_components/evidence-pack-dialog.tsx` contains the exact substring `EvidencePackDialogProps`
    - `app/(workspace)/audit/_components/evidence-pack-dialog.tsx` still contains the substring `Export Evidence Pack` (default trigger label preserved)
    - `app/(workspace)/dashboard/_components/auditor-dashboard.tsx` contains the exact substring `import { EvidencePackDialog }`
    - `app/(workspace)/dashboard/_components/auditor-dashboard.tsx` contains the exact substring `<EvidencePackDialog`
    - `app/(workspace)/dashboard/_components/auditor-dashboard.tsx` contains the exact substring `Export Evidence Pack (ZIP)` (button label preserved)
    - `app/(workspace)/dashboard/_components/auditor-dashboard.tsx` does NOT contain the substring `render={<Link href="/audit" />}` immediately before a `FileArchive` icon block (the "View Full Audit Trail" button's use of `render={<Link href="/audit" />}` on line 58 must remain; only the Export Evidence Pack usage is removed)
    - A grep of `auditor-dashboard.tsx` for `render={<Link href="/audit"` still returns exactly 1 match (the View Full Audit Trail button, NOT the Export Evidence Pack button)
    - `app/(workspace)/audit/page.tsx` is NOT modified (git diff empty) — the zero-prop `<EvidencePackDialog />` call at line 33 must still work unchanged
    - `.planning/phases/09-public-portal-compliance/09-VERIFICATION.md` frontmatter contains the exact line `status: passed`
    - `.planning/phases/09-public-portal-compliance/09-VERIFICATION.md` frontmatter contains `verified: 2026-04-13T00:00:00Z`
    - `.planning/phases/09-public-portal-compliance/09-VERIFICATION.md` frontmatter contains `re_verification: true`
    - `.planning/REQUIREMENTS.md` line 148 contains `- [x] **FIX-05**`
    - `.planning/REQUIREMENTS.md` line 149 contains `- [x] **FIX-06**`
    - `.planning/REQUIREMENTS.md` line 176 contains `- [x] **EV-08**`
    - `.planning/REQUIREMENTS.md` line 150 still contains `- [ ] **FIX-07**` (untouched)
    - No files in `app/(workspace)/policies/` are modified by this task
  </acceptance_criteria>

  <done>
    `EvidencePackDialog` accepts an optional `trigger` ReactNode prop; auditor dashboard's "Export Evidence Pack (ZIP)" button now renders the dialog in-place without any `/audit` navigation. Phase 9 VERIFICATION.md is stamped `status: passed`, REQUIREMENTS.md marks all three (FIX-05, FIX-06, EV-08) complete, and the full project typechecks and lints clean. The props-less `<EvidencePackDialog />` call site at `audit/page.tsx:33` continues to work unchanged.
  </done>
</task>

</tasks>

<verification>
Whole-plan sanity checks after all three tasks land:

1. `pnpm exec tsc --noEmit` — zero errors across the repo
2. `pnpm lint` on the touched files — zero warnings/errors
3. `grep -c "<FeedbackDetailSheet" app/\(workspace\)/policies/\[id\]/feedback/_components/feedback-inbox.tsx` returns ≥ 1
4. `grep -c "Traceability" app/\(workspace\)/policies/\[id\]/_components/policy-tab-bar.tsx` returns ≥ 1
5. `grep -c "EvidencePackDialog" app/\(workspace\)/dashboard/_components/auditor-dashboard.tsx` returns ≥ 1
6. `grep -c "render={<Link href=\"/audit\"" app/\(workspace\)/dashboard/_components/auditor-dashboard.tsx` returns exactly 1 (View Full Audit Trail button remains)
7. All three VERIFICATION.md files have `status: passed` at the top of their frontmatter
8. REQUIREMENTS.md shows `[x]` for FIX-05 (line 148), FIX-06 (line 149), EV-08 (line 176)
9. `git diff app/(workspace)/audit/page.tsx` is empty (backwards compatibility preserved)
10. `git diff app/(workspace)/_components/workspace-nav.tsx` is empty (out of scope)
</verification>

<success_criteria>
This plan succeeds when ALL FOUR ROADMAP success criteria for Phase 15 are TRUE:

1. ✅ Stakeholder clicking a feedback row in `/feedback` opens FeedbackDetailSheet — verified by Task 1 grep of `feedback-inbox.tsx` for the wired JSX + props.

2. ✅ Traceability page is reachable via PolicyTabBar from any policy detail page — verified by Task 2 grep of `policy-tab-bar.tsx` for the `Traceability` tab entry + `canViewTrace` gate + layout.tsx passing the prop.

3. ✅ Auditor dashboard "Export Evidence Pack (ZIP)" button opens `EvidencePackDialog` directly — implemented by Task 3 via the new optional `trigger` prop pattern and the `auditor-dashboard.tsx` replacement.

4. ✅ Phase 4 and Phase 7 VERIFICATION.md are updated to `status: passed` with 2026-04-13 re-verified timestamps (and Phase 9 follows along). REQUIREMENTS.md checkboxes for FIX-05, FIX-06, EV-08 are ticked.

Plus: full TypeScript + lint clean, no collateral damage to `audit/page.tsx` (backwards compat), no collateral damage to `workspace-nav.tsx` (out of scope), no touching of unrelated warning-level findings from the old verification reports.
</success_criteria>

<output>
After completion, create `.planning/phases/15-stale-verification-closeout/15-01-SUMMARY.md` documenting:
- Which surfaces were re-verified with no code change (FIX-05, FIX-06)
- The single real code fix (EV-08: optional trigger prop + dashboard in-place dialog mount)
- Confirmation that all three v0.1 audit gaps are now closed and REQUIREMENTS.md reflects it
- Any deviations from the plan (should be zero — this is a surgical re-verification + one-file fix)
</output>
