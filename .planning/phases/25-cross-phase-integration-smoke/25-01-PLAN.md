---
phase: 25-cross-phase-integration-smoke
plan: 01
type: execute
wave: 1
depends_on:
  - 25-00
files_modified:
  - .planning/v0.2-INTEGRATION-SMOKE.md
  - .planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-HUMAN-UAT.md
  - .planning/phases/17-workshop-lifecycle-recording-pipeline-groq/17-VERIFICATION.md
  - .planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-HUMAN-UAT.md
  - .planning/phases/20-cal-com-workshop-register/20-HUMAN-UAT.md
autonomous: false
requirements:
  - INTEGRATION-01
user_setup:
  - service: Resend
    why: "Email dispatch for welcome emails, nudges, feedback-reviewed notifications"
    env_vars:
      - name: RESEND_API_KEY
        source: "https://resend.com/api-keys -> Create API Key"
  - service: Groq
    why: "LLM consultation summary generation (llama-3.3-70b-versatile)"
    env_vars:
      - name: GROQ_API_KEY
        source: "https://console.groq.com/keys -> Create API Key"
  - service: Blockfrost
    why: "Cardano preview-net transaction submission and confirmation"
    env_vars:
      - name: BLOCKFROST_PROJECT_ID
        source: "https://blockfrost.io/dashboard -> Add Project -> Network: Cardano preview -> copy project_id (starts with 'preview')"
  - service: Cardano Wallet
    why: "Signing Cardano anchor transactions on preview-net"
    env_vars:
      - name: CARDANO_WALLET_MNEMONIC
        source: "Generate a 24-word mnemonic (e.g. via cardano-cli or any BIP39 tool), then fund with tADA from https://docs.cardano.org/cardano-testnets/tools/faucet/"
  - service: Cal.com
    why: "Workshop event type provisioning and webhook handling"
    env_vars:
      - name: CALCOM_API_KEY
        source: "https://app.cal.com/settings/developer/api-keys -> Create API Key"
      - name: CAL_WEBHOOK_SECRET
        source: "https://app.cal.com/settings/developer/webhooks -> Create webhook -> copy signing secret. NOTE: D-01 names this CALCOM_WEBHOOK_SECRET but the source code reads process.env.CAL_WEBHOOK_SECRET -- set CAL_WEBHOOK_SECRET in .env.local."
  - service: Cloudflare Turnstile
    why: "CAPTCHA on /participate form"
    env_vars:
      - name: NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY
        source: "Use test key 0x00000000000000000000AA (already in .env.example) or real key from Cloudflare dashboard"
      - name: CLOUDFLARE_TURNSTILE_SECRET_KEY
        source: "Use test secret 0x0000000000000000000000000000000AA (already in .env.example) or real secret from Cloudflare dashboard"
  - service: Workshop Feedback JWT
    why: "Signing deep-link tokens for post-workshop feedback emails"
    env_vars:
      - name: WORKSHOP_FEEDBACK_JWT_SECRET
        source: "Any random 32+ character string (e.g. openssl rand -hex 32)"
  - service: Inngest (local dev)
    why: "Async function orchestration -- dev server runs keyless so no API keys needed"
    env_vars: []
    note: "INNGEST_EVENT_KEY and INNGEST_SIGNING_KEY (listed in D-01) are NOT needed for local dev. The codebase sets INNGEST_DEV=1 in .env.local which runs the Inngest dev server in keyless mode. These keys are only required for Inngest Cloud in production."
must_haves:
  truths:
    - "Fresh visitor submits /participate and receives welcome email via Clerk invite"
    - "Workshop booking via cal.com creates workshopRegistrations row"
    - "MEETING_ENDED webhook transitions workshop to completed with attendance populated"
    - "Stakeholder submits feedback via workshop deep-link"
    - "Admin reviews feedback triggering Inngest notification + email + auto-draft CR"
    - "CR merge publishes version with Cardano anchor and consultation summary"
    - "Milestone marked ready produces Cardano tx with Verified badge on /portal"
    - "Integration smoke report written to .planning/v0.2-INTEGRATION-SMOKE.md"
  artifacts:
    - path: ".planning/v0.2-INTEGRATION-SMOKE.md"
      provides: "Pass/fail evidence for all 9 success criteria"
      contains: "Status:"
  key_links:
    - from: "/participate form"
      to: "participateIntakeFn"
      via: "POST /api/intake/participate -> Inngest event"
      pattern: "participate.intake"
    - from: "cal.com BOOKING_CREATED webhook"
      to: "workshopRegistrations"
      via: "POST /api/webhooks/cal"
      pattern: "BOOKING_CREATED"
    - from: "feedback.decide mutation"
      to: "feedbackReviewedFn"
      via: "Inngest event feedback.reviewed"
      pattern: "feedback.reviewed"
    - from: "version.published event"
      to: "versionAnchorFn + consultationSummaryGenerateFn"
      via: "Inngest fan-out"
      pattern: "version.published"
    - from: "milestone.ready event"
      to: "milestoneReadyFn"
      via: "Inngest 5-step pipeline"
      pattern: "milestone.ready"
---

<objective>
Walk the full 9-criterion integration chain end-to-end per INTEGRATION-01, fixing any breakages inline, and produce the structured smoke report at `.planning/v0.2-INTEGRATION-SMOKE.md`. Then update all 4 deferred UAT files to resolved-by-phase-25 per D-06.

Purpose: Prove the entire v0.2 chain works as a single product -- from public /participate intake through Cardano anchoring. Per D-05, this subsumes all deferred smoke walks from phases 16, 17, 19, and 20.
Output: `.planning/v0.2-INTEGRATION-SMOKE.md` with pass/fail evidence for all 9 criteria, plus 4 deferred UAT files marked resolved.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cross-phase-integration-smoke/25-RESEARCH.md
@.planning/phases/25-cross-phase-integration-smoke/25-CONTEXT.md
@.planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-SMOKE.md
@.planning/phases/17-workshop-lifecycle-recording-pipeline-groq/17-VERIFICATION.md
@.planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-HUMAN-UAT.md
@.planning/phases/20-cal-com-workshop-register/20-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke report template with full walk procedures</name>
  <files>.planning/v0.2-INTEGRATION-SMOKE.md</files>
  <read_first>
    - .planning/phases/25-cross-phase-integration-smoke/25-RESEARCH.md (verification queries and walk procedures per criterion)
    - .planning/phases/25-cross-phase-integration-smoke/25-CONTEXT.md (D-09 and D-10 report format decisions)
    - .planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-SMOKE.md (Flow 5 walk procedure for Criterion 6)
  </read_first>
  <action>
Create `.planning/v0.2-INTEGRATION-SMOKE.md` as the structured smoke report. Per D-09: each criterion gets pass/fail status, observed behavior, evidence (DB query or Inngest run ID), and notes. Per D-10: auditable checklist, not exhaustive dumps.

The file structure:

1. **Frontmatter:** `status: in-progress`, `requirement: INTEGRATION-01`, `started: 2026-04-16`

2. **Pre-Conditions table** with 7 checklist items:
   - `npx tsc --noEmit` clean
   - `npm test` 0 failures
   - Dev server running (localhost:3000)
   - Inngest Dev Server running (localhost:8288)
   - All required env vars set in .env.local
   - At least one workshop has calcomEventTypeId (SQL: `SELECT id, title, calcom_event_type_id FROM workshops WHERE calcom_event_type_id IS NOT NULL LIMIT 5;`)
   - Cardano preview-net wallet funded >= 2 tADA

3. **Required Environment Variables table** listing all 9 env vars with Set checkbox and Source URL

4. **9 Criterion sections**, each containing:
   - Title (from ROADMAP success criteria 1-9)
   - Status checkbox (PASS / FAIL / GATED)
   - Walk Procedure (step-by-step from 25-RESEARCH.md)
   - Verification Queries (SQL from 25-RESEARCH.md)
   - Inngest Functions to Watch (from research)
   - Observed (blank -- filled during walk)
   - Evidence (blank -- filled during walk)
   - Notes (blank -- filled during walk)

**Criterion-specific walk procedures to include (from 25-RESEARCH.md):**

**Criterion 1:** /participate submit -> Clerk invite -> welcome email -> stakeholder dashboard
- Visit http://localhost:3000/participate (unauthenticated)
- Fill form with fresh test email, select role/orgType
- Turnstile widget auto-passes with test keys
- Submit -> POST /api/intake/participate returns 200
- Check Inngest Dev UI for `participate-intake` run (2 steps)
- Check Clerk dashboard for pending invitation
- Check Resend dashboard for welcome email delivery
- Click invite link -> set password -> land on /dashboard
- SQL: `SELECT id, email, role FROM users WHERE email = '<test-email>';`

**Criterion 2:** /workshops -> cal.com booking -> confirmation
- Visit http://localhost:3000/workshops
- Find workshop with cal.com embed (requires calcomEventTypeId)
- Book a slot through the embed
- Check Inngest Dev UI for `workshop-registration-received` run
- For live cal.com webhooks: either use tunnel (ngrok) or simulate BOOKING_CREATED payload via curl with HMAC
- SQL: `SELECT id, workshop_id, booking_uid, email, name, status FROM workshop_registrations WHERE email = '<test-email>' ORDER BY created_at DESC LIMIT 3;`

**Criterion 3:** Reminder emails (spot-check)
- Cal.com sends native reminder emails (not controlled by code) -- verify event type has reminders enabled in cal.com dashboard
- For evidence nudge emails: fast-forward the `workshop-completed` Inngest run sleeps in Inngest Dev UI (sleep-72h, sleep-7d)
- Check Resend dashboard for nudge email delivery

**Criterion 4:** MEETING_ENDED webhook -> workshop completed -> attendance -> feedback email
- Simulate MEETING_ENDED webhook via curl with computed HMAC signature against CAL_WEBHOOK_SECRET
- Workshop status transitions to `completed`
- Attendance auto-populated via `workshopRegistrations.attendedAt`
- `workshopFeedbackInviteFn` fires -> Resend email with signed JWT deep-link
- SQL: `SELECT id, status, updated_at FROM workshops WHERE calcom_event_type_id IS NOT NULL LIMIT 5;`
- SQL: `SELECT id, email, attended_at, attendance_source FROM workshop_registrations WHERE workshop_id = '<workshop-id>';`

**Criterion 5:** Stakeholder feedback via deep-link
- Open the feedback deep-link URL from the email (contains workshopId + JWT token)
- Submit feedback form
- SQL: `SELECT id, readable_id, status, source, submitter_id FROM feedback_items WHERE source = 'workshop' ORDER BY created_at DESC LIMIT 3;`
- SQL: `SELECT * FROM workshop_feedback_links WHERE feedback_id = '<feedback-id>';`
- Note: workflow_transitions is NOT written for feedback submissions; evidence is the workshopFeedbackLinks row and feedbackItems timestamp

**Criterion 6:** Admin reviews feedback -> Inngest pipeline -> notification + email + auto-draft CR
- Admin signs in, opens /feedback, finds workshop feedback item
- Clicks Start Review -> triggers `notification.create` event
- Clicks Decide -> Accept -> enters rationale (>= 20 chars) -> submits
- `feedbackReviewedFn` runs 6 steps in Inngest Dev UI
- SQL: `SELECT id, user_id, type, title, idempotency_key FROM notifications WHERE entity_id = '<feedback-id>';`
- SQL: `SELECT id, readable_id, status, owner_id FROM change_requests ORDER BY created_at DESC LIMIT 3;`

**Criterion 7:** CR merge -> version publish -> Cardano anchor + consultation summary
- Admin reviews and approves the auto-drafted CR
- Admin merges CR -> new version created
- Admin publishes version -> `version.published` event fires
- Two Inngest functions fan out: `consultation-summary-generate` + `version-anchor`
- `versionAnchorFn` confirm-loop may take up to 10 minutes on preview-net (do not close Inngest dev server)
- Moderator reviews LLM summary, marks approved
- SQL: `SELECT id, version_label, is_published, tx_hash, anchored_at FROM document_versions WHERE document_id = '<doc-id>' ORDER BY created_at DESC LIMIT 3;`

**Criterion 8:** Milestone ready -> Cardano tx -> Verified badge
- Admin marks milestone ready -> `milestoneReadyFn` 5-step pipeline
- Wallet needs >= 2 tADA (1.5 ADA for tx + fees)
- Watch all 5 steps in Inngest Dev UI: compute-hash, persist-hash, check-existing-tx, submit-tx, confirm-loop
- Visit /portal/<policyId> -> Verified State badge with Cardanoscan link
- SQL: `SELECT tx_hash, anchored_at, status FROM milestones WHERE id = '<milestone-id>';`

**Criterion 9:** Smoke report finalized
- All criteria pass/fail documented
- Evidence fields filled
- Status updated to completed

5. **Deferred Walk Resolution (D-06)** section with checklist of 4 UAT files to update

6. **Fixes Applied During Walk** table (empty, filled if gaps found)
  </action>
  <verify>
    <automated>grep -c "## Criterion" .planning/v0.2-INTEGRATION-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/v0.2-INTEGRATION-SMOKE.md exists
    - File contains exactly 9 `## Criterion` sections (grep count = 9)
    - File contains `## Pre-Conditions` with 7 checklist items
    - File contains `## Required Environment Variables` with 9 env vars
    - File contains `## Deferred Walk Resolution` referencing 4 UAT files
    - File frontmatter contains `status: in-progress`
    - Each criterion section has subsections: Walk Procedure, Verification Queries, Observed, Evidence, Notes
  </acceptance_criteria>
  <done>Smoke report template at .planning/v0.2-INTEGRATION-SMOKE.md has all 9 criteria with complete walk procedures, SQL verification queries, Inngest function names, and blank evidence fields ready for the human walk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the 9-criterion integration walk</name>
  <files>.planning/v0.2-INTEGRATION-SMOKE.md</files>
  <action>
Human walks the 9-criterion integration chain using the smoke report template created in Task 1. Each criterion is tested in sequence (data from earlier criteria feeds later ones). User provisions env vars, starts dev server + Inngest dev server, and follows the walk procedure in `.planning/v0.2-INTEGRATION-SMOKE.md`, filling in Observed/Evidence/Notes as they go. Per D-07 any broken step is fixed before closing. Per D-08 all 9 must pass fully.
  </action>
  <what-built>
Complete smoke report template with walk procedures for all 9 INTEGRATION-01 success criteria. Step-by-step instructions, verification SQL queries, Inngest function names to watch, and evidence fields to fill in.

All pre-condition code fixes are complete (Plan 00): TypeScript compiles clean (0 errors), all tests pass (0 failures), .env.example includes WORKSHOP_FEEDBACK_JWT_SECRET.
  </what-built>
  <how-to-verify>
**Setup (one-time):**

1. Provision all missing env vars in `.env.local`:
   - RESEND_API_KEY from https://resend.com/api-keys
   - GROQ_API_KEY from https://console.groq.com/keys
   - BLOCKFROST_PROJECT_ID from https://blockfrost.io/dashboard (preview network, starts with 'preview')
   - CARDANO_WALLET_MNEMONIC: generate 24-word mnemonic, fund from https://docs.cardano.org/cardano-testnets/tools/faucet/
   - CALCOM_API_KEY from https://app.cal.com/settings/developer/api-keys
   - CAL_WEBHOOK_SECRET from https://app.cal.com/settings/developer/webhooks (NOTE: D-01 names this CALCOM_WEBHOOK_SECRET, but the source code at `app/api/webhooks/cal/route.ts:81` reads `process.env.CAL_WEBHOOK_SECRET` -- set `CAL_WEBHOOK_SECRET` in .env.local, not CALCOM_WEBHOOK_SECRET)
   - NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY: 0x00000000000000000000AA (test key)
   - CLOUDFLARE_TURNSTILE_SECRET_KEY: 0x0000000000000000000000000000000AA (test secret)
   - WORKSHOP_FEEDBACK_JWT_SECRET: run `openssl rand -hex 32`

   **Inngest keys note:** INNGEST_EVENT_KEY and INNGEST_SIGNING_KEY (listed in D-01) are NOT needed for local dev. The codebase sets `INNGEST_DEV=1` in `.env.local`, which runs the Inngest dev server in keyless mode. These keys are only required for Inngest Cloud in production -- no action required for this walk.

2. Start dev processes:
   - Terminal 1: `npm run dev`
   - Terminal 2: `npx inngest-cli@latest dev -u http://localhost:3000/api/inngest`

3. Open browsers:
   - App: http://localhost:3000
   - Inngest Dev UI: http://localhost:8288

4. Walk criteria 1-8 following `.planning/v0.2-INTEGRATION-SMOKE.md`, filling Observed/Evidence/Notes as you go.

5. After all criteria pass, mark status PASS and update frontmatter to `status: completed`.

Per D-07: Fix any broken step before closing. Per D-08: All 9 must pass fully.
  </how-to-verify>
  <verify>Human confirms all 9 criteria pass in the smoke report</verify>
  <done>All 9 INTEGRATION-01 success criteria pass. Smoke report .planning/v0.2-INTEGRATION-SMOKE.md has status: completed with evidence for each criterion.</done>
  <resume-signal>Type "all 9 pass" with the smoke report filled in, or describe which criteria failed and what was observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update deferred UAT files to resolved-by-phase-25 (D-06)</name>
  <files>.planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-HUMAN-UAT.md, .planning/phases/17-workshop-lifecycle-recording-pipeline-groq/17-VERIFICATION.md, .planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-HUMAN-UAT.md, .planning/phases/20-cal-com-workshop-register/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/v0.2-INTEGRATION-SMOKE.md (confirm all 9 criteria passed)
    - .planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-HUMAN-UAT.md
    - .planning/phases/17-workshop-lifecycle-recording-pipeline-groq/17-VERIFICATION.md
    - .planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-HUMAN-UAT.md
    - .planning/phases/20-cal-com-workshop-register/20-HUMAN-UAT.md
  </read_first>
  <action>
Per D-06: Update deferred UAT files to resolved-by-phase-25 with a link to the smoke report.

**16-HUMAN-UAT.md:**
- Frontmatter: change `status: partial` to `status: resolved-by-phase-25`
- Add `resolved_by: .planning/v0.2-INTEGRATION-SMOKE.md` to frontmatter
- Add `resolved_date: 2026-04-16` to frontmatter
- Replace `## Current Test` content with: `[Resolved by Phase 25 integration smoke walk -- see .planning/v0.2-INTEGRATION-SMOKE.md]`
- In `## Summary`: change `pending: 1` to `pending: 0`, change `passed: 0` to `passed: 1`

**17-VERIFICATION.md:**
- Frontmatter: change `status: human_needed` to `status: resolved-by-phase-25`
- Add `resolved_by: .planning/v0.2-INTEGRATION-SMOKE.md` to frontmatter
- Add `resolved_date: 2026-04-16` to frontmatter
- Keep all existing content intact (comprehensive verification data), only update status

**19-HUMAN-UAT.md:**
- Frontmatter: change `status: partial` to `status: resolved-by-phase-25`
- Add `resolved_by: .planning/v0.2-INTEGRATION-SMOKE.md` to frontmatter
- Add `resolved_date: 2026-04-16` to frontmatter
- Replace `## Current Test` content with: `[Resolved by Phase 25 integration smoke walk -- see .planning/v0.2-INTEGRATION-SMOKE.md]`
- In `## Summary`: change `pending: 7` to `pending: 0`, change `passed: 0` to `passed: 7`

**20-HUMAN-UAT.md:**
- Frontmatter: change `status: partial` to `status: resolved-by-phase-25`
- Add `resolved_by: .planning/v0.2-INTEGRATION-SMOKE.md` to frontmatter
- Add `resolved_date: 2026-04-16` to frontmatter
- Replace `## Current Test` content with: `[Resolved by Phase 25 integration smoke walk -- see .planning/v0.2-INTEGRATION-SMOKE.md]`
- In `## Summary`: change `pending: 4` to `pending: 0`, change `passed: 0` to `passed: 4`

Also update the Deferred Walk Resolution checklist in `.planning/v0.2-INTEGRATION-SMOKE.md` itself: mark all 4 `[ ]` items as `[x]`.
  </action>
  <verify>
    <automated>grep -c "resolved-by-phase-25" .planning/phases/16-flow-5-smoke-notification-dispatch-migration/16-HUMAN-UAT.md .planning/phases/17-workshop-lifecycle-recording-pipeline-groq/17-VERIFICATION.md .planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-HUMAN-UAT.md .planning/phases/20-cal-com-workshop-register/20-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files contain `status: resolved-by-phase-25` in frontmatter
    - All 4 files contain `resolved_by: .planning/v0.2-INTEGRATION-SMOKE.md` in frontmatter
    - 16-HUMAN-UAT.md summary shows `passed: 1` and `pending: 0`
    - 19-HUMAN-UAT.md summary shows `passed: 7` and `pending: 0`
    - 20-HUMAN-UAT.md summary shows `passed: 4` and `pending: 0`
    - .planning/v0.2-INTEGRATION-SMOKE.md Deferred Walk Resolution section has 4 `[x]` items
  </acceptance_criteria>
  <done>All 4 deferred UAT files updated to resolved-by-phase-25 with smoke report link. Phase 25 integration walk is fully documented and closed.</done>
</task>

</tasks>

<verification>
```bash
# Smoke report exists with all 9 criteria
grep -c "## Criterion" .planning/v0.2-INTEGRATION-SMOKE.md

# All deferred UAT files resolved
grep "resolved-by-phase-25" .planning/phases/16-*/16-HUMAN-UAT.md .planning/phases/17-*/17-VERIFICATION.md .planning/phases/19-*/19-HUMAN-UAT.md .planning/phases/20-*/20-HUMAN-UAT.md

# Smoke report completed
grep "status:" .planning/v0.2-INTEGRATION-SMOKE.md | head -1
```
</verification>

<success_criteria>
- `.planning/v0.2-INTEGRATION-SMOKE.md` exists with all 9 criteria documented as PASS
- All 4 deferred UAT files have `status: resolved-by-phase-25`
- No known gaps remain (per D-07)
- All 9 success criteria pass fully (per D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/25-cross-phase-integration-smoke/25-01-SUMMARY.md`
</output>
