---
phase: 24-stakeholder-engagement-tracking-lite
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/server/routers/__tests__/engagement.test.ts
  - src/server/routers/__tests__/user-activity.test.ts
autonomous: true
requirements:
  - UX-08
  - UX-09
  - UX-10
  - UX-11

must_haves:
  truths:
    - "Test stubs exist and are runnable via npx vitest run before any production code is written"
    - "engagement.test.ts declares pending tests for engagement score computation (UX-09, UX-10)"
    - "user-activity.test.ts declares pending tests for touchActivity middleware behavior (UX-08)"
  artifacts:
    - path: "src/server/routers/__tests__/engagement.test.ts"
      provides: "Test stubs for engagement score queries (UX-09, UX-10, UX-11)"
      contains: "describe"
    - path: "src/server/routers/__tests__/user-activity.test.ts"
      provides: "Test stubs for touchActivity middleware (UX-08)"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files declared in VALIDATION.md: `engagement.test.ts` and `user-activity.test.ts`. These are skeleton test files with `describe` blocks and `it.todo()` placeholders that Plans 01 and 02 will flesh out during implementation.

Purpose: Satisfy Nyquist compliance -- test files must exist before production code so verify blocks can reference them.
Output: Two runnable (but pending) test files under `src/server/routers/__tests__/`.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-stakeholder-engagement-tracking-lite/24-VALIDATION.md
@.planning/phases/24-stakeholder-engagement-tracking-lite/24-RESEARCH.md

<interfaces>
<!-- Existing test pattern from the codebase -->

From src/server/routers/__tests__/milestone.test.ts (mock pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/src/db', () => ({ db: { ... } }))
// Test structure: describe block per procedure, it() per behavior
```

From vitest.config.mts:
```typescript
// Covers src/**/*.test.ts and app/**/*.test.ts
// Environment: jsdom
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create engagement.test.ts and user-activity.test.ts stubs</name>
  <files>
    src/server/routers/__tests__/engagement.test.ts,
    src/server/routers/__tests__/user-activity.test.ts
  </files>
  <read_first>
    src/server/routers/__tests__/milestone.test.ts
  </read_first>
  <action>
**1. Create `src/server/routers/__tests__/engagement.test.ts`:**

```typescript
import { describe, it } from 'vitest'

describe('listUsersWithEngagement', () => {
  it.todo('returns all users with engagementScore field (UX-09)')
  it.todo('engagementScore equals feedbackCount + attendedWorkshopCount (UX-10, D-01)')
  it.todo('users with zero feedback and zero attendance have engagementScore 0')
  it.todo('filters out workshopRegistrations where attendedAt IS NULL')
  it.todo('filters out workshopRegistrations where userId IS NULL (synthetic walk-ins)')
})

describe('getUserProfile', () => {
  it.todo('returns profile, attendedWorkshops, userFeedback, engagementScore (UX-11)')
  it.todo('attendedWorkshops only includes rows where attendedAt IS NOT NULL')
  it.todo('userFeedback is limited to 20 items ordered by createdAt desc (D-07)')
  it.todo('throws NOT_FOUND for nonexistent userId')
})
```

**2. Create `src/server/routers/__tests__/user-activity.test.ts`:**

```typescript
import { describe, it } from 'vitest'

describe('touchActivity middleware', () => {
  it.todo('updates lastActivityAt after a mutation resolves (UX-08)')
  it.todo('does NOT update lastActivityAt on query procedures')
  it.todo('does NOT block the mutation response (fire-and-forget)')
  it.todo('silently catches database update failures')
})
```

Ensure the `__tests__` directory exists (it should from `milestone.test.ts`). If not, create it.
  </action>
  <verify>
    <automated>npx vitest run src/server/routers/__tests__/engagement.test.ts src/server/routers/__tests__/user-activity.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    Both test files exist under src/server/routers/__tests__/.
    engagement.test.ts has describe blocks for listUsersWithEngagement (5 todos) and getUserProfile (4 todos).
    user-activity.test.ts has describe block for touchActivity middleware (4 todos).
    npx vitest run reports all tests as pending/todo (no failures).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/server/routers/__tests__/engagement.test.ts src/server/routers/__tests__/user-activity.test.ts --reporter=verbose` runs without errors
- Both files contain `describe` and `it.todo` blocks
- No test failures (all are pending stubs)
</verification>

<success_criteria>
- engagement.test.ts exists with 9 it.todo() stubs covering UX-09, UX-10, UX-11
- user-activity.test.ts exists with 4 it.todo() stubs covering UX-08
- Both files pass vitest run (pending tests, zero failures)
</success_criteria>

<output>
After completion, create `.planning/phases/24-stakeholder-engagement-tracking-lite/24-00-SUMMARY.md`
</output>
