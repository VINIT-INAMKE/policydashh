---
phase: 05-change-requests
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-lifecycle-actions.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/linked-feedback-list.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/affected-sections-table.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/add-section-dialog.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-decision-log.tsx
autonomous: true
requirements:
  - CR-04
  - CR-05
  - CR-06
  - CR-07
  - CR-08

must_haves:
  truths:
    - "Policy Lead can transition a CR through all lifecycle states via action buttons"
    - "Approve fires immediately; Merge and Close open confirmation dialogs with required text"
    - "Merging a CR shows the next version label preview and creates a version on confirm"
    - "Linked feedback items are visible on the CR detail page with FB-NNN IDs"
    - "Affected sections are visible and can be added/removed while CR is in drafting state"
    - "Closing requires a rationale of at least 20 characters"
    - "Decision log shows chronological transitions with actor, timestamp, and metadata"
  artifacts:
    - path: "app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx"
      provides: "CR detail page server component"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx"
      provides: "CR detail view with all sections"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/cr-lifecycle-actions.tsx"
      provides: "State-based lifecycle action buttons"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx"
      provides: "Merge confirmation with summary textarea and version preview"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx"
      provides: "Close without merge dialog with rationale textarea"
  key_links:
    - from: "cr-lifecycle-actions.tsx"
      to: "changeRequest.submitForReview / approve / merge / close"
      via: "tRPC useMutation calls"
      pattern: "trpc.*changeRequest\\.(submitForReview|approve|merge|close)"
    - from: "merge-dialog.tsx"
      to: "changeRequest.merge"
      via: "tRPC useMutation"
      pattern: "trpc.*changeRequest\\.merge"
    - from: "affected-sections-table.tsx"
      to: "changeRequest.addSection / removeSection"
      via: "tRPC useMutation calls"
      pattern: "trpc.*changeRequest\\.(addSection|removeSection)"
---

<objective>
Build the CR detail page with lifecycle action buttons, merge dialog, close dialog, linked feedback list, affected sections table with add/remove, and decision log. This completes the CR workflow UI.

Purpose: Enables Policy Leads to manage the full CR lifecycle -- review, approve, merge (creating versions), or close with rationale. Provides the traceability display showing which feedback items and sections a CR affects.

Output: 1 page component, 8 client components.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-change-requests/05-RESEARCH.md
@.planning/phases/05-change-requests/05-UI-SPEC.md

# Prior plan summaries
@.planning/phases/04-feedback-system/04-03-SUMMARY.md
@.planning/phases/05-change-requests/05-01-SUMMARY.md
@.planning/phases/05-change-requests/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 and Plan 02 that this plan consumes -->

From src/server/routers/changeRequest.ts (Plan 01):
```typescript
// Transition procedures
submitForReview: requirePermission('cr:manage').input(z.object({ id: z.string().uuid() })).mutation(...)
approve: requirePermission('cr:manage').input(z.object({ id: z.string().uuid() })).mutation(...)
requestChanges: requirePermission('cr:manage').input(z.object({ id: z.string().uuid() })).mutation(...)
merge: requirePermission('cr:manage').input(z.object({ id: z.string().uuid(), mergeSummary: z.string().min(20).max(2000) })).mutation(...)
close: requirePermission('cr:manage').input(z.object({ id: z.string().uuid(), rationale: z.string().min(20).max(2000) })).mutation(...)

// Data queries
getById: requirePermission('cr:read').input(z.object({ id: z.string().uuid() })).query(...)
// returns { id, readableId, documentId, title, description, status, ownerName, approverId, mergedVersionId, closureRationale, linkedFeedback: Array<{id, readableId, title, status, sectionId}>, linkedSections: Array<{id, title}>, createdAt, updatedAt }

listTransitions: requirePermission('cr:read').input(z.object({ crId: z.string().uuid() })).query(...)
// returns Array<{ id, fromState, toState, actorName, timestamp, metadata }>

getNextVersionLabel: requirePermission('cr:manage').input(z.object({ documentId: z.string().uuid() })).query(...)
// returns { versionLabel: string }

addSection: requirePermission('cr:manage').input(z.object({ crId: z.string().uuid(), sectionId: z.string().uuid() })).mutation(...)
removeSection: requirePermission('cr:manage').input(z.object({ crId: z.string().uuid(), sectionId: z.string().uuid() })).mutation(...)
```

From Plan 02 components:
```typescript
// CRStatusBadge: { status: CRStatus } -> renders semantic color badge
import { CRStatusBadge } from './cr-status-badge'
```

From Phase 4 patterns:
```typescript
// RationaleDialog pattern: mandatory text input with min-length client validation
// DecisionLog pattern: chronological entries with status change badges
// TriageActions pattern: state-based conditional button rendering
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CR detail page, lifecycle actions, merge dialog, close dialog</name>
  <files>
    app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-lifecycle-actions.tsx
    app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx
    app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx
  </files>
  <read_first>
    app/(workspace)/policies/[id]/feedback/_components/triage-actions.tsx
    app/(workspace)/policies/[id]/feedback/_components/rationale-dialog.tsx
    app/(workspace)/policies/[id]/feedback/_components/feedback-detail-sheet.tsx
    app/(workspace)/policies/[id]/feedback/_components/decision-log.tsx
    components/ui/dialog.tsx
    components/ui/button.tsx
    components/ui/textarea.tsx
    components/ui/separator.tsx
    .planning/phases/05-change-requests/05-UI-SPEC.md
    src/server/routers/changeRequest.ts
    node_modules/next/dist/docs/
  </read_first>
  <action>
    **1. Create CR detail page** (page.tsx):
    Server component. Read `params.id` (documentId) and `params.crId`. Render CRDetail client component passing crId and documentId.
    Read Next.js route conventions from `node_modules/next/dist/docs/` before writing.

    **2. Create CRDetail client component** (cr-detail.tsx):
    Fetches data via `changeRequest.getById` with crId.

    Layout (per UI-SPEC, single-column, max-width 800px, centered):
    - Back link: Button ghost size="sm", ArrowLeft icon + "Back to Change Requests" -> navigates to `/policies/${documentId}/change-requests`
    - CR-NNN badge: Badge secondary, var(--font-geist-mono), 14px. aria-label="Change Request ID {CR-NNN}"
    - Title: h1, 20px/600
    - Status badge: CRStatusBadge
    - Owner row: "Owner" label (12px/400, muted-foreground) + name (14px/400)
    - Created/Updated row: "Created" + date + "Updated" + date (12px/400, muted-foreground)
    - Separator
    - Description section: h2 "DESCRIPTION" (12px/400, uppercase, tracking-wide, muted-foreground) + body (14px/400)
    - CRLifecycleActions component
    - Separator
    - LinkedFeedbackList component
    - Separator
    - AffectedSectionsTable component
    - Separator
    - CRDecisionLog component

    Loading: skeleton heading + metadata rows + description lines + actions row (per UI-SPEC).

    **3. Create CRLifecycleActions component** (cr-lifecycle-actions.tsx):
    Following Phase 4 TriageActions pattern -- conditional button rendering based on CR status.

    Props: { crId: string, status: CRStatus, documentId: string, onStatusChange: () => void }

    State-based rendering (per UI-SPEC):
    - drafting: "Submit for Review" (default button, ArrowRightCircle icon)
    - in_review: "Approve" (default, CheckCircle icon) + "Close without Merge" (outline destructive text, XCircle icon)
    - approved: "Merge" (default, GitMerge icon) + "Close without Merge" (outline destructive text, XCircle icon)
    - merged: Static text "This change request has been merged." (14px/400, muted-foreground)
    - closed: Static text "This change request is closed." (14px/400, muted-foreground)

    Button behavior:
    - "Submit for Review": fires `changeRequest.submitForReview` immediately with loading spinner. On success: toast "Change request submitted for review.", invalidate query cache, call onStatusChange.
    - "Approve": fires `changeRequest.approve` immediately with loading spinner. On success: toast "Change request approved.", invalidate, onStatusChange.
    - "Merge": opens MergeDialog
    - "Close without Merge": opens CloseDialog

    Loading state: Loader2 spin icon replaces left icon, text unchanged, pointer-events-none.

    Container: flex row, gap-2 (8px), border-t, pt-6 (24px), mt-6.

    **4. Create MergeDialog** (merge-dialog.tsx):
    Per UI-SPEC MergeDialog spec.

    Props: { open: boolean, onOpenChange: (open) => void, crId: string, readableId: string, title: string, documentId: string, onMerged: () => void }

    Content:
    - Title: "Merge Change Request" (20px/600)
    - Description: per UI-SPEC copy
    - CR context row: bg-muted rounded-md padding sm/md, "Merging: {CR-NNN} -- {title}"
    - Merge summary textarea: required, minLength 20, maxLength 2000, rows 6, min-height 96px, placeholder per UI-SPEC
    - Character counter: "{N}/2000" right-aligned, 12px/400, muted-foreground
    - Version preview: fetch `changeRequest.getNextVersionLabel` query. Display "New version will be created:" label + version badge (monospace)
    - Cancel (outline) + "Merge and Create Version" (default). Merge disabled until summary >= 20 chars.

    On merge: call `changeRequest.merge` mutation. Loading: full dialog disabled (opacity-50), spinner on Merge button, "Merging..." text. On success: close dialog, toast "Change request merged. Version {v0.N} created.", call onMerged. On error: toast per UI-SPEC error copy, dialog remains open.

    **5. Create CloseDialog** (close-dialog.tsx):
    Following Phase 4 RationaleDialog pattern exactly.

    Props: { open: boolean, onOpenChange: (open) => void, crId: string, onClosed: () => void }

    Content:
    - Title: "Close without Merging" (20px/600)
    - Description: per UI-SPEC copy
    - Rationale textarea: required, minLength 20, maxLength 2000, rows 6, min-height 96px, placeholder per UI-SPEC
    - Character counter: "{N}/2000" right-aligned
    - Cancel (outline) + "Close without Merging" (destructive). Disabled until rationale >= 20 chars.

    On confirm: call `changeRequest.close` mutation with rationale. On success: close dialog, toast "Change request closed.", call onClosed. On error: toast error, dialog stays open.
  </action>
  <acceptance_criteria>
    - The file app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx exists
    - grep -r "CRDetail" app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx returns match
    - grep -r "changeRequest.getById" app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx returns match
    - grep -r "CRLifecycleActions" app/(workspace)/policies/[id]/change-requests/_components/cr-lifecycle-actions.tsx returns match
    - grep -r "submitForReview\|approve" app/(workspace)/policies/[id]/change-requests/_components/cr-lifecycle-actions.tsx returns match
    - grep -r "MergeDialog" app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx returns match
    - grep -r "changeRequest.merge" app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx returns match
    - grep -r "getNextVersionLabel" app/(workspace)/policies/[id]/change-requests/_components/merge-dialog.tsx returns match
    - grep -r "CloseDialog\|close-dialog" app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx returns match
    - grep -r "changeRequest.close" app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx returns match
    - grep -r "minLength.*20\|min.*20\|length.*20" app/(workspace)/policies/[id]/change-requests/_components/close-dialog.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - CR detail page renders at /policies/[id]/change-requests/[crId]
    - All 5 lifecycle states render correct action buttons per UI-SPEC
    - Submit for Review and Approve fire immediately with loading spinners
    - Merge dialog shows summary textarea, character counter, and version preview
    - Close dialog enforces >= 20 char rationale
    - Merged/Closed show terminal state messages
    - Toasts fire on success/error per UI-SPEC copy
  </done>
</task>

<task type="auto">
  <name>Task 2: Linked feedback list, affected sections table, add section dialog, decision log</name>
  <files>
    app/(workspace)/policies/[id]/change-requests/_components/linked-feedback-list.tsx
    app/(workspace)/policies/[id]/change-requests/_components/affected-sections-table.tsx
    app/(workspace)/policies/[id]/change-requests/_components/add-section-dialog.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-decision-log.tsx
  </files>
  <read_first>
    app/(workspace)/policies/[id]/feedback/_components/decision-log.tsx
    app/(workspace)/policies/[id]/feedback/_components/evidence-list.tsx
    components/ui/table.tsx
    components/ui/alert-dialog.tsx
    components/ui/select.tsx
    components/ui/separator.tsx
    .planning/phases/05-change-requests/05-UI-SPEC.md
    src/server/routers/changeRequest.ts
    node_modules/next/dist/docs/
  </read_first>
  <action>
    **1. Create LinkedFeedbackList component** (linked-feedback-list.tsx):
    Per UI-SPEC LinkedFeedbackList spec.

    Props: { linkedFeedback: Array<{ id, readableId, title, status, sectionTitle }> }

    Layout:
    - Container: bg-muted, rounded-md, padding md (16px), max-height 320px with overflow-y-auto (per UI-SPEC spacing exceptions)
    - Empty state: "No feedback linked." (14px/400, muted-foreground)
    - Each feedback row: FB-NNN badge (monospace, 12px, Badge secondary) | Title (14px/400, truncated 1 line) | feedback StatusBadge (from Phase 4) | Section name (12px, muted-foreground) | ExternalLink icon button (ghost, size="sm", aria-label="Open feedback {FB-NNN}")
    - Row divider: Separator horizontal between rows

    The ExternalLink button navigates to the feedback detail (e.g. `/policies/${documentId}/feedback?selected=${feedbackId}`).

    Import StatusBadge from Phase 4 feedback components for feedback status rendering. Do NOT use CRStatusBadge for feedback items.

    **2. Create AffectedSectionsTable component** (affected-sections-table.tsx):
    Per UI-SPEC spec.

    Props: { crId: string, crStatus: CRStatus, linkedSections: Array<{ id, title }>, documentId: string, onSectionsChange: () => void }

    Layout: shadcn Table component.
    - Columns: Section Title, Section ID (truncated UUID, 8 chars, monospace, with tooltip showing full UUID), Action
    - Empty state: full-width colspan row, "No sections linked yet." (14px/400, muted-foreground)
    - "Add Section" button: outline, size="sm", Plus icon -- opens AddSectionDialog. Only shown when CR status is drafting.
    - Remove section button: ghost icon, size="sm", Trash2 icon, aria-label="Remove section {title} from change request" -- opens AlertDialog confirmation. Only shown when CR status is drafting.
    - When CR is merged or closed: no add/remove buttons, table rows are read-only.

    Remove section: on confirm, call `changeRequest.removeSection` mutation with { crId, sectionId }. Toast "Section removed." on success. Call onSectionsChange to invalidate.

    AlertDialog for remove confirmation per UI-SPEC:
    - Title: "Remove Section"
    - Description: "Remove \"{section title}\" from this change request? The section will not be deleted."
    - Cancel: "Keep Section"
    - Confirm: "Remove Section" (destructive)

    **3. Create AddSectionDialog** (add-section-dialog.tsx):
    Per UI-SPEC spec.

    Props: { open: boolean, onOpenChange: (open) => void, crId: string, documentId: string, existingSectionIds: string[], onAdded: () => void }

    Content (Dialog, max-width 420px):
    - Title: "Add Affected Section"
    - Description: "Select a policy section affected by this change request."
    - Section select: Select component listing all sections in the document via `document.listSections` or `document.getById` query (use whichever is available). Exclude sections already linked (filter by existingSectionIds).
    - Cancel (outline) + "Add Section" (default). Disabled until selection.

    On add: call `changeRequest.addSection` mutation with { crId, sectionId }. On success: close dialog, toast "Section added.", call onAdded. On error: toast error.

    **4. Create CRDecisionLog component** (cr-decision-log.tsx):
    Following Phase 4 DecisionLog pattern exactly.

    Props: { crId: string }

    Data: `changeRequest.listTransitions` query.

    Layout:
    - Section heading: h2 "DECISION LOG" (12px/400, uppercase, tracking-wide, muted-foreground)
    - Each entry: status change badges (fromState -> toState), actor name, relative timestamp, metadata (rationale if present, mergeSummary if merge event)
    - Empty state: "No transitions recorded yet." (14px/400, muted-foreground)
    - Use CRStatusBadge for rendering fromState and toState values

    **5. Wire all components into CRDetail** (modify cr-detail.tsx if needed):
    Ensure CRDetail imports and renders:
    - CRLifecycleActions (from Task 1)
    - LinkedFeedbackList (this task)
    - AffectedSectionsTable (this task)
    - CRDecisionLog (this task)

    Pass the correct props from the getById query response. Use `useUtils().changeRequest.getById.invalidate()` after any mutation to refresh data.
  </action>
  <acceptance_criteria>
    - grep -r "LinkedFeedbackList" app/(workspace)/policies/[id]/change-requests/_components/linked-feedback-list.tsx returns match
    - grep -r "FB-" app/(workspace)/policies/[id]/change-requests/_components/linked-feedback-list.tsx returns match (renders FB-NNN)
    - grep -r "AffectedSectionsTable" app/(workspace)/policies/[id]/change-requests/_components/affected-sections-table.tsx returns match
    - grep -r "changeRequest.removeSection" app/(workspace)/policies/[id]/change-requests/_components/affected-sections-table.tsx returns match
    - grep -r "AddSectionDialog\|add-section-dialog" app/(workspace)/policies/[id]/change-requests/_components/add-section-dialog.tsx returns match
    - grep -r "changeRequest.addSection" app/(workspace)/policies/[id]/change-requests/_components/add-section-dialog.tsx returns match
    - grep -r "CRDecisionLog" app/(workspace)/policies/[id]/change-requests/_components/cr-decision-log.tsx returns match
    - grep -r "listTransitions" app/(workspace)/policies/[id]/change-requests/_components/cr-decision-log.tsx returns match
    - grep -r "LinkedFeedbackList\|AffectedSectionsTable\|CRDecisionLog" app/(workspace)/policies/[id]/change-requests/_components/cr-detail.tsx returns match (all wired in)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - Linked feedback list shows FB-NNN IDs, titles, statuses with external link buttons
    - Affected sections table shows section titles, truncated UUIDs, add/remove actions
    - Add/remove section only available in drafting state
    - Remove section shows AlertDialog confirmation
    - Decision log shows chronological transitions with actor names and metadata
    - All components wired into CRDetail with proper query invalidation
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `ls app/(workspace)/policies/[id]/change-requests/[crId]/page.tsx` -- detail page exists
- `ls app/(workspace)/policies/[id]/change-requests/_components/` -- all 12 component files exist (5 from Plan 02 + 8 from Plan 03, minus 1 shared = 12 unique)
- `npx vitest run` -- all tests pass
- Manually verify the CR detail page renders with all sections per UI-SPEC
</verification>

<success_criteria>
- CR detail page shows full CR information with lifecycle actions (CR-04, CR-05 UI)
- Merge dialog creates version with summary and preview (CR-06 UI)
- Linked feedback list displays FB-NNN traceability (CR-02, CR-07 visible)
- Close dialog enforces rationale (CR-08 UI)
- Affected sections table shows section links with add/remove (CR-02 UI)
- Decision log shows all transitions (CR-04 audit trail)
</success_criteria>

<output>
After completion, create `.planning/phases/05-change-requests/05-03-SUMMARY.md`
</output>
