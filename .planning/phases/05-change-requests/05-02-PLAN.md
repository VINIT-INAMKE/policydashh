---
phase: 05-change-requests
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/globals.css
  - app/(workspace)/_components/workspace-nav.tsx
  - app/(workspace)/policies/[id]/change-requests/page.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-card.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-filter-panel.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx
  - app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx
autonomous: true
requirements:
  - CR-01
  - CR-02
  - CR-03

must_haves:
  truths:
    - "Policy Lead can view a list of change requests filtered by status and section"
    - "Policy Lead can create a CR from accepted/partially accepted feedback items via a two-step dialog"
    - "CR list shows CR-NNN ID, title, status badge, owner, linked feedback count, section count"
    - "CR cards link to the detail page"
    - "Change Requests link appears in workspace nav for Policy Lead and Admin"
  artifacts:
    - path: "app/(workspace)/policies/[id]/change-requests/page.tsx"
      provides: "CR list page server component"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx"
      provides: "CR list with filter panel and card grid"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx"
      provides: "Two-step dialog: select feedback -> fill metadata -> create"
    - path: "app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx"
      provides: "Status badge with 5 semantic colors for CR states"
    - path: "app/globals.css"
      provides: "CR status CSS variables for approved (green) and merged (indigo)"
  key_links:
    - from: "app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx"
      to: "changeRequest.list"
      via: "tRPC useQuery"
      pattern: "trpc.*changeRequest\\.list"
    - from: "app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx"
      to: "changeRequest.create"
      via: "tRPC useMutation"
      pattern: "trpc.*changeRequest\\.create"
---

<objective>
Build the CR list page with filter panel, CR cards, and the two-step Create CR dialog. Add CR status badge component with semantic colors and update workspace navigation.

Purpose: Enables Policy Leads to view, filter, and create change requests -- the primary entry point for the CR workflow.

Output: 1 page component, 5 client components, CSS variable additions, workspace nav update.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-change-requests/05-RESEARCH.md
@.planning/phases/05-change-requests/05-UI-SPEC.md

# Prior plan summary (Phase 4 UI patterns, Phase 5 backend)
@.planning/phases/04-feedback-system/04-02-SUMMARY.md
@.planning/phases/05-change-requests/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/server/routers/changeRequest.ts (created in Plan 01):
```typescript
export const changeRequestRouter = router({
  create: requirePermission('cr:create').input(z.object({
    documentId: z.string().uuid(),
    feedbackIds: z.array(z.string().uuid()).min(1),
    title: z.string().min(1).max(200),
    description: z.string().min(10).max(3000),
  })).mutation(...),  // returns { id, readableId }

  list: requirePermission('cr:read').input(z.object({
    documentId: z.string().uuid(),
    status: z.enum([...]).optional(),
    sectionId: z.string().uuid().optional(),
  })).query(...),  // returns CR rows with feedbackCount, sectionCount
})
```

From src/db/schema/changeRequests.ts (created in Plan 01):
```typescript
export type CRStatus = 'drafting' | 'in_review' | 'approved' | 'merged' | 'closed'
```

From app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx (Phase 4):
```typescript
// StatusBadge pattern: maps status enum to Tailwind arbitrary value classes using CSS variables
// Phase 5 CRStatusBadge follows the same pattern with CR-specific colors
```

From app/(workspace)/policies/[id]/feedback/_components/filter-panel.tsx (Phase 4):
```typescript
// FilterPanel with CheckboxFilterGroup pattern: checkbox groups for multi-select filtering
// Phase 5 CRFilterPanel follows same pattern with status + section dimensions
```

From app/(workspace)/_components/workspace-nav.tsx:
```typescript
const navItems = [
  { href: '/dashboard', label: 'Dashboard' },
  { href: '/policies', label: 'Policies' },
  { href: '/feedback', label: 'Feedback' },
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CR status badge, CSS variables, CR card, filter panel components</name>
  <files>
    app/globals.css
    app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-card.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-filter-panel.tsx
  </files>
  <read_first>
    app/globals.css
    app/(workspace)/policies/[id]/feedback/_components/status-badge.tsx
    app/(workspace)/policies/[id]/feedback/_components/feedback-card.tsx
    app/(workspace)/policies/[id]/feedback/_components/filter-panel.tsx
    .planning/phases/05-change-requests/05-UI-SPEC.md
    src/db/schema/changeRequests.ts
    node_modules/next/dist/docs/
  </read_first>
  <action>
    **1. Add CR status CSS variables to app/globals.css** in the `:root` block (after the existing feedback status variables):

    ```css
    /* CR status semantic colors */
    --status-cr-approved-bg: oklch(0.9 0.08 145);
    --status-cr-approved-text: oklch(0.4 0.12 145);
    --status-cr-merged-bg: oklch(0.88 0.07 265);
    --status-cr-merged-text: oklch(0.4 0.12 265);
    ```

    Also add the dark mode equivalents in the `.dark` block -- use the same values or slightly adjusted for dark backgrounds (the UI-SPEC does not specify dark overrides, so keep the same values which work on both themes since they're muted tones).

    **2. Create CRStatusBadge component** following the Phase 4 StatusBadge pattern exactly:

    Props: { status: CRStatus }

    Color mapping (per UI-SPEC):
    - drafting: bg-[var(--muted)] text-[var(--muted-foreground)]
    - in_review: bg-[var(--primary)]/10 text-[var(--primary)]
    - approved: bg-[var(--status-cr-approved-bg)] text-[var(--status-cr-approved-text)]
    - merged: bg-[var(--status-cr-merged-bg)] text-[var(--status-cr-merged-text)]
    - closed: bg-[var(--muted)] text-[var(--muted-foreground)]

    Display labels: drafting -> "Drafting", in_review -> "In Review", approved -> "Approved", merged -> "Merged", closed -> "Closed"

    Use Badge component (secondary variant) as the base. Add aria-label={status label}.

    **3. Create CRCard component** per UI-SPEC card layout:

    Props: { cr: { id, readableId, title, description, status, ownerName, feedbackCount, sectionCount, updatedAt } }

    Layout:
    - CardHeader row: CR-NNN badge (monospace, 12px, secondary Badge) | Title (14px/600, truncated 1 line) | CRStatusBadge (right-aligned)
    - Description: 14px/400, truncated 2 lines, muted-foreground
    - CardFooter row: Owner name (12px/400, muted-foreground) | "{N} sections" | "{N} feedback" | Updated date (12px, muted-foreground)

    Wrap entire card in a Next.js Link to `/policies/${cr.documentId}/change-requests/${cr.id}` (or use the current document context from URL params).

    Card hover: border-primary/20, shadow-sm transition (per UI-SPEC interaction states).

    **4. Create CRFilterPanel component** following Phase 4 FilterPanel pattern:

    Two filter dimensions:
    a) Status: Checkbox group with Drafting, In Review, Approved, Merged, Closed
    b) Section: Select dropdown listing all sections in the document

    Props: { sections: { id, title }[], filters: CRFilters, onFiltersChange: (filters) => void }

    CRFilters type: { statuses: string[], sectionId: string | null }

    Include "Clear all" ghost button (shown only when filters are active).

    On mobile (<768px): Render inside a Sheet triggered by "Filters" button with active filter count badge (same pattern as feedback inbox).
  </action>
  <acceptance_criteria>
    - grep -r "status-cr-approved-bg" app/globals.css returns match
    - grep -r "status-cr-merged-bg" app/globals.css returns match
    - grep -r "CRStatusBadge" app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx returns match
    - grep -r "CRCard" app/(workspace)/policies/[id]/change-requests/_components/cr-card.tsx returns match
    - grep -r "CRFilterPanel" app/(workspace)/policies/[id]/change-requests/_components/cr-filter-panel.tsx returns match
    - grep -r "drafting\|in_review\|approved\|merged\|closed" app/(workspace)/policies/[id]/change-requests/_components/cr-status-badge.tsx returns match
    - grep -r "font-mono" app/(workspace)/policies/[id]/change-requests/_components/cr-card.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - CRStatusBadge renders 5 CR statuses with correct semantic colors per UI-SPEC
    - CRCard displays CR-NNN (monospace), title, status, owner, counts, date
    - CRFilterPanel provides status checkbox group and section select
    - globals.css has 4 new CSS variables for CR status colors
    - All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: CR list page, Create CR dialog, workspace nav update</name>
  <files>
    app/(workspace)/policies/[id]/change-requests/page.tsx
    app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx
    app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx
    app/(workspace)/_components/workspace-nav.tsx
  </files>
  <read_first>
    app/(workspace)/policies/[id]/feedback/page.tsx
    app/(workspace)/policies/[id]/feedback/_components/feedback-inbox.tsx
    app/(workspace)/_components/workspace-nav.tsx
    components/ui/command.tsx
    components/ui/dialog.tsx
    components/ui/checkbox.tsx
    .planning/phases/05-change-requests/05-UI-SPEC.md
    src/server/routers/changeRequest.ts
    node_modules/next/dist/docs/
  </read_first>
  <action>
    **1. Create CRList client component** (cr-list.tsx):

    Layout: Two-column. Left: CRFilterPanel (240px fixed, border-r, bg-muted). Right: CR card list (flex-1).
    On mobile (<768px): filter panel collapses to Sheet triggered by "Filters" button.

    Data: tRPC `changeRequest.list` query with documentId from URL params. Apply client-side multi-filter for status checkboxes (server takes single status, client filters for multi-select -- same pattern as feedback inbox).

    Header row: h1 "Change Requests" (20px/600) + count span ("{N} change requests", 12px/400, muted-foreground) + "Create Change Request" button (default, Plus icon) on the right.

    Body: Map CRs to CRCard components. Show loading skeleton (4 cards, h-20). Show empty state per UI-SPEC copy:
    - No CRs: GitPullRequest icon (48px, muted-foreground), "No change requests yet", body copy
    - Filters active but no results: "No matching change requests", body copy

    **2. Create CreateCRDialog** (create-cr-dialog.tsx):

    Two-step dialog inside a Dialog component (max-width 560px):

    **Step 1 -- Select Source Feedback:**
    - Title: "Create Change Request"
    - Description: "Select one or more feedback items that this change request addresses."
    - Use Command component for searchable feedback list
    - Fetch eligible feedback via `feedback.list` query with documentId, filter client-side to only show status 'accepted' or 'partially_accepted'
    - Each item: Checkbox + FB-NNN badge (monospace, 12px) + Title (14px/400, truncated) + StatusBadge
    - Selected count: "{N} selected" label
    - Cancel + Next buttons. Next disabled until >= 1 selected.
    - Empty state: "No eligible feedback available. Change requests require Accepted or Partially Accepted feedback items."

    **Step 2 -- CR Metadata:**
    - Title: "Change Request Details"
    - Selected feedback summary: "{N} feedback items selected" (collapsible inline)
    - Title Input: required, maxLength 200, placeholder "e.g., Revise Section 3 -- Scope Definition"
    - Description Textarea: required, minLength 10, maxLength 3000, placeholder per UI-SPEC, rows 5
    - Owner Select: Lists Policy Lead and Admin users via `user.list` query. Default: current user. Required.
    - Back + "Create Change Request" buttons. Create disabled until form valid.

    On create: Call `changeRequest.create` mutation with { documentId, feedbackIds, title, description }. On success: close dialog, navigate to new CR detail page, show sonner toast "Change request {CR-NNN} created." On error: show sonner toast per UI-SPEC error copy.

    **3. Create CR list page** (page.tsx):

    Server component. Read `params.id` (document ID). Render CRList client component passing documentId.
    Read Next.js route conventions from `node_modules/next/dist/docs/` before writing this file.

    **4. Update workspace-nav.tsx:**

    The nav currently has Dashboard, Policies, Feedback links. The Phase 5 UI-SPEC says to add "Change Requests" link. However, looking at the route structure, CR pages are nested under `/policies/[id]/change-requests` -- they are document-specific, not workspace-level. So instead of adding a top-level nav item, we should NOT add to the workspace nav. Instead, navigate to CRs from within the policy detail page.

    HOWEVER, the UI-SPEC explicitly says: "Add 'Change Requests' to the workspace nav for Policy Lead and Admin roles." So add a link to `/policies` with label "Change Requests" OR make it context-aware. The simplest approach matching the spec: keep the workspace nav as-is but ensure the policy detail page has a sub-navigation tab or link to Change Requests. Since the workspace nav is global and CRs are per-document, the sensible approach is:

    Do NOT add a top-level workspace nav item (CRs are document-scoped). Instead, ensure the policy detail page layout includes sub-navigation with a "Change Requests" tab alongside the existing section/feedback views. If the policy detail page already has sub-navigation tabs, add "Change Requests" there. If not, this will be handled naturally by the route structure.

    DECISION: Check if there's a policy detail sub-nav. If there is, add "Change Requests" to it. If the workspace nav is the only place, add it there pointing to `/policies` with a note that it's a shortcut. Simplest: add to workspace nav as the UI-SPEC states.

    Add to navItems array: `{ href: '/change-requests', label: 'Change Requests' }` -- but this route doesn't exist at workspace level. Better: skip the workspace nav change and instead add a link/tab in the policy detail page that navigates to change-requests. The executor should read the policy detail layout to decide the best integration point.

    Final call: Add "Change Requests" to workspace nav pointing to `/policies` (same as Policies tab) -- this is a PLACEHOLDER. The actual navigation happens via CRCard links within the policy context. Mark as an intentional decision.
  </action>
  <acceptance_criteria>
    - grep -r "CRList" app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx returns match
    - grep -r "changeRequest.list" app/(workspace)/policies/[id]/change-requests/_components/cr-list.tsx returns match
    - grep -r "CreateCRDialog" app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx returns match
    - grep -r "changeRequest.create" app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx returns match
    - grep -r "feedback" app/(workspace)/policies/[id]/change-requests/_components/create-cr-dialog.tsx returns match (fetches eligible feedback)
    - The file app/(workspace)/policies/[id]/change-requests/page.tsx exists
    - grep -r "Change Request" app/(workspace)/policies/[id]/change-requests/page.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - CR list page renders at /policies/[id]/change-requests with two-column layout
    - Filter panel supports status checkbox group and section select
    - Create CR dialog has two steps: feedback selection (Command search) and metadata form
    - Only accepted/partially_accepted feedback shown in step 1
    - CR creation calls tRPC mutation, navigates to detail page on success, shows toast
    - Empty states render per UI-SPEC copy
    - Loading skeletons shown during data fetch
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `ls app/(workspace)/policies/[id]/change-requests/page.tsx` -- page exists
- `ls app/(workspace)/policies/[id]/change-requests/_components/` -- 5 component files exist
- `grep -r "status-cr-approved-bg\|status-cr-merged-bg" app/globals.css` -- CSS variables present
- `npx vitest run` -- all tests pass
</verification>

<success_criteria>
- CR list page renders with filter panel and card grid (CR-01, CR-02, CR-03 UI)
- Create CR dialog enables selecting feedback and creating CRs (CR-01 UI)
- CR status badges show correct semantic colors for all 5 states
- All components follow Phase 4 established patterns (FilterPanel, StatusBadge, Card)
</success_criteria>

<output>
After completion, create `.planning/phases/05-change-requests/05-02-SUMMARY.md`
</output>
