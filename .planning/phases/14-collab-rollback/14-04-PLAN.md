---
phase: 14-collab-rollback
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - package.json
  - package-lock.json
  - .env.local
  - .env.example
  - app/globals.css
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - COLLAB-ROLLBACK-01
must_haves:
  truths:
    - "package.json has zero @hocuspocus/* or @tiptap/extension-collaboration* dependencies"
    - "npm install reconciles lockfile with no yjs/hocuspocus packages in node_modules"
    - ".env.local no longer contains any NEXT_PUBLIC_HOCUSPOCUS_URL references (commented or otherwise)"
    - ".env.example verified to not contain NEXT_PUBLIC_HOCUSPOCUS_URL (already absent per RESEARCH — task asserts absence)"
    - "hocuspocus-server/ directory verified absent (already absent per RESEARCH — task asserts absence)"
    - "globals.css no longer contains .collaboration-cursor__caret, .collaboration-cursor__label, or .inline-comment-mark CSS blocks"
    - "REQUIREMENTS.md EDIT-06, EDIT-07, EDIT-08 entries each contain the string 'rolled back in v0.2 Phase 14' (verified to already be present per RESEARCH § Wave 7 — task asserts presence)"
    - "Full test suite passes modulo 3 pre-existing baseline failures — FINAL acceptance gate"
    - "Render test for block editor (section-content-view.test.tsx) still green"
  artifacts:
    - path: "package.json"
      provides: "Dependencies list without collab packages"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EDIT-06/07/08 annotated as rolled back"
      contains: "rolled back in v0.2 Phase 14"
  key_links:
    - from: "npm test final run"
      to: "baseline failures only"
      via: "no new failures introduced by rollback"
      pattern: "Test Files"
---

<objective>
Final rollback wave: remove the 3 Hocuspocus/Collaboration npm packages, clean `.env.local`, audit `.env.example` and `hocuspocus-server/` for absence (both are already absent per RESEARCH but success criteria #2 explicitly demands verification), strip the collab-specific CSS blocks from `app/globals.css`, verify the REQUIREMENTS.md EDIT-06/07/08 annotations are present (already done per RESEARCH § Wave 7 — but success criterion #4 demands verification), and run the final full-suite acceptance gate.

Purpose: Closes out the rollback. After this plan COLLAB-ROLLBACK-01 and COLLAB-ROLLBACK-02 are both fully satisfied. The "verify absence" tasks for hocuspocus-server and .env.example are deliberately included because success criteria #2 and #4 state them as requirements — even if RESEARCH confirms they are already true, Phase 14 must produce an audit trail proving it.

Output: Clean repo. Final `npm test` green modulo baseline. Phase 14 ready for `/gsd:verify-work`.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-collab-rollback/14-RESEARCH.md
@.planning/phases/14-collab-rollback/14-VALIDATION.md
@.planning/phases/14-collab-rollback/14-03-SUMMARY.md
@./CLAUDE.md
@./AGENTS.md

<interfaces>
Dependencies to remove from package.json (from RESEARCH § Subsystem: npm Packages):
- "@hocuspocus/provider": "^3.4.4"
- "@tiptap/extension-collaboration": "^3.20.5"
- "@tiptap/extension-collaboration-caret": "^3.20.5"

Note per RESEARCH: `yjs` is NOT a direct dependency — it is transitive via the packages above. After removing the 3 packages above and running `npm install`, `yjs` should no longer appear in `node_modules/` top-level.

CSS blocks to remove from app/globals.css per RESEARCH lines 361-401:
- `.collaboration-cursor__caret { ... }`
- `.collaboration-cursor__label { ... }`
- `.collaboration-cursor__caret[data-idle] { ... }` (if present)
- `@media (prefers-reduced-motion) { .collaboration-cursor__caret { ... } }` (remove only the collab rules inside, or the whole block if it only contains collab rules)
- `.inline-comment-mark { ... }`
- `.inline-comment-mark.active { ... }`

REQUIREMENTS.md expected annotations (already verified present by RESEARCH § Wave 7, but must be asserted):
- Line ~35: EDIT-06 annotation containing "rolled back in v0.2 Phase 14"
- Line ~36: EDIT-07 annotation containing "rolled back in v0.2 Phase 14"
- Line ~37: EDIT-08 annotation containing "rolled back in v0.2 Phase 14"

Baseline failures (NOT introduced by this rollback, per RESEARCH § Open Question 2):
- src/__tests__/feedback-permissions.test.ts — 2 failures on feedback:read_own auditor permission
- src/__tests__/document-router-scope.test.ts — 1 failure
Acceptance signal for "tests pass" = "no new test failures beyond these 3."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove collab npm packages; clean .env.local; verify .env.example and hocuspocus-server absence; clean globals.css</name>
  <files>
    package.json,
    package-lock.json,
    .env.local,
    .env.example,
    app/globals.css
  </files>
  <read_first>
    - package.json (READ — confirm the 3 collab packages are in dependencies)
    - .env.local (READ — confirm line 1-2 has commented `# NEXT_PUBLIC_HOCUSPOCUS_URL` per RESEARCH)
    - .env.example (READ — confirm NEXT_PUBLIC_HOCUSPOCUS_URL is already absent)
    - app/globals.css (READ lines 350-410 — find the .collaboration-cursor__* and .inline-comment-mark blocks)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Subsystem: npm Packages, § Subsystem: Env / Config, § Wave 6, § Pitfall 7)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-VALIDATION.md (§ Per-Wave Test Cadence Wave 6)
  </read_first>
  <action>
    **Part A — Remove collab packages:**
    Run: `npm uninstall @hocuspocus/provider @tiptap/extension-collaboration @tiptap/extension-collaboration-caret`

    This edits `package.json` dependencies and updates `package-lock.json` in one command. Alternative if npm uninstall has issues: manually edit `package.json` to remove the three dependency keys, then run `npm install` to reconcile the lockfile.

    After uninstall, verify `node_modules/@hocuspocus` directory is gone: `test ! -d node_modules/@hocuspocus`.

    **Part B — Clean .env.local:**
    Remove any line matching `NEXT_PUBLIC_HOCUSPOCUS_URL` from `.env.local`, whether commented or not. Per RESEARCH line 142 the current content is `# NEXT_PUBLIC_HOCUSPOCUS_URL=ws://localhost:1234`. Delete this line entirely. Preserve all other env vars in the file untouched.

    **Part C — Verify .env.example absence (success criterion #2):**
    Assert the var is already absent. If the assertion fails (i.e., the var IS present), remove it. Per RESEARCH this should already pass — the task is a formal audit.

    **Part D — Verify hocuspocus-server/ directory absence (success criterion #2):**
    Assert the directory does not exist: `test ! -e hocuspocus-server`. Per RESEARCH line 168-170 this directory was never committed to the repo. If unexpectedly present, `rm -rf hocuspocus-server`.

    **Part E — Clean app/globals.css:**
    Open `app/globals.css` and locate the CSS blocks in roughly lines 361-401 (per RESEARCH). Remove the following CSS rule blocks entirely:
    - `.collaboration-cursor__caret { ... }`
    - `.collaboration-cursor__label { ... }`
    - Any `.collaboration-cursor__caret[data-idle] { ... }` selector
    - The `@media (prefers-reduced-motion)` block IF AND ONLY IF its only contents are collaboration-cursor rules. If it contains other unrelated rules, only remove the collaboration-cursor selectors inside it and leave the @media block + other rules intact.
    - `.inline-comment-mark { ... }` and `.inline-comment-mark.active { ... }` (and any `.inline-comment-mark:hover` variants)

    Do NOT remove any other CSS rules. Preserve all Tailwind directives, base styles, and unrelated component styles.

    **Verification loop** (self-check before signalling done):
    - `grep -c "@hocuspocus\|@tiptap/extension-collaboration" package.json` returns 0
    - `test ! -d node_modules/@hocuspocus`
    - `grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.local` returns 0
    - `grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.example` returns 0
    - `test ! -e hocuspocus-server`
    - `grep -c "collaboration-cursor\|inline-comment-mark" app/globals.css` returns 0
  </action>
  <verify>
    <automated>grep -c "@hocuspocus\|@tiptap/extension-collaboration" package.json | grep -q "^0$" &amp;&amp; test ! -d node_modules/@hocuspocus &amp;&amp; grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.local 2>/dev/null | grep -q "^0$" &amp;&amp; grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.example 2>/dev/null | grep -q "^0$" &amp;&amp; test ! -e hocuspocus-server &amp;&amp; grep -c "collaboration-cursor\|inline-comment-mark" app/globals.css | grep -q "^0$" &amp;&amp; npm test -- src/__tests__/section-content-view.test.tsx src/__tests__/editor-extensions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@hocuspocus/provider" package.json` returns `0`
    - `grep -c "@tiptap/extension-collaboration" package.json` returns `0`
    - `grep -c "@tiptap/extension-collaboration-caret" package.json` returns `0`
    - `test ! -d node_modules/@hocuspocus` exits 0
    - `grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.local` returns `0`
    - `grep -c "NEXT_PUBLIC_HOCUSPOCUS_URL" .env.example` returns `0` (success criterion #2 — verified absent)
    - `test ! -e hocuspocus-server` exits 0 (success criterion #2 — directory absent)
    - `grep -c "collaboration-cursor" app/globals.css` returns `0`
    - `grep -c "inline-comment-mark" app/globals.css` returns `0`
    - `package-lock.json` file still exists (was updated, not deleted)
  </acceptance_criteria>
  <done>
    3 collab npm packages removed; lockfile reconciled; env files clean; CSS stripped; directory/example absences formally verified (success criterion #2 fully audited).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify REQUIREMENTS.md EDIT-06/07/08 annotations + FINAL full-suite acceptance run</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 30-40 and lines 140-150 — confirm EDIT-06/07/08 annotations and COLLAB-ROLLBACK-01/02 entries)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Wave 7 — confirms annotations are already present)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-VALIDATION.md (§ Per-Wave Test Cadence Wave 7, § Sampling Rate "Before /gsd:verify-work: Full suite must be green")
  </read_first>
  <action>
    **Part A — Verify REQUIREMENTS.md annotations (success criterion #4):**
    Grep the file for the required annotation string. The count must be at least 3:
    `grep -c "rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` (expect >= 3)

    If the count is less than 3, inspect lines 30-40 of REQUIREMENTS.md and add/fix the annotations so each of EDIT-06, EDIT-07, EDIT-08 lines contains the exact substring `rolled back in v0.2 Phase 14`. Per RESEARCH the text should already be present — this task is a formal audit.

    Also verify the exact entries exist:
    - `grep -c "EDIT-06.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "EDIT-07.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "EDIT-08.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns 1

    **Part B — FINAL full-suite acceptance gate (success criterion #5 + COLLAB-ROLLBACK-02):**
    Run the full test suite: `npm test`

    Acceptance: no new failures beyond the 3 pre-existing baseline failures documented in RESEARCH § Open Question 2:
    - `src/__tests__/feedback-permissions.test.ts` — 2 failures on `feedback:read_own` auditor permission
    - `src/__tests__/document-router-scope.test.ts` — 1 failure

    Any NEW failure (any test file other than these 3, or any new failure count within these 3) is a blocker. If the final run shows new failures, STOP and return control for diagnosis — do not attempt to fix in this task.

    **Part C — Targeted render-gate re-run (success criterion #3 + #5):**
    `npm test -- src/__tests__/section-content-view.test.tsx src/__tests__/editor-extensions.test.ts`
    Both must exit 0.

    **Part D — Final grep audit across the entire codebase for residual collab references:**
    `grep -rn "HocuspocusProvider\|hocuspocus-server\|providerRef\|@hocuspocus\|from 'yjs'\|from '@tiptap/extension-collaboration\|inline-comment-mark" src/ app/ --include="*.ts" --include="*.tsx" --include="*.css" 2>/dev/null`

    Expected: zero matches. Any match is a leftover that must be cleaned up (or is a false positive in e.g. a comment — verify manually).

    **Part E — TypeScript compile clean:**
    `npx tsc --noEmit` must exit 0.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -40 &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; grep -c "rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns a number >= 3
    - `grep -c "EDIT-06.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "EDIT-07.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "EDIT-08.*rolled back in v0.2 Phase 14" .planning/REQUIREMENTS.md` returns `1`
    - `npm test` full suite completes with ONLY the 3 pre-existing baseline failures (no new failures introduced) — COLLAB-ROLLBACK-02 final acceptance
    - `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render gate still green — success criterion #3)
    - `npm test -- src/__tests__/editor-extensions.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
    - Full-codebase grep audit (`grep -rn "HocuspocusProvider\|hocuspocus-server\|providerRef\|@hocuspocus\|from 'yjs'\|from '@tiptap/extension-collaboration\|inline-comment-mark" src/ app/ --include="*.ts" --include="*.tsx" --include="*.css"`) returns zero matches
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md annotations audited and confirmed present. Final full-test-suite run green modulo baseline. TypeScript compile clean. Full-codebase residual grep audit returns zero matches. Phase 14 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<verification>
- Task 1 wave gate: `npm test` exits 0 (or shows only the 3 baseline failures) after package/env/CSS cleanup (VALIDATION.md Wave 6 row)
- Task 2 is itself the final phase-wide acceptance gate
- Final render-gate test `section-content-view.test.tsx` + extensions test `editor-extensions.test.ts` both green confirms COLLAB-ROLLBACK-02
- REQUIREMENTS.md grep count confirms success criterion #4
- Zero-match residual grep confirms COLLAB-ROLLBACK-01 comprehensive removal
</verification>

<success_criteria>
- 3 collab npm packages removed from package.json; lockfile reconciled
- .env.local cleaned; .env.example and hocuspocus-server absence formally verified
- app/globals.css stripped of .collaboration-cursor__* and .inline-comment-mark rules
- REQUIREMENTS.md EDIT-06/07/08 annotations confirmed (grep count >= 3)
- `npm test` full suite green modulo 3 baseline failures
- `npx tsc --noEmit` clean
- Full-codebase residual grep returns zero matches
- Phase 14 COLLAB-ROLLBACK-01 and COLLAB-ROLLBACK-02 both observably satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-collab-rollback/14-04-SUMMARY.md` documenting:
- npm packages removed (list the 3)
- .env.local and CSS edits summary
- Formal absence audits for .env.example and hocuspocus-server/
- REQUIREMENTS.md annotation audit (grep counts)
- Final `npm test` output tail (must show only the 3 baseline failures)
- `npx tsc --noEmit` output confirmation
- Final codebase residual grep result (should be empty)
- Confirmation phase is ready for `/gsd:verify-work`
</output>
