---
phase: 14-collab-rollback
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/(workspace)/policies/[id]/_components/block-editor.tsx
  - src/lib/tiptap-extensions/build-extensions.ts
  - src/lib/tiptap-extensions/inline-comment-mark.ts
  - src/__tests__/editor-extensions.test.ts
autonomous: true
requirements:
  - COLLAB-ROLLBACK-01
  - COLLAB-ROLLBACK-02
must_haves:
  truths:
    - "block-editor.tsx contains zero references to providerRef, HocuspocusProvider, PresenceBar, ConnectionStatus, CommentPanel, CommentBubble, getPresenceColor, useSession, useUser"
    - "handleUpdate and handleBlur call debouncedSave unconditionally — auto-save fires regardless of editor idle state"
    - "buildExtensions() does not import or register Collaboration, CollaborationCaret, or InlineComment extensions"
    - "inline-comment-mark.ts no longer exists on disk"
    - "BuildExtensionsOptions interface has no `collaboration?` field"
    - "section-content-view.test.tsx (render gate) and editor-extensions.test.ts both pass"
    - "COLLAB-ROLLBACK-02 is observably true: the editor loads, accepts input, and auto-saves in single-user mode without Yjs/Collaboration imports"
  artifacts:
    - path: "app/(workspace)/policies/[id]/_components/block-editor.tsx"
      provides: "Clean single-user block editor; no collab code paths remain"
      min_lines: 100
    - path: "src/lib/tiptap-extensions/build-extensions.ts"
      provides: "buildExtensions() returning extensions array with zero Collaboration/InlineComment entries"
    - path: "src/lib/tiptap-extensions/inline-comment-mark.ts"
      provides: "ABSENT — file removed"
  key_links:
    - from: "block-editor.tsx handleUpdate"
      to: "debouncedSave"
      via: "unconditional call"
      pattern: "debouncedSave\\(editor\\.getJSON"
    - from: "buildExtensions return value"
      to: "Tiptap extensions array"
      via: "no Collaboration or InlineComment entries"
      pattern: "(?!.*Collaboration)(?!.*InlineComment).*extensions\\.push"
---

<objective>
Excise all Yjs/Hocuspocus/InlineComment code from the editor client surface. Two tightly coupled sub-waves:
- Wave 2a: Rewrite `block-editor.tsx` to remove all `providerRef.current` access sites (5 in total per RESEARCH), Hocuspocus provider init useEffect, collab-related state, Clerk session/user imports (only used for collab), and PresenceBar/ConnectionStatus/CommentBubble/CommentPanel JSX + imports.
- Wave 2b: Rewrite `build-extensions.ts` to drop the `collaboration?` option, the conditional Collaboration/CollaborationCaret push block, the `InlineComment` mark import + array entry, and the `undoRedo: options?.collaboration ? false : undefined` hack. Delete `inline-comment-mark.ts`. Update `editor-extensions.test.ts` to drop `inlineComment` from expected names.

Purpose: This is the single most error-prone step in the phase. RESEARCH § Pitfall 1 identifies 5 distinct `providerRef.current` access sites that must all be removed together — a partial deletion produces silent runtime auto-save bugs. Validation is end-of-plan via the render test (`section-content-view.test.tsx`) and the extensions unit test (`editor-extensions.test.ts`).

Output: Clean single-user block editor. All `npm test` files green modulo baseline.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-collab-rollback/14-RESEARCH.md
@.planning/phases/14-collab-rollback/14-VALIDATION.md
@.planning/phases/14-collab-rollback/14-01-SUMMARY.md
@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- Target post-rollback state extracted verbatim from RESEARCH.md § Post-Rollback BlockEditor Target State and § Post-Rollback buildExtensions Target State -->

Post-rollback block-editor.tsx must have:
1. No `HocuspocusProvider`, `useSession`, `useUser` (Clerk) imports
2. No `providerRef`, `connectionStatus`, `providerReady` state
3. `buildExtensions` called WITHOUT `collaboration:` option
4. `content` prop always set unconditionally from `section.content ?? { type: 'doc', content: [{ type: 'paragraph' }] }`
5. `handleUpdate` unconditionally calls `debouncedSave(editor.getJSON() as Record<string, unknown>)`
6. `handleBlur` unconditionally calls `debouncedSave.flush()`
7. No `PresenceBar`, `ConnectionStatus`, `CommentBubble`, `CommentPanel` JSX or imports
8. No `commentPanelOpen`, `pendingComment`, `activeCommentId` state
9. No comment-click `useEffect`
10. No `getPresenceColor` import
11. No `HOCUSPOCUS_URL` const
12. No `CollabConnectionStatus` type alias

Post-rollback buildExtensions target (from RESEARCH.md lines 219-241):
```typescript
// Remove these imports:
// import { Collaboration } from '@tiptap/extension-collaboration'
// import { CollaborationCaret } from '@tiptap/extension-collaboration-caret'
// import type { HocuspocusProvider } from '@hocuspocus/provider'
// import type * as Y from 'yjs'
// import { InlineComment } from './inline-comment-mark'

export interface BuildExtensionsOptions {
  onSlashCommand?: Partial<SuggestionOptions>
  // Remove: collaboration?: { ... }
}

// Remove in StarterKit.configure:
// undoRedo: options?.collaboration ? false : undefined
// → just delete the undoRedo key entirely (use Tiptap default)

// Remove from extensions array:
// InlineComment,

// Remove entire block:
// if (options?.collaboration) { extensions.push(Collaboration..., CollaborationCaret...) }
```

Example post-rollback handleUpdate (from RESEARCH.md § Code Examples):
```typescript
const handleUpdate = useCallback(
  ({ editor }: { editor: Editor }) => {
    isDirtyRef.current = true
    debouncedSave(editor.getJSON() as Record<string, unknown>)
  },
  [debouncedSave],
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite block-editor.tsx to remove all collab/comment code paths</name>
  <files>app/(workspace)/policies/[id]/_components/block-editor.tsx</files>
  <read_first>
    - app/(workspace)/policies/[id]/_components/block-editor.tsx (READ IN FULL — you must see current state before editing)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Subsystem: Editor Client with exact line-number inventory, § Pitfall 1 "Leaving providerRef.current Access Sites", § Post-Rollback BlockEditor Target State)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-VALIDATION.md (§ Per-Wave Test Cadence Wave 2 row)
    - ./AGENTS.md ("This is NOT the Next.js you know" — if you touch any Next.js API, consult `node_modules/next/dist/docs/` first)
  </read_first>
  <behavior>
    After this task:
    - Test 1: `grep -c "providerRef" block-editor.tsx` returns 0 (all 5 access sites from RESEARCH Pitfall 1 eliminated)
    - Test 2: `grep -c "HocuspocusProvider\|@hocuspocus/provider" block-editor.tsx` returns 0
    - Test 3: `grep -c "PresenceBar\|ConnectionStatus\|CommentPanel\|CommentBubble\|PendingComment" block-editor.tsx` returns 0
    - Test 4: `grep -c "getPresenceColor\|presence-colors" block-editor.tsx` returns 0
    - Test 5: `grep -c "useSession\|useUser" block-editor.tsx` returns 0 (Clerk hooks were only used for collab)
    - Test 6: `grep -c "HOCUSPOCUS_URL" block-editor.tsx` returns 0
    - Test 7: `grep -c "commentPanelOpen\|pendingComment\|activeCommentId" block-editor.tsx` returns 0
    - Test 8: `handleUpdate` calls `debouncedSave(editor.getJSON() as Record<string, unknown>)` unconditionally (no `if (!providerRef.current || ...)` guard)
    - Test 9: `handleBlur` calls `debouncedSave.flush()` unconditionally
    - Test 10: `buildExtensions` call site passes no `collaboration:` option
    - Test 11: `content:` prop in `useEditor` is set from `section.content ?? { type: 'doc', content: [{ type: 'paragraph' }] }` unconditionally (no ternary on providerRef.current)
    - Test 12: `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render gate for COLLAB-ROLLBACK-02)
  </behavior>
  <action>
    Open `app/(workspace)/policies/[id]/_components/block-editor.tsx` and perform the following edits. Use the line numbers from RESEARCH.md as starting reference, but verify against the current file contents before each edit.

    **Imports to REMOVE** (from top of file):
    - `import { HocuspocusProvider } from '@hocuspocus/provider'` (line ~6)
    - `import { useSession, useUser } from '@clerk/nextjs'` (line ~8) — verify nothing else in the file uses these before removing
    - `import { getPresenceColor } from '@/lib/collaboration/presence-colors'` (line ~19)
    - `import { PresenceBar } from './presence-bar'` (line ~28)
    - `import { ConnectionStatus } from './connection-status'` (line ~29)
    - `import { CommentBubble, type PendingComment } from './comment-bubble'` (line ~30)
    - `import { CommentPanel } from './comment-panel'` (line ~31)

    **Type aliases to REMOVE**:
    - `type CollabConnectionStatus = ...`

    **Const to REMOVE**:
    - `const HOCUSPOCUS_URL = process.env.NEXT_PUBLIC_HOCUSPOCUS_URL` (line ~73)

    **State declarations to REMOVE** (inside the component):
    - `const [commentPanelOpen, setCommentPanelOpen] = useState(...)` (line ~82)
    - `const [pendingComment, setPendingComment] = useState<PendingComment | null>(null)` (line ~83)
    - `const [activeCommentId, setActiveCommentId] = useState<string | null>(null)` (line ~84)
    - `const providerRef = useRef<HocuspocusProvider | null>(null)` (line ~87)
    - `const [connectionStatus, setConnectionStatus] = useState<CollabConnectionStatus>('disconnected')` (line ~88)
    - `const [providerReady, setProviderReady] = useState(false)` (line ~89)
    - `const { session } = useSession()` (line ~92)
    - `const { user } = useUser()` (line ~93)
    - Any `connectionStatusRef = useRef(connectionStatus)` sync ref (check file)

    **Callback edits**:
    - `handleUpdate`: remove the `if (!providerRef.current || connectionStatusRef.current === 'disconnected')` guard so it becomes:
      ```typescript
      const handleUpdate = useCallback(
        ({ editor }: { editor: Editor }) => {
          isDirtyRef.current = true
          debouncedSave(editor.getJSON() as Record<string, unknown>)
        },
        [debouncedSave],
      )
      ```
    - `handleBlur`: remove the `if (!providerRef.current || ...)` guard so it calls `debouncedSave.flush()` unconditionally

    **useEffect to REMOVE ENTIRELY**:
    - The provider-init useEffect that spans ~lines 161-221 (the one that constructs `new HocuspocusProvider(...)`, sets up `onStatus`, `onConnect`, `onDisconnect`, writes to `providerRef.current`, and sets `providerReady`). Delete the whole block from `useEffect(() => {` to the closing `}, [...])`.
    - The comment-click useEffect that spans ~lines 412-431 (the one listening for clicks on `.inline-comment-mark` elements). Delete entirely.

    **useEditor config edits**:
    - Change `extensions: buildExtensions({ ..., collaboration: providerRef.current ? { ... } : undefined })` to `extensions: buildExtensions({ onSlashCommand: <existing config> })` — drop the entire `collaboration:` key (lines ~264-274)
    - Change `content: providerRef.current ? undefined : (section.content ?? { type: 'doc', content: [{ type: 'paragraph' }] })` to `content: section.content ?? { type: 'doc', content: [{ type: 'paragraph' }] }` (line ~347-352)

    **Comment handlers to REMOVE**:
    - `handleCreateComment`, `handleCloseCommentPanel`, `handleClearPending` (lines ~393-431)

    **JSX to REMOVE**:
    - `{providerRef.current && <PresenceBar provider={providerRef.current} />}` (line ~455)
    - `{providerRef.current && <ConnectionStatus status={connectionStatus} />}` (line ~465)
    - `<CommentBubble ... />` (line ~527)
    - `<CommentPanel ... />` (lines ~541-551)

    **Verification loop**: After editing, grep the file for `providerRef`, `HocuspocusProvider`, `PresenceBar`, `ConnectionStatus`, `CommentBubble`, `CommentPanel`, `PendingComment`, `getPresenceColor`, `useSession`, `useUser`, `HOCUSPOCUS_URL`, `commentPanelOpen`, `pendingComment`, `activeCommentId` — all must return 0 matches. If any match remains, it's a leftover that will cause a compile error or a silent auto-save bug (Pitfall 1).

    **Next.js 16 note**: This file uses `'use client'`; no server APIs touched. No need to consult `node_modules/next/dist/docs/` unless you discover a Next.js API in the file that you don't recognize.
  </action>
  <verify>
    <automated>grep -c "providerRef" "app/(workspace)/policies/[id]/_components/block-editor.tsx" | grep -q "^0$" &amp;&amp; grep -c "HocuspocusProvider\|@hocuspocus/provider" "app/(workspace)/policies/[id]/_components/block-editor.tsx" | grep -q "^0$" &amp;&amp; grep -c "PresenceBar\|ConnectionStatus\|CommentPanel\|CommentBubble" "app/(workspace)/policies/[id]/_components/block-editor.tsx" | grep -q "^0$" &amp;&amp; grep -c "useSession\|useUser" "app/(workspace)/policies/[id]/_components/block-editor.tsx" | grep -q "^0$" &amp;&amp; npm test -- src/__tests__/section-content-view.test.tsx 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "providerRef" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0` (RESEARCH Pitfall 1: all 5 access sites gone)
    - `grep -c "HocuspocusProvider" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "@hocuspocus/provider" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "PresenceBar" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "ConnectionStatus" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "CommentPanel" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "CommentBubble" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "getPresenceColor" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "useSession\|useUser" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "HOCUSPOCUS_URL" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "commentPanelOpen\|pendingComment\|activeCommentId" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0`
    - `grep -c "collaboration:" "app/(workspace)/policies/[id]/_components/block-editor.tsx"` returns `0` (no collaboration option passed to buildExtensions)
    - `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render-gate test green; COLLAB-ROLLBACK-02 verified)
  </acceptance_criteria>
  <done>
    block-editor.tsx contains zero references to any Yjs/Hocuspocus/comment concept. handleUpdate/handleBlur are unconditional. Render gate test passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite build-extensions.ts, delete inline-comment-mark.ts, update editor-extensions.test.ts</name>
  <files>
    src/lib/tiptap-extensions/build-extensions.ts,
    src/lib/tiptap-extensions/inline-comment-mark.ts,
    src/__tests__/editor-extensions.test.ts
  </files>
  <read_first>
    - src/lib/tiptap-extensions/build-extensions.ts (READ IN FULL)
    - src/lib/tiptap-extensions/inline-comment-mark.ts (confirm it is the mark targeted for deletion)
    - src/__tests__/editor-extensions.test.ts (READ IN FULL — this test stays and needs minor updates)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Post-Rollback buildExtensions Target State, § Pitfall 2 "Forgetting InlineComment in editor-extensions.test.ts")
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-VALIDATION.md (§ Per-Wave Test Cadence Wave 3 row)
  </read_first>
  <behavior>
    After this task:
    - Test 1: `grep -c "Collaboration\|CollaborationCaret\|@hocuspocus/provider\|from 'yjs'" src/lib/tiptap-extensions/build-extensions.ts` returns 0
    - Test 2: `grep -c "InlineComment\|inline-comment-mark" src/lib/tiptap-extensions/build-extensions.ts` returns 0
    - Test 3: `grep -c "collaboration?" src/lib/tiptap-extensions/build-extensions.ts` returns 0
    - Test 4: `grep -c "undoRedo" src/lib/tiptap-extensions/build-extensions.ts` returns 0 (Tiptap default is used)
    - Test 5: `ls src/lib/tiptap-extensions/inline-comment-mark.ts` exits non-zero
    - Test 6: `grep -c "inlineComment" src/__tests__/editor-extensions.test.ts` returns 0 (test no longer expects inlineComment)
    - Test 7: `npm test -- src/__tests__/editor-extensions.test.ts` exits 0 — extension count still ≥ 15 (per RESEARCH line 427, the 17 remaining extensions after InlineComment removal)
    - Test 8: `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render gate remains green)
  </behavior>
  <action>
    **Part A — Edit `src/lib/tiptap-extensions/build-extensions.ts`:**

    Remove these imports (verbatim):
    - `import { Collaboration } from '@tiptap/extension-collaboration'` (line ~9)
    - `import { CollaborationCaret } from '@tiptap/extension-collaboration-caret'` (line ~10)
    - `import type { HocuspocusProvider } from '@hocuspocus/provider'` (line ~14)
    - `import type * as Y from 'yjs'` (line ~15)
    - `import { InlineComment } from './inline-comment-mark'` (line ~21)

    Edit `BuildExtensionsOptions` interface: remove the `collaboration?: { ydoc: Y.Doc; provider: HocuspocusProvider; user: {...} }` field entirely. Final interface should only have `onSlashCommand?: Partial<SuggestionOptions>` (preserve any other non-collab options that exist).

    Edit `StarterKit.configure(...)`: remove the `undoRedo: options?.collaboration ? false : undefined` key entirely (don't replace with anything — Tiptap's default for undoRedo is correct for single-user mode).

    Edit extensions array: remove the `InlineComment,` line (line ~121 per RESEARCH).

    Remove the entire `if (options?.collaboration) { extensions.push(Collaboration.configure({ document: options.collaboration.ydoc }), CollaborationCaret.configure({ provider: options.collaboration.provider, user: options.collaboration.user })) }` block (lines ~124-134).

    **Part B — Delete the mark file:**
    - `rm src/lib/tiptap-extensions/inline-comment-mark.ts`

    **Part C — Update `src/__tests__/editor-extensions.test.ts`:**
    - Remove `inlineComment` from the list of expected extension names (the test asserts specific extension names are present). RESEARCH § Pitfall 2 notes the test also checks `>= 15`; after InlineComment removal the remaining count is 17 (StarterKit, CodeBlockLowlight, Image, FileHandler, Table, TableRow, TableCell, TableHeader, Details, DetailsSummary, DetailsContent, NodeRange, Callout, FileAttachment, LinkPreview, SlashCommands, Placeholder) so the `>= 15` assertion still holds — do NOT change the numeric threshold, just drop `inlineComment` from the expected names array.
    - If there is any other reference to `inlineComment` or `InlineComment` in this test file, remove it as well.

    **Verification loop**: grep each file. If any `Collaboration`, `InlineComment`, `HocuspocusProvider`, or `yjs` reference remains in build-extensions.ts, fix it. Run the two target tests.
  </action>
  <verify>
    <automated>grep -c "Collaboration\|CollaborationCaret\|@hocuspocus/provider\|from 'yjs'\|InlineComment\|inline-comment-mark" src/lib/tiptap-extensions/build-extensions.ts | grep -q "^0$" &amp;&amp; test ! -e src/lib/tiptap-extensions/inline-comment-mark.ts &amp;&amp; grep -c "inlineComment" src/__tests__/editor-extensions.test.ts | grep -q "^0$" &amp;&amp; npm test -- src/__tests__/editor-extensions.test.ts src/__tests__/section-content-view.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Collaboration" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "CollaborationCaret" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "@hocuspocus/provider" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "from 'yjs'" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "InlineComment" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "inline-comment-mark" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "collaboration?" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `grep -c "undoRedo" src/lib/tiptap-extensions/build-extensions.ts` returns `0`
    - `ls src/lib/tiptap-extensions/inline-comment-mark.ts` exits non-zero
    - `grep -c "inlineComment" src/__tests__/editor-extensions.test.ts` returns `0`
    - `npm test -- src/__tests__/editor-extensions.test.ts` exits 0 (Wave 3 acceptance signal per VALIDATION.md: buildExtensions returns array with zero collaboration* entries, count still ≥ 15)
    - `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render gate stays green)
  </acceptance_criteria>
  <done>
    build-extensions.ts is collab-free; inline-comment-mark.ts deleted; editor-extensions.test.ts updated and passing; render gate passing. COLLAB-ROLLBACK-02 verified at the extension-building level.
  </done>
</task>

</tasks>

<verification>
- After Task 1: `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (Wave 2 acceptance gate per VALIDATION.md)
- After Task 2: `npm test -- src/__tests__/editor-extensions.test.ts src/__tests__/section-content-view.test.tsx` exits 0 (Wave 3 acceptance gate)
- Final: `npm test` shows no new failures beyond the 3 pre-existing baseline failures
- Grep audit: `grep -rn "HocuspocusProvider\|providerRef\|InlineComment" "app/(workspace)/policies/[id]/_components/block-editor.tsx" "src/lib/tiptap-extensions/build-extensions.ts"` returns zero matches
</verification>

<success_criteria>
- block-editor.tsx contains zero Yjs/Hocuspocus/comment imports, state, or JSX
- handleUpdate/handleBlur call debouncedSave unconditionally
- buildExtensions() returns extensions array without Collaboration, CollaborationCaret, or InlineComment
- inline-comment-mark.ts removed from disk
- editor-extensions.test.ts no longer expects `inlineComment` in extension names
- Both render gate (section-content-view.test.tsx) and unit gate (editor-extensions.test.ts) pass
- COLLAB-ROLLBACK-02 verifiable via automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-collab-rollback/14-02-SUMMARY.md` documenting:
- Summary of edits to block-editor.tsx (list of removed imports/state/effects/JSX)
- Summary of edits to build-extensions.ts
- Confirmation inline-comment-mark.ts deleted
- Test output for editor-extensions.test.ts and section-content-view.test.tsx
</output>
