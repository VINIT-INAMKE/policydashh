---
phase: 14-collab-rollback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(workspace)/policies/[id]/_components/presence-bar.tsx
  - app/(workspace)/policies/[id]/_components/connection-status.tsx
  - app/(workspace)/policies/[id]/_components/comment-thread.tsx
  - app/(workspace)/policies/[id]/_components/comment-bubble.tsx
  - app/(workspace)/policies/[id]/_components/comment-panel.tsx
  - src/__tests__/inline-comment-mark.test.ts
  - src/__tests__/build-extensions-collab.test.ts
  - src/__tests__/comments-router.test.ts
  - src/lib/hooks/use-presence.ts
  - src/lib/collaboration/presence-colors.ts
autonomous: true
requirements:
  - COLLAB-ROLLBACK-01
must_haves:
  truths:
    - "Standalone collab UI components (PresenceBar, ConnectionStatus, CommentPanel, CommentBubble, CommentThread) no longer exist in the repo"
    - "Collab-specific test files (inline-comment-mark, build-extensions-collab, comments-router) are removed so they do not reference files deleted in later waves"
    - "Presence hook and presence-colors utility are removed; src/lib/collaboration/ directory no longer exists"
    - "Full test suite passes (excluding the 3 pre-existing baseline failures) — no Wave 1 deletion causes an import crash"
  artifacts:
    - path: "app/(workspace)/policies/[id]/_components/presence-bar.tsx"
      provides: "ABSENT — grep-verified zero hits"
    - path: "src/lib/collaboration/"
      provides: "ABSENT — directory removed entirely"
  key_links:
    - from: "npm test"
      to: "deleted files"
      via: "test suite still passes"
      pattern: "Test Files.*passed"
---

<objective>
Delete 10 standalone Yjs/comment UI + hook + utility files that have no inbound dependencies from other remaining code (after Wave 1, `block-editor.tsx` still imports several of these — that is expected and handled in Plan 02). Also remove collab-specific test files BEFORE their underlying source is removed to prevent test-suite breakage in later waves.

Purpose: Establishes a clean deletion beachhead. Each file on this list is either leaf-only (no dependents outside the soon-to-be-edited `block-editor.tsx`) or is a test that targets code slated for deletion in a later plan. Deleting tests first (Pitfall 3 in RESEARCH.md) prevents import-of-deleted-code failures.

Output: 10 files removed from disk; `src/lib/collaboration/` directory removed; `npm test` passes (excluding the 3 pre-existing baseline failures documented in 14-RESEARCH.md § Open Questions 2).
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-collab-rollback/14-RESEARCH.md
@.planning/phases/14-collab-rollback/14-VALIDATION.md
@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- Files slated for deletion in this plan. These are the files the executor will REMOVE. -->
<!-- Note: `block-editor.tsx` still imports PresenceBar/ConnectionStatus/CommentBubble/CommentPanel/getPresenceColor at the end of Wave 1 — that will produce a transient TypeScript error for block-editor.tsx only, which is expected and resolved in Plan 02 Task 1. The `section-content-view.test.tsx` render test is insulated because it mocks `next/dynamic` and stubs BlockEditor. Run `npm test` not `tsc --noEmit` as the Wave 1 acceptance gate. -->

Files to delete (from RESEARCH.md § Wave 1):
1. app/(workspace)/policies/[id]/_components/presence-bar.tsx
2. app/(workspace)/policies/[id]/_components/connection-status.tsx
3. app/(workspace)/policies/[id]/_components/comment-thread.tsx
4. app/(workspace)/policies/[id]/_components/comment-bubble.tsx
5. app/(workspace)/policies/[id]/_components/comment-panel.tsx
6. src/__tests__/inline-comment-mark.test.ts
7. src/__tests__/build-extensions-collab.test.ts
8. src/__tests__/comments-router.test.ts
9. src/lib/hooks/use-presence.ts
10. src/lib/collaboration/presence-colors.ts  (then remove the now-empty src/lib/collaboration/ directory)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete collab-specific test files (must precede source deletions)</name>
  <files>
    src/__tests__/inline-comment-mark.test.ts,
    src/__tests__/build-extensions-collab.test.ts,
    src/__tests__/comments-router.test.ts
  </files>
  <read_first>
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Pitfall 3 "Deleting Tests Before Removing Code" and § Wave 1 ordering)
    - src/__tests__/inline-comment-mark.test.ts (confirm it is the test for the mark being deleted in Plan 02)
    - src/__tests__/build-extensions-collab.test.ts (confirm it tests the collaboration option path in buildExtensions)
    - src/__tests__/comments-router.test.ts (confirm it tests commentRouter procedures deleted in Plan 03)
  </read_first>
  <action>
    Delete the following three test files from disk (exact paths):
    1. `src/__tests__/inline-comment-mark.test.ts`
    2. `src/__tests__/build-extensions-collab.test.ts`
    3. `src/__tests__/comments-router.test.ts`

    Use filesystem delete (not git rm — git will pick up deletions on commit). On Windows bash use `rm src/__tests__/inline-comment-mark.test.ts src/__tests__/build-extensions-collab.test.ts src/__tests__/comments-router.test.ts`.

    Rationale: per RESEARCH § Pitfall 3, tests must be deleted BEFORE their target source files, otherwise `npm test` fails on import errors when the test module tries to import deleted code. These three tests target `inline-comment-mark.ts` (Plan 02 Task 2), the `collaboration:` option in `buildExtensions` (Plan 02 Task 2), and `commentRouter` (Plan 03 Task 1) respectively.

    Do NOT delete `src/__tests__/editor-extensions.test.ts` — that test stays and will be updated in Plan 02. Do NOT delete `src/__tests__/section-content-view.test.tsx` — that is the render-gate test per VALIDATION.md.
  </action>
  <verify>
    <automated>ls src/__tests__/inline-comment-mark.test.ts src/__tests__/build-extensions-collab.test.ts src/__tests__/comments-router.test.ts 2>&amp;1 | grep -c "No such file\|cannot find" | grep -q "^3$" &amp;&amp; npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/__tests__/inline-comment-mark.test.ts` exits non-zero (file absent)
    - `ls src/__tests__/build-extensions-collab.test.ts` exits non-zero (file absent)
    - `ls src/__tests__/comments-router.test.ts` exits non-zero (file absent)
    - `ls src/__tests__/editor-extensions.test.ts` exits 0 (still present)
    - `ls src/__tests__/section-content-view.test.tsx` exits 0 (still present)
    - `npm test` exits 0 OR shows only the 3 pre-existing baseline failures (feedback-permissions.test.ts x2, document-router-scope.test.ts x1) and no new failures
  </acceptance_criteria>
  <done>
    Three collab-specific test files removed; test suite still green modulo baseline failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete 7 standalone collab source files + src/lib/collaboration directory</name>
  <files>
    app/(workspace)/policies/[id]/_components/presence-bar.tsx,
    app/(workspace)/policies/[id]/_components/connection-status.tsx,
    app/(workspace)/policies/[id]/_components/comment-thread.tsx,
    app/(workspace)/policies/[id]/_components/comment-bubble.tsx,
    app/(workspace)/policies/[id]/_components/comment-panel.tsx,
    src/lib/hooks/use-presence.ts,
    src/lib/collaboration/presence-colors.ts
  </files>
  <read_first>
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-RESEARCH.md (§ Wave 1 deletion list, § Subsystem: Editor Client)
    - D:/aditee/policydash/.planning/phases/14-collab-rollback/14-VALIDATION.md (§ Per-Wave Test Cadence Wave 1 row)
    - app/(workspace)/policies/[id]/_components/presence-bar.tsx (confirm it only imports HocuspocusProvider + usePresence)
    - app/(workspace)/policies/[id]/_components/comment-panel.tsx (confirm it uses trpc.comments.*)
    - src/lib/collaboration/presence-colors.ts (confirm it is the only file in src/lib/collaboration/)
  </read_first>
  <action>
    Delete all 7 source files from disk:
    1. `app/(workspace)/policies/[id]/_components/presence-bar.tsx`
    2. `app/(workspace)/policies/[id]/_components/connection-status.tsx`
    3. `app/(workspace)/policies/[id]/_components/comment-thread.tsx`
    4. `app/(workspace)/policies/[id]/_components/comment-bubble.tsx`
    5. `app/(workspace)/policies/[id]/_components/comment-panel.tsx`
    6. `src/lib/hooks/use-presence.ts`
    7. `src/lib/collaboration/presence-colors.ts`

    After deleting `presence-colors.ts`, verify that `src/lib/collaboration/` is now empty and remove the now-empty directory:
    `rmdir src/lib/collaboration 2>/dev/null || rm -rf src/lib/collaboration`

    Delete ordering within the set does not matter — none of these 7 files import each other except comment-panel.tsx → comment-thread.tsx and presence-bar.tsx → use-presence.ts → presence-colors.ts, and all three levels of each chain are being deleted together.

    Expected transient state after this task: `block-editor.tsx` will contain broken imports for PresenceBar, ConnectionStatus, CommentBubble, PendingComment, CommentPanel, and getPresenceColor. This is INTENTIONAL and is resolved in Plan 02 Task 1. DO NOT edit block-editor.tsx in this plan.

    Acceptance signal for Wave 1 is `npm test` passing — NOT `tsc --noEmit`. The render test stubs BlockEditor via `vi.mock('next/dynamic', ...)`, so jsdom does not exercise the real block-editor.tsx module and the transient compile error does not surface during test execution.
  </action>
  <verify>
    <automated>test ! -e "app/(workspace)/policies/[id]/_components/presence-bar.tsx" &amp;&amp; test ! -e "app/(workspace)/policies/[id]/_components/connection-status.tsx" &amp;&amp; test ! -e "app/(workspace)/policies/[id]/_components/comment-thread.tsx" &amp;&amp; test ! -e "app/(workspace)/policies/[id]/_components/comment-bubble.tsx" &amp;&amp; test ! -e "app/(workspace)/policies/[id]/_components/comment-panel.tsx" &amp;&amp; test ! -e "src/lib/hooks/use-presence.ts" &amp;&amp; test ! -e "src/lib/collaboration" &amp;&amp; npm test 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls "app/(workspace)/policies/[id]/_components/presence-bar.tsx"` exits non-zero
    - `ls "app/(workspace)/policies/[id]/_components/connection-status.tsx"` exits non-zero
    - `ls "app/(workspace)/policies/[id]/_components/comment-thread.tsx"` exits non-zero
    - `ls "app/(workspace)/policies/[id]/_components/comment-bubble.tsx"` exits non-zero
    - `ls "app/(workspace)/policies/[id]/_components/comment-panel.tsx"` exits non-zero
    - `ls src/lib/hooks/use-presence.ts` exits non-zero
    - `ls src/lib/collaboration/presence-colors.ts` exits non-zero
    - `test -d src/lib/collaboration` exits non-zero (directory gone)
    - `npm test` shows no NEW failures beyond the 3 pre-existing baseline failures (acceptance signal for Wave 1 per VALIDATION.md)
    - `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render test still green because BlockEditor is mocked)
  </acceptance_criteria>
  <done>
    7 standalone source files + empty src/lib/collaboration/ directory removed. Test suite passes (modulo baseline). block-editor.tsx compile errors are transient and expected — Plan 02 wave 2 will resolve them.
  </done>
</task>

</tasks>

<verification>
- Run `npm test` after Task 2 — must show no new failures beyond the 3 pre-existing baseline failures
- Run `grep -r "from.*presence-bar\|from.*comment-panel\|from.*comment-bubble\|from.*comment-thread\|from.*use-presence\|from.*presence-colors" src/ app/ --include="*.ts" --include="*.tsx"` — only hits should be inside `block-editor.tsx` (transient, fixed in Plan 02)
- Render test gate: `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (success criterion #5 — render test pass after each deletion step)
</verification>

<success_criteria>
- 10 files deleted (3 tests + 7 source/utility)
- `src/lib/collaboration/` directory no longer exists
- `npm test` passes modulo 3 pre-existing baseline failures
- `npm test -- src/__tests__/section-content-view.test.tsx` exits 0 (render-gate test green)
- No new TypeScript files created
- No edits to block-editor.tsx, build-extensions.ts, inline-comment-mark.ts, comments.ts, schema, package.json, or REQUIREMENTS.md in this plan (those are later plans)
</success_criteria>

<output>
After completion, create `.planning/phases/14-collab-rollback/14-01-SUMMARY.md` documenting:
- Files deleted (list the 10)
- `npm test` output tail confirming no new failures
- Confirmation of render-gate test pass
- Note that block-editor.tsx has intentional transient broken imports awaiting Plan 02
</output>
