---
phase: 09-public-portal-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/lib/permissions.ts
  - app/(workspace)/audit/page.tsx
  - app/(workspace)/audit/_components/audit-filter-panel.tsx
  - app/(workspace)/audit/_components/audit-event-table.tsx
  - app/(workspace)/audit/_components/evidence-pack-dialog.tsx
  - app/api/export/evidence-pack/route.ts
  - src/server/services/evidence-pack.service.ts
  - app/(workspace)/_components/workspace-nav.tsx
  - app/(workspace)/dashboard/_components/auditor-dashboard.tsx
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06

must_haves:
  truths:
    - "Auditor can view the full audit trail at /audit with filtering by action type, entity type, actor role, and date range"
    - "Audit trail table shows all audit events with pagination (50 per page default)"
    - "Admin/Auditor can export a milestone evidence pack as a structured ZIP containing stakeholder list, feedback matrix, version history, decision logs, and workshop placeholder"
    - "Evidence pack ZIP contains an INDEX.md file documenting contents"
    - "Non-auditor/non-admin users cannot access /audit or export evidence packs"
    - "Auditor dashboard 'View Full Audit Trail' button links to /audit"
    - "WorkspaceNav shows 'Audit' link for auditor/admin roles only"
  artifacts:
    - path: "app/(workspace)/audit/page.tsx"
      provides: "Audit trail viewer page"
      contains: "Audit Trail"
    - path: "app/(workspace)/audit/_components/audit-event-table.tsx"
      provides: "Paginated audit event table with expandable metadata"
    - path: "app/(workspace)/audit/_components/audit-filter-panel.tsx"
      provides: "Horizontal filter bar for audit events"
    - path: "app/(workspace)/audit/_components/evidence-pack-dialog.tsx"
      provides: "Export Evidence Pack dialog with policy selector"
    - path: "app/api/export/evidence-pack/route.ts"
      provides: "ZIP evidence pack export route handler"
    - path: "src/server/services/evidence-pack.service.ts"
      provides: "Evidence pack assembly logic"
    - path: "src/lib/constants.ts"
      provides: "EVIDENCE_PACK_EXPORT action constant"
      contains: "evidence_pack.export"
  key_links:
    - from: "app/(workspace)/audit/page.tsx"
      to: "src/server/routers/audit.ts"
      via: "tRPC audit.list query"
      pattern: "trpc.*audit.*list"
    - from: "app/api/export/evidence-pack/route.ts"
      to: "src/server/services/evidence-pack.service.ts"
      via: "service function call"
      pattern: "buildEvidencePack"
    - from: "app/api/export/evidence-pack/route.ts"
      to: "fflate"
      via: "zipSync for ZIP assembly"
      pattern: "zipSync"
    - from: "app/(workspace)/dashboard/_components/auditor-dashboard.tsx"
      to: "/audit"
      via: "enabled View Full Audit Trail button"
      pattern: "href.*audit"
---

<objective>
Build the audit trail viewer page and milestone evidence pack ZIP export for compliance review.

Purpose: Auditors need a dedicated interface to review the full system audit trail with filtering, and must be able to export structured evidence packs for milestone governance reviews.
Output: Authenticated /audit page with filter panel and paginated event table, evidence pack service producing ZIP bundles, export API route, and integration with existing auditor dashboard.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-public-portal-compliance/09-RESEARCH.md
@.planning/phases/09-public-portal-compliance/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/server/routers/audit.ts:
```typescript
export const auditRouter = router({
  list: requirePermission('audit:read')
    .input(z.object({
      limit: z.number().min(1).max(100).default(50),
      offset: z.number().min(0).default(0),
      entityType: z.string().optional(),
      actorId: z.string().optional(),
      action: z.string().optional(),
      from: z.string().datetime().optional(),
      to: z.string().datetime().optional(),
    }).optional())
    .query(async ({ input }) => { /* returns audit events array */ }),
})
```

From src/db/schema/audit.ts:
```typescript
export const auditEvents = pgTable('audit_events', {
  id:          uuid('id').primaryKey().defaultRandom(),
  timestamp:   timestamp('timestamp', { withTimezone: true }).notNull().defaultNow(),
  actorId:     text('actor_id').notNull(),
  actorRole:   text('actor_role').notNull(),
  action:      text('action').notNull(),
  entityType:  text('entity_type').notNull(),
  entityId:    text('entity_id').notNull(),
  payload:     jsonb('payload').$type<Record<string, unknown>>().notNull().default({}),
  ipAddress:   inet('ip_address'),
})
```

From src/lib/constants.ts:
```typescript
export const ACTIONS = {
  // ... existing actions
  TRACE_EXPORT: 'trace.export',
  // Must add: EVIDENCE_PACK_EXPORT: 'evidence_pack.export'
}
```

From src/lib/permissions.ts:
```typescript
export const PERMISSIONS = {
  'audit:read': [ROLES.ADMIN, ROLES.AUDITOR],
  'trace:export': [ROLES.ADMIN, ROLES.POLICY_LEAD, ROLES.AUDITOR],
  // Must add: 'evidence:export': [ROLES.ADMIN, ROLES.AUDITOR]
}
export function can(role: Role, permission: Permission): boolean
```

From src/lib/audit.ts:
```typescript
export async function writeAuditLog(params: {
  actorId: string, actorRole: string, action: Action | string,
  entityType: string, entityId: string, payload?: Record<string, unknown>,
  ipAddress?: string | null,
}): Promise<void>
```

From app/api/export/traceability/csv/route.ts (export route pattern):
```typescript
// auth() -> find user in DB -> can(role, permission) -> query data -> build output -> writeAuditLog -> Response
```

From app/(workspace)/dashboard/_components/auditor-dashboard.tsx:
```typescript
// Line 57-59: Button is currently disabled:
// <Button size="sm" disabled>View Full Audit Trail</Button>
// Must enable with href="/audit"
```

From app/(workspace)/_components/workspace-nav.tsx:
```typescript
// Currently hardcoded navItems, no role-based filtering.
// Must conditionally add { href: '/audit', label: 'Audit' } for auditor/admin.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fflate, add constants/permissions, evidence pack service, export route, and workspace-nav/dashboard wiring</name>
  <read_first>
    - src/lib/constants.ts (ACTIONS to extend)
    - src/lib/permissions.ts (PERMISSIONS to extend)
    - src/lib/audit.ts (writeAuditLog pattern)
    - app/api/export/traceability/csv/route.ts (export route handler auth pattern)
    - app/(workspace)/_components/workspace-nav.tsx (nav items to extend)
    - app/(workspace)/dashboard/_components/auditor-dashboard.tsx (disabled button to enable)
    - app/(workspace)/layout.tsx (layout for role passing context)
    - src/db/schema/changeRequests.ts (documentVersions, changeRequests, crFeedbackLinks)
    - src/db/schema/feedback.ts (feedbackItems)
    - src/db/schema/users.ts (users)
    - .planning/phases/09-public-portal-compliance/09-RESEARCH.md (fflate pattern, evidence pack structure)
  </read_first>
  <files>
    src/lib/constants.ts,
    src/lib/permissions.ts,
    src/server/services/evidence-pack.service.ts,
    app/api/export/evidence-pack/route.ts,
    app/(workspace)/_components/workspace-nav.tsx,
    app/(workspace)/dashboard/_components/auditor-dashboard.tsx
  </files>
  <action>
    **Install fflate dependency first:**
    ```bash
    npm install fflate
    ```

    1. **Update src/lib/constants.ts** (per AUDIT-05): Add `EVIDENCE_PACK_EXPORT: 'evidence_pack.export'` to the ACTIONS object, after TRACE_EXPORT.

    2. **Update src/lib/permissions.ts** (per AUDIT-05): Add `'evidence:export': [ROLES.ADMIN, ROLES.AUDITOR] as readonly Role[]` to the PERMISSIONS object, in a new "Evidence export (Phase 9)" comment section.

    3. **Create src/server/services/evidence-pack.service.ts** (per AUDIT-05, AUDIT-06): Service that assembles all evidence data for a document:
       - Export async function `buildEvidencePack(documentId: string): Promise<Record<string, Uint8Array>>` that returns a map of filename -> content bytes.
       - Import `db` from `@/src/db`, all relevant schema tables, `Papa` from `papaparse`.
       - **Stakeholders CSV:** Query users who submitted feedback for this document (JOIN feedbackItems ON submitterId). Columns: name (or "Anonymous" if isAnonymous), role, orgType, feedbackCount. Use Papa.unparse() for CSV. Filename: `stakeholders.csv`.
       - **Feedback matrix CSV:** Query all feedbackItems for this document with LEFT JOINs to users, crFeedbackLinks, changeRequests, crSectionLinks, policySections, documentVersions. Columns: Feedback ID, Type, Priority, Impact, Status, Decision Rationale, Section, CR ID, Version. Anonymize names unconditionally for evidence pack export. Use Papa.unparse(). Filename: `feedback-matrix.csv`.
       - **Version history JSON:** Query all documentVersions for this document (ORDER BY createdAt ASC). Include id, versionLabel, mergeSummary, createdAt, publishedAt, isPublished, changelog. Filename: `version-history.json` (JSON.stringify with 2-space indent).
       - **Decision log JSON:** Query workflowTransitions (from src/db/schema/workflow.ts) for all feedback and CR entities belonging to this document. Include entityType, entityId, fromState, toState, actorId, rationale, timestamp. Filename: `decision-log.json`.
       - **Workshop evidence JSON:** Placeholder since Phase 10 is not built. Content: `{ "note": "Workshop module is pending (Phase 10). No workshop data available at this time." }`. Filename: `workshop-evidence.json`.
       - **INDEX.md:** Markdown file listing all contents with descriptions:
         ```
         # Evidence Pack — {documentTitle}
         Generated: {ISO date}

         ## Contents
         - stakeholders.csv — List of stakeholders who submitted feedback
         - feedback-matrix.csv — Full feedback traceability matrix
         - version-history.json — Complete version history with changelogs
         - decision-log.json — All workflow transitions and decision rationale
         - workshop-evidence.json — Workshop evidence (pending Phase 10)
         ```
       - Use `new TextEncoder().encode(...)` to convert strings to Uint8Array for all files.

    4. **Create app/api/export/evidence-pack/route.ts** (per AUDIT-05, AUDIT-06): Route Handler following the traceability CSV export pattern exactly:
       - `export async function GET(request: NextRequest)`.
       - Auth check: `const { userId } = await auth()`. Return 401 if no userId.
       - Find user in DB: `db.query.users.findFirst({ where: eq(users.clerkId, userId) })`. Return 401 if not found.
       - Permission check: `can(user.role as Role, 'evidence:export')`. Return 403 if false.
       - Parse `documentId` from `request.nextUrl.searchParams.get('documentId')`. Return 400 if missing.
       - Call `buildEvidencePack(documentId)` to get the file map.
       - Import `zipSync` from `fflate`. Call `const zipped = zipSync(files, { level: 6 })`.
       - Write audit log: `writeAuditLog({ actorId: user.id, actorRole: user.role, action: ACTIONS.EVIDENCE_PACK_EXPORT, entityType: 'document', entityId: documentId, payload: { format: 'zip' } })`.
       - Return `new Response(zipped, { headers: { 'Content-Type': 'application/zip', 'Content-Disposition': 'attachment; filename="evidence-pack-${documentId}.zip"' } })`.

    5. **Update app/(workspace)/_components/workspace-nav.tsx** (per AUDIT-04): Add role-based "Audit" nav link. The component is currently a client component with hardcoded navItems. Since it uses `usePathname` (client), it cannot call `auth()`. Instead:
       - Add an optional `userRole` prop: `interface WorkspaceNavProps { userRole?: string }`.
       - Conditionally include `{ href: '/audit', label: 'Audit' }` in navItems when `userRole === 'admin' || userRole === 'auditor'`.
       - Update `app/(workspace)/layout.tsx` to fetch the current user's role from the DB (using auth() + db.query.users.findFirst) and pass it as prop to WorkspaceNav. Reuse the userId already obtained from auth().

    6. **Update app/(workspace)/dashboard/_components/auditor-dashboard.tsx** (per AUDIT-04): Enable the "View Full Audit Trail" button:
       - Remove `disabled` prop from the Button on line ~57.
       - Add `render={<Link href="/audit" />}` using the base-nova render prop pattern (per Phase 4/8 decisions), importing Link from next/link.
       - Also update the "Export Audit Log (CSV)" button in Export Controls to link to `/api/export/traceability/csv` (or keep it for later -- it needs documentId param). For now, add a note that CSV export requires a policy context. Instead, add an "Export Evidence Pack" button that opens the evidence pack dialog (to be wired by Task 2).
       - Actually, keep the CSV button as-is for now (it already exists). Add a second button: "Export Evidence Pack (ZIP)" that will be wired to the EvidencePackDialog in Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "EVIDENCE_PACK_EXPORT" src/lib/constants.ts confirms new action constant
    - grep -q "evidence:export" src/lib/permissions.ts confirms new permission
    - grep -q "buildEvidencePack" src/server/services/evidence-pack.service.ts confirms service function
    - grep -q "zipSync" app/api/export/evidence-pack/route.ts confirms ZIP generation
    - grep -q "evidence:export" app/api/export/evidence-pack/route.ts confirms permission check
    - grep -q "audit" "app/(workspace)/_components/workspace-nav.tsx" confirms Audit nav link added
    - grep -q 'href="/audit"' "app/(workspace)/dashboard/_components/auditor-dashboard.tsx" OR grep -q "audit" "app/(workspace)/dashboard/_components/auditor-dashboard.tsx" confirms button enabled
    - npm ls fflate confirms fflate is installed
  </acceptance_criteria>
  <done>fflate installed. EVIDENCE_PACK_EXPORT action and evidence:export permission added. Evidence pack service assembles all 6 artifacts (INDEX.md, stakeholders CSV, feedback matrix CSV, version history JSON, decision log JSON, workshop placeholder JSON). Export route enforces auth + permission, generates ZIP, writes audit log. WorkspaceNav shows Audit link for auditor/admin. Auditor dashboard button links to /audit.</done>
</task>

<task type="auto">
  <name>Task 2: Audit trail viewer page with filter panel, event table, and evidence pack dialog</name>
  <read_first>
    - src/server/routers/audit.ts (auditRouter.list query shape and filters)
    - app/(workspace)/layout.tsx (workspace layout for role context)
    - app/(workspace)/dashboard/_components/auditor-dashboard.tsx (updated in Task 1)
    - src/server/services/evidence-pack.service.ts (created in Task 1)
    - .planning/phases/09-public-portal-compliance/09-UI-SPEC.md (Surface 2: Audit Viewer specs)
    - src/db/schema/audit.ts (auditEvents columns)
    - src/lib/constants.ts (ROLES, ACTIONS)
  </read_first>
  <files>
    app/(workspace)/audit/page.tsx,
    app/(workspace)/audit/_components/audit-filter-panel.tsx,
    app/(workspace)/audit/_components/audit-event-table.tsx,
    app/(workspace)/audit/_components/evidence-pack-dialog.tsx
  </files>
  <action>
    1. **Create app/(workspace)/audit/page.tsx** (per AUDIT-04): Server Component wrapper that enforces auditor/admin role:
       - Call `auth()` to get userId. Redirect to /dashboard if no userId.
       - Query user from DB to get role. If role is not 'admin' or 'auditor', redirect to /dashboard (or render a 403 message).
       - Render page heading "Audit Trail" (h1, 20px/600) with subheading "Complete immutable record of all system actions." per UI-SPEC.
       - Action row: "Export Evidence Pack" Button (default, sm) that triggers EvidencePackDialog, and "Export Audit Log (CSV)" Button (outline, sm).
       - Render AuditTrailClient component (client component wrapper for filter + table).

    2. **Create a client component `app/(workspace)/audit/_components/audit-trail-client.tsx`** that manages filter state and passes it to children:
       - State: `{ action?: string, entityType?: string, actorRole?: string, from?: string, to?: string }`.
       - Pagination state: `{ page: number, pageSize: number }` (default page=0, pageSize=50).
       - Call `trpc.audit.list.useQuery({ ...filters, limit: pageSize, offset: page * pageSize })`.
       - Note: the existing auditRouter.list does NOT have an `actorRole` filter. The executor should either: (a) filter client-side for actorRole, or (b) add actorRole filter to the auditRouter.list procedure. Prefer (b) -- add `actorRole: z.string().optional()` to the input schema and `eq(auditEvents.actorRole, filters.actorRole)` to conditions in the router. This is a small, backward-compatible change.
       - Render AuditFilterPanel + AuditEventTable as children, passing filter state and data.

    3. **Create app/(workspace)/audit/_components/audit-filter-panel.tsx** (per AUDIT-04): Client component. Props: `{ filters: FilterState, onFilterChange: (filters) => void }`.
       - Horizontal filter bar: `flex items-center gap-3 flex-wrap`.
       - Filters per UI-SPEC:
         - **Action type**: Select component. Populate options from the ACTIONS constants (map to human-readable labels). Placeholder: "All actions".
         - **Actor role**: Select component. Options: all 7 roles from ROLES constant. Placeholder: "All roles".
         - **Entity type**: Select component. Options: feedback, change_request, document_version, policy_section, user, document. Placeholder: "All entities".
         - **Date from**: `<Input type="date">` with label "From".
         - **Date to**: `<Input type="date">` with label "To".
       - "Clear" ghost button: only visible when any filter is active. Resets all filters.
       - Each filter has a visible `<label>` (12px/400, muted-foreground) above the control.

    4. **Create app/(workspace)/audit/_components/audit-event-table.tsx** (per AUDIT-04): Client component. Props: `{ events: AuditEvent[], isLoading: boolean, page: number, pageSize: number, onPageChange, onPageSizeChange }`.
       - Table with columns: Timestamp, Actor, Role, Action, Entity Type, Entity ID, Metadata (7 columns per UI-SPEC).
       - TableHeader: `bg-muted`, sticky.
       - Each row min-height 44px, hover `bg-muted/50`.
       - **Timestamp cell**: `font-mono text-xs text-muted-foreground`, formatted with `format(new Date(timestamp), 'yyyy-MM-dd HH:mm:ss')` from date-fns.
       - **Actor cell**: 8-char prefix of actorId in mono. Tooltip with full ID on hover.
       - **Role cell**: Badge variant secondary with actor role.
       - **Action cell**: Badge variant secondary with action string.
       - **Entity Type cell**: text-sm entity type.
       - **Entity ID cell**: font-mono text-xs 8-char prefix.
       - **Metadata cell**: Truncated JSON (40 chars). Clickable to expand -- toggle `expanded` state per row. When expanded, render full metadata as formatted JSON in `font-mono text-xs bg-muted p-2 rounded` div below the row (use `aria-expanded` for accessibility).
       - **Loading state**: 10 Skeleton rows matching table dimensions.
       - **Empty state**: Search icon (48px, muted-foreground), appropriate message per UI-SPEC.
       - **Pagination**: "Showing {start}-{end} of {total}" + Prev/Next Button (outline, sm). Results per page: Select with options 25, 50, 100. Default 50.
       - Table caption: "Audit Trail" for accessibility (`<caption>`).

    5. **Create app/(workspace)/audit/_components/evidence-pack-dialog.tsx** (per AUDIT-05, AUDIT-06): Client component. Dialog triggered by button in page header and auditor dashboard.
       - Dialog max-width 480px.
       - Title: "Export Evidence Pack" (20px/600). Description per UI-SPEC.
       - **Policy selector**: Select component. Fetch policies via tRPC `document.list` (or direct query). Label "Policy", required.
       - **Contents checklist**: 5 Checkbox items, all checked by default: "Stakeholder list", "Feedback matrix", "Version history", "Decision logs", "Workshop evidence (if any)".
       - **Cancel** (outline) and **Export ZIP** (default) buttons. Export disabled until policy selected.
       - On Export: Set loading state. Call `fetch('/api/export/evidence-pack?documentId=${policyId}')`. Show Progress bar 0->100% with "Preparing evidence pack..." label.
       - On complete: Show CheckCircle icon + "Evidence pack ready." + download link. The fetch response blob is converted to a download URL via `URL.createObjectURL(blob)`. Render `<a>` styled as Button for "Download ZIP".
       - On error: Show error message below progress, retry button.
       - Dialog stays open after export completes so user can download.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Audit Trail" "app/(workspace)/audit/page.tsx" confirms audit page exists
    - grep -q "audit:read\|auditor\|admin" "app/(workspace)/audit/page.tsx" confirms role enforcement
    - grep -q "trpc.*audit" "app/(workspace)/audit/_components/audit-event-table.tsx" OR grep -q "events" "app/(workspace)/audit/_components/audit-event-table.tsx" confirms table receives audit data
    - grep -q "Select\|filter" "app/(workspace)/audit/_components/audit-filter-panel.tsx" confirms filter controls
    - grep -q "evidence-pack" "app/(workspace)/audit/_components/evidence-pack-dialog.tsx" confirms evidence pack dialog
    - grep -q "Export Evidence Pack\|Export ZIP" "app/(workspace)/audit/_components/evidence-pack-dialog.tsx" confirms export button
    - grep -q "aria-expanded" "app/(workspace)/audit/_components/audit-event-table.tsx" confirms metadata expand accessibility
    - grep -q "caption\|Audit Trail" "app/(workspace)/audit/_components/audit-event-table.tsx" confirms table accessibility
  </acceptance_criteria>
  <done>Audit trail viewer at /audit displays all audit events in a paginated table with filter panel (action type, actor role, entity type, date range). Metadata rows expand inline. Evidence pack dialog allows policy selection and triggers ZIP download. Role enforcement ensures only auditor/admin can access.</done>
</task>

</tasks>

<verification>
1. Navigate to /audit as auditor role -- should see audit trail with filter panel and paginated table
2. Navigate to /audit as stakeholder role -- should be redirected to /dashboard (403 enforcement)
3. Apply filters (action type, entity type, date range) -- table results update
4. Click a metadata cell -- row expands to show full JSON
5. Click "Export Evidence Pack" -- dialog opens with policy selector
6. Select a policy and click "Export ZIP" -- ZIP downloads with INDEX.md, 4 data files, workshop placeholder
7. Auditor dashboard "View Full Audit Trail" button navigates to /audit
8. WorkspaceNav shows "Audit" link for auditor/admin, hidden for other roles
9. TypeScript compilation passes with no errors
</verification>

<success_criteria>
- Auditor can view full audit trail with filtering by action, entity type, role, and date range (AUDIT-04)
- Evidence pack export produces ZIP with all 6 files: INDEX.md, stakeholders.csv, feedback-matrix.csv, version-history.json, decision-log.json, workshop-evidence.json (AUDIT-05, AUDIT-06)
- Evidence pack route enforces authentication and evidence:export permission (AUDIT-05)
- Evidence pack export writes audit log entry (AUDIT-06)
- Non-auditor/non-admin users cannot access /audit page
- Auditor dashboard button correctly links to /audit
</success_criteria>

<output>
After completion, create `.planning/phases/09-public-portal-compliance/09-02-SUMMARY.md`
</output>
