---
phase: 20.5
slug: public-research-framework-content-pages
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 20.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Planner populates the Per-Task Verification Map after creating PLAN.md files.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x |
| **Config file** | `vitest.config.mts` |
| **Quick run command** | `npx vitest run tests/phase-20.5/ src/__tests__/phase-20.5/` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~30 seconds (phase subset) / ~120 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command (phase 20.5 subset)
- **After every plan wave:** Run full suite (`npm test`)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*Populated by planner after PLAN.md files are created. Each task ID maps to a Wave 0 contract test or to an automated verify command.*

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 files that lock the RED test contract before implementation (planner assigns to Plan 20.5-00):

- [ ] `tests/phase-20.5/section-status-derivation.test.ts` — PUB-07 section status rules (Validated / Under Review / Draft)
- [ ] `tests/phase-20.5/framework-log-aggregator.test.ts` — PUB-08 what-changed log (privacy, ordering, 20-entry limit)
- [ ] `tests/phase-20.5/set-public-draft-mutation.test.ts` — `document.setPublicDraft` tRPC mutation (permission guard + audit log)
- [ ] `tests/phase-20.5/research-page-render.test.ts` — `/research` page renders required sections + PDF link + join-consultation CTA
- [ ] `tests/phase-20.5/framework-page-render.test.ts` — `/framework` page renders section status badges + what-changed log + no-drafts empty state
- [ ] `tests/phase-20.5/proxy-public-routes.test.ts` — proxy.ts public matcher includes `/research(.*)` and `/framework(.*)`

*Canonical TDD pattern:* variable-path dynamic import via array.join + `/* @vite-ignore */` (Phase 16/17/18/19/20 canonical) for any file that doesn't exist yet at test-write time.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| PDF download triggers in-browser download | PUB-06 | Browser-side `download` attribute behavior cannot be asserted in node-environment vitest | Smoke walk at end-of-milestone: open `/research`, click Download, confirm PDF downloads |
| Visual badge colors render with correct CSS vars | PUB-07 | CSS var resolution requires browser rendering | Smoke walk: open `/framework`, visually confirm Draft (neutral) / Under Review (amber) / Validated (green) badge colors |
| Sticky section nav scroll behavior on `/framework` | PUB-07 | Scroll/intersection observer behavior is browser-only | Smoke walk: scroll `/framework`, confirm active section highlight updates |

*Per user workflow preference, all manual smoke walks deferred to end-of-milestone v0.2 smoke walk — NOT per-phase.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
