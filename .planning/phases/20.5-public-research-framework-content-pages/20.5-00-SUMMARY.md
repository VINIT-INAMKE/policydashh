---
phase: 20.5-public-research-framework-content-pages
plan: 00
subsystem: testing
tags: [vitest, tdd, red-contract, trpc, drizzle, react-server-components]

requires:
  - phase: 20-public-workshops-cal-integration
    provides: canonical vi.hoisted + vi.mock drizzle chain pattern and variable-path dynamic import convention
  - phase: 18-async-evidence-pack-export
    provides: tRPC `_def.procedures.<name>` probe pattern that defeats createCaller Proxy false-positives
provides:
  - Six Wave 0 RED contract tests under tests/phase-20.5/ locking PUB-06 / PUB-07 / PUB-08 / D-17
  - Section-status derivation contract (Validated > Under Review > Draft precedence)
  - Framework-log aggregator contract (20-cap, descending merge date, PUB-05 privacy, orphan skip)
  - document.setPublicDraft tRPC mutation contract (router registration + audit log shape)
  - /research page render contract (hero, 4 anchors, PDF download anchor, Join Consultation CTA)
  - /framework page empty-state contract (heading + body copy)
  - proxy.ts public-route matcher contract (/research(.*) + /framework(.*) literals + comment header)
affects:
  - 20.5-01-PLAN.md (must implement framework-log.service + setPublicDraft to flip unit RED -> GREEN)
  - 20.5-02-PLAN.md (must implement app/(public)/research/page.tsx to flip research-page RED -> GREEN)
  - 20.5-03-PLAN.md (must implement app/(public)/framework/page.tsx + proxy.ts matcher to flip remaining RED -> GREEN)

tech-stack:
  added: []
  patterns:
    - "Variable-path dynamic import (array.join + /* @vite-ignore */) for RED probes of not-yet-created modules"
    - "tRPC _def.procedures.<name> direct probe (Phase 18 canonical) for router contract assertions"
    - "vi.hoisted + vi.mock('@/src/db') drizzle chain stubbing for server-component RED page tests"

key-files:
  created:
    - tests/phase-20.5/section-status-derivation.test.ts
    - tests/phase-20.5/framework-log-aggregator.test.ts
    - tests/phase-20.5/set-public-draft-mutation.test.ts
    - tests/phase-20.5/proxy-public-routes.test.ts
    - tests/phase-20.5/research-page-render.test.tsx
    - tests/phase-20.5/framework-page-render.test.tsx
  modified:
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md

key-decisions:
  - "Followed plan exactly: variable-path dynamic import pattern used for every module that does not yet exist on disk, preventing vitest static collection failures"
  - "Used Object.keys + JSON.stringify substring scan to assert PUB-05 privacy contract (ensures cr/crReadableId/feedbackIds cannot leak even through nested/accidental spreads)"
  - "Kept buildFrameworkLog contract as a pure function over fixture arrays (no drizzle mocks) since the signature in the plan takes publishedVersions directly"

patterns-established:
  - "RED contract pattern: every future artifact gets a test that names it explicitly via dynamic import path segments, so failure messages pin downstream plans to exact file paths"
  - "Static source-file string-match tests (proxy-public-routes.test.ts) for infra files that cannot be executed from vitest"

requirements-completed: []

duration: 7min
completed: 2026-04-14
---

# Phase 20.5 Plan 00: Wave 0 RED Contract Summary

**Six failing Wave 0 tests that lock every PUB-06 / PUB-07 / PUB-08 / D-17 contract downstream Plans 01, 02, 03 must satisfy — using canonical variable-path dynamic import to probe not-yet-created modules without breaking vitest collection.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-14T21:00:00Z (approx)
- **Completed:** 2026-04-14T21:07:15Z
- **Tasks:** 3 / 3
- **Files created:** 6 test files + 1 VALIDATION.md modification

## Accomplishments

- Authored 4 service/mutation/proxy RED tests (section-status, framework-log, setPublicDraft, proxy matcher) that fail with messages naming the exact future artifacts Plan 01 and Plan 03 must create
- Authored 2 page-render RED tests (/research, /framework) that fail with `ERR_MODULE_NOT_FOUND` pointing at `app/(public)/research/page` and `app/(public)/framework/page`
- Flipped VALIDATION.md Wave 0 gate (`wave_0_red_verified: true`) and marked 00-T1 and 00-T2 as red-verified
- Phase 20 suite (38 tests across 4 files) remains fully GREEN — no regression from new Wave 0 additions
- Proved the Nyquist contract: `npx vitest run tests/phase-20.5/` reports 6 test files as failing and none are false-positives

## Task Commits

1. **Task 1: Author service + mutation + proxy RED tests** — `a5edb5d` (test)
2. **Task 2: Author page-render RED tests (research + framework)** — `77a97aa` (test)
3. **Task 3: Flip VALIDATION.md Wave 0 gate + populate Per-Task Verification Map** — `a09e500` (docs)

## Files Created/Modified

- `tests/phase-20.5/section-status-derivation.test.ts` — PUB-07 three-state precedence (Validated > Under Review > Draft), 4 assertions using vi.hoisted + mockOpenCRs drizzle chain stub
- `tests/phase-20.5/framework-log-aggregator.test.ts` — PUB-08 20-entry cap, descending merge date, PUB-05 privacy (Object.keys + JSON.stringify substring scan), orphan section skip
- `tests/phase-20.5/set-public-draft-mutation.test.ts` — document.setPublicDraft router registration probe (via `_def.procedures`) + audit log shape assertion (action / entityType / entityId / payload)
- `tests/phase-20.5/proxy-public-routes.test.ts` — static readFileSync scan for `'/research(.*)'`, `'/framework(.*)'`, and `// Phase 20.5 ... PUB-06 ... PUB-07 ... PUB-08` comment header
- `tests/phase-20.5/research-page-render.test.tsx` — hero heading, 4 anchor ids, PDF download anchor with download attribute, Join Consultation CTA (renderToStaticMarkup)
- `tests/phase-20.5/framework-page-render.test.tsx` — empty-state heading + body copy with mocked db returning `[]` for isPublicDraft query
- `.planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md` — added `wave_0_red_verified: true`, replaced Per-Task Verification Map with canonical 8-row layout from plan

## Verification

```
$ npx vitest run tests/phase-20.5/
Test Files  6 failed (6)
     Tests  4 failed | 14 skipped (18)
```

Breakdown:
- **section-status-derivation.test.ts** — RED via `Cannot find package '@/src/server/services/framework-log.service'` (beforeAll fails → 4 inner tests skipped)
- **framework-log-aggregator.test.ts** — RED via same `Cannot find package` (beforeAll fails → 4 inner tests skipped)
- **set-public-draft-mutation.test.ts** — RED via `documentRouter._def.procedures.setPublicDraft` undefined and TRPCError `No procedure found on path "setPublicDraft"` (2 tests fail)
- **proxy-public-routes.test.ts** — RED via `expected '...' to contain '/framework(.*)'` (2 tests fail — literals not yet in proxy.ts)
- **research-page-render.test.tsx** — RED via `Cannot find package '@/app/(public)/research/page'` (beforeAll fails → 4 inner tests skipped)
- **framework-page-render.test.tsx** — RED via `Cannot find package '@/app/(public)/framework/page'` (beforeAll fails → 2 inner tests skipped)

Phase 20 spot-check:
```
$ npx vitest run tests/phase-20/
Test Files  4 passed (4)
     Tests  38 passed (38)
```
No regression.

## Decisions Made

- Kept skipped inner tests in failed suites as acceptable RED state. When `beforeAll` cannot import a module, vitest reports the suite as FAILED even though inner tests are marked `skipped` — this satisfies both "all six files fail" and the inner assertion contracts still exist in the source for Plans 01–03 to flip green.
- Marked 00-T1 and 00-T2 as `red-verified` in VALIDATION.md rather than `pending` — the RED gate is verified once the tests exist and fail with the expected module-not-found / procedure-missing signals.

## Deviations from Plan

None — plan executed exactly as written. All four Task 1 test files, both Task 2 test files, and Task 3 VALIDATION.md edits follow the `<action>` blocks verbatim.

## Issues Encountered

None. Variable-path dynamic import pattern prevented vitest collection from static-resolving the not-yet-created `framework-log.service`, `document` router export, `app/(public)/research/page`, and `app/(public)/framework/page` — exactly as the Phase 16–20 canonical pattern guarantees.

## User Setup Required

None — RED contract authoring is pure test code.

## Next Phase Readiness

- **Plan 20.5-01 (framework log service + setPublicDraft mutation):** unblocked. Must implement `src/server/services/framework-log.service.ts` exporting `getSectionPublicStatuses` and `buildFrameworkLog`, plus add `setPublicDraft` to `src/server/routers/document.ts` with audit-log side effect. Tests will flip GREEN when contracts are met.
- **Plan 20.5-02 (/research page):** unblocked. Must create `app/(public)/research/page.tsx` meeting the hero / 4-anchor / PDF-download / Join-CTA contract.
- **Plan 20.5-03 (/framework page + proxy matcher):** unblocked. Must create `app/(public)/framework/page.tsx` for the empty state and add `'/research(.*)'` + `'/framework(.*)'` to `proxy.ts` `createRouteMatcher` array under a `// Phase 20.5 — PUB-06, PUB-07, PUB-08` comment header.

## Self-Check: PASSED

All artifacts verified on disk:
- `tests/phase-20.5/section-status-derivation.test.ts` — FOUND
- `tests/phase-20.5/framework-log-aggregator.test.ts` — FOUND
- `tests/phase-20.5/set-public-draft-mutation.test.ts` — FOUND
- `tests/phase-20.5/proxy-public-routes.test.ts` — FOUND
- `tests/phase-20.5/research-page-render.test.tsx` — FOUND
- `tests/phase-20.5/framework-page-render.test.tsx` — FOUND
- `.planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md` — FOUND
- `.planning/phases/20.5-public-research-framework-content-pages/20.5-00-SUMMARY.md` — FOUND

All commits verified in git history:
- `a5edb5d` (Task 1) — FOUND
- `77a97aa` (Task 2) — FOUND
- `a09e500` (Task 3) — FOUND

---
*Phase: 20.5-public-research-framework-content-pages*
*Completed: 2026-04-14*
