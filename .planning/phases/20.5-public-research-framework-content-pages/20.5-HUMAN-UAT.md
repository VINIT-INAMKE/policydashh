---
status: partial
phase: 20.5-public-research-framework-content-pages
source: [20.5-VERIFICATION.md]
started: 2026-04-15T00:00:00Z
updated: 2026-04-15T00:00:00Z
rollup: v0.2-milestone-smoke-walk
---

## Current Test

[rolled into v0.2 milestone smoke walk per user workflow preference]

## Tests

### 1. ResearchTocAside IntersectionObserver scroll active-state
expected: On `/research`, as the user scrolls, the sticky TOC on desktop (≥lg) highlights the currently-visible section anchor link. Uses IntersectionObserver client-side.
result: [pending]

### 2. PDF download via browser `download` attribute
expected: On `/research`, clicking the "Download Research Report" button triggers an in-browser download of `consultation-research-report.pdf`.
result: [pending]

### 3. `/framework` section nav scroll behavior
expected: On `/framework` (detail view), scrolling the content highlights the matching entry in the left sticky section nav.
result: [pending]

### 4. Workspace toggle end-to-end (Clerk auth + live DB)
expected: Logged in as admin (or policy_lead) on the workspace policy detail page, toggling Publish-as-Draft flips `policy_documents.is_public_draft` in the live DB via `trpc.document.setPublicDraft`, writes an audit log row with action `document.set_public_draft`, and causes the document to appear at `/framework` on next page load. Toggling off reverses the effect.
result: [pending]

### 5. SectionStatusBadge CSS custom property color rendering
expected: On `/framework` with a draft document, section status badges visually render with the correct colors — Draft (neutral), Under Review (amber via `--status-cr-approved-*`), Validated (green via `--status-cr-merged-*`). CSS custom properties resolve in the browser.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

None — all items are browser-only smoke checks deferred to the v0.2 milestone smoke walk per user workflow preference (feedback_defer_smoke_walks). Automated verification (18/18 tests green, tsc clean, full suite passing) has confirmed every backend and structural requirement.
