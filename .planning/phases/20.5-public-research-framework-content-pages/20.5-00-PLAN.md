---
phase: 20.5-public-research-framework-content-pages
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/phase-20.5/section-status-derivation.test.ts
  - tests/phase-20.5/framework-log-aggregator.test.ts
  - tests/phase-20.5/set-public-draft-mutation.test.ts
  - tests/phase-20.5/research-page-render.test.tsx
  - tests/phase-20.5/framework-page-render.test.tsx
  - tests/phase-20.5/proxy-public-routes.test.ts
autonomous: true
requirements: [PUB-06, PUB-07, PUB-08]
must_haves:
  truths:
    - "Six RED contract tests exist under tests/phase-20.5/ and fail with exact messages matching 'Cannot find module' or assertion failures for pending contracts"
    - "Each test locks a named behavior Plans 01/02/03 must satisfy"
    - "Variable-path dynamic import pattern is used for any module that does not yet exist on disk"
  artifacts:
    - path: "tests/phase-20.5/section-status-derivation.test.ts"
      provides: "PUB-07 section status rules: Validated > Under Review > Draft precedence"
    - path: "tests/phase-20.5/framework-log-aggregator.test.ts"
      provides: "PUB-08 20-entry limit, descending merge date, PUB-05 privacy exclusions"
    - path: "tests/phase-20.5/set-public-draft-mutation.test.ts"
      provides: "document.setPublicDraft exists on router, requires document:update, writes audit log"
    - path: "tests/phase-20.5/research-page-render.test.tsx"
      provides: "/research renders four anchor sections + PDF download link + Join Consultation CTA"
    - path: "tests/phase-20.5/framework-page-render.test.tsx"
      provides: "/framework no-drafts empty state copy matches UI-SPEC"
    - path: "tests/phase-20.5/proxy-public-routes.test.ts"
      provides: "proxy.ts source includes '/research(.*)' and '/framework(.*)'"
  key_links:
    - from: "tests/phase-20.5/section-status-derivation.test.ts"
      to: "src/server/services/framework-log.service.ts (future)"
      via: "variable-path dynamic import via array.join + /* @vite-ignore */"
      pattern: "await import\\(\\/\\* @vite-ignore \\*\\/.*\\.join"
    - from: "tests/phase-20.5/set-public-draft-mutation.test.ts"
      to: "src/server/routers/document.ts router._def.procedures.setPublicDraft"
      via: "router procedure probe (Phase 18 pattern)"
      pattern: "_def\\.procedures\\.setPublicDraft"
---

<objective>
Lock the Nyquist RED contract for Phase 20.5 by authoring six failing tests under `tests/phase-20.5/` that constrain every behavior Plans 20.5-01, 20.5-02, and 20.5-03 must satisfy.

Purpose: Establishes a deterministic contract surface so downstream plans cannot land green by accident. Satisfies the `tests/**/*.test.ts(x)` include glob already present in `vitest.config.mts` (Phase 19 convention).

Output: Six test files that `npx vitest run tests/phase-20.5/` executes and reports as failing with specific expected-but-not-implemented messages.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.5-public-research-framework-content-pages/20.5-CONTEXT.md
@.planning/phases/20.5-public-research-framework-content-pages/20.5-RESEARCH.md
@.planning/phases/20.5-public-research-framework-content-pages/20.5-UI-SPEC.md
@.planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md

<interfaces>
<!-- Contracts the RED tests must probe. Extracted from RESEARCH.md patterns 5, 6, 7, 9. -->

From (future) src/server/services/framework-log.service.ts:
```typescript
export type SectionStatus = 'draft' | 'under_review' | 'validated'

export async function getSectionPublicStatuses(
  documentId: string,
  publishedVersions: Array<typeof documentVersions.$inferSelect>,
): Promise<Map<string, SectionStatus>>

export interface PublicLogEntry {
  sectionTitle: string   // resolved title, never raw sectionId
  mergeDate: string      // ISO string
  summary: string        // entry.summary ONLY — no crId, crReadableId, feedbackIds
}

export function buildFrameworkLog(
  publishedVersions: Array<typeof documentVersions.$inferSelect>,
  limit?: number,  // default 20
): PublicLogEntry[]
```

From (future) src/server/routers/document.ts:
```typescript
setPublicDraft: requirePermission('document:update')
  .input(z.object({ id: z.string().uuid(), isPublicDraft: z.boolean() }))
  .mutation(...)
```

ChangelogEntry / SectionSnapshot shape (src/server/services/version.service.ts) — already exists:
```typescript
interface ChangelogEntry {
  crId: string
  crReadableId: string
  crTitle: string
  summary: string
  affectedSectionIds: string[]
  feedbackIds: string[]
}
interface SectionSnapshot {
  sectionId: string
  title: string
  orderIndex: number
  content: unknown
}
```

Canonical variable-path dynamic import (Phase 16/17/18/19/20 Pattern 2):
```typescript
const segs = ['@', 'src', 'server', 'services', 'framework-log.service']
const mod = await import(/* @vite-ignore */ segs.join('/'))
```

tRPC procedure probe (Phase 18 pattern from evidence-request-export.test.ts):
```typescript
const mod = await import(/* @vite-ignore */ ['@', 'src', 'server', 'routers', 'document'].join('/'))
expect(mod.documentRouter._def.procedures.setPublicDraft).toBeDefined()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author service + mutation + proxy RED tests (section-status, framework-log, set-public-draft, proxy-public-routes)</name>
  <files>tests/phase-20.5/section-status-derivation.test.ts, tests/phase-20.5/framework-log-aggregator.test.ts, tests/phase-20.5/set-public-draft-mutation.test.ts, tests/phase-20.5/proxy-public-routes.test.ts</files>
  <read_first>
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-RESEARCH.md (Patterns 5, 6, 7; Pitfall 8; Validation Architecture section)
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md (Wave 0 Requirements — 6 file list)
    - tests/phase-20/workshop-feedback-submit.test.ts (canonical Phase 20 vi.hoisted + vi.mock pattern for drizzle)
    - src/server/routers/__tests__/evidence-request-export.test.ts (canonical Phase 18 tRPC procedure probe pattern)
    - src/server/services/version.service.ts (existing ChangelogEntry / SectionSnapshot type shapes)
    - src/db/schema/changeRequests.ts (documentVersions + changeRequests + crSectionLinks table exports)
    - proxy.ts (current createRouteMatcher array tail to assert against)
  </read_first>
  <behavior>
    ## section-status-derivation.test.ts — 4 assertions (PUB-07)
    - **Test A (Validated precedence):** Given `publishedVersions` with `changelog[0].affectedSectionIds = ['sec-A']`, `getSectionPublicStatuses(docId, publishedVersions).then(m => m.get('sec-A'))` resolves to `'validated'`.
    - **Test B (Under Review):** Given zero published versions and an open CR with `status: 'in_review'` linked to `sec-B` via crSectionLinks, `m.get('sec-B')` resolves to `'under_review'`. Use vi.mock('@/src/db') with deep chain stub: `select → from → innerJoin → where` returning `[{ sectionId: 'sec-B' }]`.
    - **Test C (Validated beats Under Review):** Given both conditions holding for the same sectionId (published changelog + open CR), status is `'validated'`.
    - **Test D (Draft default):** Given zero published versions and zero open CR rows, `m.get('sec-X')` resolves to `'draft'`.
    - Import via `const mod = await import(/* @vite-ignore */ ['@', 'src', 'server', 'services', 'framework-log.service'].join('/'))` inside `beforeAll`.

    ## framework-log-aggregator.test.ts — 4 assertions (PUB-08)
    - **Test A (20-entry cap):** Build a fixture with 25 changelog entries spread across versions; `buildFrameworkLog(versions)` returns exactly 20 entries.
    - **Test B (Descending merge date):** Given versions with `publishedAt` of 2026-01-01, 2026-02-01, 2026-03-01, first entry's `mergeDate` parses to 2026-03-01, last entry parses to an earlier date.
    - **Test C (PUB-05 privacy — no CR/feedback identifiers):** For a fixture entry with `crId: 'cr-1'`, `crReadableId: 'CR-42'`, `feedbackIds: ['fb-1']`, the returned `PublicLogEntry` shape contains only keys `sectionTitle`, `mergeDate`, `summary`. Use `Object.keys(entries[0]).sort()` to deep-equal `['mergeDate','sectionTitle','summary']`. Also assert `JSON.stringify(entries).includes('cr-1') === false` and `.includes('CR-42') === false` and `.includes('fb-1') === false`.
    - **Test D (Title fallback skip):** Given a changelog entry with `affectedSectionIds: ['orphan-sid']` where `orphan-sid` is not present in that version's `sectionsSnapshot`, the orphan entry is omitted (no `sectionTitle: undefined`).
    - `buildFrameworkLog` is pure; DO NOT mock drizzle — pass fixture arrays directly.

    ## set-public-draft-mutation.test.ts — 2 assertions (PUB-07)
    - **Test A (Procedure exists on router):** Via `mod.documentRouter._def.procedures.setPublicDraft` assertion — defeat createCaller Proxy false-positives per Phase 18 Pattern.
    - **Test B (Permission + audit log on success):** Invoke via `createCaller` with a mocked ctx (role: 'admin'), stub drizzle `update → set → where → returning` to return `[{ id: 'doc-1', isPublicDraft: true }]`, and assert `writeAuditLog` mock was called with `action: 'document.set_public_draft'`, `entityType: 'document'`, `entityId: 'doc-1'`, `payload: { isPublicDraft: true }`. Use `vi.hoisted` + `vi.mock('@/src/lib/audit')` to capture the call.
    - Import document router via variable-path dynamic import.

    ## proxy-public-routes.test.ts — 2 assertions (D-17)
    - **Test A:** `readFileSync('proxy.ts', 'utf8')` includes the literal string `'/research(.*)'`.
    - **Test B:** Same file includes `'/framework(.*)'` AND includes the comment header `// Phase 20.5: PUB-06, PUB-07, PUB-08` OR `// Phase 20.5 —` prefix (accept either dash or colon).
    - This is a static string-match test; it does NOT execute proxy.ts.
  </behavior>
  <action>
    Create four files under `tests/phase-20.5/`:

    ### tests/phase-20.5/section-status-derivation.test.ts
    ```typescript
    import { describe, it, expect, vi, beforeAll } from 'vitest'

    const { dbMock } = vi.hoisted(() => ({
      dbMock: {
        select: vi.fn(),
      },
    }))
    vi.mock('@/src/db', () => ({ db: dbMock }))

    let getSectionPublicStatuses: any
    beforeAll(async () => {
      const segs = ['@', 'src', 'server', 'services', 'framework-log.service']
      const mod = await import(/* @vite-ignore */ segs.join('/'))
      getSectionPublicStatuses = mod.getSectionPublicStatuses
    })

    function mockOpenCRs(rows: Array<{ sectionId: string }>) {
      dbMock.select.mockReturnValueOnce({
        from: vi.fn().mockReturnValue({
          innerJoin: vi.fn().mockReturnValue({
            where: vi.fn().mockResolvedValue(rows),
          }),
        }),
      })
    }

    describe('getSectionPublicStatuses — PUB-07 derivation rules', () => {
      it('returns validated for sections in published changelog', async () => {
        mockOpenCRs([])
        const versions = [{
          id: 'v1', documentId: 'doc-1', isPublished: true,
          changelog: [{ crId: 'x', crReadableId: 'CR-1', crTitle: 't', summary: 's', affectedSectionIds: ['sec-A'], feedbackIds: [] }],
          sectionsSnapshot: [], publishedAt: new Date(), createdAt: new Date(),
        }] as any
        const m = await getSectionPublicStatuses('doc-1', versions)
        expect(m.get('sec-A')).toBe('validated')
      })

      it('returns under_review for sections with open in_review CR', async () => {
        mockOpenCRs([{ sectionId: 'sec-B' }])
        const m = await getSectionPublicStatuses('doc-1', [])
        expect(m.get('sec-B')).toBe('under_review')
      })

      it('prefers validated over under_review when both hold', async () => {
        mockOpenCRs([{ sectionId: 'sec-C' }])
        const versions = [{
          id: 'v1', documentId: 'doc-1', isPublished: true,
          changelog: [{ crId: 'x', crReadableId: 'CR-1', crTitle: 't', summary: 's', affectedSectionIds: ['sec-C'], feedbackIds: [] }],
          sectionsSnapshot: [], publishedAt: new Date(), createdAt: new Date(),
        }] as any
        const m = await getSectionPublicStatuses('doc-1', versions)
        expect(m.get('sec-C')).toBe('validated')
      })

      it('returns draft for unknown section id', async () => {
        mockOpenCRs([])
        const m = await getSectionPublicStatuses('doc-1', [])
        expect(m.get('sec-X')).toBe('draft')
      })
    })
    ```

    ### tests/phase-20.5/framework-log-aggregator.test.ts
    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest'

    let buildFrameworkLog: any
    beforeAll(async () => {
      const segs = ['@', 'src', 'server', 'services', 'framework-log.service']
      const mod = await import(/* @vite-ignore */ segs.join('/'))
      buildFrameworkLog = mod.buildFrameworkLog
    })

    function makeVersion(opts: { publishedAt: Date; entries: number; prefix: string; sectionIds: string[]; sectionTitles: Record<string, string> }) {
      return {
        id: `v-${opts.prefix}`, documentId: 'doc-1', isPublished: true,
        publishedAt: opts.publishedAt, createdAt: opts.publishedAt,
        sectionsSnapshot: Object.entries(opts.sectionTitles).map(([sectionId, title], i) => ({ sectionId, title, orderIndex: i, content: {} })),
        changelog: Array.from({ length: opts.entries }).map((_, i) => ({
          crId: `cr-${opts.prefix}-${i}`,
          crReadableId: `CR-${opts.prefix}-${i}`,
          crTitle: `title-${i}`,
          summary: `${opts.prefix} summary ${i}`,
          affectedSectionIds: opts.sectionIds,
          feedbackIds: [`fb-${opts.prefix}-${i}`],
        })),
      } as any
    }

    describe('buildFrameworkLog — PUB-08 aggregation rules', () => {
      it('caps total entries at 20', () => {
        const v = makeVersion({
          publishedAt: new Date('2026-03-01'),
          entries: 25, prefix: 'a',
          sectionIds: ['sec-A'],
          sectionTitles: { 'sec-A': 'Section A' },
        })
        const out = buildFrameworkLog([v])
        expect(out.length).toBe(20)
      })

      it('orders entries newest first by merge date', () => {
        const vOld = makeVersion({ publishedAt: new Date('2026-01-01'), entries: 1, prefix: 'old', sectionIds: ['sec-A'], sectionTitles: { 'sec-A': 'A' } })
        const vNew = makeVersion({ publishedAt: new Date('2026-03-01'), entries: 1, prefix: 'new', sectionIds: ['sec-A'], sectionTitles: { 'sec-A': 'A' } })
        const out = buildFrameworkLog([vOld, vNew])
        expect(new Date(out[0].mergeDate).getTime()).toBe(new Date('2026-03-01').getTime())
        expect(new Date(out[out.length - 1].mergeDate).getTime()).toBe(new Date('2026-01-01').getTime())
      })

      it('excludes CR/feedback identifiers from output shape (PUB-05)', () => {
        const v = makeVersion({ publishedAt: new Date('2026-03-01'), entries: 1, prefix: 'p', sectionIds: ['sec-A'], sectionTitles: { 'sec-A': 'A' } })
        const out = buildFrameworkLog([v])
        expect(Object.keys(out[0]).sort()).toEqual(['mergeDate', 'sectionTitle', 'summary'])
        const json = JSON.stringify(out)
        expect(json.includes('cr-p-0')).toBe(false)
        expect(json.includes('CR-p-0')).toBe(false)
        expect(json.includes('fb-p-0')).toBe(false)
      })

      it('skips entries whose sectionId is not in sectionsSnapshot', () => {
        const v = makeVersion({ publishedAt: new Date('2026-03-01'), entries: 1, prefix: 'orphan', sectionIds: ['orphan-sid'], sectionTitles: { 'sec-A': 'A' } })
        const out = buildFrameworkLog([v])
        expect(out.length).toBe(0)
      })
    })
    ```

    ### tests/phase-20.5/set-public-draft-mutation.test.ts
    ```typescript
    import { describe, it, expect, vi, beforeAll } from 'vitest'

    const { dbMock, writeAuditLogMock } = vi.hoisted(() => ({
      dbMock: { update: vi.fn() },
      writeAuditLogMock: vi.fn().mockResolvedValue(undefined),
    }))
    vi.mock('@/src/db', () => ({ db: dbMock }))
    vi.mock('@/src/lib/audit', () => ({ writeAuditLog: writeAuditLogMock }))

    let documentRouter: any
    beforeAll(async () => {
      const segs = ['@', 'src', 'server', 'routers', 'document']
      const mod = await import(/* @vite-ignore */ segs.join('/'))
      documentRouter = mod.documentRouter
    })

    describe('document.setPublicDraft — PUB-07 mutation contract', () => {
      it('is registered as a procedure on documentRouter', () => {
        expect(documentRouter._def.procedures.setPublicDraft).toBeDefined()
      })

      it('writes audit log with action document.set_public_draft on success', async () => {
        dbMock.update.mockReturnValue({
          set: vi.fn().mockReturnValue({
            where: vi.fn().mockReturnValue({
              returning: vi.fn().mockResolvedValue([{ id: 'doc-1', isPublicDraft: true }]),
            }),
          }),
        })
        const caller = documentRouter.createCaller({
          user: { id: 'u-admin', role: 'admin' },
        })
        await caller.setPublicDraft({ id: '00000000-0000-4000-8000-000000000001', isPublicDraft: true })
        expect(writeAuditLogMock).toHaveBeenCalled()
        const call = writeAuditLogMock.mock.calls[0][0]
        expect(call.action).toBe('document.set_public_draft')
        expect(call.entityType).toBe('document')
        expect(call.entityId).toBe('doc-1')
        expect(call.payload).toEqual({ isPublicDraft: true })
      })
    })
    ```

    ### tests/phase-20.5/proxy-public-routes.test.ts
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    import path from 'node:path'

    describe('proxy.ts public-route matcher — D-17', () => {
      const src = readFileSync(path.join(process.cwd(), 'proxy.ts'), 'utf8')

      it("includes '/research(.*)' in createRouteMatcher", () => {
        expect(src).toContain("'/research(.*)'")
      })

      it("includes '/framework(.*)' in createRouteMatcher and Phase 20.5 comment header", () => {
        expect(src).toContain("'/framework(.*)'")
        expect(src).toMatch(/\/\/ Phase 20\.5[\s\S]*PUB-06[\s\S]*PUB-07[\s\S]*PUB-08/)
      })
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/phase-20.5/section-status-derivation.test.ts tests/phase-20.5/framework-log-aggregator.test.ts tests/phase-20.5/set-public-draft-mutation.test.ts tests/phase-20.5/proxy-public-routes.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File tests/phase-20.5/section-status-derivation.test.ts exists and contains the literal string `getSectionPublicStatuses` (grep).
    - File tests/phase-20.5/framework-log-aggregator.test.ts exists and contains the literal string `buildFrameworkLog` (grep).
    - File tests/phase-20.5/set-public-draft-mutation.test.ts exists and contains the literal string `_def.procedures.setPublicDraft` (grep).
    - File tests/phase-20.5/proxy-public-routes.test.ts exists and contains the literal string `'/research(.*)'` (grep).
    - All four tests FAIL when `npx vitest run tests/phase-20.5/` is invoked (RED state is expected — failures must mention either "Cannot find module" for the service file, or `setPublicDraft` undefined, or proxy.ts literal not found).
    - Zero TypeScript compile errors from vitest collection — variable-path dynamic import prevents static module resolution failures.
  </acceptance_criteria>
  <done>Four test files committed; `npx vitest run tests/phase-20.5/` reports RED for these four files with module-not-found / assertion-failure messages; the failure messages name the future artifacts that Plans 01, 02, 03 must create.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author page-render RED tests (research + framework)</name>
  <files>tests/phase-20.5/research-page-render.test.tsx, tests/phase-20.5/framework-page-render.test.tsx</files>
  <read_first>
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-UI-SPEC.md (Page Layouts + Copywriting Contract + SectionStatusBadge spec)
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-RESEARCH.md (Code Examples — Research Page Minimal Structure)
    - tests/phase-20/participate-mode-switch.test.tsx (canonical server-component test pattern for this project)
    - app/(public)/portal/[policyId]/page.tsx (canonical portal page render shape)
  </read_first>
  <behavior>
    ## research-page-render.test.tsx — 4 assertions (PUB-06)
    - **Test A (Hero heading):** rendered HTML contains text `Understanding the Landscape`.
    - **Test B (Four anchor sections):** rendered HTML contains `id="overview"`, `id="key-themes"`, `id="outputs"`, `id="join-consultation"`.
    - **Test C (PDF download link):** rendered HTML contains `href="/research/consultation-research-report.pdf"` AND the `download` attribute on that anchor.
    - **Test D (Join Consultation CTA):** rendered HTML contains `href="/participate"` linked from a button labeled `Join Consultation`.
    - Import page via variable-path dynamic: `const mod = await import(/* @vite-ignore */ ['@', 'app', '(public)', 'research', 'page'].join('/'))`. Render using React's `renderToStaticMarkup`.

    ## framework-page-render.test.tsx — 2 assertions (PUB-07/PUB-08 empty state)
    - **Test A (No drafts empty state heading):** When `policyDocuments.select → from → where` returns `[]`, rendered HTML contains `No drafts under consultation`.
    - **Test B (No drafts empty state body):** Same render contains `No framework documents are currently open for public review.`
    - Mock `@/src/db` so `select().from().where()` resolves `[]`. Use renderToStaticMarkup on the default export.
  </behavior>
  <action>
    Create two files under `tests/phase-20.5/`:

    ### tests/phase-20.5/research-page-render.test.tsx
    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest'
    import { renderToStaticMarkup } from 'react-dom/server'
    import React from 'react'

    let ResearchPage: any
    beforeAll(async () => {
      const segs = ['@', 'app', '(public)', 'research', 'page']
      const mod = await import(/* @vite-ignore */ segs.join('/'))
      ResearchPage = mod.default
    })

    describe('/research page — PUB-06', () => {
      it('renders hero heading "Understanding the Landscape"', async () => {
        const html = renderToStaticMarkup(React.createElement(ResearchPage))
        expect(html).toContain('Understanding the Landscape')
      })

      it('renders four anchor section ids', async () => {
        const html = renderToStaticMarkup(React.createElement(ResearchPage))
        expect(html).toContain('id="overview"')
        expect(html).toContain('id="key-themes"')
        expect(html).toContain('id="outputs"')
        expect(html).toContain('id="join-consultation"')
      })

      it('renders PDF download anchor with download attribute', async () => {
        const html = renderToStaticMarkup(React.createElement(ResearchPage))
        expect(html).toContain('href="/research/consultation-research-report.pdf"')
        expect(html).toMatch(/href="\/research\/consultation-research-report\.pdf"[^>]*download/)
      })

      it('renders Join Consultation CTA linking to /participate', async () => {
        const html = renderToStaticMarkup(React.createElement(ResearchPage))
        expect(html).toContain('href="/participate"')
        expect(html).toContain('Join Consultation')
      })
    })
    ```

    ### tests/phase-20.5/framework-page-render.test.tsx
    ```typescript
    import { describe, it, expect, vi, beforeAll } from 'vitest'
    import { renderToStaticMarkup } from 'react-dom/server'
    import React from 'react'

    const { dbMock } = vi.hoisted(() => ({
      dbMock: {
        select: vi.fn(() => ({
          from: vi.fn(() => ({
            where: vi.fn().mockResolvedValue([]),
          })),
        })),
      },
    }))
    vi.mock('@/src/db', () => ({ db: dbMock }))

    let FrameworkPage: any
    beforeAll(async () => {
      const segs = ['@', 'app', '(public)', 'framework', 'page']
      const mod = await import(/* @vite-ignore */ segs.join('/'))
      FrameworkPage = mod.default
    })

    describe('/framework page — PUB-07 empty state', () => {
      it('renders "No drafts under consultation" heading when zero isPublicDraft docs', async () => {
        const element = await FrameworkPage()
        const html = renderToStaticMarkup(element)
        expect(html).toContain('No drafts under consultation')
      })

      it('renders empty-state body copy per UI-SPEC', async () => {
        const element = await FrameworkPage()
        const html = renderToStaticMarkup(element)
        expect(html).toContain('No framework documents are currently open for public review.')
      })
    })
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/phase-20.5/research-page-render.test.tsx tests/phase-20.5/framework-page-render.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File tests/phase-20.5/research-page-render.test.tsx exists and contains the literal string `Understanding the Landscape` (grep).
    - File tests/phase-20.5/research-page-render.test.tsx contains the literal string `href="/research/consultation-research-report.pdf"` (grep).
    - File tests/phase-20.5/framework-page-render.test.tsx exists and contains both `No drafts under consultation` and `No framework documents are currently open for public review.` (grep).
    - Both tests FAIL with a "Cannot find module" error for `app/(public)/research/page` and `app/(public)/framework/page`.
    - Zero TypeScript compile errors from vitest collection.
  </acceptance_criteria>
  <done>Two page-render tests committed and failing with module-not-found messages pointing at `app/(public)/research/page` and `app/(public)/framework/page`.</done>
</task>

<task type="auto">
  <name>Task 3: Flip VALIDATION.md Wave 0 gate + populate Per-Task Verification Map</name>
  <files>.planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md</files>
  <read_first>
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md (current draft state)
    - .planning/phases/20.5-public-research-framework-content-pages/20.5-00-PLAN.md (this file — task list)
  </read_first>
  <action>
    Edit `.planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md`:

    1. Change frontmatter: `status: draft` → `status: approved`; `nyquist_compliant: false` → `nyquist_compliant: true`; `wave_0_complete: false` → `wave_0_complete: true`.

    2. Replace the Per-Task Verification Map table body with these exact rows (six Wave 0 RED contracts + Plans 01/02/03 tasks):

    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|-----------|-------------------|-------------|--------|
    | 00-T1 | 20.5-00 | 0 | PUB-07, PUB-08 | unit (RED) | `npx vitest run tests/phase-20.5/section-status-derivation.test.ts tests/phase-20.5/framework-log-aggregator.test.ts tests/phase-20.5/set-public-draft-mutation.test.ts tests/phase-20.5/proxy-public-routes.test.ts` | yes (after Task 1) | ⬜ pending |
    | 00-T2 | 20.5-00 | 0 | PUB-06, PUB-07 | smoke (RED) | `npx vitest run tests/phase-20.5/research-page-render.test.tsx tests/phase-20.5/framework-page-render.test.tsx` | yes (after Task 2) | ⬜ pending |
    | 01-T1 | 20.5-01 | 1 | PUB-07 | DDL probe | `node scripts/apply-migration-0012.mjs && npx vitest run tests/phase-20.5/set-public-draft-mutation.test.ts` | yes (after Plan 01) | ⬜ pending |
    | 01-T2 | 20.5-01 | 1 | PUB-07, PUB-08 | unit (GREEN) | `npx vitest run tests/phase-20.5/section-status-derivation.test.ts tests/phase-20.5/framework-log-aggregator.test.ts tests/phase-20.5/set-public-draft-mutation.test.ts` | yes (after Plan 01) | ⬜ pending |
    | 02-T1 | 20.5-02 | 1 | PUB-06 | unit (GREEN) | `npx vitest run tests/phase-20.5/research-page-render.test.tsx` | yes (after Plan 02) | ⬜ pending |
    | 03-T1 | 20.5-03 | 2 | PUB-07, PUB-08 | unit (GREEN) | `npx vitest run tests/phase-20.5/framework-page-render.test.tsx` | yes (after Plan 03) | ⬜ pending |
    | 03-T2 | 20.5-03 | 2 | D-17 | unit (GREEN) | `npx vitest run tests/phase-20.5/proxy-public-routes.test.ts` | yes (after Plan 03) | ⬜ pending |
    | 03-T3 | 20.5-03 | 2 | Phase gate | full suite | `npm test` | yes (final) | ⬜ pending |

    3. Under "Validation Sign-Off" section, check off all 6 items.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md && grep -q "00-T1" .planning/phases/20.5-public-research-framework-content-pages/20.5-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter contains `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved` (grep).
    - File contains row `| 00-T1 | 20.5-00 | 0 |` (grep).
    - File contains row `| 03-T3 | 20.5-03 | 2 |` (grep).
    - File contains `npm test` in at least one row (grep).
  </acceptance_criteria>
  <done>VALIDATION.md frontmatter flipped, Per-Task Verification Map populated, sign-off boxes checked.</done>
</task>

</tasks>

<verification>
Run the full Wave 0 RED suite:
```bash
npx vitest run tests/phase-20.5/ --reporter=verbose 2>&1 | tail -60
```
Expected: 6 files listed, all in FAIL state, failure messages reference `framework-log.service`, `setPublicDraft`, `app/(public)/research/page`, `app/(public)/framework/page`, or `'/research(.*)'` literal absence.

Then verify no pre-existing phase test has regressed (spot-check Phase 20 suite):
```bash
npx vitest run tests/phase-20/ 2>&1 | tail -10
```
Expected: all Phase 20 tests remain GREEN (Wave 0 adds tests only, never modifies existing files).
</verification>

<success_criteria>
- Six test files exist under `tests/phase-20.5/`
- `npx vitest run tests/phase-20.5/` produces 6 RED files (no false passes)
- Failure messages name the exact future artifacts downstream plans must create
- VALIDATION.md `nyquist_compliant: true` + Per-Task Verification Map fully populated
- Phase 20 (and earlier) tests remain unchanged and green
</success_criteria>

<output>
After completion, create `.planning/phases/20.5-public-research-framework-content-pages/20.5-00-SUMMARY.md`
</output>
