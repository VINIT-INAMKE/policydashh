---
phase: 19-public-participate-intake-clerk-invite-turnstile
plan: 05
type: execute
wave: 3
depends_on: ['19-00', '19-01', '19-02', '19-03', '19-04']
files_modified:
  - proxy.ts
autonomous: false
requirements: [INTAKE-01, INTAKE-07]

must_haves:
  truths:
    - "Unauthenticated GET /participate returns 200 (not redirected to /sign-in)"
    - "Unauthenticated POST /api/intake/participate reaches the Route Handler (not 401)"
    - "proxy.ts isPublicRoute matcher contains /participate(.*) AND /api/intake(.*)"
    - "No other existing public routes are removed or reordered"
    - "Full Phase 19 E2E flow is wired end-to-end (form submit → route → Inngest → Clerk invite → welcome email)"
  artifacts:
    - path: "proxy.ts"
      provides: "clerkMiddleware with extended isPublicRoute whitelist"
      contains: "/participate(.*)"
  key_links:
    - from: "proxy.ts"
      to: "app/(public)/participate/page.tsx"
      via: "isPublicRoute matcher match on /participate(.*)"
      pattern: "['\"]\\/participate\\(\\.\\*\\)['\"]"
    - from: "proxy.ts"
      to: "app/api/intake/participate/route.ts"
      via: "isPublicRoute matcher match on /api/intake(.*)"
      pattern: "['\"]\\/api\\/intake\\(\\.\\*\\)['\"]"
---

<objective>
Add `/participate(.*)` and `/api/intake(.*)` to `proxy.ts` `isPublicRoute` matcher so both the public page and the submit endpoint are reachable without a Clerk session. Then verify the full Phase 19 E2E flow end-to-end: form submit → Turnstile verify → Inngest event → participateIntakeFn → Clerk invitation → welcome email. This plan closes INTAKE-01 + INTAKE-07 and satisfies `tests/phase-19/public-routes.test.ts`.

Purpose: Flip the final public-route bit so the entire Phase 19 chain works for an unauthenticated visitor.
Output: Edited `proxy.ts` + human-verified end-to-end smoke walk.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-RESEARCH.md
@proxy.ts

<interfaces>
Current proxy.ts state (must be EXTENDED, not rewritten):
```typescript
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server'

const isPublicRoute = createRouteMatcher([
  '/',
  '/sign-in(.*)',
  '/sign-up(.*)',
  '/api/webhooks(.*)',
  '/api/inngest(.*)',
  '/portal(.*)',
  '/api/export/policy-pdf(.*)',
])

export default clerkMiddleware(async (auth, request) => {
  if (!isPublicRoute(request)) {
    await auth.protect()
  }
})

export const config = {
  matcher: [
    '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
    '/(api|trpc)(.*)',
  ],
}
```

Phase 19 Wave 0 contract from tests/phase-19/public-routes.test.ts (locked in Plan 19-00):
- Test 4.1: `proxySource` MUST match regex `/['"]\/participate\(\.\*\)['"]/`
- Test 4.2: `proxySource` MUST match `/['"]\/api\/intake\(\.\*\)['"]/` OR `/['"]\/api\/intake\/participate\(\.\*\)['"]/`

Pitfall 7 (from 19-RESEARCH.md): "clerkMiddleware in proxy.ts calls auth.protect() on the route, returning 401 before the page renders. /participate is not in the existing isPublicRoute matcher. Add '/participate(.*)' to the createRouteMatcher([...]) array in proxy.ts. This is the ONLY change needed in proxy.ts."

Phase 01 decision (from STATE.md): "proxy.ts uses default export per Next.js 16 convention; Clerk clerkMiddleware returns NextMiddleware compatible with this." Do NOT change the `export default` shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /participate(.*) and /api/intake(.*) to proxy.ts isPublicRoute matcher</name>
  <files>proxy.ts</files>
  <read_first>
    - proxy.ts (full file — verify current matcher state)
    - tests/phase-19/public-routes.test.ts (the 2 regex contracts this task must satisfy)
    - .planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-RESEARCH.md (Pitfall 7 section)
    - app/(public)/participate/page.tsx (confirm Plan 19-04 created it)
    - app/api/intake/participate/route.ts (confirm Plan 19-01 created it)
  </read_first>
  <behavior>
    After this task:
    - Unauthenticated GET /participate → 200 (page renders, NOT /sign-in redirect)
    - Unauthenticated GET /participate/anything → 200 (wildcard match)
    - Unauthenticated POST /api/intake/participate → reaches Route Handler (may return 400/403/200 based on body + Turnstile, but NOT 401)
    - All pre-existing public routes (/, /sign-in(.*), /sign-up(.*), /api/webhooks(.*), /api/inngest(.*), /portal(.*), /api/export/policy-pdf(.*)) MUST still be public
    - No authenticated routes accidentally made public
    - tests/phase-19/public-routes.test.ts tests 4.1 and 4.2 GREEN
  </behavior>
  <action>
    Edit `proxy.ts`. The ONLY change is adding two entries to the `createRouteMatcher` array — do NOT touch imports, the middleware body, or the `config.matcher`.

    Before (current file):
    ```typescript
    const isPublicRoute = createRouteMatcher([
      '/',
      '/sign-in(.*)',
      '/sign-up(.*)',
      '/api/webhooks(.*)',
      '/api/inngest(.*)',
      '/portal(.*)',
      '/api/export/policy-pdf(.*)',
    ])
    ```

    After (exact replacement — preserve indentation, single quotes, trailing comma):
    ```typescript
    const isPublicRoute = createRouteMatcher([
      '/',
      '/sign-in(.*)',
      '/sign-up(.*)',
      '/api/webhooks(.*)',
      '/api/inngest(.*)',
      '/portal(.*)',
      '/api/export/policy-pdf(.*)',
      // Phase 19 — public intake form + submit endpoint (INTAKE-01, INTAKE-07)
      '/participate(.*)',
      '/api/intake(.*)',
    ])
    ```

    Constraints:
    - Entries MUST be added at the END of the array, after `/api/export/policy-pdf(.*)`
    - Use EXACT literal strings `'/participate(.*)'` and `'/api/intake(.*)'` (single quotes, no spaces inside)
    - Preserve the trailing comma on the final entry
    - DO NOT reorder pre-existing entries
    - DO NOT modify the middleware body, imports, or `config.matcher`
    - DO NOT add an exception for `/api/intake/participate` specifically — the broader `/api/intake(.*)` allows for future intake endpoints (Phase 20 cal.com webhook handler is a different path under `/api/webhooks`, already public)

    After editing, run the Wave 0 contract test to confirm the regex match flips GREEN.
  </action>
  <verify>
    <automated>grep -c "'/participate(.*)'" proxy.ts && grep -c "'/api/intake(.*)'" proxy.ts && grep -c "'/portal(.*)'" proxy.ts && npm test -- --run tests/phase-19/public-routes.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'/participate(.*)'" proxy.ts` succeeds (exact literal with single quotes)
    - `grep -q "'/api/intake(.*)'" proxy.ts` succeeds
    - `grep -q "'/portal(.*)'" proxy.ts` succeeds (pre-existing entry preserved)
    - `grep -q "'/api/webhooks(.*)'" proxy.ts` succeeds (pre-existing entry preserved)
    - `grep -q "'/api/inngest(.*)'" proxy.ts` succeeds (pre-existing entry preserved)
    - `grep -q "'/api/export/policy-pdf(.*)'" proxy.ts` succeeds (pre-existing entry preserved)
    - `grep -q "export default clerkMiddleware" proxy.ts` succeeds (default export unchanged)
    - `grep -c "createRouteMatcher" proxy.ts` returns exactly 1
    - `npm test -- --run tests/phase-19/public-routes.test.ts` — tests 4.1 AND 4.2 PASS
    - `npx tsc --noEmit` — no TypeScript errors in proxy.ts
    - git diff proxy.ts shows ONLY additions (no removals) — the edit is a strict superset
  </acceptance_criteria>
  <done>proxy.ts extended; /participate and /api/intake now public; Wave 0 public-routes contract GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end Phase 19 smoke walk</name>
  <what-built>
    The complete Phase 19 chain is now wired:
    1. Public `/participate` page (Plan 19-04) reachable unauthenticated (Plan 19-05 whitelist)
    2. Client form POSTs to `/api/intake/participate` (Plan 19-01 Route Handler)
    3. Route Handler verifies Cloudflare Turnstile server-side (Plan 19-01)
    4. On success, fires `participate.intake` Inngest event (Plan 19-01 + events.ts)
    5. `participateIntakeFn` (Plan 19-02) rate-limits per emailHash, calls Clerk `invitations.createInvitation({ ignoreExisting: true })`
    6. `sendWelcomeEmail` (Plan 19-03) renders 1 of 6 org-bucket variants via `renderWelcomeEmail` and sends via Resend
    7. Response returns 200 in under 500ms; form replaces with success panel; sonner toast fires

    This checkpoint verifies the entire chain works end-to-end with real external services (Cloudflare Turnstile test keys, Inngest Dev Server, Clerk invitation API, Resend).
  </what-built>
  <how-to-verify>
    Preconditions:
    1. Ensure dev server is running: `npm run dev`
    2. Ensure Inngest Dev Server is running: `npx inngest-cli@latest dev` (http://localhost:8288)
    3. Ensure these env vars are set in `.env.local`:
       - `NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY=0x00000000000000000000AA` (Cloudflare always-passes test site key)
       - `CLOUDFLARE_TURNSTILE_SECRET_KEY=0x0000000000000000000000000000000AA` (Cloudflare always-passes test secret)
       - `CLERK_SECRET_KEY` set to a real Clerk dev secret
       - `RESEND_API_KEY` set to a real Resend test key (or leave unset — welcome email will silently no-op)
       - `NEXT_PUBLIC_APP_URL=http://localhost:3000`

    Smoke walk:
    4. Open an incognito / private browser window (NO Clerk session)
    5. Visit http://localhost:3000/participate
       - VERIFY: page loads without redirecting to /sign-in (INTAKE-01 + INTAKE-07 confirmed)
       - VERIFY: headline "Join the Consultation" visible in Newsreader serif
       - VERIFY: form shows all 8 fields + Turnstile widget + "Request Access" button
       - VERIFY: "Request Access" button is DISABLED on page load
    6. Wait for Turnstile widget to resolve (1-2 seconds with test site key)
       - VERIFY: "Request Access" button becomes ENABLED
    7. Try submitting with EMPTY fields
       - VERIFY: field-level errors appear in red below each empty/invalid field
       - VERIFY: no network request to /api/intake/participate (check DevTools Network tab)
    8. Fill the form with valid data:
       - Full name: "Dr. Test User"
       - Email: "test+phase19-$(date +%s)@policydash.app" (unique email to avoid rate limit)
       - Role: "Academic / Researcher"
       - Org type: "Academia"
       - Org name: "Test University"
       - Expertise: "Testing the Phase 19 intake pipeline end-to-end for smoke verification."
       - How heard: "Colleague / referral"
    9. Click "Request Access"
       - VERIFY: Button shows "Submitting…" briefly
       - VERIFY: Sonner toast "Request received — check your inbox." appears top-right
       - VERIFY: Form replaced by success panel showing:
         * Green check icon (CheckCircle2)
         * Headline "You're on the list."
         * Body paragraph mentioning "Academia" bucket and the email used
         * Badge reading "Academia"
    10. Check DevTools Network tab:
        - VERIFY: POST /api/intake/participate returned 200
        - VERIFY: response body is `{"success":true}`
        - VERIFY: request body includes `turnstileToken` (not empty)
    11. Check Inngest Dev Server (http://localhost:8288):
        - VERIFY: a `participate.intake` event arrived with the submitted email/orgType
        - VERIFY: `participate-intake` function ran and completed successfully
        - VERIFY: the function run shows 2 steps: `create-clerk-invitation` and `send-welcome-email`
    12. Check Clerk Dashboard (dashboard.clerk.com, your dev instance, Users → Invitations):
        - VERIFY: a pending invitation exists for the test email (INTAKE-04)
        - VERIFY: invitation `publicMetadata` contains `{ "role": "stakeholder", "orgType": "academia" }`
    13. (If Resend key configured) Check Resend dashboard or the test inbox:
        - VERIFY: an email arrived with subject "Welcome to the consultation, Test"
        - VERIFY: body contains "academic or researcher" copy (matching the academia bucket)
        - VERIFY: CTA button reads "Accept Invitation & Sign In"
    14. Rate limit test (OPTIONAL but recommended):
        - Submit the SAME email a second time within 15 minutes
        - VERIFY: Route Handler returns 200 (same response shape — no info leak)
        - VERIFY: Inngest Dev Server shows the second event was rate-limited (run dropped or no second run for that emailHash)
    15. Turnstile failure test:
        - Edit DevTools → Network → intercept POST /api/intake/participate
        - Replace `turnstileToken` in the request body with an invalid string
        - Replay request
        - VERIFY: response is 403 with `{"error":"Security check failed"}`
        - VERIFY: NO Inngest event fired (check dev server)

    Negative checks:
    16. Visit http://localhost:3000/policies in the same incognito window
        - VERIFY: redirects to /sign-in (confirms only /participate was made public — no regression on authenticated routes)
    17. Run the full phase-19 test suite:
        - `npm test -- --run tests/phase-19`
        - VERIFY: all 4 test files GREEN
    18. Run full repo suite:
        - `npm test -- --run`
        - VERIFY: no new failures introduced by Phase 19
  </how-to-verify>
  <resume-signal>Type "approved" if all 18 checks pass (or document known deferred items like Resend inbox check if Resend key is not configured), or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- `grep -q "'/participate(.*)'" proxy.ts` — TRUE
- `grep -q "'/api/intake(.*)'" proxy.ts` — TRUE
- `npm test -- --run tests/phase-19/public-routes.test.ts` — GREEN
- `npm test -- --run tests/phase-19` — all 4 files GREEN (full Phase 19 test suite)
- `npm test -- --run` — no regressions
- `npx tsc --noEmit` — clean
- Human checkpoint signed off (E2E flow verified)
</verification>

<success_criteria>
- [ ] proxy.ts contains literal `'/participate(.*)'` entry
- [ ] proxy.ts contains literal `'/api/intake(.*)'` entry
- [ ] All pre-existing public routes still present
- [ ] tests/phase-19/public-routes.test.ts — both tests GREEN
- [ ] Full tests/phase-19 suite — all 4 files GREEN
- [ ] Unauthenticated GET /participate returns 200
- [ ] Unauthenticated POST /api/intake/participate reaches the Route Handler
- [ ] Authenticated routes (e.g. /policies) still redirect to /sign-in for unauthenticated visitors
- [ ] End-to-end smoke walk passes: form submit → Turnstile → Inngest → Clerk invite → (Resend if configured)
- [ ] Human checkpoint signed off
</success_criteria>

<output>
After completion, create `.planning/phases/19-public-participate-intake-clerk-invite-turnstile/19-05-SUMMARY.md`. This is the last plan in Phase 19 — also flip `19-VALIDATION.md` frontmatter `status: approved` and update `.planning/STATE.md` to reflect Phase 19 completion readiness.
</output>
