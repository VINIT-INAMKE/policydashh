---
phase: 2
slug: policy-documents-sections
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Policy Documents & Sections. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (must initialize before implementation) |
| Preset | Initialize with `npx shadcn@latest init` -- use default style, Geist font, CSS variables enabled |
| Component library | Radix (via shadcn) |
| Icon library | lucide-react (ships with shadcn) |
| Font | Geist Sans (`--font-geist-sans`, already loaded in root layout) |

**Initialization requirement:** Run `npx shadcn@latest init` at the start of Plan 01. Accept defaults: New York style, Zinc base color, CSS variables enabled. This creates `components.json` and updates `globals.css` with the full shadcn CSS variable palette.

**shadcn components needed for this phase:**

| Component | Purpose |
|-----------|---------|
| `button` | All CTAs, toolbar actions |
| `card` | Policy list items, section cards |
| `dialog` | Create policy modal, delete confirmations, import preview |
| `input` | Title fields, search |
| `textarea` | Description fields |
| `label` | Form labels |
| `separator` | Visual dividers |
| `dropdown-menu` | Policy card actions (edit, delete), section actions |
| `badge` | Section count, document status indicators |
| `skeleton` | Loading states for policy list, section list |
| `toast` | Success/error notifications (via sonner) |
| `scroll-area` | Section sidebar scroll |
| `tooltip` | Icon button labels |
| `alert-dialog` | Destructive action confirmations (delete policy, delete section) |
| `table` | Policy list table view |
| `tabs` | Policy detail sub-navigation (if needed) |

Install command: `npx shadcn@latest add button card dialog input textarea label separator dropdown-menu badge skeleton sonner scroll-area tooltip alert-dialog table tabs`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, icon-to-text spacing |
| sm | 8px | Compact element spacing, card internal gaps, form field gaps |
| md | 16px | Default element spacing, card padding, list item padding |
| lg | 24px | Section padding, dialog body padding, group spacing |
| xl | 32px | Layout column gaps, page section margins |
| 2xl | 48px | Major section breaks, sidebar-to-content gap |
| 3xl | 64px | Page-level top/bottom spacing |

Exceptions:
- Drag handle touch target: 44px min height/width for accessibility (WCAG 2.5.5 Target Size)
- Section sidebar width: 280px fixed
- Policy detail content max-width: 768px for readability

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | Descriptions, metadata, section content preview |
| Label | 12px | 400 (regular) | 1.4 | Form labels, badges, timestamps, metadata labels |
| Heading | 20px | 600 (semibold) | 1.2 | Page titles, policy titles, dialog headings |
| Display | 28px | 600 (semibold) | 1.2 | Policy document title on detail page |

Font family: `var(--font-geist-sans)` for all text. `var(--font-geist-mono)` reserved for code blocks and section UUIDs in dev/debug views only.

**Declared weights: 400 (regular) and 600 (semibold) only.** Body and Label use 400. Heading and Display use 600.

---

## Color

Uses shadcn Zinc-based CSS variable palette. All values reference CSS custom properties for dark mode compatibility.

| Role | Light Value | Dark Value | Usage |
|------|-------------|------------|-------|
| Dominant (60%) | `hsl(var(--background))` white | `hsl(var(--background))` zinc-950 | Page background, content areas |
| Secondary (30%) | `hsl(var(--muted))` zinc-100 | `hsl(var(--muted))` zinc-800 | Cards, sidebar background, section list, table header |
| Accent (10%) | `hsl(var(--primary))` zinc-900 | `hsl(var(--primary))` zinc-50 | See reserved list below |
| Destructive | `hsl(var(--destructive))` red-500 | `hsl(var(--destructive))` red-500 | Delete buttons, delete confirmation dialogs |

**Accent reserved for these specific elements only:**
1. "Create Policy" primary CTA button (filled)
2. "Add Section" primary action button (filled)
3. "Import" primary action button (filled)
4. Active sidebar section highlight (left border or background)
5. "Confirm Import" button in markdown import flow (filled)
6. Drag handle active/hover state

**Do NOT use accent for:** links (use foreground with underline), badges (use muted), secondary actions (use outline variant), navigation items (use muted hover).

**Additional semantic colors:**

| Semantic | Value | Usage |
|----------|-------|-------|
| Border | `hsl(var(--border))` | Card borders, separator lines, input borders |
| Muted foreground | `hsl(var(--muted-foreground))` | Secondary text, timestamps, placeholder text |
| Ring | `hsl(var(--ring))` | Focus rings on interactive elements |

---

## Component Inventory

### Page: Policy List (`/policies`)

**Layout:** Full-width within workspace layout. Header row with title + actions. Content area with policy cards.

**Focal point declaration:**
- Empty state: the eye should land first on the FileText icon (48px, muted-foreground, centered), then read down to the "No policies yet" heading and body copy, then to the "Create Policy" CTA button. The icon anchors the empty-state composition.
- Populated view: the eye should land first on the "Create Policy" button (top-right, filled accent), then scan the policy card grid left-to-right. Each card's focal point is the policy title (16px/600), then description (truncated, muted), then the footer badge+timestamp row.

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Page heading | `<h1>` | Heading (20px/600) | "Policies" |
| Create button | `Button` | default, size="default" | "Create Policy" with Plus icon (16px) |
| Import button | `Button` | outline, size="default" | "Import Markdown" with Upload icon (16px) |
| Policy card | `Card` | default | Title (16px/600), description truncated 2 lines (14px/400), section count badge, updated-at timestamp |
| Card actions | `DropdownMenu` | -- | Trigger: MoreHorizontal icon button (`aria-label="More options for {policy title}"`). Items: "Edit Details", separator, "Delete Policy" (destructive text) |
| Empty state | Custom div | -- | Centered, icon (FileText, 48px, muted-foreground), heading + body copy |
| Loading state | `Skeleton` | -- | 3 skeleton cards matching card dimensions |
| Section count | `Badge` | secondary | "{N} sections" inside each policy card |

**Policy card layout (inside Card):**
```
[CardHeader]
  Row: Title (left) ... DropdownMenu trigger (right)
  Description (truncated, muted-foreground)
[CardFooter]
  Row: Badge "{N} sections" (left) ... "Updated {relative time}" (right, muted-foreground, 12px)
```

### Page: Policy Detail (`/policies/[id]`)

**Layout:** Two-column. Left: section sidebar (280px fixed). Right: content area (flex-1, max-width 768px centered).

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Back link | `Button` | ghost, size="sm" | ArrowLeft icon + "Back to Policies" |
| Policy title | `<h1>` | Display (28px/600) | Editable inline on click (or edit via dialog) |
| Policy description | `<p>` | Body (14px/400), muted-foreground | Below title |
| Policy metadata | Custom row | -- | Status badge, created date, updated date |
| Edit metadata button | `Button` | outline, size="sm" | Pencil icon + "Edit" |
| Section sidebar | `ScrollArea` + custom list | -- | 280px wide, border-r, bg muted |
| Sidebar heading | `<h2>` | Label (12px/400), uppercase tracking-wide | "SECTIONS" |
| Add section button | `Button` | default, size="sm" | Plus icon + "Add Section" in sidebar header |
| Section list item | Custom button | -- | 14px/400, px-3 py-2, rounded-md. Active: bg-background, font-semibold. Hover: bg-background/50 |
| Drag handle | GripVertical icon | -- | 16px, muted-foreground, visible on hover, cursor-grab |
| Section actions | `DropdownMenu` | -- | Trigger: MoreHorizontal (visible on hover), `aria-label="More options for {section title}"`. Items: "Rename", separator, "Delete Section" (destructive) |
| Content area (no section selected) | Custom div | -- | Centered prompt: "Select a section from the sidebar" |
| Content area (section selected) | Custom div | -- | Section title (Heading 20px/600), content preview as read-only rendered Tiptap JSON. Placeholder if empty: "This section has no content yet. Content editing will be available in the block editor phase." |

**Sidebar section item layout:**
```
Row (hover group):
  [DragHandle (show on hover)] [Section title (truncate)] [MoreHorizontal (show on hover)]
```

**Drag and drop interaction:**
- Use `@dnd-kit/core` + `@dnd-kit/sortable` for section reordering
- Drag handle: GripVertical icon, visible on hover/focus
- Drag preview: section item with slight opacity (0.8) and elevated shadow (shadow-md)
- Drop indicator: 2px primary-colored horizontal line at insertion point
- On drop: optimistic reorder, tRPC mutation fires, revert on error with toast

### Dialog: Create Policy

**Trigger:** "Create Policy" button on policy list page.

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Dialog | `Dialog` | -- | max-width 480px |
| Title | `DialogTitle` | Heading (20px/600) | "Create Policy" |
| Description | `DialogDescription` | Body (14px/400) | "Add a new policy document to your workspace." |
| Title field | `Input` | -- | Label: "Title", placeholder: "e.g., Data Protection Policy", required, autofocus |
| Description field | `Textarea` | -- | Label: "Description", placeholder: "Brief summary of this policy's scope and purpose", rows: 3, optional |
| Discard button | `Button` | outline | "Discard" |
| Submit button | `Button` | default | "Create Policy" -- disabled until title is non-empty |

**Validation:**
- Title: required, min 1 character, max 200 characters
- Description: optional, max 1000 characters
- On submit: close dialog, navigate to new policy detail page, show success toast

### Dialog: Edit Policy Metadata

Same structure as Create Policy dialog, pre-populated with current values. Submit label: "Save Changes".

### Dialog: Delete Policy (Alert Dialog)

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Alert Dialog | `AlertDialog` | -- | max-width 420px |
| Title | `AlertDialogTitle` | Heading (20px/600) | "Delete Policy" |
| Description | `AlertDialogDescription` | Body (14px/400) | "This will permanently delete \"{policy title}\" and all {N} sections within it. This action cannot be undone." |
| Cancel button | `AlertDialogCancel` | outline | "Cancel" |
| Confirm button | `AlertDialogAction` | destructive | "Delete Policy" |

### Dialog: Add Section

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Dialog | `Dialog` | -- | max-width 420px |
| Title | `DialogTitle` | Heading (20px/600) | "Add Section" |
| Title field | `Input` | -- | Label: "Section Title", placeholder: "e.g., Scope and Applicability", required, autofocus |
| Discard button | `Button` | outline | "Discard" |
| Submit button | `Button` | default | "Add Section" -- disabled until title is non-empty |

**On submit:** New section appended at end of list, sidebar scrolls to new section, section becomes active/selected, success toast shown.

### Dialog: Rename Section

Same as Add Section dialog. Title: "Rename Section". Pre-populated with current title. Discard button: "Discard". Submit label: "Rename Section".

### Dialog: Delete Section (Alert Dialog)

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Alert Dialog | `AlertDialog` | -- | max-width 420px |
| Title | `AlertDialogTitle` | Heading (20px/600) | "Delete Section" |
| Description | `AlertDialogDescription` | Body (14px/400) | "This will permanently delete the section \"{section title}\" and all its content. This action cannot be undone." |
| Cancel button | `AlertDialogCancel` | outline | "Cancel" |
| Confirm button | `AlertDialogAction` | destructive | "Delete Section" |

### Flow: Markdown Import

**Step 1 -- File Upload Dialog:**

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Dialog | `Dialog` | -- | max-width 560px |
| Title | `DialogTitle` | Heading (20px/600) | "Import from Markdown" |
| Description | `DialogDescription` | Body (14px/400) | "Upload a markdown file to create a new policy document. Headings (## H2) will be used to split the content into sections." |
| Drop zone | Custom div | -- | Dashed border (2px, border-dashed, rounded-lg), 160px min-height, centered icon (Upload, 32px, muted-foreground) + text. Hover/dragover: border-primary, bg-primary/5 |
| Drop zone text | -- | -- | "Drag and drop a .md file here, or click to browse" (14px/400, muted-foreground) |
| File input | Hidden `<input type="file">` | -- | accept=".md,.markdown,text/markdown" |
| Selected file indicator | Custom row | -- | FileText icon + filename + file size + Remove button (X icon) |
| Discard button | `Button` | outline | "Discard" |
| Next button | `Button` | default | "Preview Import" -- disabled until file selected |

**Step 2 -- Preview & Confirm (same dialog, replaces step 1):**

| Element | Component | Variant | Notes |
|---------|-----------|---------|-------|
| Title | `DialogTitle` | Heading (20px/600) | "Preview Import" |
| Description | `DialogDescription` | Body (14px/400) | "Review the detected structure before importing." |
| Policy title field | `Input` | -- | Label: "Policy Title", pre-filled from first H1 or filename, editable |
| Detected sections list | Custom list | -- | Numbered list of detected section titles with section icon. Each item: "{index}. {heading text}" (14px/400). Shows content preview truncated to 2 lines below each heading (12px/400, muted-foreground) |
| Section count summary | -- | -- | "{N} sections detected" (Label 12px/400) above the list |
| Back button | `Button` | outline | "Back" (returns to step 1) |
| Confirm button | `Button` | default | "Import Policy" |

**On confirm:** Dialog closes, navigates to new policy detail page, success toast: "Policy imported with {N} sections".

**Parsing rules (for executor reference):**
- First `# H1` becomes policy title (if no H1, use filename without extension)
- Each `## H2` heading starts a new section
- Content before the first `## H2` becomes a preamble section titled "Introduction"
- Nested headings (`### H3` and below) stay within their parent `## H2` section
- Content is converted to Tiptap JSON per-section

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Create Policy" |
| Secondary CTA | "Import Markdown" |
| Empty state heading (policy list) | "No policies yet" |
| Empty state body (policy list) | "Create your first policy document to get started, or import an existing one from a markdown file." |
| Empty state heading (no sections) | "No sections" |
| Empty state body (no sections) | "Add sections to organize your policy document. Sections provide stable anchors for feedback and change tracking." |
| Empty state (section content) | "This section has no content yet. The block editor will be available in a future update." |
| Empty state (no section selected) | "Select a section from the sidebar to view its content." |
| Error: policy creation failed | "Couldn't create the policy. Check your connection and try again." |
| Error: section reorder failed | "Couldn't reorder sections. The original order has been restored." |
| Error: import parse failed | "Couldn't parse the markdown file. Make sure it's a valid .md file and try again." |
| Error: file too large | "This file is too large. The maximum file size is 5 MB." |
| Success: policy created | "Policy created successfully." |
| Success: policy deleted | "Policy deleted." |
| Success: section added | "Section added." |
| Success: section deleted | "Section deleted." |
| Success: section renamed | "Section renamed." |
| Success: sections reordered | (no toast -- silent success for drag-drop) |
| Success: policy imported | "Policy imported with {N} sections." |
| Destructive: delete policy | "Delete Policy": "This will permanently delete \"{title}\" and all {N} sections within it. This action cannot be undone." |
| Destructive: delete section | "Delete Section": "This will permanently delete the section \"{title}\" and all its content. This action cannot be undone." |
| Dialog dismiss: Create Policy | "Discard" |
| Dialog dismiss: Add Section | "Discard" |
| Dialog dismiss: Rename Section | "Discard" |
| Dialog dismiss: Import Markdown Step 1 | "Discard" |
| Dialog submit: Rename Section | "Rename Section" |

---

## Interaction States

### Button States

| State | Visual Treatment |
|-------|-----------------|
| Default | Per shadcn variant defaults |
| Hover | Per shadcn variant hover states |
| Focus | Ring (2px, ring offset 2px, ring color) |
| Disabled | opacity-50, pointer-events-none |
| Loading | Spinner icon (Loader2, animate-spin) replaces left icon, text unchanged, disabled |

### Card States (Policy List)

| State | Visual Treatment |
|-------|-----------------|
| Default | bg-card, border, rounded-lg |
| Hover | border-primary/20, shadow-sm transition |
| Click | Navigate to policy detail |
| Dropdown open | Card retains hover state |

### Section Sidebar Item States

| State | Visual Treatment |
|-------|-----------------|
| Default | bg-transparent, text-foreground |
| Hover | bg-background/50, drag handle + actions visible |
| Active/Selected | bg-background, font-semibold, left border 2px primary |
| Dragging | opacity-80, shadow-md, scale(1.02) |
| Drop target | 2px primary horizontal line at insertion point |

### Form Field States

| State | Visual Treatment |
|-------|-----------------|
| Default | Per shadcn input defaults |
| Focus | Ring, border-ring |
| Error | border-destructive, error message below (12px, destructive color) |
| Disabled | opacity-50, bg-muted |

### File Drop Zone States

| State | Visual Treatment |
|-------|-----------------|
| Default | border-2 border-dashed border-muted-foreground/25, rounded-lg |
| Hover/Dragover | border-primary, bg-primary/5 |
| File selected | Solid border, file info row replaces drop prompt |
| Error | border-destructive, error message below |

---

## Loading & Skeleton Strategy

| View | Skeleton Pattern |
|------|-----------------|
| Policy list | 3 skeleton cards: CardHeader skeleton (h-5 w-3/4 + h-4 w-full), CardFooter skeleton (h-4 w-20 + h-4 w-32) |
| Policy detail (sidebar) | 5 skeleton rectangles (h-8 w-full) with 8px gaps |
| Policy detail (content) | Skeleton heading (h-7 w-1/2) + 4 skeleton text lines (h-4 w-full, last w-2/3) |
| Section content | Skeleton heading (h-6 w-2/5) + 3 skeleton text lines (h-4 w-full) |

Use `Suspense` boundaries at the page level. tRPC queries use `suspense: true` where appropriate. Skeleton components render during the suspense fallback.

---

## Responsive Behavior

| Breakpoint | Behavior |
|------------|----------|
| >= 1024px (lg) | Two-column layout: sidebar (280px) + content area |
| 768px - 1023px (md) | Sidebar collapses to sheet/drawer triggered by hamburger button. Content area fills width. |
| < 768px (sm) | Single column. Policy list as stacked cards. Policy detail: collapsible sidebar as bottom sheet or top toggle. |

Policy list cards: single column on mobile, 2 columns on md, 3 columns on lg+. Use CSS grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`.

---

## Accessibility Requirements

| Requirement | Implementation |
|-------------|----------------|
| Keyboard navigation | All interactive elements reachable via Tab. Sidebar sections navigable with Arrow Up/Down. |
| Focus management | Dialog auto-focuses first input. On dialog close, focus returns to trigger. After section delete, focus moves to adjacent section. |
| Drag and drop keyboard | Section reorder accessible via keyboard: select item, use Alt+Arrow Up/Down to move. Announce position changes via aria-live. |
| Screen reader | Drag handles: `aria-label="Reorder {section title}"`. Policy cards: `aria-label="{title}, {N} sections, updated {date}"`. MoreHorizontal on policy cards: `aria-label="More options for {policy title}"`. MoreHorizontal on section items: `aria-label="More options for {section title}"`. |
| Color contrast | All text meets WCAG 2.1 AA (4.5:1 for body, 3:1 for large text). shadcn default palette satisfies this. |
| Motion | Drag animations respect `prefers-reduced-motion`: disable scale/shadow transitions, keep only position changes. |
| File upload | Drop zone has `role="button"` and `aria-label="Upload markdown file"`. File input is keyboard-accessible via Enter/Space on drop zone. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, card, dialog, input, textarea, label, separator, dropdown-menu, badge, skeleton, sonner, scroll-area, tooltip, alert-dialog, table, tabs | not required |

No third-party registries. All components sourced from the official shadcn registry.

**Additional npm packages required (not shadcn):**

| Package | Purpose | Safety |
|---------|---------|--------|
| `@dnd-kit/core` | Drag-and-drop primitives for section reorder | Well-maintained, 15k+ GitHub stars, no known vulnerabilities |
| `@dnd-kit/sortable` | Sortable list preset for section sidebar | Companion to @dnd-kit/core |
| `@dnd-kit/utilities` | CSS utilities for transforms | Companion to @dnd-kit/core |
| `lucide-react` | Icon library (ships with shadcn init) | Official shadcn icon dependency |

---

## Phase-Specific Notes

### What This Phase Does NOT Include

- **Block editor UI**: Section content is stored as Tiptap JSON but the interactive editor ships in Phase 3. Content is displayed as read-only rendered output or placeholder text.
- **Section-level permissions**: AUTH-05 (section scoping) is Phase 4. All authenticated users with document access can see all sections.
- **Search/filtering**: SRCH-01 through SRCH-04 are Phase 7. No search bar on policy list in this phase.
- **Status workflows**: No policy status state machine in this phase. Documents exist as simple draft entities.

### Navigation Integration

Add to workspace header navigation:
- "Dashboard" link (existing, `/dashboard`)
- "Policies" link (new, `/policies`)

Use active state: font-semibold + underline or primary color indicator for current page.

### Route Structure

| Route | Page |
|-------|------|
| `/policies` | Policy list (grid of cards) |
| `/policies/[id]` | Policy detail with section sidebar |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
