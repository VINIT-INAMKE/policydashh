---
phase: 02-policy-documents-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components.json
  - app/globals.css
  - package.json
  - app/(workspace)/layout.tsx
  - app/(workspace)/policies/page.tsx
  - app/(workspace)/policies/_components/policy-card.tsx
  - app/(workspace)/policies/_components/create-policy-dialog.tsx
  - app/(workspace)/policies/_components/edit-policy-dialog.tsx
  - app/(workspace)/policies/_components/delete-policy-dialog.tsx
  - app/(workspace)/policies/_components/policy-list-skeleton.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-06

must_haves:
  truths:
    - "User can see a list of all policy documents on /policies"
    - "Admin or Policy Lead can create a new policy document via dialog"
    - "Admin or Policy Lead can edit a policy document's title and description"
    - "Admin or Policy Lead can delete a policy document with confirmation"
    - "Multiple policy documents coexist in the workspace"
    - "Navigation includes a Policies link in the workspace header"
  artifacts:
    - path: "components.json"
      provides: "shadcn configuration"
      contains: "shadcn"
    - path: "app/(workspace)/policies/page.tsx"
      provides: "Policy list page"
      min_lines: 30
    - path: "app/(workspace)/policies/_components/policy-card.tsx"
      provides: "Individual policy card component"
      min_lines: 20
    - path: "app/(workspace)/policies/_components/create-policy-dialog.tsx"
      provides: "Create policy dialog"
      min_lines: 30
  key_links:
    - from: "app/(workspace)/policies/page.tsx"
      to: "src/server/routers/document.ts"
      via: "trpc.document.list.useQuery"
      pattern: "trpc\\.document\\.list"
    - from: "app/(workspace)/policies/_components/create-policy-dialog.tsx"
      to: "src/server/routers/document.ts"
      via: "trpc.document.create.useMutation"
      pattern: "trpc\\.document\\.create"
---

<objective>
Initialize shadcn/ui, install all UI dependencies, and build the policy list page with create/edit/delete document flows.

Purpose: Give users the entry point to the policy documents feature -- a card grid listing all policies with full CRUD dialog flows for document metadata.
Output: shadcn initialized, /policies page with card grid, create/edit/delete dialogs, workspace nav updated.
</objective>

<execution_context>
@D:/aditee/policydash/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/policydash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-policy-documents-sections/02-RESEARCH.md
@.planning/phases/02-policy-documents-sections/02-UI-SPEC.md
@.planning/phases/02-policy-documents-sections/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/server/routers/document.ts (created in Plan 01):
```typescript
export const documentRouter = router({
  list: requirePermission('document:read').query(...)
    // Returns: Array<{ id: string, title: string, description: string | null, createdAt: Date, updatedAt: Date, sectionCount: number }>

  create: requirePermission('document:create')
    .input(z.object({ title: z.string().min(1).max(200), description: z.string().max(1000).optional() }))
    .mutation(...)
    // Returns: { id: string, title: string, description: string | null, createdAt: Date, updatedAt: Date }

  update: requirePermission('document:update')
    .input(z.object({ id: z.string().uuid(), title: z.string().min(1).max(200).optional(), description: z.string().max(1000).optional() }))
    .mutation(...)

  delete: requirePermission('document:delete')
    .input(z.object({ id: z.string().uuid() }))
    .mutation(...)
    // Returns: { success: true }
})
```

From src/trpc/client.tsx:
```typescript
export const trpc = createTRPCReact<AppRouter>()
// Usage: trpc.document.list.useQuery(), trpc.document.create.useMutation()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and install all dependencies</name>
  <files>
    components.json,
    app/globals.css,
    package.json
  </files>
  <read_first>
    package.json,
    app/globals.css,
    app/layout.tsx,
    .planning/phases/02-policy-documents-sections/02-UI-SPEC.md
  </read_first>
  <action>
    IMPORTANT: Before running any shadcn commands, read the Next.js docs at node_modules/next/dist/docs/ for any relevant CSS or configuration guidance for Next.js 16.

    **1. Initialize shadcn/ui:**
    ```bash
    npx shadcn@latest init --defaults
    ```
    This creates components.json and updates globals.css with the CSS variable palette. If the `--defaults` flag does not exist, run `npx shadcn@latest init` and select: New York style, Zinc base color, CSS variables enabled.

    Verify components.json was created and globals.css now contains `--background`, `--foreground`, `--primary`, `--muted` variables.

    **2. Install all shadcn components needed for Phase 2:**
    ```bash
    npx shadcn@latest add button card dialog input textarea label separator dropdown-menu badge skeleton sonner scroll-area tooltip alert-dialog table tabs
    ```

    **3. Install @dnd-kit packages:**
    ```bash
    npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
    ```

    **4. Verify lucide-react was installed** (ships with shadcn init). If not, install it: `npm install lucide-react`

    **5. Add the Sonner Toaster to the root layout.** Read app/layout.tsx, then add:
    - Import `{ Toaster } from 'sonner'` (or from the shadcn sonner component path if shadcn wraps it)
    - Add `<Toaster />` inside the body, after the TRPCReactProvider wrapper

    **6. Verify globals.css** has the shadcn CSS variable palette. The UI spec requires Zinc-based palette with:
    - `--background`, `--foreground`, `--card`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--ring`
    - Both light and dark mode values

    If shadcn init overwrites existing Tailwind v4 CSS, reconcile: keep both the Tailwind v4 `@import "tailwindcss"` and the shadcn CSS variables. shadcn v4 should handle this automatically with Tailwind v4.
  </action>
  <verify>
    <automated>cd D:/aditee/policydash && test -f components.json && grep -c "background" app/globals.css && node -e "const p=require('./package.json'); console.log(p.dependencies['@dnd-kit/core'] ? 'dnd-ok' : 'dnd-missing')" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File components.json exists in project root
    - grep "--background" app/globals.css returns a match
    - grep "--primary" app/globals.css returns a match
    - grep "--destructive" app/globals.css returns a match
    - grep "@dnd-kit/core" package.json returns a match
    - grep "@dnd-kit/sortable" package.json returns a match
    - grep "lucide-react" package.json returns a match
    - grep "sonner" package.json returns a match
    - A components/ui directory exists with button.tsx, card.tsx, dialog.tsx, etc.
    - grep "Toaster" app/layout.tsx returns a match
  </acceptance_criteria>
  <done>
    shadcn/ui initialized with Zinc palette. All 16 shadcn components installed. @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities installed. lucide-react and sonner available. Toaster added to root layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Policy list page with card grid and CRUD dialogs</name>
  <files>
    app/(workspace)/layout.tsx,
    app/(workspace)/policies/page.tsx,
    app/(workspace)/policies/_components/policy-card.tsx,
    app/(workspace)/policies/_components/create-policy-dialog.tsx,
    app/(workspace)/policies/_components/edit-policy-dialog.tsx,
    app/(workspace)/policies/_components/delete-policy-dialog.tsx,
    app/(workspace)/policies/_components/policy-list-skeleton.tsx
  </files>
  <read_first>
    app/(workspace)/layout.tsx,
    app/(workspace)/dashboard/page.tsx,
    src/trpc/client.tsx,
    src/trpc/server.tsx,
    src/server/routers/document.ts,
    .planning/phases/02-policy-documents-sections/02-UI-SPEC.md
  </read_first>
  <action>
    Follow the UI-SPEC.md precisely for all visual/interaction/copy decisions. Reference Next.js 16 docs in node_modules/next/dist/docs/ before writing route files.

    **1. Update app/(workspace)/layout.tsx** -- Add "Policies" navigation link:
    - In the header, next to the "PolicyDash" heading, add a nav element with:
      - "Dashboard" link to `/dashboard`
      - "Policies" link to `/policies`
    - Active state: detect current path using `usePathname()` from `next/navigation` -- BUT this is a server component, so either convert to client component for the nav OR extract nav into a client component `_components/workspace-nav.tsx`
    - Active link styling: `font-semibold` when pathname starts with the link's href
    - Keep the existing `<UserButton />` on the right

    **2. Create app/(workspace)/policies/page.tsx** (Policy List Page):
    - This should be a client component ('use client') since it uses tRPC hooks
    - Import `trpc` from `@/src/trpc/client`
    - Call `trpc.document.list.useQuery()` to fetch policies
    - Layout per UI spec:
      - Page heading: `<h1>` with text "Policies" (Heading: 20px/600)
      - Top-right row: "Create Policy" button (default variant, Plus icon 16px) + "Import Markdown" button (outline variant, Upload icon 16px)
      - Use CSS grid for cards: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    - Empty state when no policies: centered div with FileText icon (48px, muted-foreground), heading "No policies yet", body text "Create your first policy document to get started, or import an existing one from a markdown file.", "Create Policy" CTA button
    - Loading state: 3 skeleton cards via PolicyListSkeleton component
    - Each policy rendered as PolicyCard component
    - Clicking a card navigates to `/policies/[id]`

    **3. Create app/(workspace)/policies/_components/policy-card.tsx:**
    - Uses shadcn Card (CardHeader, CardFooter)
    - Card layout per UI spec:
      - CardHeader: Row with title (16px/600) on left, DropdownMenu trigger (MoreHorizontal icon, `aria-label="More options for {policy title}"`) on right
      - Description below title: truncated to 2 lines, muted-foreground (use `line-clamp-2` class)
      - CardFooter: Badge "{N} sections" (secondary variant) on left, "Updated {relative time}" on right (12px, muted-foreground)
    - Use `formatDistanceToNow` from `date-fns` for relative time
    - DropdownMenu items: "Edit Details" (opens edit dialog), separator, "Delete Policy" (destructive text, opens delete dialog)
    - Card hover: `border-primary/20 shadow-sm transition`
    - Card click: navigate to `/policies/${id}` via Next.js router. The dropdown trigger should stopPropagation to prevent card click.
    - Props: `policy: { id, title, description, sectionCount, updatedAt }`

    **4. Create app/(workspace)/policies/_components/create-policy-dialog.tsx:**
    - Uses shadcn Dialog
    - Per UI spec: max-width 480px, title "Create Policy", description "Add a new policy document to your workspace."
    - Title field: Input, label "Title", placeholder "e.g., Data Protection Policy", required, autofocus
    - Description field: Textarea, label "Description", placeholder "Brief summary of this policy's scope and purpose", rows 3, optional
    - Discard button (outline): "Discard"
    - Submit button (default): "Create Policy" -- disabled until title is non-empty, shows Loader2 spinner when submitting
    - Validation: title required, min 1, max 200 chars; description optional, max 1000 chars
    - On submit: call `trpc.document.create.useMutation()`, on success close dialog, navigate to `/policies/${newDoc.id}`, show toast "Policy created successfully."
    - On error: show toast "Couldn't create the policy. Check your connection and try again."
    - Invalidate document.list query on success

    **5. Create app/(workspace)/policies/_components/edit-policy-dialog.tsx:**
    - Same structure as CreatePolicyDialog but pre-populated with current values
    - Submit label: "Save Changes"
    - Calls `trpc.document.update.useMutation()`
    - On success: close dialog, invalidate queries, show toast
    - Props: `policy: { id, title, description }`, `open: boolean`, `onOpenChange: (open: boolean) => void`

    **6. Create app/(workspace)/policies/_components/delete-policy-dialog.tsx:**
    - Uses shadcn AlertDialog
    - Per UI spec: max-width 420px
    - Title: "Delete Policy"
    - Description: `This will permanently delete "{policy title}" and all {N} sections within it. This action cannot be undone.`
    - Cancel: "Keep Policy" (outline)
    - Confirm: "Delete Policy" (destructive variant)
    - Calls `trpc.document.delete.useMutation()`
    - On success: close dialog, invalidate queries, show toast "Policy deleted."
    - Props: `policy: { id, title, sectionCount }`, `open: boolean`, `onOpenChange: (open: boolean) => void`

    **7. Create app/(workspace)/policies/_components/policy-list-skeleton.tsx:**
    - 3 skeleton cards in the same grid layout
    - Each card: CardHeader skeleton (h-5 w-3/4 + h-4 w-full), CardFooter skeleton (h-4 w-20 + h-4 w-32)
    - Per UI spec Loading & Skeleton Strategy

    **All copy text must match the UI-SPEC.md Copywriting Contract exactly.** Do not invent copy.
  </action>
  <verify>
    <automated>cd D:/aditee/policydash && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "Policies" app/(workspace)/layout.tsx returns a match (nav link added)
    - grep "trpc.document.list" app/(workspace)/policies/page.tsx returns a match
    - grep "No policies yet" app/(workspace)/policies/page.tsx returns a match
    - grep "Create Policy" app/(workspace)/policies/_components/create-policy-dialog.tsx returns a match
    - grep "trpc.document.create" app/(workspace)/policies/_components/create-policy-dialog.tsx returns a match
    - grep "Delete Policy" app/(workspace)/policies/_components/delete-policy-dialog.tsx returns a match
    - grep "trpc.document.delete" app/(workspace)/policies/_components/delete-policy-dialog.tsx returns a match
    - grep "Save Changes" app/(workspace)/policies/_components/edit-policy-dialog.tsx returns a match
    - grep "trpc.document.update" app/(workspace)/policies/_components/edit-policy-dialog.tsx returns a match
    - grep "formatDistanceToNow" app/(workspace)/policies/_components/policy-card.tsx returns a match
    - grep "MoreHorizontal" app/(workspace)/policies/_components/policy-card.tsx returns a match
    - grep "Skeleton" app/(workspace)/policies/_components/policy-list-skeleton.tsx returns a match
    - grep "grid-cols-1" app/(workspace)/policies/page.tsx returns a match
    - TypeScript compilation succeeds (npx tsc --noEmit)
  </acceptance_criteria>
  <done>
    /policies page renders card grid of all documents. Create Policy dialog creates new documents with title/description. Edit Policy dialog updates metadata. Delete Policy dialog with confirmation deletes documents with cascade. Workspace nav includes "Policies" link. All copy matches UI spec. Empty state and loading skeleton implemented. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` succeeds
- components.json exists with shadcn config
- All shadcn components present in components/ui/
- @dnd-kit packages in package.json
- /policies page has empty state, loading skeleton, and card grid
- Create/Edit/Delete dialogs call correct tRPC mutations
- Workspace layout has Policies navigation link
- All copy matches UI-SPEC.md Copywriting Contract
</verification>

<success_criteria>
Users can navigate to /policies, see all policy documents as cards with section count and timestamp, create new policies via dialog, edit policy metadata, and delete policies with confirmation. shadcn/ui is initialized and all Phase 2 UI dependencies are installed. The page is ready for the policy detail + sections work in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-policy-documents-sections/02-02-SUMMARY.md`
</output>
